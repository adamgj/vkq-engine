[package]
name = "quake-cvar"
version = "0.1.0"
description = "cvar/cmd/alias registry with C-callable callbacks"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# ADR-005: the C-printf-compatible float formatter (no new crate)
quake-util = { path = "../quake-util" }

[lints]
workspace = true
