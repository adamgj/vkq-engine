[package]
name = "quake-util"
version = "0.1.0"
description = "crc16, folded MD4, strl*, q_ctype, C-printf-compatible float formatter (ADR-005)"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
# platform strtod call-through for json number parsing (ADR-012); safe
# wrappers only, so this crate stays forbid(unsafe_code)
quake-c-sys = { path = "../quake-c-sys" }

[lints]
workspace = true
