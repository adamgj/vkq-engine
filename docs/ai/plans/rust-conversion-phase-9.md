# Feature task plan: Rust migration Phase 9 — Host inversion + platform (`quake-platform`, Rust `main()`)

Status: approved (plan mode, 2026-09-14)
Owner: adamgj / Claude Opus 5
Baseline: `feature/rust-conversion-phase-9-fd0f10` at `5394a4f2` (master = merged Phase 8 M11-M12, PR #40)
Last materially updated: 2026-09-14
Commit target once approved: `docs/ai/plans/rust-conversion-phase-9.md` (first commit of M1)

For Rust-migration work, this task plan is subordinate to `docs/rust-migration/PLAN.md`, `ROADMAP.md`, and applicable ADRs. It cannot change phase ordering or compatibility policy without an explicit approved documentation/ADR change.

## Context

Phases 0–8 are complete on `master`; the ROADMAP's next open phase is **Phase 9 — Host inversion + platform (M)** (`docs/rust-migration/ROADMAP.md:270-287`). It moves the process entry point and the platform layer to Rust: the Phase 5 carry-overs `net_wins.c` (Windows WINS landrivers) plus the driver-table files `net_bsd.c`/`net_win.c`; `main_sdl.c` (client + dedicated loops); `sys_sdl.c`/`sys_sdl_unix.c`/`sys_sdl_win.c` (file handles, timers, console I/O, store-location discovery, `Sys_Error` policy incl. the DbgHelp stack trace); `pl_win.c`/`pl_linux.c`/`pl_osx.m` (icon, clipboard, error dialog); `in_sdl.c`/`in_sdl3.c`/`in_sdl.h` (event pump, joystick, scancode table); the ADR-009 error-path end state; `quakedef.h`/PCH retirement; and the build-orchestration decision.

The user asked to "implement phase 9 … then review and validate". Per `CLAUDE.md`/`AGENTS.md` and `docs/ai/FABLE5_WORKFLOW.md`, this is **one bounded architecture pass (this plan) followed by implementation one milestone at a time**, each milestone leaving every configured build and gate green and landing as its own commit.

**Scope of the session that follows approval (decision, not a question):** land the milestones **in order (M1 → M8)**, each with its targeted verification and evidence row, and stop honestly at whichever milestone the session's verification stops being current — exactly as Phase 8 did. The Windows dev box is the local oracle (`build-c`, `build-rs` and the new `build-rs-cplatform` leg; `run_corpus.py --compare`; `save_diff.py`; cargo gates); Linux/macOS/Docker/MinGW checks are CI-only and are recorded as *not run locally*. No agent-parallel execution; `compatibility-reviewer` at the STOP points.

Governing: ADR-001 (C oracle until this phase's soak completes), ADR-003 (new direct crates `sdl2`, `windows-sys`; `socket2` on Windows — all reviewed in D2), ADR-004 (unsafe concentrated in `quake-platform`/`quake-net::udp::sys`/`quake-capi`; every block documented), ADR-007 (cvars stay C-owned statics in glue TUs during the phase), ADR-009 (no `longjmp` through Rust frames; `Host_Guard`/`Host_Reraise` status-core pattern), ADR-010 (per-platform parity; no sim-observable change), ADR-017 (SDL2 and SDL3 both; Windows SDL3-only; SDL2 audio stays C), ADR-019 (gates first; the full suite at phase exit).

## Objective

Make the Rust staticlib own the process entry point (`main`/`WinMain`), the SDL event/input pump, the `Sys_*` platform layer, the `PL_*` desktop-integration layer and the Windows UDP landrivers behind one new meson switch (`use_rust_platform`), verified against the C build by the existing differential harness (headless corpus, save/config/condump diffs, interop matrix incl. a new Windows UDP runtime leg) plus manual windowed checks on the dev box — while keeping the C-only build (`-Duse_rust=disabled`) buildable and in CI for the release-cycle soak the ROADMAP requires before any C file is deleted.

## Requirements and non-goals

- R1: every `Sys_*` (`sys.h`), `PL_*` (`platform.h`), `IN_*` (`input.h`) and `main_sdl.c` behaviour is preserved observably: argument handling (`COM_InitArgv`, `-dedicated`, `Harness_CheckArgs`), banner text and order on stdout, `Sys_Error` text (`"\nERROR-OUT BEGIN\n\n"` / `"\nQUAKE ERROR: "`), exit codes (`exit(1)` on error, `exit(0)` on quit), dedicated-server console semantics (256-char line buffer, `\r`/`\b` handling, `console_unqueryable` degrade), file-handle table semantics (`file_handle_t`, `Sys_MemFileOpenRead`, `Sys_DuplicateHandle`), `Sys_DoubleTime` source (`SDL_GetPerformanceCounter/Frequency`), timer resolution (`timeBeginPeriod(1)` on Windows), frame pacing (`sys_ticrate`, focus/minimised `SDL_Delay(16/32)`, harness fixed-dt and ticrate pacing), store-location discovery (Steam/GOG/EGS/Nightdive registry+manifest logic), `Sys_FindFirst/Next/Close` ordering and attribute mapping, key/scancode mapping (the `IN_SDL_ScancodeToQuakeKey` table and `buttonremap`), joystick/gamepad handling incl. `gamecontrollerdb.txt` loading order (basedir then userdir), and the SDL2/SDL3 differences already encoded in `in_sdl.c` vs `in_sdl3.c`.
- R2: the Windows UDP landriver (`net_wins.c`) ports onto the existing `quake-net::udp` core with a Windows `sys` arm; observable behaviour (WSAStartup/Cleanup pairing, `gethostbyname` name resolution incl. the `-ip` parameter, `FIONBIO` non-blocking sockets, `WSAECONNRESET` read → 0, `WSAEWOULDBLOCK` read → 0, local address enumeration, textual address forms) is identical; `net_win.c`'s driver table flips to the Rust landriver entries under `USE_RUST_NET`.
- R3: every existing gate (`run_corpus --check/--compare`, save/config/condump/capture/record/netreplay diffs, interop/physics matrices, formats corpus, sndhash, render corpus, ctest suite, fmt/clippy/deny/audit, bindgen-smoke, capi-signature parity, header checks) stays green after every milestone on every existing leg, including the C-only legs.
- R4: SDL2 and SDL3 both work with the Rust platform layer on Linux/macOS (the Linux harness job builds SDL2 for every leg); Windows stays SDL3-only (ADR-017); MinGW stays a C-only leg.
- R5: no `longjmp` ever crosses a Rust frame: every C callee that can `Host_Error`/`Sys_Error`-longjmp (`Key_Event`, `Char_Event`, `Cbuf_AddText`, `CL_Disconnect`, `Cvar_RegisterVariable`, `Host_Frame`, `Host_Init`, `Host_Shutdown`, cvar callbacks) is reached from Rust only through a `Host_Guard` trampoline, and every Rust entry point reachable from C that can observe a raise returns the ADR-009 `Raise` status which the C glue wrapper `Host_Reraise`s.
- R6: packaging (`Packaging/AppImage`, `Packaging/Windows`, mac archive job) is reproduced with `use_rust=enabled` (Rust `main`) once the Rust `main` lands; the harness jobs keep their `build-c` legs for the soak.
- NG1: no port of `q_thread_sdl.c` (not in the Phase 9 scope list; `std::thread` is used by Rust code, the C primitives stay for the C remnant — Phase 8 NG2 wording).
- NG2: no deletion of any C file in this phase's work sessions: the ROADMAP exit criteria require a release-cycle soak with C-only CI *before* the deletion list executes and `c-reference/final` is tagged. The deletion list and the soak record are captured at M8; the deletion itself is the soak-exit action (see D8 and the amendment log).
- NG3: no modernisation of the platform layer (no async runtime, no `winit`, no `SDL_MAIN_USE_CALLBACKS`); the SDL C API is called through `sdl3-sys`/`sdl2-sys` exactly as the C code does.
- NG4: no Linux goldens; no changes to simulation, parsing, the VM, save formats or the network protocol (R3 is the guard).
- NG5: `objc2` is not introduced: `pl_osx.m`'s two behaviours (window icon no-op; clipboard via `NSPasteboard`) are reproduced with `SDL_GetClipboardText` (see D5, accepted divergence recorded).

## Invariants and compatibility surfaces

- I1: the headless harness (`-headless`, `no_rendering`) exercises `main`, `Sys_*`, `COM_InitArgv`, the dedicated loop and the file layer; `Sys_SendKeyEvents` runs every client frame regardless of `no_rendering` (`main_sdl.c` has no gate around it), so the Rust event pump runs in the headless CI legs too, but the SDL window/input events themselves are never generated there. So `run_corpus.py --compare build-c build-rs-cplatform build-rs` observes the loop/pacing/`Sys_DoubleTime` port; windowed behaviour (focus, minimise, mouse/keyboard, gamepad, clipboard, icon, error dialog) needs manual dev-box checks recorded as such.
- I2: `Sys_Error` on a worker thread must not touch `host_parms->errstate`, must not longjmp, and writes through `houtput` on Windows (`sys_sdl_win.c:656-719`); the Rust port keeps the `Tasks_IsWorker()` branch verbatim.
- I3: `quakeparms_t` (`quakedef.h:224-234`) is already mirrored in `rust/quake-c-sys/src/host.rs:34-67` with `host_parms` extern; the Rust `main` owns the `static parms` and sets `host_parms` before `COM_InitArgv`.
- I4: `file_handle_t`/`sys_handles` (`sys_sdl.c`) are consumed only through `Sys_File*`; `Sys_MemFileOpenRead` memory ownership (`Mem_Free` on close) and `Sys_DuplicateHandle` sharing of the `FILE*` are preserved. `Sys_fopen`/`Sys_FileType`/`Sys_fseek`/`Sys_ftell`/`Sys_mkdir`/`Sys_FindFirst`… are called from the Rust side through `quake-c-sys` today — the Rust port must keep exporting them with the same C symbols (`check_capi_signatures.sh` parity).
- I5: `Sys_Error`/`Sys_Printf` are variadic; Rust stable cannot define c-variadic functions, so both keep a C wrapper in `Quake/sys_glue.c` that formats (`q_vsnprintf`) and calls `quake_rs_sys_error(const char *)` / `quake_rs_sys_printf(const char *)` — the `Con_Printf` precedent (`Quake/console_glue.c:287`).
- I6: `main_sdl.c` includes `<SDL3/SDL_main.h>` under SDL3 to get the inline `WinMain` shim (Windows GUI subsystem). With Rust `main`, Windows needs an exported `WinMain` — provided by Rust (`extern "system" fn WinMain`) which calls `SDL_RunApp(0, null, quake_main, null)`; non-Windows exports `#[no_mangle] extern "C" fn main(argc, argv)`. The staticlib is linked by Meson into `vkqr-engine` exactly as today; no cargo bin.
- I7: `net_win.c`/`net_bsd.c` driver tables reference the landriver structs by name (`net_wins`/`net_udp`); under `USE_RUST_NET` they must reference the Rust landriver entries already exported for Unix (`rust_udp4_*`/`rust_udp6_*`/`rust_udp_*` in `quake-capi/src/net_udp.rs`). The driver-table files stay C until the soak deletion (they are two small tables).
- I8: the meson PCH is `Quake/quakedef.h`; every remaining C TU includes it. It is **not** a Rust-side input (`rust/quake-c-sys/bindings_wrapper.h` uses explicit headers). Retiring it means removing 160 (M8 recount; 157 at planning) `#include "quakedef.h"` lines and the `c_pch` argument — a C-remnant refactor that changes nothing observable and can only be done once the C oracle is no longer built from the same tree. It is therefore scheduled with the soak deletion (D7), and M8 records the exact recipe.
- I9: ADR-009 residual: after M7, the remaining `setjmp` sites are (a) `Host_Glue_FrameInner`'s frame guard for the **C-platform** builds and (b) the `Host_Guard` trampoline itself, used by every glue TU as the only legal longjmp landing pad. Both are C-remnant machinery deleted with the soak exit; the Rust `main` loop owns frame-abort recovery via the `Raise` result of `Host_Guard(Host_Frame, …)`.
- Platforms/configs: Windows (clang-cl, MSVC, MinGW C-only, arm64), Linux (gcc, ASan), macOS (MoltenVK); SDL2 (Linux/macOS) and SDL3; configs `build-c`, `build-rs`, `build-rs-cplatform` (new), all existing `build-rs-c<module>` legs; packaging: AppImage docker, Windows NSIS scripts, mac archive.

## Migration authority

- Roadmap phase: **Phase 9** `[ ]` → `[~]` at M1; exit block at M8 with the soak/deletion record.
- ADRs: 001, 003, 004, 007, 009, 010, 017, 019 (constraints above); ADR-003 amendment at M1 (D2).
- C oracle: `build-c` (`-Duse_rust=disabled`) and the per-switch leg `build-rs-cplatform` (`-Duse_rust_platform=disabled`), plus `build-rs-cnet` for M2.
- Deferred/deletion restrictions: NG1/NG2; ADR-005 formatter gaps untouched; Phase 10 items untouched.

## Repository evidence

| Fact/dependency | Evidence | Confidence |
|---|---|---|
| Phase 9 scope/exit/deletes | `ROADMAP.md:270-287` | confirmed |
| `main_sdl.c` loops, banner, pacing, harness hooks | `Quake/main_sdl.c` (full read) | confirmed |
| `sys_sdl_win.c` API incl. DbgHelp trace, console input, registry/store discovery | `Quake/sys_sdl_win.c` (986 lines, full read) | confirmed |
| `sys_sdl.c` shared file-handle layer, `Sys_SelectFolder` (SDL3), `Sys_GetPrefPath`, `ChooseQuakeFlavor` | `Quake/sys_sdl.c` (458 lines) | confirmed |
| `sys_sdl_unix.c` (`Sys_Error` non-longjmp worker path at `:566-597`, userdir, Steam paths) | `Quake/sys_sdl_unix.c` | confirmed |
| `in_sdl.c`/`in_sdl3.c` differ only in SDL API names, text-input filter, gamepad IDs (`0` invalid in SDL3, `-1` in SDL2) | both files read | confirmed |
| `in_sdl.h` scancode table + `buttonremap` | `Quake/in_sdl.h:12-243` | confirmed |
| `pl_win.c`, `pl_linux.c`, `pl_osx.m` (icon, clipboard, error dialog) | files read | confirmed |
| `net_wins.c` = `net_udp.c` twin with WSA calls; `net_win.c`/`net_bsd.c` driver tables | files read; Phase 5 M7b note in `net_udp.rs` header | confirmed |
| `quake-net::udp` core is `cfg(unix)` (`lib.rs:20`), `socket2`/`libc` unix-only deps; `udp/sys.rs` uses `std::os::fd` | `rust/quake-net/Cargo.toml`, `src/udp/sys.rs` | confirmed |
| `quake-capi/src/net_udp.rs` is `#![cfg(unix)]` and exports `rust_udp*` landriver functions | file header | confirmed |
| `quake-platform` has only `snd_sdl3` behind feature `sdl3`; `sdl3 = "0.18.4"` via `sdl3::sys` | `rust/quake-platform/Cargo.toml`, `src/lib.rs` | confirmed |
| `quake-host` is a stub whose doc says it becomes the bin in Phase 9 | `rust/quake-host/src/lib.rs` | confirmed |
| Meson: `rust_feature_list` (`meson.build:413-470`, `sdl3` at 466), executable block with `c_pch: 'Quake/quakedef.h'`, `win_subsystem: 'windows'` | `meson.build:1195-1250` | confirmed |
| Linux harness job builds SDL2 for every leg (`-Duse_sdl3=disabled`) and is the only job running `interop_matrix.py` | `.github/workflows/build-linux.yml:71-151` | confirmed |
| Windows CI has no UDP runtime leg; mac harness uses SDL3 | `build-windows.yml:20-109`, `build-mac.yml:37-86` | confirmed |
| Packaging jobs all configure with `use_rust=disabled`; AppImage docker image has no Rust toolchain | `Packaging/AppImage/{run-in-docker.sh,docker/Dockerfile}`, `build-windows.yml` packaging job, `build-mac.yml` archive job | confirmed |
| ADR-003 pre-approved `socket2`, `libc`, `windows-sys` (Phase 5 M1 amendment) and lists `sdl2` as an expected direct dependency; `windows-sys` 0.61.2 already in `Cargo.lock` transitively | `docs/rust-migration/adr/ADR-003-dependency-policy.md:85-130` | confirmed |
| Raise/`Host_Reraise` glue pattern | `rust/quake-capi/src/keys.rs`, `Quake/keys_glue.c`, `Quake/host_glue.c` (`Host_Guard`/`Host_Reraise`, `Host_Glue_FrameInner`) | confirmed |
| `quakeparms_t` mirror + `host_parms` | `rust/quake-c-sys/src/host.rs:34-67` | confirmed |
| Variadic `Con_Printf` keeps a C wrapper | `Quake/console_glue.c:287` | confirmed |
| Baseline `build-c`/`build-rs` build green on the dev box (2026-09-14) | ninja run exit 0 | confirmed |

## Architecture and decisions

### D1: One switch, `use_rust_platform`; the Rust `main` is exported from the staticlib

- New meson option `use_rust_platform` (feature, `auto` follows `use_rust`, effective only with `use_rust`) → cargo feature `platform` on `quake-capi` (→ `quake-platform/platform`). It gates, in Meson, dropping `main_sdl.c`, `sys_sdl.c`, `sys_sdl_unix.c`/`sys_sdl_win.c`, `pl_*.{c,m}`, `in_sdl.c`/`in_sdl3.c` from the source list (each replaced at its milestone; between milestones the switch drops only what has landed) and defines `USE_RUST_PLATFORM` for the glue TUs.
- Build orchestration: **Meson retained (thin)**. The C remnant after Phase 9 is still >150k lines (codecs, mimalloc, miniz, stb, shaders/resource compile, packaging, four toolchains); cargo+`cc` would re-implement `meson.build`'s option matrix for no gain. The Rust `main` is a `#[no_mangle]` export from the `quake_rs` staticlib, linked by Meson into `vkqr-engine` as today; `quake-host` stays a library crate (doc updated). Recorded as the ROADMAP's "decide by remnant size" outcome at M8.
- CI leg `build-rs-cplatform` (`-Duse_rust_platform=disabled`) on all three harness jobs; MinGW/arm64 legs stay C-only (`use_rust=disabled`) as today.

### D2: Dependencies (ADR-003 introduction bar, recorded in the ADR-003 amendment at M1)

| Crate | License | Use | Notes |
|---|---|---|---|
| `sdl2` (with `sdl2-sys`) | MIT | `quake-platform` feature `sdl2`: SDL2 event/input/timer/clipboard/messagebox via `sdl2::sys` | on ADR-003's expected-direct list; no `bundled`/`raw-window-handle` features, so the Linux SDL2 harness leg links the distro SDL2 as the C code does |
| `windows-sys` 0.61 | MIT OR Apache-2.0 | `quake-net` (WinSock, IP helper), `quake-platform` (console, registry, DbgHelp, shell/known folders, COM, clipboard, messagebox, timers, threading) | pre-approved (Phase 5 M1 amendment); already transitive in the lock file; features enumerated per crate |
| `socket2` 0.6 | MIT OR Apache-2.0 | made unconditional in `quake-net` (Windows arm of `udp::sys`) | pre-approved |

`cargo deny check` and `cargo audit` must stay clean; `sdl2`/`sdl3` features are mutually exclusive, enforced with `compile_error!` in `quake-platform`.

### D3: `net_wins.c` is a Windows arm of the existing `quake-net::udp` core (M2)

- `quake-net/src/udp/sys.rs` gains `#[cfg(windows)]` implementations of the same `sys` API (`INVALID`, socket open/close, non-blocking, recv/send, `strerror` via `WSAGetLastError`+`FormatMessageW`, local-address enumeration, `gethostbyname`); `SysSocket` width must match `SOCKET` on Windows (verify the `quake-types::net` definition at M2). `WSAStartup` in `init`, `WSACleanup` in `shutdown`, mirroring `net_wins.c`.
- `quake-capi/src/net_udp.rs` drops `#![cfg(unix)]`; `net_win.c`'s table references the Rust entries under `USE_RUST_NET`; Meson drops `net_wins.c` under `use_rust_net` on Windows (as it drops `net_udp.c` on Unix today).
- Verification: a **Windows UDP runtime CI leg** (`interop_matrix.py` on the Windows harness job) is added so the deferral reason from Phase 5 M7b is closed, plus a local two-process Windows session between `build-c` and `build-rs`.

### D4: Input → `quake-platform::input` with `sdl2`/`sdl3` backend modules (M3)

- `input/mod.rs` (backend-neutral: `IN_Init/Shutdown/Commands/Move/ClearStates/Activate/Deactivate/…`, joystick axis math, dead-zone/easing functions, `gamecontrollerdb.txt` loading order), `input/scancode.rs` (the `in_sdl.h` table as a `match` on the SDL scancode integer — identical for SDL2/SDL3 because both use USB HID scancode values), `input/sdl2.rs`/`input/sdl3.rs` (event pump, gamepad handling, mouse filter).
- Cvars (`joy_*`, `in_debugkeys`, …) stay C-owned statics in `Quake/in_sdl_glue.c` (ADR-007 dual-view as every Phase 6–8 glue); Rust reads them through `quake-c-sys` externs; registration via a `Host_Guard` trampoline.
- Raise-capable callees (`Key_Event`, `Key_EventWithKeycode`, `Char_Event`, `Cbuf_AddText`, `CL_Disconnect`, cvar callbacks, `Con_Mousemove`) are reached through `Host_Guard` trampolines defined in the glue; `IN_SendKeyEvents`/`IN_Commands`/`IN_Move`/`IN_Init` glue wrappers `Host_Reraise` the `Raise` returned by the Rust cores. `SDL_EVENT_QUIT` → `CL_Disconnect` (guarded) then `Sys_Quit` (never returns).

### D5: `PL_*` → `quake-platform::pl` (M4)

- `pl_win.rs` via `windows-sys` (icon via `SetClassLongPtrW` on the HWND from the SDL3 window properties, `OpenClipboard`/`GetClipboardData(CF_TEXT)`, `MessageBoxA`); `pl_sdl.rs` for Linux/macOS via `SDL_SetWindowIcon` (Linux embedded icon data as today), `SDL_GetClipboardText`, `SDL_ShowSimpleMessageBox`. `pl_osx.m`'s `NSPasteboard` read is replaced by `SDL_GetClipboardText` (**accepted divergence**: same clipboard through SDL's Cocoa backend; recorded in the amendment log), so no `objc2` (NG5) and no kept ObjC stub.
- `PL_GetClipboardData` allocates with `Mem_Alloc` (caller `Mem_Free`s) exactly as C.

### D6: `Sys_*` → `quake-platform::sys` (M5) and `main` → `quake-platform::main` (M6)

- `sys/handles.rs` (the `sys_sdl.c` table with the same index/free semantics, `FILE*` I/O via the C runtime so `Sys_DuplicateHandle` sharing is preserved), `sys/win.rs`, `sys/unix.rs`, `sys/common.rs` (`Sys_GetPrefPath`, `Sys_MessageBoxWarning`, `Sys_SelectFolder` under SDL3, `ChooseQuakeFlavor`).
- `Sys_Error`/`Sys_Printf` C wrappers in `Quake/sys_glue.c` (I5); `Sys_Error` Rust core: `errstate` bump unless worker, `Sys_DebugBreak`, stack trace unless in debugger, `PR_SwitchQCVM(NULL)`, text output, `PL_ErrorDialog` or console + 3 s delay, `exit(1)`.
- Windows DbgHelp stack trace via `windows-sys` `Win32_System_Diagnostics_Debug`, output formats verbatim; MSYS2 DWARF offset branch kept under `cfg(target_env = "gnu")`.
- `main`: `quake_main(argc, argv)` does exactly `main_sdl.c`'s sequence; the frame loop calls `Host_Guard(Host_Frame_Trampoline, &time)` and inspects the `Raise` (frame abort → continue loop, as `Host_Glue_FrameInner`'s setjmp did). `Host_Init`/`Sys_Init` likewise guarded. Windows: `WinMain` → `SDL_RunApp`. `main_sdl.c` is dropped from the build under `use_rust_platform`; `USE_RUST_PLATFORM` makes `Host_Glue_FrameInner` a plain call (no setjmp) — the frame guard lives in the Rust loop.
- Packaging flip (R6) lands with M6: AppImage Dockerfile gains rustup (1.97.1 pinned, `cbindgen`), `run-in-docker.sh` configures `-Duse_rust=enabled`; Windows packaging job and mac archive job configure `-Duse_rust=enabled`; harness jobs keep `build-c`.

### D7: Error-path end state (M7) and `quakedef.h` (M8) are scoped to what the C oracle permits

- ROADMAP says "last `setjmp`/`longjmp` removed". While the C oracle is built from the same tree and 50+ glue TUs use `Host_Guard`, the trampoline pair must remain. Phase 9's end state is: (a) the Rust `main` loop is the sole owner of frame-abort recovery (no setjmp in `Host_Glue_FrameInner` under `USE_RUST_PLATFORM`); (b) `HostError` (`quake-host::error`) is the typed result the Rust loop consumes; (c) a committed **residual setjmp inventory** (grep-generated, per TU) with each site mapped to "deleted with soak exit" or "converted at Phase 10"; (d) every `Host_Guard` use from Rust-owned callers inventoried. Recorded as an amendment to the ROADMAP exit wording (Phase 7/8 precedent for deferring deletions).
- `quakedef.h`: same reasoning (I8): retirement recipe + PCH removal recorded at M8, executed with the soak deletion. `libquake_c.a` is not materialised (Meson thin, D1).

### D8: Soak and deletion protocol (M8 record)

- After M6 lands on `master` (with packaging flipped), the release-cycle soak begins: C-only legs stay in CI; any C-vs-Rust divergence found is fixed in Rust. Soak exit = one tagged release built from Rust `main` on all three OSes + green harness matrix. Then, as a separate PR: tag `c-reference/final`, delete the Phase 9 list, execute the `quakedef.h` recipe, retire C-only CI. M8 writes this protocol into the ROADMAP exit block and the amendment log so the deletion PR needs no new plan.

## Change boundary

### Expected to change

- New: `rust/quake-platform/src/{input/,pl/,sys/,main.rs}`, `rust/quake-capi/src/{input.rs,pl.rs,sys.rs,platform_main.rs}`, `Quake/{in_sdl_glue.c,pl_glue.c,sys_glue.c}`, `Quake/net_glue.c` additions, Windows UDP CI leg wiring, ADR-003 amendment, ROADMAP Phase 9 status, this plan.
- Modified: `meson_options.txt`, `meson.build`, `rust/quake-{capi,platform,net,host,c-sys}/Cargo.toml`, `rust/Cargo.lock`, `rust/quake-net/src/{lib.rs,udp/sys.rs}`, `rust/quake-capi/src/{lib.rs,net_udp.rs}`, `Quake/net_win.c` (table under `USE_RUST_NET`), `Quake/host_glue.c` (`USE_RUST_PLATFORM` frame guard), `cbindgen.toml`/`check_capi_signatures.sh` (new exports), `.github/workflows/build-{linux,windows,mac}.yml`, `Packaging/AppImage/*`, `Misc/harness/README.md`.

### Must not change without plan amendment

- Any simulation/parse/VM/protocol/save/config code (R3/NG4); `Quake/tasks*.c`, `q_thread_sdl.c` (NG1); the SDL2 audio C backend (ADR-017); the C-only build's behaviour; the deletion list (NG2); ADR texts other than the ADR-003 amendment and the ADR-009/ROADMAP exit-wording amendment recorded here.

## Acceptance matrix

| ID | Acceptance criterion | Verification/gate | Status |
|---|---|---|---|
| AC1 | `use_rust_platform` switch + `platform`/`sdl2`/`sdl3` features: `build-rs-cplatform` configures, builds and passes every gate identically to `build-rs`; `cargo check --features platform,sdl2` compiles on the dev box; CI legs added to all three harness jobs | meson configs + `run_corpus.py --compare` + CI | local gates green (M1, 2026-09-14); CI legs not run locally |
| AC2 | ADR-003 amendment for `sdl2`/`windows-sys`/`socket2`; `cargo deny check` + `cargo audit` clean | `rust.yml` `lint`/`audit` | local `cargo deny check` clean (M1, 2026-09-14); `cargo audit` CI job not run locally |
| AC3 | Windows UDP landriver in Rust: `build-rs` on Windows links no `net_wins.c`; local two-process session C↔Rust works; Windows CI `interop_matrix.py` leg green | M2 targeted + CI | local gates green (M2, 2026-09-15: `build-rs/build.ninja` has no `net_wins` rule, `interop_matrix.py` IPv4 PASS incl. Csv/Rcl and Rsv/Ccl); Windows CI interop step added, not run locally |
| AC4 | Input in Rust (SDL2 + SDL3): scancode table unit test; `run_corpus.py --compare` identical; manual windowed check (keys, mouse, wheel, console text, gamepad connect/disconnect, quit) on the dev box | M3 targeted + manual | done (Windows/SDL3: keys, mouse, wheel, console text, quit; gamepad and the SDL2 arm windowed: not run) |
| AC5 | `PL_*` in Rust: icon/clipboard/error dialog manual check on Windows; Linux/macOS via CI build only (behaviour manual-not-run, recorded) | M4 targeted + manual | met on Windows for icon + `Sys_Error` dialog (M4); clipboard paste manual-not-run: retried at M8, the game window still cannot take the foreground while the desktop session owns it, and a PostMessage-only variant (no foreground change) does not reach SDL's keyboard path (`qconsole.log` shows no console input) — covered instead by the STOP B desk-check of `pl_win.c` vs `pl/win.rs` (`MAX_CLIPBOARDTXT`, `GlobalSize + 1` chop, `q_strlcpy`); Linux/macOS pending CI |
| AC6 | `Sys_*` in Rust: `run_corpus --compare`, `save_diff`, `config_diff`, `condump_diff` identical `build-c` vs `build-rs`; dedicated console manual check; `Sys_Error` path manual check (stack trace text, dialog, exit code 1) | M5 targeted + manual | met on Windows (M5, 2026-09-15): four harness diffs identical build-c vs build-rs, dedicated stdout identical modulo exe stamp, `Sys_Error` dialog + trace + exit 1; Linux/macOS arms pending CI (compile-checked with the Linux cross target only) |
| AC7 | Rust `main`/`WinMain`: `build-rs` links without `main_sdl.c`; banner/exit codes identical; every gate identical; packaging jobs flipped to `use_rust=enabled` | M6 targeted + CI + packaging jobs | met on Windows (M6, 2026-09-15): `build-rs` links `WinMain` without `main_sdl.c` (GUI subsystem, `WinMainCRTStartup`), the CI package shape (clang-cl, release and debug) links and runs, dedicated stdout identical to `build-c` modulo exe stamp, harness gates identical; packaging jobs flipped — CI verification pending push |
| AC8 | Error-path end state: no setjmp in the Rust-main frame path; residual inventory committed; `HostError` consumed by the loop | grep inventory + M7 targeted | met (M7: `setjmp_inventory.py --check` OK, 40 sites classified; `recover` in `main_sdl.rs`; `run_corpus --compare` 8/8; manual `changelevel nonexistent` recovery on both builds) |
| AC9 | Phase exit record: ROADMAP exit block with soak protocol, `quakedef.h` recipe, build-orchestration decision, deletion list | docs review | met (M8: ROADMAP Phase 9 status / evidence / deviations / soak + deletion protocol block; PLAN.md and harness README aligned) |

## Milestones

Graph: `M1 → M2 → M3 → M4 → M5 → M6 → M7 → M8` (serial; M2 is independent of M3–M6 but is first because it closes the Phase 5 deferral and its CI leg is needed by later legs). STOP A (compatibility-reviewer) after M3; STOP B after M6.

### M1 — Gates first (no porting)

- Scope: D1 switch + cargo features `platform` (capi → platform), `sdl2` in `quake-platform` (crate `sdl2` added, `compile_error!` on both features), Meson maps `sdl2`/`sdl3` feature by `use_sdl3`; CI legs `build-rs-cplatform` on the three harness jobs; ADR-003 amendment (D2); ROADMAP Phase 9 `[~]`; this plan committed.
- Files: `meson_options.txt`, `meson.build`, `rust/quake-capi/Cargo.toml`, `rust/quake-platform/{Cargo.toml,src/lib.rs}`, `rust/Cargo.lock`, workflows, ADR-003, ROADMAP, plan.
- AC: AC1, AC2.
- Targeted: `meson setup build-rs-cplatform -Duse_rust=enabled -Duse_rust_platform=disabled` + `ninja`; `ninja -C build-rs`/`build-c`; `cargo check -p quake-capi --features platform,sdl2` (SDL2 arm compiles without an SDL2 install — `sdl2-sys` emits link directives only); `cargo fmt/clippy/test/deny`; `run_corpus.py --compare build-c build-rs-cplatform`.
- Landmines: `sdl2-sys` default features must not pull `pkg-config`/`bundled`; the Linux harness leg links SDL2 from apt, matching C.

### M2 — `net_wins.c`/`net_win.c`/`net_bsd.c` → Rust Windows UDP arm + Windows UDP CI leg

- Scope: D3. `quake-net`: `socket2` unconditional, `windows-sys` (`Win32_Networking_WinSock`, `Win32_NetworkManagement_IpHelper`, `Win32_Foundation`), `udp/sys.rs` Windows arm; `quake-capi/net_udp.rs` on all targets; `net_win.c` table flips under `USE_RUST_NET`; Meson drops `net_wins.c` on Windows under `use_rust_net`; Windows harness job runs `interop_matrix.py` (`build-c`↔`build-rs`).
- AC: AC3.
- Targeted: `ninja -C build-rs` (Windows) links; two-process local session (`build-c -dedicated` ↔ `build-rs` client and vice-versa) via `interop_matrix.py` run locally; `run_corpus.py --compare build-c build-rs-cnet build-rs`; `cargo test -p quake-net` on Windows; `check_capi_signatures.sh`.
- Landmines: `SysSocket` width on Windows (`SOCKET` = `usize`), `INVALID_SOCKET` = `!0`; `WSAECONNRESET` on `recvfrom` must return 0 not −1 (`net_wins.c` behaviour); `ioctlsocket(FIONBIO)`; `interop_matrix.py` may assume POSIX process/port behaviour — read before wiring.

### M3 — `in_sdl.c`/`in_sdl3.c`/`in_sdl.h` → `quake-platform::input`

- Scope: D4 in full; `Quake/in_sdl_glue.c` (cvars, trampolines, `Host_Reraise` wrappers); Meson drops the three input files under `use_rust_platform`; `quake-c-sys` externs for `Key_*`, `Char_Event`, `Con_Mousemove`, `VID_*`, `S_BlockSound/S_UnblockSound`, `vid` struct fields used (`vid.width/height/restart_next_frame`), `key_dest`, `cl.paused`.
- AC: AC4.
- Targeted: scancode unit test; `run_corpus.py --compare` (headless pump path); manual windowed check list (AC4); `check_capi_signatures.sh`; cargo gates; STOP A review.
- Landmines: `vid` is a C struct with SDL-free layout (`vid.h`) — mirror only the fields touched or use accessor trampolines; `Cvar_FindVar("scr_conscale")->callback(NULL)` — a cvar callback may raise → trampoline; SDL2 `SDL_TEXTINPUT` vs SDL3 `SDL_EVENT_TEXT_INPUT` string ownership; `IN_FilterMouseEvents` filter function pointer identity check (`SDL_GetEventFilter` compares against our own `extern "C"` fn — keep one static fn).

### M4 — `pl_win.c`/`pl_linux.c`/`pl_osx.m` → `quake-platform::pl`

- Scope: D5; Meson drops `pl_*` under `use_rust_platform` (the `.m` file's `objc` language add stays for the C-only build).
- AC: AC5.
- Targeted: build on Windows; manual: window icon present, clipboard paste in console, `Sys_Error` dialog; cargo gates; CI build on Linux/macOS.
- Landmines: `pl_win.c` obtains the HWND through SDL3 window properties (Windows is SDL3-only); the Windows icon resource ID comes from the `.rc` compiled by Meson.

### M5 — `sys_sdl.c`/`sys_sdl_unix.c`/`sys_sdl_win.c` → `quake-platform::sys`

- Scope: D6 sys part; `Quake/sys_glue.c` (variadic wrappers, `PR_SwitchQCVM` extern); Meson drops the three sys files under `use_rust_platform`; the Rust side's existing use of `Sys_*` through `quake-c-sys` re-verified.
- AC: AC6.
- Targeted: `run_corpus --compare`, `save_diff`, `config_diff`, `condump_diff`; dedicated console manual; `Sys_Error` manual; cargo gates.
- Landmines: `Sys_fopen` wide-char path; `Sys_Init`'s AllocConsole ordering relative to the `Sys_Printf` banner; `Sys_DoubleTime` uses `counter_freq` set in `Sys_Init` (float division order preserved); `Sys_ConsoleInput` static buffer wrap `& 0xff`; `Sys_GetGOGQuakeDir` registry key set exactly; the MSVC `#pragma comment(linker, "/manifestdependency")` in `sys_sdl.c` must move to a TU that remains in the build (`sys_glue.c`).

### M6 — `main_sdl.c` → Rust `main`/`WinMain`; packaging flipped

- Scope: D6 main part; `Quake/host_glue.c` `USE_RUST_PLATFORM` frame guard change; Meson drops `main_sdl.c`; `quake-host` doc updated (library crate, `HostError` consumed by the loop); AppImage docker + `run-in-docker.sh`, Windows packaging job, mac archive job to `use_rust=enabled`.
- AC: AC7.
- Targeted: `ninja -C build-rs` (Windows, GUI subsystem links `WinMain`); banner diff (`build-c` vs `build-rs` stdout, `-dedicated -headless`); every harness gate `--compare`; `save_diff`; cargo gates; STOP B review; packaging jobs verified via CI (cannot run Docker/NSIS locally — record).
- Landmines: with Rust `WinMain` calling `SDL_RunApp`, no C TU may include `SDL_main.h`'s `main` redefinition (grep); `atexit(Sys_AtExit)` order; `Harness_CheckArgs` before `Sys_InitSDL`; `sys_ticrate`/`cls.timedemo`/`harness_*` externs.

### M7 — Error-path end state (ADR-009 scoped)

- Scope: D7; residual setjmp inventory (`docs/rust-migration/setjmp-inventory.md`), `HostError` type consumed by the loop (`quake-host/src/error.rs`), ROADMAP exit wording amendment recorded here and in the amendment log.
- AC: AC8.
- Targeted: grep inventory; `run_corpus --compare`; a manual `Host_Error` (e.g. `map nonexistent`) in the Rust-main build recovers to the console; cargo gates.

### M8 — Phase exit record

- Scope: D8; ROADMAP Phase 9 exit block (`[~]` stays until soak exit; record what is complete, the soak protocol and the deletion PR recipe incl. `quakedef.h`/PCH removal and the Meson thin decision); plan evidence table completed; `Misc/harness/README.md` updated with the Windows UDP leg.
- AC: AC9.
- Targeted: docs review; final verification list below.

## Final verification

- Formatting: `cargo fmt --all -- --check`; `./format.sh` for touched C.
- Rust: `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked`; `cargo deny check`; `cargo audit` (CI).
- Meson (Windows dev box, PowerShell for vcvars per memory note): `build-c`, `build-rs`, `build-rs-cplatform`, `build-rs-cnet`.
- Harness: `run_corpus.py --check` (windows-x86_64 goldens) and `--compare` (C vs each leg), `save_diff`, `config_diff`, `condump_diff`, `interop_matrix.py` (Windows local + CI), `check_capi_signatures.sh`, `check_headers.sh`, `check_ctest_symbols.sh`.
- CI workflows affected: `build-linux.yml`, `build-windows.yml`, `build-mac.yml`, `rust.yml`.
- Cannot run locally: Linux/macOS/MinGW/arm64 legs, AppImage docker, NSIS packaging, SDL2 runtime — push after each milestone and read CI.

## Risks, assumptions, and open questions

- RA1: `sdl2` crate API drift vs the system SDL2 on the Linux runner — only `sdl2::sys` raw bindings are used, and only symbols that exist in 2.0.x. Mitigation: no `bundled`; if the CI leg fails on a missing symbol, drop to the symbol set of the C code (which builds against the same library).
- RA2: Windows GUI-subsystem entry point: the linker expects `WinMain` when `win_subsystem: 'windows'`; SDL3's `SDL_main.h` currently supplies it inline in `main_sdl.c`. Rust exporting `WinMain` from a staticlib is standard (`#[no_mangle] pub extern "system" fn WinMain`); the linker's undefined-symbol search for the entry point pulls it from the archive — verify by linking at M6.
- RA3: `sdl3-sys` may not expose `SDL_RunApp` (declared in `SDL_main.h`). If missing, declare it via `extern "C"` in `quake-platform` (documented unsafe).
- RA4: the DbgHelp trace under `windows-sys` requires `SYMBOL_INFO` with a trailing name buffer — use a byte buffer exactly as the C code does.
- RA5: `interop_matrix.py` Windows compatibility (process spawning, port reuse semantics differ) — read the script before M2.
- RA6: the AppImage docker image build adds a rustup download — acceptable; `linuxdeploy` runs with `NO_STRIP=1` already.
- Assumption A1: Windows CI runners allow loopback UDP (GitHub-hosted runners do).
- Assumption A2: the harness's `-headless` path still calls `IN_Init`/`SDL_PollEvent` — verified by reading `main_sdl.c` (no `no_rendering` gate around `Sys_SendKeyEvents`); the Rust pump therefore runs in CI legs too.

## Plan amendment log

- 2026-09-14 (plan approval): ROADMAP "last `setjmp`/`longjmp` removed" is executed as D7's scoped end state (Rust-main loop owns frame recovery; residual inventory tied to the soak-exit deletion) — the trampoline pair cannot be deleted while the C oracle is built from the same tree (Phase 7/8 precedent for deferring deletions).
- 2026-09-14: `quakedef.h` retirement and `libquake_c.a` are recorded as the soak-exit deletion PR's recipe (I8, D7); Meson is retained thin (D1) — this is the ROADMAP's "decide by remnant size" outcome.
- 2026-09-14: `pl_osx.m` NSPasteboard read replaced by `SDL_GetClipboardText` (D5, accepted divergence — same clipboard through SDL's Cocoa backend); no `objc2`, no ObjC stub kept.
- 2026-09-14: `q_thread_sdl.c` is a Phase 9 non-goal (NG1) — not in the ROADMAP's Phase 9 scope or deletion list.
- 2026-09-15 (M2): the Windows `udp::sys` arm is `windows-sys` only (`Win32_Networking_WinSock`); `socket2` stays `cfg(unix)` (it would route socket creation through `WSASocketW` and hide the `WSAStartup`/`WSACleanup` refcount net_wins.c reproduces). ADR-003 amended accordingly. `strerror` is the in-tree `wsaerror.h` table; `getaddrinfo`/`freeaddrinfo` are linked directly instead of net_wins.c's `LoadLibrary` probe (the SDK floor already has them).
- 2026-09-15 (M2): the Windows landriver lives in a separate `quake-capi/src/net_wins.rs` (`#![cfg(windows)]`) rather than un-`cfg`-ing `net_udp.rs`: net_wins.c splits `GetNameFromAddr` per family, honours `masked` in `AddrToString`, has no unknown-family branch in `AddrCompare`/`GetSocketPort`/`SetSocketPort`, and leaves `*addr` untouched when `WINS_Read` fails. Those semantics are pure `*_wins` helpers in `quake-net::udp`, unit-tested on every target and differential-tested on Windows against a `net_wins.c` c_ref (`quake-ctest/tests/net_wins_differential.rs`). Two `// COMPAT:` deviations: `-ip6` resolution takes the resolved `sin6_addr` (the C code memcpy's 28 bytes out of a 16-byte `in6_addr`), and `WINIPv4_GetAddresses` reports an empty name when `gethostname` fails instead of reading an uninitialised buffer.
- 2026-09-15 (M2): plan premise correction — `net_udp.c` was still unconditionally in the unix Meson source list; Meson now gates both `net_udp.c` and `net_wins.c` under `use_rust_net` (D3), not `use_rust_platform`.
- 2026-09-15 (M2): the Windows SDK defines `IPPROTO_IPV6` as an enumerator, not a macro, so `#ifdef IPPROTO_IPV6` in `net_win.c`/`net_wins.c` is false on MSVC/clang-cl: the C oracle ships one landriver ("IPv4 UDP Initialized" only; no `WINIPv6_*` symbols in `build-c`'s `net_wins.c.obj`). The Rust arm of `net_win.c` keeps the same guard so `build-rs` matches; `rust_udp6_*` exports are therefore unreferenced on that toolchain, `quake-ctest` probes `const.IPPROTO_IPV6_defined` and the differential test skips AF_INET6 cases when it is 0, and the IPv6 interop leg is not runnable on Windows against the C oracle (the C/C cell cannot bind `::`). Enabling IPv6 on MSVC is a post-deletion (Phase 10) behaviour change, not a port-time one. MinGW headers are unverified locally. Also: every hand-written prototype in `cbindgen.toml` `after_includes` must be in the `[export] exclude` list or cbindgen emits a second prototype with an unresolved `QSockAddr` type.

- 2026-09-15 (M3): only the three `IN_*` entry points that can raise (`IN_Init`, `IN_SendKeyEvents`, `IN_Commands`) get status-returning `quake_rs_in_*` cores plus `Host_Reraise` wrappers in `in_sdl_glue.c`; `IN_Move` and the rest of `input.h` cannot raise (they touch cvars/`cl` state and SDL only) and are exported under their C names directly from `quake-capi/src/in_sdl.rs`. `Con_Mousemove` is a plain non-raising Rust export under `use_rust_console`, so it is called directly rather than through a trampoline (D4 listed it among the guarded callees). The SDL2/SDL3 backend is selected with `#[path = "sdl3.rs"] mod backend;` so the module name does not shadow the `sdl3`/`sdl2` extern crates.
- 2026-09-15 (M3): `powf` was added to the `quake-c-sys::libm` shim (joystick easing); the `K_*` key constants the pump emits are spelled locally in `quake-platform::input::keys` (they are `keys.h` `#define`s, not exported symbols); gamepad add/remove/remap is handled inside the backend pump and `joy_active_controller` is no longer a global C symbol (nothing outside `in_sdl.c` referenced it); the twelve `joy_*` cvars and `in_debugkeys` become non-`static` in the glue TU because Rust reaches them by extern.
- 2026-09-15 (M3): the `IN_*` family is declared by hand in `cbindgen.toml` `after_includes` under `_QUAKE_INPUT_H` (excluded from generation, like the Phase 6-8 engine-header families) and `input.h` is added to the first `check_capi_signatures.sh` TU so the hand copies are drift-checked. `quake-platform`'s input unit tests (`scancode` + easing/dead-zone/button-map) link `-lSDL3`/`-lSDL2` because the crate depends on the sys crate, so they are local-only (`RUSTFLAGS=-L native=Windows/SDL3/lib64`); `rust.yml` runs clippy for the `platform,sdl3`/`platform,sdl2` arms and `cargo test --workspace` without `platform`.
- 2026-09-15 (M3): the harness `-headless` flag runs the client without video/audio/input (`harness.h:26`), so the corpus compare proves link/init parity only; the event pump itself is verified by the manual windowed check. The manual check is driven by a PowerShell script (`SendKeys`/`mouse_event` into the game window, `WM_CLOSE` for the quit path) — it has to own the foreground and aborted twice when another application took focus mid-run, so the C-oracle and Rust runs used for evidence are the ones whose log shows every marker.
- 2026-09-15 (STOP A): `quake-platform::input` declares the `cl`/`cls`/`r_refdef` externs itself (`quake-c-sys` cannot name the `quake-types` mirrors without a dependency cycle) — this extends the `quake-capi/src/view.rs` precedent from Phase 7 to a non-capi crate and is the only ADR-011 exception in the platform crate.
- 2026-09-15 (STOP A): `compatibility-reviewer` review of M1-M3 found no blockers; verdict "proceed to M4". Fixed before M4: the joystick view-angle update now runs in `f64` and rounds to `f32` once, matching `in_sdl.c:569-570`'s promoted compound assignment (F1, parity); `JOY_AXISSTATE` is read per axis through `addr_of!` instead of copying the whole struct (F2); `load_mappings` sizes its path buffer with `quake_c_sys::MAX_OSPATH` instead of a hardcoded 1024 (F3); `filter_mouse_motion` carries its `# Safety` section (F5). F6 (`q_min` NaN semantics) was a mention only; the Rust `min` is the C macro's comparison, so nothing changed.
- 2026-09-15 (M4): `pl_osx.m` was never in the Meson source list (only `pl_win.c` on Windows, `pl_linux.c` everywhere else), so the "`objc` language add stays" wording in the M4 scope is moot and the macOS C oracle already uses the SDL clipboard path; the D5 divergence therefore only concerns the retired Makefile/Xcode builds. Rust keeps the same split: `pl/win.rs` (`windows-sys`) on `cfg(windows)`, `pl/sdl.rs` elsewhere. Meson gates `pl_win.c`/`pl_linux.c` under `use_rust_platform` after the option is defined (line ~556).
- 2026-09-15 (M4): `SetClassLongPtrA` rather than D5's `SetClassLongPtrW` — the C build is not compiled with `UNICODE`, and for `GCLP_HICON` the two are the same store; `CF_TEXT` is spelled as a local `const` (it lives in `Win32_System_Ole`, a whole feature for one constant); the HWND comes from `SDL_GetPointerProperty (SDL_GetWindowProperties (VID_GetWindow ()), SDL_PROP_WINDOW_WIN32_HWND_POINTER)` under `sdl3` and is null under the `sdl2` arm on Windows (a non-configuration in C too: `pl_win.c` is SDL3-only there). `PL_GetClipboardData` returns null when the clipboard is empty and otherwise a `Mem_Alloc`ed copy capped at `MAXCMDLINE` (256) via `q_strlcpy`, both arms sharing `copy_clipboard`; the SDL arm keeps `pl_linux.c`'s leak of the `SDL_GetClipboardText` buffer (never `SDL_free`d there either — parity over hygiene, Phase 10 candidate).
- 2026-09-15 (M4): the Linux icon blob is not transcribed by hand: `quake-platform/build.rs` parses `Quake/qs_bmp.h` (3014 comma-separated bytes) into `OUT_DIR/qs_bmp.bin` and `pl/sdl.rs` `include_bytes!`s it, so the header stays the single source until Phase 10 deletes it. The `sdl.rs` arm is type-checked on the Windows dev box with `--target x86_64-unknown-linux-gnu` (std installed via `rustup +1.97.1 target add`), since it never compiles on `cfg(windows)`.
- 2026-09-15 (M4): the manual AC5 checks were re-scripted without foreground keystroke injection because another application (the user's browser) held the foreground during the run — the PID-gated `SendKeys` script aborted before `Ctrl+V` each time, and on one earlier run the gate passed just before a focus change so a stray `Ctrl+V` may have reached the browser (no effect observed; recorded honestly). Icon: `GetClassLongPtr (hwnd, GCLP_HICON)` read from outside the process is non-zero on `build-c`, `build-rs-cplatform` and `build-rs`; `WM_CLOSE` → exit 0 with a 61-line flushed log on all three. `Sys_Error` dialog: `-basedir <empty dir>` makes `W_LoadWadFile` raise; a `#32770` window titled `Quake Error` appears for `build-c` and `build-rs`, is dismissed with `WM_CLOSE`, and both exit 1 with the same `QUAKE ERROR: W_LoadWadFile: couldn't load gfx.wad` on stdout. Clipboard paste (`Key_Console` → `PL_GetClipboardData`) needs a real `Ctrl+V` in the focused game window and is recorded as manual-not-run; the gated script (`m4_manual.ps1`) is to be re-run at final verification when the foreground is free.
- 2026-09-15 (M6): the Rust entry is split in two: `quake-platform::main_sdl` carries `main_sdl.c`'s body (`quake_main`, the SDL init and both frame loops) and `quake-capi::entry` only spells the symbol each C runtime looks for — `main` off-Windows, `WinMain` (`extern "system"`, forwarding to `SDL_RunApp` through `main_sdl::run_app` so `quake-capi` keeps no SDL dependency) on Windows+SDL3, `SDL_main` on Windows+SDL2 (what `SDL2main.lib`'s `WinMain` calls). The module is `main_sdl`, not D6's `main`: rustc's `special_module_name` lint (`-D warnings`) rejects a library module named `main`. `entry` is compiled under `platform` and `not(test)` so the crate's own test binary keeps its harness `main`. The symbols are listed in `cbindgen.toml`'s exclude list; `quake_rs.h` must never declare them.
- 2026-09-15 (M6): raise policy of the Rust loop. `main_sdl.c`'s `Host_Init`/`Sys_Init` calls ran outside any `setjmp` (a `Host_Error` during them was a jump to a dead buffer); the Rust loop guards them with `SysGlue_SysInit`/`SysGlue_HostInit` and turns a non-OK status into `Sys_Error ("<fn>: Host_Error during startup")` — defined behaviour where the C had none. `SysGlue_HostFrame` wraps `Host_Frame` in `Host_Guard`, which is now the outermost `setjmp` target under `USE_RUST_PLATFORM` (`Host_Glue_FrameInner`'s own `setjmp (host_abortserver)` is compiled out there); a status of 1 (`ABORTSERVER`) or 2 (`SCREEN_ERROR`) ends the frame and the loop continues, exactly what the C `setjmp` return did, with one divergence recorded here at M6 and removed at STOP C: the raised frame's `serverprofile` accumulation (`Host_Frame`'s timing tail after the `setjmp`) was skipped because `Host_Glue_FrameInner` was a plain call; it is a status-returning guard since the review fix and the tail runs (see the STOP C amendment). Everything else the C did between the calls (`Harness_CheckArgs` before SDL init, `atexit (Sys_AtExit)` after `SDL_Init`, `Detected N CPUs.`/`Initializing`/banner/`Host_Init` prints, `sys_ticrate` pacing with `SDL_Delay (1)`, the `no_rendering` 16/32 ms sleeps, the harness pacing) is in the same order with the same strings.
- 2026-09-15 (M6): the CI package job (clang-cl, `--buildtype=debug`) links with `lld-link`, which — unlike the MSVC release link — has no `legacy_stdio_definitions.lib` on the line, so `quake_rs_host_shutdown`'s `printf` reference (`recursive shutdown`) failed to resolve once no C TU emitted the inline copy. It now prints with `fputs` + `stdout_stream ()` like the M5 arms; `quake-c-sys::host` no longer declares `printf`. Verified by building that exact shape locally (`build-rs-debug`) and the release variant (`build-rs-clang-rel`), both with `-Duse_rust=enabled`, before flipping the workflow.
- 2026-09-15 (M6): packaging flip (R6). The AppImage image installs rustup 1.97.1 (`--profile minimal -c clippy -c rustfmt`, matching `rust/rust-toolchain.toml` so the pin resolves without a download at build time) and `cbindgen` 0.29.4 (the version on the dev box) under `/opt/rustup`/`/opt/cargo`; `run-in-docker.sh` configures both the shipped SDL3 build and the SDL2 compile-check with `-Duse_rust=enabled`. The Windows package job and the mac archive job gain the same toolchain steps the harness jobs use (`dtolnay/rust-toolchain@stable` 1.97.1; `taiki-e/install-action` / `brew install cbindgen`) and configure `-Duse_rust=enabled`; the harness jobs keep their `build-c` oracle. MinGW and MSYS2 arm64 workflows are untouched (Phase 9 M1 recorded them C-only). `docs/rust-migration/PLAN.md:43,124` and ROADMAP.md:272 still say `quake-host` "becomes the bin"; the crate's own docs now say library crate, and the ROADMAP/PLAN wording is corrected with the phase exit record (M8), where D1's "Meson retained" outcome is written up.
- 2026-09-15 (STOP B): the `compatibility-reviewer` pass over M4-M6 produced three findings and an explicit coverage gap. F1 claimed the Windows dedicated console accepts high-bit bytes the C dropped; refuted by the `windows-sys` 0.61.2 source (`AsciiChar: i8`), so `console_input` is unchanged. F2 (`# Safety` sections on the entry symbols) and F3 (the Dockerfile comment implied the crate registry was pre-fetched; it is deleted from the image and `cargo build --locked` fetches it at AppImage build time) are applied as written. The reviewer exhausted its turn budget before reading the M4 `pl` layer, the unix `sys` bodies and the direct `Sys_*` exports; rather than a second agent pass those were desk-checked against the C in this session (evidence row) and one divergence fixed: `Sys_Exec`'s child built its `argv` on the heap after `fork`, which the SAFETY comment's async-signal-safety claim did not cover (the C used a stack array); the array is now built in the parent. The `Sys_*` export signatures are not desk-checked individually because `check_capi_signatures.sh` compiles the cbindgen header against `sys.h`/`steam.h`/`platform.h` and is green. Remaining not-examined-by-anyone surface: none of the M4-M6 source; the residual risk is runtime-only (unix arms, packaging jobs) and is CI-gated.
- 2026-09-15 (M7): ROADMAP Phase 9's exit wording "last `setjmp`/`longjmp` removed; `HostError` Results end-to-end" is amended per D7 to what the C oracle permits. Reached: no `setjmp` in the Rust-main frame path (`Host_Glue_FrameInner`'s is compiled out under `USE_RUST_PLATFORM`; the loop's `recover` owns frame-abort recovery), `HostError` consumed by the loop as a `Result`, and a committed inventory (`docs/rust-migration/setjmp-inventory.md`, regenerated by `scripts/setjmp_inventory.py`) classifying all 40 residual sites: the 21 soak-exit ones (the 20 in `host.c`/`gl_screen.c` plus `Host_Glue_FrameInner`'s compiled-out `setjmp`) go with the C-oracle deletion, the 19 in `host_glue.c`/`gl_screen_glue.c` (the trampoline pair, the two `jmp_buf`s, the raise sites; `quakedef.h` holds only the declarations — STOP C correction) convert at Phase 10 when `Host_Error` itself is ported. Deferred, and why: the `Host_Guard`/`Host_Reraise` pair cannot go while 39 glue TUs (344 guards) still let C raise past Rust frames, and `HostError` carries no message because the C `Host_Error` prints before it jumps (ADR-009 amendment). Two decisions taken on the way: a `Host_Guard` status outside 0/1/2 has no C precedent and is a `Sys_Error` rather than ignored; the manual recovery check had to run with a short `argv[0]` (`cmd /c cd && build-rs\vkqr-engine.exe ...`) because `COM_InitArgv` clamps the reconstituted command line to `CMDLINE_LENGTH - 1` = 255 bytes and the worktree path pushed `+echo` past it in both builds (`Unknown command "ech"` on `build-rs`, an empty `echo` on `build-c`, one byte apart from `build-c`/`build-rs`) -- not a divergence, but easy to misread as one. The ROADMAP status block itself is written at M8.
- 2026-09-15 (M8): the ROADMAP Phase 9 block is written as the phase exit record with `[~]` retained. Three wording corrections it carries: (1) the roadmap's Crates line ("`quake-host` (becomes the bin)") and PLAN.md's ownership-inversion paragraph ("`main()` moves to the Rust `quake-host` binary crate and the remaining C compiles as a static library that Rust links") described the pre-D1 shape; both now state the D1 outcome (Rust `main` exported from the `quake_rs` staticlib by `quake-capi`, Meson links, `quake-host` is a library, no `libquake_c.a`). (2) I8 counted 157 `#include "quakedef.h"` lines; a fresh `grep -rl` over `Quake/*.{c,h,m}` gives 160 files, which is the number the deletion recipe uses. (3) The completion-gate item "ADR ... cited in commits": the nine Phase 9 commits name ADR-003/004/007/009/011/017 where each applied; ADR-010 and ADR-019 are satisfied through the harness rows (state-hash `--compare`, save/config/condump diffs) rather than named in a commit subject, and are cited in the M8 commit instead. Not done at M8 and stated as such in the ROADMAP: pushing the branch / CI (the phase-exit criteria "three OSes" and "packaging reproduced" are CI-gated and remain open until the PR runs).
- 2026-09-15 (STOP C, review fixes): the fresh-context `compatibility-reviewer` over M7-M8 accepted the M7/M8 shape and returned one Medium: the M6 claim that `Host_Glue_FrameInner` could be "a plain call" under `USE_RUST_PLATFORM` was an ADR-009 rule-3 violation, since `Host_Frame`'s `Host_Reraise` then `longjmp`ed through `quake_rs_host_frame`. Fixed in code rather than re-worded: `Host_Glue_FrameInner` is a status-returning `Host_Guard` there (the `#ifndef` shell keeps the C build's `setjmp` and returns `HOST_GUARD_OK`), and `quake_rs_host_frame` returns the status after its `serverprofile` tail. That also removes the M6 amendment's one stated divergence (the raised frame's `serverprofile` accumulation is no longer skipped; the profile tail runs as after `_Host_Frame`'s early return in the C). Three Low findings applied with it: the Phase 10 attribution named `quakedef.h` as a site holder (it holds only the `Host_Guard`/`Host_Reraise` declarations; the script's dead rule is removed); `ScreenError` reaching the loop is pre-shutdown state and is now documented as such in `HostError`, `recover` and ADR-009 (it is not routed to `Sys_Error`: the C's behaviour for that case was a jump to a dead buffer, and continuing with the server as it was is what `SCR_DrawGUI`'s own recovery point does); and `setjmp_inventory.py --check` now runs in CI (`rust.yml`, header-smoke job) with its scope (`Quake/*.{c,h,m}`, harness stubs excluded) stated in the generated file. The guard/re-raise counts move from 342/136 to 343/137 (the new frame guard); the 40-site split is unchanged.
- 2026-09-16 (PR #41 review / CI): Copilot's two review findings were legitimate and are applied — the `_Host_Frame` key pump was the one remaining plain call from `quake_rs_host_frame_core` that can raise (ADR-009 rule 3; STOP C's F1 fixed the frame wrapper, this closes the pump), and the Unix `Sys_...` fork child leaked the `/dev/null` descriptor. The one failing CI job was a harness race, not the port: `interop_matrix.py`'s readiness probe bound the server's own port and could make the server's bind fail; it now asks the server (`CCREQ_SERVER_INFO`), which is also the exact "map loaded" signal. The guard/re-raise counts move 343/137 -> 344/137; the 40-site split is unchanged.
- 2026-09-16 (PR #41 adversarial review, second round): the human review of `2bfbcb7f` confirmed the Blocking item is the interop bind race fixed in `0723b2cb` and added three notes, all applied: the `use_rust_platform` option text no longer claims `net_wins.c` (it follows `use_rust_net`); the Windows + SDL2 `SDL_main` arm is documented as unreachable from Meson rather than removed; and `split_host_port` clamps at the target's `unsigned long` (`c_ulong`), which restores `WINIPv4_GetAddrFromName`'s `0xFFFF` for `host:4294967296` on Windows and fixes the sign-with-overflow case on 64-bit too, with a width-conditional unit test and a C-reference differential test on both drivers. The review's non-blocking notes (`userdirs` uncovered by CI, `handles.rs` literals, no MinGW+Rust job) are accepted residual risk for the soak.
- 2026-09-15 (M5): the `sys.h` surface follows the M3 split — only `Sys_Error`, `Sys_Quit` and `Sys_SendKeyEvents` can raise (`Host_Shutdown`, `Host_Guard`-wrapped as `SysGlue_HostShutdown`; the key pump), so they get status-returning `quake_rs_sys_*` cores behind `Host_Reraise` frames in `Quake/sys_glue.c`; the variadic `Sys_Error`/`Sys_Printf` format in C with `q_vstrcatf (NULL, ...)` (I5; `Sys_Error`'s core owns the buffer, `Sys_Printf` frees after the call) and every other entry point is exported under its C name from `quake-capi/src/sys.rs`. `isDedicated` storage moves to Rust (`quake-c-sys` keeps the extern). `sys.h`'s `Sys_FileSize` is declared but was never defined in C, so it is not exported. The family is hand-declared in `cbindgen.toml` under `_QUAKE_SYS_H` / `_STEAM_H_` (spellings from the headers: `FUNC_NORETURN`, `qfilesize_t`, `findfile_t`, `#ifdef USE_SDL3` around `Sys_SelectFolder`); `check_capi_signatures.sh` therefore diffs `Sys_SelectFolder` only when the probe TU defines `USE_SDL3`, which it does not — same as any C TU without the define. The script's own `cbindgen` invocation must run with the pinned toolchain visible (`rust-toolchain.toml` is found by directory): on this box the header is generated from inside `rust/` and passed as the argument.
- 2026-09-15 (M5): the MSVC UCRT's `printf` is a header-inline wrapper, so a Rust object referencing it fails to link in the release build (`LNK2019 unresolved external symbol printf`; only the debug recipe adds `legacy_stdio_definitions.lib`). `quake-c-sys::sys` exports `fputs` plus `stdout_stream ()` (`__acrt_iob_func (1)` on MSVC, `__stdoutp` on Apple, the `stdout` extern elsewhere) and both arms print with `fputs`, which is what `sys_sdl_win.c` did and what `vprintf` amounted to on unix.
- 2026-09-15 (M5): accepted divergences from the C originals, all hygiene rather than behaviour: `Sys_StackTrace` always returns a `Mem_Alloc`ed string (C returned the literal `"[Not available.]\n"` when DbgHelp was unavailable and the callers `Mem_Free` it); the Windows `Sys_Explore` error messages render the wide path lossily rather than through `%ls`; the MSYS2 ImageBase probe bounds-checks the DOS/NT headers against the mapped size before reading them (unconditional, like the C — D6's `cfg(target_env = "gnu")` wording was not applied because the C code is not `#ifdef`ed either); the unix `Sys_Explore` child silences stdout/stderr with `open ("/dev/null") + dup2` instead of `freopen` (same file descriptors, no libc `stdout` static needed); the macOS debugger probe declares `kinfo_proc`'s `p_flag` (byte offset 32) and `P_TRACED` (0x800) by hand because the libc crate does not carry them; `Sys_PinCurrentThread` rejects a core index beyond `CPU_SETSIZE` instead of `CPU_SET`ing out of range; `host_parms->errstate++` happens after the message is formatted (in the Rust core) rather than before (C's `Sys_Error` bumped it before `vsnprintf`) — observable only if formatting itself raised, which it cannot. The Rust `Sys_Error` stack trace carries two extra leading frames (`quake_platform::sys::imp::stack_trace`, `error_prologue`, `error_core` above `Sys_Error - sys_glue.c`) — trace text is diagnostic output, not a compatibility surface. The macOS arm is desk-checked only (no Apple target on the dev box); CI's mac job compiles it.
- 2026-09-15 (M5, observation, out of scope): a windowed `+cl_startdemos 0 +map start +echo M3_MAP_UP` run logs the echo truncated, differently per build but deterministically per build — `M3_MAP_ ` on build-c, `M3_MAP ` on build-rs, `Unknown command "e"` (and the `cl_startdemos` not applied) on build-rs-cplatform — while `-dedicated` prints `M3_MAP_UP` intact on all three. It reproduces on the pure-C `build-c`, so it predates Phase 9 (the M3/M4 close-path scripts had shown the same line since M3 without it being noticed); to be triaged outside this plan (command-buffer handling during the map load), not by M5-M8.

## Verification evidence and handoff

| Milestone | Changed files/behavior | Check and result | Acceptance IDs | Remaining risk/next action |
|---|---|---|---|---|
| M1 (2026-09-14) | `meson_options.txt` (`use_rust_platform`), `meson.build` (switch, `platform`/`sdl2`/`sdl3` feature mapping, `-DUSE_RUST_PLATFORM`), `rust/quake-platform/{Cargo.toml,src/lib.rs}` (`sdl2` 0.38 optional dep, `platform` feature, ADR-017 mutual-exclusion `compile_error!`), `rust/quake-capi/Cargo.toml` (feature passthrough), `rust/Cargo.lock` (+sdl2, sdl2-sys, bitflags 1.3.2, version-compare), `.github/workflows/build-{linux,windows,mac}.yml` (`build-rs-cplatform` leg + signature check + mixed-identity corpus compare), `.github/workflows/rust.yml` (clippy `platform,sdl3` / `platform,sdl2` arms), ADR-003 amendment. No engine behaviour change: no C file dropped yet, no Rust symbol exported. | Targeted: `meson setup build-rs-cplatform -Duse_rust=enabled -Duse_rust_platform=disabled` + `ninja` 101/101 linked; `ninja -C build-rs` relinked (cargo fingerprint `quake-platform` features `["platform","sdl3"]`, cplatform build has no `platform`); `ninja -C build-c` no work; `cargo +1.97.1 check -p quake-capi --features platform,sdl2` OK (SDL2 arm compiles without SDL2 installed). Broad: `cargo fmt --check` OK; `cargo clippy --workspace --all-targets --locked -D warnings` OK plus `-p quake-capi --features platform,sdl3` and `platform,sdl2` OK; `cargo test --workspace --locked` all `test result: ok` (0 failed); `cargo deny check` advisories/bans/licenses/sources ok (bitflags 1.3.2+2.13.1 duplicate is a `multiple-versions = warn`); `run_corpus.py --tier shareware --compare` build-c vs build-rs-cplatform 8 ran/0 failed identical, build-c vs build-rs 8/0 identical; `check_capi_signatures.sh build-rs-cplatform/quake_rs.h` OK (vcvars, clang-cl). Not run: the new CI legs themselves (Linux/mac/Windows `build-rs-cplatform`, `rust.yml` clippy arms, `cargo audit`); `-Duse_rust_platform=enabled` with `use_rust` disabled (Meson `error()` path). | AC1, AC2 | Risk: CI leg YAML untested until the PR runs; SDL2 arm only type-checked (no SDL2 dev box). Next: M2 `net_wins.c` → `quake-net::udp::sys` Windows arm. |
| M2 (2026-09-15) | `rust/quake-net/src/udp/sys.rs` (dispatcher) + `udp/sys/unix.rs` (git-mv of the old `sys.rs`) + `udp/sys/windows.rs` (net_wins.c syscall arm, `windows-sys` `Win32_Networking_WinSock` only); `rust/quake-net/{Cargo.toml,src/lib.rs,src/udp.rs}` (`udp` on all targets, Winsock AF consts, `*_wins` pure helpers + 3 unit tests); `rust/quake-capi/src/net_wins.rs` (new, `#![cfg(windows)]`: `rust_udp4_*`/`rust_udp6_*`/`rust_udp_*` landriver exports incl. per-family `GetNameFromAddr`), `lib.rs` (`pub mod net_wins` under `net`), `net_dgrm_orch.rs` (uses `udp::set_family`), `cbindgen.toml` (prototypes on Windows, `_WIN32` split of `GetNameFromAddr`, exclude entries); `Quake/net_win.c` (`USE_RUST_NET` designated-initializer table, IPv6 entry under `#ifdef IPPROTO_IPV6` like the C table); `meson.build` (`net_udp.c`/`net_wins.c` only when `use_rust_net` is off); `rust/quake-ctest/{build.rs,include/c_ref_prelude.h,stubs/abi_probe.c,tests/net_abi.rs}` (net_wins.c c_ref on Windows + ws2_32, `IPPROTO_IPV6_defined` probe) + `tests/net_wins_differential.rs` (new, 5 tests); `.github/workflows/build-windows.yml` (interop-matrix step); ADR-003 amendment; `rust/Cargo.lock`. | Targeted (Windows, MSVC/clang-cl): `ninja -C build-rs` links, `build-rs/build.ninja` has no `net_wins` rule (`build-c` keeps it); `ninja -C build-c` and `build-rs-cplatform` exit 0; `-dedicated` prints `IPv4 UDP Initialized` only for both `build-c` and `build-rs`; `interop_matrix.py --vkquake-c build-c --vkquake-rs build-rs` → `interop_matrix: PASS` (all 24 cells incl. Csv/Rcl and Rsv/Ccl over the Rust Winsock driver); `run_corpus.py --tier shareware --compare` build-c vs build-rs 8 ran/0 failed identical and build-c vs build-rs-cplatform 8/0 identical; `save_diff.py` (2 saves identical), `condump_diff.py`, `config_diff.py` build-c vs build-rs identical; `check_capi_signatures.sh build-rs/quake_rs.h` OK (vcvars, clang-cl); `cargo test -p quake-ctest --test net_wins_differential --test net_abi` 5 + 2 passed (c_ref archive has `c_ref_WINIPv4_*`, no `WINIPv6_*`). Broad: `cargo +1.97.1 fmt --check` OK; `cargo clippy --workspace --all-targets --locked -D warnings` OK plus `-p quake-capi --features platform,sdl3` and `platform,sdl2` OK; `cargo test --workspace --locked` 123 `test result: ok`, 0 failed; `cargo deny check` advisories/bans/licenses/sources ok. Not run: the IPv6 interop leg against the C oracle (not runnable on MSVC, see amendment log); Windows CI interop step; MinGW build; Linux/mac builds of the reshaped `udp::sys` (unix arm is a pure file move, `cfg(unix)` code unchanged — CI). | AC3 | Risk: `rust_udp6_*` is dead code on MSVC until Phase 10 flips the guard (behaviour parity preserved); MinGW may define `IPPROTO_IPV6` as a macro and get a second landriver in both oracles (untested). Next: M3 input (`quake-platform::input`, SDL2/SDL3 backends, `in_sdl_glue.c`). |
| M3 (2026-09-15) | `rust/quake-platform/src/input/{mod.rs,keys.rs,scancode.rs,sdl2.rs,sdl3.rs}` (new: `IN_*` bodies, joystick axis math, `gamecontrollerdb.txt` load order, per-SDL event pumps with the mouse-motion filter, scancode table), `rust/quake-platform/{Cargo.toml,src/lib.rs}` (`quake-types` dep, `input` under `platform`); `rust/quake-c-sys/src/input.rs` (new: glue cvars, `InSdl_Glue_*` trampolines, `VID_*`/`S_*BlockSound`/`Key_TextEntry`/`Con_Mousemove` externs) + `lib.rs`/`libm.rs` (`powf`); `rust/quake-capi/src/in_sdl.rs` (new: `quake_rs_in_{init,send_key_events,commands}` cores + direct `IN_*` exports) + `lib.rs` + `cbindgen.toml` (`_QUAKE_INPUT_H` hand declarations, excludes); `Quake/in_sdl_glue.c` (new: `in_debugkeys`/`joy_*` cvars, `Host_Guard` trampolines for `Key_Event`/`Key_EventWithKeycode`/`Char_Event`/`CL_Disconnect`/`Sys_Quit`/`scr_conscale` callback/`Cvar_RegisterVariable`, re-raising `IN_Init`/`IN_SendKeyEvents`/`IN_Commands`); `meson.build` (`in_sdl.c`/`in_sdl2.c`/`in_sdl3.c` only when `use_rust_platform` is off, else `in_sdl_glue.c`); `scripts/harness/check_capi_signatures.sh` (`input.h` in the first TU). | Targeted (Windows, MSVC/clang-cl, SDL3): `ninja -C build-rs` exit 0 with `Quake_in_sdl_glue.c.obj` and no `in_sdl.c` rule in `build.ninja`, `quake_rs.h` carries the `_QUAKE_INPUT_H` block; `ninja -C build-c` and `ninja -C build-rs-cplatform` exit 0 (the latter keeps the three C input rules); `check_capi_signatures.sh build-rs/quake_rs.h` OK (vcvars, clang-cl); `run_corpus.py --tier shareware --compare` build-c vs build-rs 8 ran/0 failed identical and build-c vs build-rs-cplatform 8/0 identical; `save_diff.py` (2 saves identical), `condump_diff.py`, `config_diff.py` build-c vs build-rs identical; local `cargo test -p quake-platform --features platform,sdl3` (with `-L native=Windows/SDL3/lib64`) 5 passed (`scancode::{literals_match_sdl3_sys,table_matches_in_sdl_h}`, `input::{controller_button_keys,deadzone_clamps_and_rescales,easing_keeps_signs_and_zero}`). Manual (windowed `build-rs`, `-window -condebug +map start`, scripted input, same script green on `build-c` first): Shift+Esc console toggle twice (deactivate/activate), `echo M3_CONSOLE_TEXT` typed into the console echoed (text input + `Char_Event`), `bind x`/`MOUSE1`/`MWHEELUP`/`MWHEELDOWN` markers all echoed from game mode (relative mouse), focus lost to another app mid-run without a crash (process stayed responsive), `WM_CLOSE` → `SDL_EVENT_QUIT` → exit code 0 with the log flushed by `Host_Shutdown` (both `build-c` and `build-rs`). Broad: `cargo +1.97.1 fmt --check` OK; `cargo clippy --workspace --all-targets --locked -D warnings` OK plus `-p quake-capi --features platform,sdl3`/`platform,sdl2` and `-p quake-platform --features platform,sdl3`/`platform,sdl2 --all-targets` OK; `cargo test --workspace --locked` 123 `test result: ok`, 0 failed; `cargo deny check` advisories/bans/licenses/sources ok. Not run: gamepad connect/disconnect (no controller on the dev box); the SDL2 arm windowed (type-checked and linted only); `quit` typed at the console and Alt+F4 on `build-rs` (foreground lost — `WM_CLOSE` covers the same `SDL_EVENT_QUIT` arm); Linux/mac builds (CI). | AC4 | Risk: the SDL2 pump is untested at runtime until a dev box with SDL2 exists; the input unit tests are not in CI (SDL link); gamepad path unexercised. Next: STOP A `compatibility-reviewer` review of M1-M3, then M4 `PL_*`. |
| STOP A (2026-09-15) | `rust/quake-platform/src/input/mod.rs` (F1 `f64` view-angle math, F2 `addr_of!` axis reads, F3 `MAX_OSPATH`, F5 `# Safety`); this plan (amendment log). | Targeted: `cargo +1.97.1 fmt`; `cargo clippy -p quake-platform --features platform,sdl3`/`platform,sdl2 --all-targets --locked -D warnings` OK; `-p quake-capi --features platform,sdl3`/`platform,sdl2` OK; local `cargo test -p quake-platform --features platform,sdl3` (SDL3 lib path) 5 passed; `ninja -C build-rs` exit 0; `run_corpus.py --tier shareware --compare` build-c vs build-rs 8 ran/0 failed identical. Not run: windowed manual re-check (the F1 change is joystick-only and no controller is on the dev box); SDL2 arm at runtime. | AC4 | Reviewer verdict: proceed to M4. Next: M4 `PL_*`. |
| M4 (2026-09-15) | `rust/quake-platform/src/pl.rs` (new: `PL_*` bodies, shared `copy_clipboard`), `src/pl/win.rs` (new: Win32 arm — `LoadIconA`/`SetClassLongPtrA`/`DestroyIcon`, `OpenClipboard`/`GetClipboardData(CF_TEXT)`/`GlobalLock`, `MessageBoxA`), `src/pl/sdl.rs` (new: SDL3 and SDL2 arms — BMP icon from `qs_bmp.bin`, `SDL_GetClipboardText`, `SDL_ShowSimpleMessageBox`), `build.rs` (new: `qs_bmp.h` → `OUT_DIR/qs_bmp.bin`), `Cargo.toml` (`windows-sys` 0.61 on `cfg(windows)`, five features), `src/lib.rs` (`pl` under `platform`); `rust/quake-capi/src/pl.rs` (new: the four `PL_*` exports) + `lib.rs` + `cbindgen.toml` (`_QUAKE_PLATFORM_H` hand declarations, excludes); `scripts/harness/check_capi_signatures.sh` (`platform.h` in the first TU); `meson.build` (`pl_win.c`/`pl_linux.c` only when `use_rust_platform` is off); `rust/Cargo.lock`; ADR-003 (adopted-at-M4 feature list); this plan. | Targeted (Windows, MSVC/clang-cl, SDL3): `ninja -C build-rs` exit 0, `build-rs/build.ninja` has no `pl_win`/`pl_linux` rule, `quake_rs.h` carries the `_QUAKE_PLATFORM_H` block; `ninja -C build-c` and `ninja -C build-rs-cplatform` exit 0 (each keeps one `Quake_pl_win.c.obj` rule); `check_capi_signatures.sh build-rs/quake_rs.h` OK (vcvars, clang-cl); `run_corpus.py --tier shareware --compare` build-c vs build-rs 8 ran/0 failed identical and build-c vs build-rs-cplatform 8/0 identical; `cargo clippy -p quake-platform --features platform,sdl3`/`platform,sdl2 --all-targets --locked -D warnings` OK on Windows and with `--target x86_64-unknown-linux-gnu` (the `pl/sdl.rs` arms compile, `qs_bmp.bin` 3014 bytes); `-p quake-capi --features platform,sdl3`/`platform,sdl2` OK. Manual (Windows, non-injecting scripts, see amendment log): window class icon non-zero on `build-c`/`build-rs-cplatform`/`build-rs` and `WM_CLOSE` → exit 0 with the log flushed (`PL_VID_Shutdown` on the quit path); `Sys_Error` → `Quake Error` message box on `build-c` and `build-rs`, exit 1, same error text. Broad: `cargo +1.97.1 fmt --check` OK; `cargo clippy --workspace --all-targets --locked -D warnings` OK; `cargo test --workspace --locked` 123 `test result: ok`, 1562 passed, 0 failed; `cargo deny check` advisories/bans/licenses/sources ok. Not run: console clipboard paste on either build (foreground owned by another app; gated script kept for final verification); the SDL2 arm and `pl/sdl.rs` at runtime (Linux/mac CI builds link them, behaviour manual-not-run per AC5); MinGW build; `save_diff`/`config_diff`/`condump_diff` (no simulation, parsing or config code touched this milestone). | AC5 | Risk: `pl/sdl.rs` runtime unverified locally (CI builds only); clipboard paste evidence outstanding on Windows. Next: M5 `Sys_*` → `quake-platform::sys` + `Quake/sys_glue.c`. |
| M5 (2026-09-15) | `rust/quake-platform/src/sys.rs` (new: `ERRORTXT1/2`, `MAX_STACK_FRAMES`, `error_prologue` — `errstate++`, `Sys_DebugBreak`, stack trace, `PR_SwitchQCVM (NULL)` —, `sys_error`/`sys_printf`, `send_key_events`), `src/sys/handles.rs` (new: the `sys_sdl.c` handle table on C stdio), `src/sys/common.rs` (new: `counter_freq`/`Sys_DoubleTime`/`Sys_Sleep`, `Sys_SelectFolder` under `sdl3`, `Sys_GetPrefPath`, `Sys_MessageBoxWarning`, `Sys_QuitNoShutdown`, `ChooseQuakeFlavor` both arms), `src/sys/win.rs` (new: `sys_sdl_win.c` — wide `Sys_fopen`, `Sys_Init` console/affinity/`timeBeginPeriod`, registry/known-folder Steam/GOG/EGS/Nightdive lookups, `Sys_Explore`, find-first/next, DbgHelp `Sys_StackTrace` incl. the MSYS2 ImageBase offset, `Sys_ConsoleInput`, `Sys_Error` core), `src/sys/unix.rs` (new: `sys_sdl_unix.c` — `fork`/`execvp` explore, `getpwuid`, XDG/Steam/`libsteam_api.so` lookups, `opendir` find, `DO_USERDIRS` under the `userdirs` feature, `select`-driven console input, `backtrace` trace, `/proc`/`sysctl` debugger probe), `Cargo.toml` (`windows-sys` +17 features, `libc` on `cfg(unix)`, `userdirs`), `src/lib.rs`; `rust/quake-c-sys/src/sys.rs` (new: `SysGlue_HostShutdown`, engine callees, C stdio incl. `stdout_stream`) + `lib.rs`; `rust/quake-capi/src/sys.rs` (new: `isDedicated` storage, the four `quake_rs_sys_*` cores, every other `sys.h`/`steam.h` entry under its C name), `Cargo.toml` (`userdirs`), `lib.rs`, `cbindgen.toml` (`_QUAKE_SYS_H`/`_STEAM_H_` hand declarations, excludes); `Quake/sys_glue.c` (new: `Sys_Error`/`Sys_Printf`/`Sys_Quit`/`Sys_SendKeyEvents` frames, `Host_Guard` around `Host_Shutdown`, comctl32 manifest pragma); `meson.build` (the three sys files only when `use_rust_platform` is off, `do_userdirs` → `userdirs`); `scripts/harness/check_capi_signatures.sh` (comment); `rust/Cargo.lock`; ADR-003 (M5 feature list, `libc` adoption); this plan. | Targeted (Windows, MSVC, SDL3): `ninja -C build-rs` exit 0 (`build.ninja` has `Quake_sys_glue.c.obj`, no `sys_sdl*` rule), `ninja -C build-rs-cplatform` and `ninja -C build-c` exit 0 (each keeps `sys_sdl.c`/`sys_sdl_win.c`); `check_capi_signatures.sh <cbindgen header>` OK (vcvars, clang-cl; the header generated inside `rust/` — see amendment); `run_corpus.py --tier shareware --compare` build-c vs build-rs 8 ran/0 failed identical and build-c vs build-rs-cplatform 8/0 identical; `save_diff.py` build-c vs build-rs save 0/1 identical (95091/95075 bytes); `config_diff.py` identical (989 bytes); `condump_diff.py` identical (340 bytes); `cargo clippy -p quake-capi --features platform,sdl3` / `platform,sdl2` / `platform,sdl3,userdirs --locked -D warnings` OK on Windows and `platform,sdl3` / `platform,sdl2,userdirs` with `--target x86_64-unknown-linux-gnu`. Manual (Windows): `-dedicated ... +echo +status +quit` stdout of build-c and build-rs identical apart from the exe timestamp and command line (19 lines each, exit 0); `Sys_Error` via empty `-basedir`: `#32770` `Quake Error` box on build-rs, exit 1, stdout byte-identical to the M4 build-rs run outside the `STACK TRACE` frames (Rust adds `stack_trace`/`error_prologue`/`error_core` frames above `Sys_Error - sys_glue.c`); `WM_CLOSE` → `Sys_Quit` → exit 0 with the log flushed on build-rs and build-c. Broad: `cargo +1.97.1 fmt --check` OK; `cargo clippy --workspace --all-targets --locked -D warnings` OK; `cargo test --workspace --locked` 123 `test result: ok`, 1562 passed, 0 failed; `cargo deny check licenses` ok. Not run: the unix arm at runtime (compile-checked for Linux only; the macOS `sysctl`/`__stdoutp`/`realpath` paths are desk-checked, no Apple target installed), `Sys_SelectFolder` and `ChooseQuakeFlavor` dialogs, GOG/EGS/Nightdive registry lookups against real installs, `Sys_ConsoleInput` from a real console (piped stdin only), MinGW build, `-Duse_sdl3=disabled` at runtime. | AC6 | Risk: unix/macOS `sys` arms unverified at runtime until CI; the pre-existing windowed `+echo` truncation (amendment log) is not Phase 9's but muddies any future console-log comparison. Next: M6 Rust `main`/`WinMain` + packaging flip. |
| M6 (2026-09-15) | `rust/quake-platform/src/main_sdl.rs` (new: `main_sdl.c` — `quake_main`, `init_sdl` with the version print/`atexit`/`_DEBUG` log priority, `cpu_count`, the dedicated and client frame loops with the `no_rendering`/harness pacing, `run_app` for the Windows SDL3 `SDL_RunApp` shim), `src/lib.rs`, `Cargo.toml` (`engine-debug`), `src/sys.rs` (`sys_error`/`sys_printf` `pub(crate)`); `rust/quake-capi/src/entry.rs` (new: `main` off-Windows, `WinMain` on Windows+SDL3, `SDL_main` on Windows+SDL2), `src/lib.rs` (`entry` under `platform`, not `test`), `src/host.rs` (`quake_rs_host_shutdown` prints the recursive-shutdown line with `fputs`), `Cargo.toml` (`engine-debug` forwards to `quake-platform`), `cbindgen.toml` (entry symbols excluded); `rust/quake-c-sys/src/sys.rs` (`SysGlue_SysInit`/`HostInit`/`HostFrame`, banner, `ENGINE_NAME_AND_VER`, `cl.paused`/`cls.timedemo` accessors, `COM_InitArgv`, `Harness_*`, `harness_fixed_dt`, `VID_HasMouseOrInputFocus`/`VID_IsMinimized`, `atexit`), `src/host.rs` (`printf` extern dropped); `Quake/sys_glue.c` (job 5: the three `Host_Guard` trampolines, `SysGlue_PrintCompilerBanner`, `SysGlue_EngineNameAndVer`, `SysGlue_ClientTimedemo`/`ClientPaused`); `Quake/host_glue.c` (`Host_Glue_FrameInner` is a plain call under `USE_RUST_PLATFORM`); `meson.build` (`main_sdl.c` only when `use_rust_platform` is off); `Packaging/AppImage/docker/Dockerfile` (rustup 1.97.1 + clippy/rustfmt + `cbindgen` 0.29.4 under `/opt/rustup`/`/opt/cargo`), `Packaging/AppImage/run-in-docker.sh` (both legs `-Duse_rust=enabled`), `.github/workflows/build-windows.yml` (package job: rust-toolchain 1.97.1 + cbindgen, `-Duse_rust=enabled`), `.github/workflows/build-mac.yml` (archive job: `brew install cbindgen`, rust-toolchain 1.97.1, `-Duse_rust=enabled`), `.github/workflows/build-linux.yml` (comment); `rust/quake-host/Cargo.toml` + `src/lib.rs` (library crate wording); this plan. | Targeted (Windows, SDL3): `ninja -C build-rs` (MSVC release) exit 0, `dumpbin /headers`: entry `WinMainCRTStartup`, subsystem 2 (Windows GUI), `build.ninja` has no `main_sdl` rule (`build-rs-cplatform` and `build-c` keep it); `ninja -C build-rs-cplatform` and `build-c` exit 0; the CI package-job shape — clang-cl, `--buildtype=debug -Ddebug=true -Dstrip=false -Duse_sdl3=enabled -Duse_rust=enabled` (`build-rs-debug`) and the same with `--buildtype=release` (`build-rs-clang-rel`) — links and runs (see amendment: the debug leg is what exposed the `printf` link failure); `check_capi_signatures.sh <cbindgen header>` OK (the header carries no `main`/`WinMain`/`SDL_main`); `run_corpus.py --tier shareware --compare` build-c vs build-rs 8 ran/0 failed identical and build-c vs build-rs-cplatform 8/0 identical; `save_diff.py` build-c vs build-rs identical (95091/95075 bytes); `config_diff.py` identical (989 bytes); `condump_diff.py` identical (340 bytes); `cargo clippy -p quake-capi --locked -D warnings` for `platform,sdl3` / `platform,sdl2` / `platform,sdl3,userdirs` / `platform,sdl3,engine-debug` / `platform,sdl2,engine-debug` on Windows and `platform,sdl3` / `platform,sdl2,userdirs` / `platform,sdl3,engine-debug` with `--target x86_64-unknown-linux-gnu`. Manual (Windows): `-dedicated -nosound +echo +status +quit` stdout of build-rs, build-rs-cplatform and build-rs-debug identical to build-c apart from the `Exe:` stamp, `Command line:` (and, for the clang-cl leg, the `Built with Clang` banner) — 19 lines each, exit 0; `WM_CLOSE` on build-rs → `Sys_Quit` → exit 0, log flushed (62 lines); `Sys_Error` via empty `-basedir` on build-rs: `#32770` `Quake Error` box, exit 1, trace `Sys_Error - sys_glue.c` ← `W_LoadWadFile` ← `Host_Guard` ← `quake_rs_host_init` ← `Host_Init` ← `Host_Guard - host_glue.c` ← `quake_platform::main_sdl::quake_main - main_sdl.rs` ← `__scrt_common_main_seh` (the `WinMain`/`SDL_RunApp` frame is unsymbolised). Broad: `cargo +1.97.1 fmt --check`, `cargo clippy --workspace --all-targets --locked -D warnings`, `cargo test --workspace --locked`, `cargo deny check licenses` all OK: 123 `test result: ok`, 1562 passed, 0 failed; `licenses ok` (the pre-existing unmatched-allowance warning). Not run: the packaging jobs themselves (Docker/NSIS/`dsymutil` are CI-only — AppImage, Windows installer, mac archive verify on push), the unix `main` at runtime (compile-checked for Linux only), the Windows SDL2 `SDL_main` arm at runtime (no SDL2 on the box; clippy-checked), MinGW/arm64 (those workflows stay `use_rust=disabled`), a `timedemo` frame-pacing comparison. | AC7 | Risk: the three packaging jobs run the mixed build for the first time on push (the mac archive job's SDL2 leg is the first macOS+SDL2 mixed compile anywhere; the AppImage image's rustup install is untested locally). Next: STOP B (`compatibility-reviewer` fresh-context review of M4-M6). |
| STOP B (2026-09-15) | `rust/quake-capi/src/entry.rs` (`# Safety` sections on `main`/`WinMain`/`SDL_main` instead of the crate-local `missing_safety_doc` allow — reviewer F2), `Packaging/AppImage/docker/Dockerfile` (comment: only the toolchain is pre-fetched, the crate registry is dropped from the image — reviewer F3), `rust/quake-platform/src/sys/unix.rs` (`exec`: `argv` built before `fork`, the child no longer allocates — own finding from covering the reviewer's gaps); this plan. | Review: `compatibility-reviewer` (fresh context, `19c42d74..HEAD`) reported no Blocker/High/Medium; F1 (`console_input` sign extension) refuted — `windows-sys` 0.61.2 spells `KEY_EVENT_RECORD_0::AsciiChar` as `i8`, so `c_int::from` sign-extends exactly like the C `CHAR`→`int` assignment; F2/F3 fixed. The reviewer ran out of turn budget before M4 (`pl.rs`, `pl/win.rs`, `pl/sdl.rs`, `build.rs`, `quake-capi/src/pl.rs`), the unix `sys` bodies and the direct `Sys_*` export signatures; covered here by hand: `pl_win.c`/`pl_linux.c` vs the three `pl` files line for line (`MAX_CLIPBOARDTXT` = `MAXCMDLINE` = 256, `GlobalSize + 1` / `strlen + 1` chop, `q_strlcpy`, `SetClassLongPtrA`, colour-key icon), `qs_bmp.h` blob 3014 literals = 3014 bytes in every `qs_bmp.bin`; `sys_sdl_unix.c` vs `unix.rs` for `Sys_GetUserdirArgs`/`Sys_GetUserdir`/`Sys_GetBasedir` (both arms)/`Sys_Init`/`Sys_Error`/`Sys_Printf`/`Sys_Quit`/`Sys_ConsoleInput`/`Sys_Exec`/`Sys_Explore`/`Sys_StackTrace`/`Sys_IsInDebugger` (both arms; `kinfo_proc.kp_proc.p_flag` at byte 32 checked against `sys/proc.h`)/`Sys_DebugBreak` — one divergence found and fixed (the fork-child allocation); the `Sys_*` signatures are covered mechanically by the drift gate below. Targeted (Windows): `cargo +1.97.1 fmt --all -- --check` 0; `cargo clippy -p quake-capi --locked -D warnings` for `platform,sdl3` / `platform,sdl2` / `platform,sdl3,engine-debug` on Windows and `platform,sdl3` / `platform,sdl2,userdirs` / `platform,sdl3,engine-debug` with `--target x86_64-unknown-linux-gnu` all 0; `cbindgen` header regenerated (0 `main`/`WinMain`/`SDL_main` matches) and `check_capi_signatures.sh` OK against it (`sys.h`, `steam.h`, `platform.h` included); `ninja -C build-rs` relinked (exit 0). Manual (Windows): `build-rs -dedicated -nosound +echo +status +quit` exit 0, stdout identical to `build-c` apart from `Exe:`, the echo tag and `Command line:`. Not run: the harness corpus/save/config/condump diffs (no simulation-side change: two doc/comment edits and a unix-only reordering), the unix arms at runtime (compile-checked via the Linux cross target only), `cargo test`/`cargo deny` (no dependency or test change). | AC5, AC6, AC7 | Risk: unchanged from M6 (Linux/macOS runtime and the three packaging jobs are CI-only). Next: M7 error-path end state (setjmp inventory, `HostError`, `Host_Guard` caller inventory). |
| M7 (2026-09-15) | `rust/quake-host/src/error.rs` (new: `HostError { AbortServer, ScreenError, Unknown(c_int) }`, `from_guard_status`/`guard_status`, `GUARD_*` constants pinned to `quakedef.h:475-477`, 2 unit tests), `rust/quake-host/src/lib.rs` (`pub mod error`; the crate is now a dependency-free library, not the bin), `rust/quake-platform/Cargo.toml` + `rust/Cargo.lock` (depends on `quake-host`), `rust/quake-platform/src/main_sdl.rs` (`host_frame` maps the `SysGlue_HostFrame` status through `HostError`; `recover` is the sole owner of frame-abort recovery -- `AbortServer`/`ScreenError` end the frame, `Unknown` is a `Sys_Error`; a raise in `Sys_Init`/`Host_Init` is a `Sys_Error`), `scripts/setjmp_inventory.py` (new, stdlib only: scans `Quake/*.c`/`*.h`, classifies every `setjmp`/`longjmp`/`jmp_buf`/`<setjmp.h>` site by enclosing function against a rule table, counts `Host_Guard`/`Host_Reraise` per TU and greps the Rust crates for callers of each thunk; `--check` exits 1 on a stale document, 2 on an unclassified site), `docs/rust-migration/setjmp-inventory.md` (generated: 40 sites = 21 "deleted with soak exit" + 19 "converted at Phase 10"; 39 guarded TUs, 342 `Host_Guard` / 136 `Host_Reraise` calls at M7 (343/137 after the STOP C frame guard, 344/137 after the PR #41 pump thunk), every thunk with a Rust caller), `docs/rust-migration/adr/ADR-009-error-handling.md` ("Amended (Phase 9 M7)": message-less `HostError` during the transition, `Result` consumption in the loop, deferred `setjmp` deletion with the inventory as the gate); this plan. | Targeted (Windows): `python scripts/setjmp_inventory.py --check` OK (40 sites, 39 guarded TUs; `host_glue.c:923` = `Host_Glue_FrameInner`'s `setjmp`, the only frame-path site, classified soak-exit because it is compiled out under `USE_RUST_PLATFORM`); `cargo +1.97.1 fmt --all -- --check` 0 (after one `fmt` on the new test); `cargo clippy -p quake-capi --locked -D warnings` for the six `platform` arms (Windows sdl3/sdl2/sdl3+engine-debug, Linux cross sdl3/sdl2+userdirs/sdl3+engine-debug) 0; `cargo clippy --workspace --all-targets --locked -D warnings` 0; `cargo test --workspace --locked` 0 (the two `HostError` tests pass); `cargo deny check licenses` ok (no new crate); `ninja -C build-rs` relinked (exit 0). Broad (Windows): `run_corpus.py --tier shareware --compare` `build-rs` vs `build-c` 8 ran / 0 skipped / 0 failed. Manual (Windows): windowed `build-rs` `-window -nosound -condebug +map start +changelevel nonexistent +echo M7_RECOVER_TAG +condump m7_recover +quit` prints `Host_Error: cannot find map maps/nonexistent.bsp`, then the echo, then `Dumped console text to m7_recover.txt.` and exits 0; the same on `build-c` gives a `qconsole.log` and condump identical apart from timestamps, `Exe:` and the stack-trace frames (the Rust trace shows the `Host_Guard` trampolines); dedicated `build-rs` `+echo +status +quit` exit 0, stdout identical to the M6 `build-c` capture apart from `Exe:`, the echo tag and `Command line:`. Not run: unix arms at runtime (CI), `build-rs-debug` relink (no debug-only change), the harness save/config diffs (no simulation-side change: the loop consumes the same `int` it did at M6). | AC8 | Risk: the inventory gate is a script, not a CI job -- a new `setjmp` site is caught only when someone runs `--check` (M8 lists it in the completion gate; a CI hook is a follow-up). Next: M8 phase exit record (ROADMAP status block, soak + deletion protocol, `quakedef.h`/PCH recipe, completion gate). |
| M8 (2026-09-15) | `docs/rust-migration/ROADMAP.md` (Phase 9 block: Crates line corrected, **Status**, **Phase-exit evidence**, **CI (not yet discharged)**, five **Exit-criterion deviations**, **Soak and deletion protocol (D8)** with the `quakedef.h`/PCH recipe; `[~]` kept), `docs/rust-migration/PLAN.md` (ownership-inversion paragraph and the crate-tree comment no longer say `quake-host` becomes the bin / C becomes `libquake_c.a`), `Misc/harness/README.md` (`interop_matrix.py` bullet: Linux + Windows CI legs, IPv4-only on Windows); this plan (AC9, completion gate, amendment log). | Docs review only (no code, build or harness change): every claim in the ROADMAP block was checked against this table before being written — the milestone commit list (`git log fc25b725 -9`), `build-rs-cplatform` on the three harness jobs (`grep -c` in `.github/workflows/build-{linux,mac,windows}.yml` = 3/3/4), the save/config/condump rows at M2/M3/M5/M6, the 24-cell interop run at M2, the 40 = 21 + 19 inventory split (`scripts/setjmp_inventory.py --check` OK at M7), and the `#include "quakedef.h"` count re-taken as **160** files (I8 said 157; corrected below). Line endings preserved (ROADMAP LF; PLAN/README/plan CRLF, `file` checked). Manual (Windows): the AC5 clipboard-paste retry (`m4_manual.ps1`, `build-rs`) aborted again on foreground ownership, and a PostMessage-only variant ran to `exit=0` with no console input recorded — AC5 row updated to say so; the icon (`GCLP_HICON` set) and `Sys_Error` dialog checks stand from M4. Not run: no build, cargo or harness gate (nothing outside `docs/` and `Misc/harness/README.md` changed; the shared CI `paths` filter skips such commits). | AC9 | Risk: the ROADMAP evidence is Windows-local; the CI paragraph is explicit that the branch is unpushed and the unix runtime / packaging jobs have not run. Next: push the branch and open the PR; when CI reports, append its outcome to the ROADMAP block (as Phase 8 did) before merge. Phase 9 implementation stops here; the soak begins on `master`. |
| PR #41 adversarial review (2026-09-16, second round) | The human adversarial review of `2bfbcb7f` (net/input/pl/sys/entry vs the C originals, verdict "the port itself is faithful") returned one Blocking and three inline notes; the Blocking item is the interop bind race already fixed in `0723b2cb` (the reviewer's diagnosis — `wait_until_bound`'s bind/close window vs the server's `bind` without `SO_REUSEADDR` — matches the fix's), the three notes are legitimate and applied. R3 (`meson_options.txt`): `use_rust_platform`'s description listed `net_wins.c`, which `meson.build` gates under `use_rust_net` (that is what makes `build-rs-cplatform` a platform-only oracle); the text now names the files the switch controls and says the landrivers follow `use_rust_net`. R4 (`rust/quake-capi/src/entry.rs`): the Windows + SDL2 `SDL_main` arm describes a configuration `meson.build` refuses (`SDL2 is not supported on Windows`) and only the Linux `platform,sdl2` clippy arm ever compiles the crate with `sdl2`, where `cfg(windows)` is false — the arm is kept (the C build's spelling; a Windows SDL2 build should be a Meson decision) but the module doc and the item doc now say it is unreachable and never type-checked, rather than a `compile_error!`. R5 (`rust/quake-net/src/udp.rs`): `split_host_port` accumulated the port in `u64` and documented the `strtoul` clamp as `ULONG_MAX` (64-bit); on Windows (LLP64) `unsigned long` is 32-bit, so `host:4294967296` is port `0xFFFF` in `WINIPv4_GetAddrFromName` and was `0` in the helper. The accumulator is now `core::ffi::c_ulong` (`checked_mul`/`checked_add`, overflow → `c_ulong::MAX` unsigned as glibc and the UCRT both return, only an in-range `-` value `wrapping_neg`s), which also removes a latent 64-bit divergence the old code had (`-<overflow>` gave 1, `strtoul` gives `ULONG_MAX`). Tests: `udp::tests::split_host_port_strtoul_width` (width-conditional expectations) and `host_port_strtoul_clamp_matches_c` in both `net_wins_differential.rs` (brings Winsock up via `udp::sys::wsa_startup` so the C `gethostbyname` works) and `net_udp_differential.rs` — `localhost:4294967296`, `:-1`, `:18446744073709551616` against the C reference, skipped only if `localhost` does not resolve. The review's three non-blocking notes (`userdirs` code unbuilt in CI, `handles.rs` literal constants, no MinGW+Rust job) are recorded here as accepted residual risk, unchanged. | Targeted: `cargo +1.97.1 fmt --all -- --check` OK; `cargo clippy -D warnings` (`--workspace --all-targets` + the six `platform` arms, plus `-p quake-net --all-targets --target x86_64-unknown-linux-gnu`) green — a first cut with a `u64` accumulator and a `C_ULONG_MAX` const passed on Windows and failed the Linux arms (`unnecessary_cast`, `absurd_extreme_comparisons` where `c_ulong` is `u64`), which is why the accumulator is `c_ulong` itself; `cargo test --workspace` 123 result sets, 1566 passed, 0 failed (the wins differential test resolved `localhost`, not skipped); `meson setup --reconfigure build-rs` exit 0 on the edited option file. Broad: `ninja -C build-c|build-rs|build-rs-cplatform` link; `run_corpus.py --tier shareware --compare` build-c vs build-rs 8/8 identical; `interop_matrix.py` 24/24 PASS (the CI invocation). Not run: the `net_udp_differential.rs` test at runtime (unix-only, `quake-ctest` cannot cross-build here — CI's Linux/macOS `cargo test` runs it), CI on this commit. | AC3, AC8, AC9 | Risk: none new; the C-vs-Rust clamp test depends on `localhost` resolving on the runner and degrades to a skip with a message if it does not. Next: push, reply on the review threads, watch CI, append the outcome to the ROADMAP block. |
| PR #41 review / CI fixes (2026-09-16) | Two Copilot findings, both legitimate and applied. R1 (`rust/quake-capi/src/host.rs`, `Quake/host_glue.c`, `rust/quake-ctest/stubs/host_glue_ref.c`, `rust/quake-c-sys/src/host.rs`): `quake_rs_host_frame_core` called `Sys_SendKeyEvents` plain, but the pump reaches `Key_Event` and, under `USE_RUST_PLATFORM`, `sys_glue.c`'s `Host_Reraise` — a raise would have `longjmp`ed through the Rust frame (ADR-009 rule 3, the same class as STOP C's F1). It is now `raise!(quake_platform::sys::send_key_events())` under the `platform` feature (the status-returning core `sys_glue.c` wraps) and `raise!(Host_Glue_Sys_SendKeyEvents())` otherwise (new `HOST_GUARD_VOID` thunk in both glue TUs; the plain `Sys_SendKeyEvents` declaration and the host_glue.c:70 "cannot longjmp" listing are corrected). R2 (`rust/quake-platform/src/sys/unix.rs`): the forked child left the `/dev/null` descriptor open after the `dup2`s (the C's `freopen` replaced the streams in place); it is closed when `> 2`. CI: 25 of 26 checks green on `2bfbcb7f` (the new inventory step included); `Harness Windows (shareware)` failed in `interop_matrix.py` — `FTE+15 Csv/Rcl`: the C server `Sys_Error`ed with `Unable to bind to 0.0.0.0:26105 (Address already in use)`. Cause (`scripts/harness/interop_matrix.py`): `wait_until_bound` probed readiness by binding the server's own port and treating `EADDRINUSE` as "listening"; a probe socket held for the microseconds between its bind and close races the server's bind, and `UDP4_OpenSocket` does not retry. Replaced by `ServerProbe`: a `CCREQ_SERVER_INFO` datagram (`net_defs.h` framing) answered with `CCREP_SERVER_INFO` only once `_Host_ServerFrame` runs `SV_CheckForNewClients`, i.e. once `sv.active` — race-free, and it subsumes the fixed 0.5s "let `map` finish" sleep. Requests go out every 0.5s (the server drains one control datagram per frame) and the probe socket stays open until the cell's teardown so late replies do not ICMP-reset the server's accept socket; a first cut that sent every 0.1s and closed on the first reply produced one `CL_Connect: connect failed` in 24 cells. The other red step, `Vulkan device probe`, is the Phase 8 `continue-on-error` probe (no GPU on the runner), not a failure. Inventory regenerated: 40 sites unchanged, 344 `Host_Guard` / 137 `Host_Reraise` (the pump thunk). | Targeted: `clang-format --dry-run -Werror` on the two C files OK; `cargo +1.97.1 fmt --all -- --check` OK; `cargo clippy -D warnings` (`--workspace --all-targets` + the six `platform` arms) green; `cargo test --workspace` 123 result sets, 1564 passed, 0 failed; `python scripts/setjmp_inventory.py` then `--check` OK; `interop_matrix.py --soak --frames 2000 --combos C/C R/R --protocols Base-666` PASS (the soak cell shares the probe). Broad: `ninja -C build-c|build-rs|build-rs-cplatform|build-rs-debug|build-rs-clang-rel` link; `run_corpus.py --tier shareware --compare` 8/8 identical for `build-c` vs `build-rs` and vs `build-rs-cplatform`; `interop_matrix.py` 24/24 PASS twice in a row with the new probe (the local-only `--ipv6` leg fails identically with the old and the new script on these Windows builds — no IPv6 landriver in the C oracle, as the ROADMAP notes — and is not in CI). Manual: `f1_recover.py` (`changelevel nonexistent` then a second `map start`) on `build-c` and `build-rs`: both recover, exit 0, condumps identical apart from `Exe:` and the stack-trace frames. Not run: CI on the fix commit (pushed after this row), Linux/macOS runtime of the `unix.rs` change (cross-clippy only). | AC3, AC8, AC9 | Risk: the probe changes how every interop/soak cell starts (Linux CI runs it too); the Linux harness job is the remaining evidence. Next: push, watch CI, append the outcome to the ROADMAP block. |
| STOP C / review fixes (2026-09-15) | Fresh-context `compatibility-reviewer` over M7-M8 (`fc25b725`, `8fce9824`): no Blocker/High; one Medium (F1) and three Low (F2-F4), all applied. F1 (`Quake/host_glue.c`, `rust/quake-ctest/stubs/host_glue_ref.c`, `rust/quake-c-sys/src/host.rs`, `rust/quake-capi/src/host.rs`, `rust/quake-platform/src/main_sdl.rs`, `Quake/sys_glue.c`, `rust/quake-c-sys/src/sys.rs`): under `USE_RUST_PLATFORM` `Host_Glue_FrameInner` was a plain call, so `Host_Frame`'s `Host_Reraise` `longjmp`ed through the Rust `quake_rs_host_frame` frame to `SysGlue_HostFrame`'s guard — an ADR-009 rule-3 violation (a Rust frame unwound by `longjmp`; `panic = "abort"`, no destructors on that path, so latent rather than observed). `Host_Glue_FrameInner` is now `int`-returning: without the switch the `setjmp` shell returning `HOST_GUARD_OK` as before, with it a `Host_Guard` whose status `quake_rs_host_frame` hands back after the `serverprofile` tail and `Host_Frame`'s own `Host_Reraise` re-issues from C. F2 (`docs/rust-migration/adr/ADR-009-error-handling.md`, ROADMAP deviation 2, this plan's M7 amendment, `scripts/setjmp_inventory.py`): the 19 Phase 10 sites are all in `host_glue.c`/`gl_screen_glue.c`; `quakedef.h` holds only the trampoline declarations and its dead `jmp_buf` rule is removed. F3 (`rust/quake-host/src/error.rs`, `main_sdl.rs` `recover` doc, ADR-009): `ScreenError` at the loop is documented as pre-shutdown state (server/client untouched, as `SCR_DrawGUI`'s own recovery point) rather than the post-guard invariant. F4 (`.github/workflows/rust.yml`, `scripts/setjmp_inventory.py`, `docs/rust-migration/setjmp-inventory.md`): `python3 scripts/setjmp_inventory.py --check` is a step of the `core headers bindgen smoke` job; the scan covers `Quake/*.{c,h,m}` and says so (harness stubs out of scope); inventory regenerated (40 sites unchanged, 39 TUs, 343 `Host_Guard` / 137 `Host_Reraise` — the new frame guard). | Targeted: `clang-format --dry-run -Werror` on the three C files OK; `cargo +1.97.1 fmt --all -- --check` OK; `cargo clippy` (`--workspace --all-targets` + the six `platform` arms) and `cargo test --workspace` (123 result sets, 1564 passed, 0 failed) green; `python scripts/setjmp_inventory.py` then `--check` OK. Broad: `ninja -C build-c|build-rs|build-rs-cplatform|build-rs-debug|build-rs-clang-rel` link (MSVC 19.44 + clang-cl); `run_corpus.py --tier shareware --compare` 8/8 identical for `build-c` vs `build-rs` and vs `build-rs-cplatform`. Manual: `-window -nosound -condebug +serverprofile 1 +map start +changelevel nonexistent +echo TAG +map start +echo TAG2 +condump +quit` on `build-c` and `build-rs`: both recover from the `Host_Error` to the second `map start`, exit 0, `qconsole.log`/condump identical apart from `Exe:` and the stack-trace frames (`f1_recover.py`). Not run: CI (branch unpushed), Linux/macOS runtime, clipboard-paste manual check (as at M4/M8). | AC3, AC8, AC9 | Risk: the reviewer's readiness verdict was "ready with stated residual risk" conditional on F1 and F2, both applied; the residual is the CI-only evidence already listed at M8. Next: push the branch and open the PR (unchanged from M8). |

## Completion gate

- [x] Requirements map to acceptance criteria (R1→AC4/5/6/7, R2→AC3, R3→AC1/3/4/6/7, R4→AC1/4, R5→AC4/6/8, R6→AC7) — all nine AC rows (AC1-AC9) carry a local Windows pass, each stating its CI-only / not-run remainder (AC1/AC2 CI legs, AC4 gamepad + SDL2 arm, AC5 clipboard paste manual + Linux/macOS, AC7 Linux/macOS/packaging).
- [x] ADR-003/004/007/009/010/017/019 constraints satisfied and cited in commits — ADR-003 (`windows-sys` only, no `objc2`, `cargo deny` green), ADR-004 (SAFETY comments; `# Safety` on the entry exports), ADR-007/017 (glue TUs, `check_capi_signatures.sh`), ADR-009 (amended at M7), ADR-010/019 (harness `--compare` rows; cited in the M8 commit, see the M8 amendment).
- [x] Each milestone landed as one commit with the tree green (`5dd7a4c2` M1, `e4eca4d7` M2, `6f038d59` M3, `19c42d74` STOP A, `11b56c3e` M4, `03a4827c` M5, `8f8762e8` M6, `d3de498e` STOP B, `fc25b725` M7, `8fce9824` M8, `2bfbcb7f` STOP C, `0723b2cb` PR #41 review/CI fixes, second-round review fixes this commit); evidence rows filled with current tool output, targeted / broad / manual / not-run distinguished per row. "Tree green" is local (Windows) up to STOP C; CI on `2bfbcb7f` was 25/26 green with the one failure a harness race fixed in the PR #41 row.
- [x] ROADMAP status and amendment log updated (M8); `compatibility-reviewer` at STOP A (`19c42d74`), STOP B (`d3de498e`) and, fresh-context over M7-M8, STOP C (this commit; the `/integration-review` equivalent for the phase).
