# Feature task plan: Rust migration Phase 8 — Renderer + task system (`quake-tasks`, `quake-render`)

Status: approved (plan mode, 2026-09-05); amended 2026-09-05 with design-agent findings (see amendment log)
Owner: adamgj / Claude Fable 5.1
Baseline: `feature/rust-conversion-phase-8-08c302` at `da03b229` (master = merged Phase 7 + fixes)
Last materially updated: 2026-09-05
Commit target once approved: `docs/ai/plans/rust-conversion-phase-8.md` (first commit of M1)

For Rust-migration work, this task plan is subordinate to `docs/rust-migration/PLAN.md`, `ROADMAP.md`, and applicable ADRs. It cannot change phase ordering or compatibility policy without an explicit approved documentation/ADR change.

## Context

Phases 0–7 are complete on `master`; the ROADMAP's next open phase is **Phase 8 — Renderer + task system (XL)** (`docs/rust-migration/ROADMAP.md:225-249`). It ports `Quake/tasks.c` (702 lines + `atomics.h`) and the whole Vulkan renderer (~37k C LOC across 23 `gl_*.c`/`r_*.c` files, incl. `gl_vidsdl.c` 5560, `gl_rmisc.c` 4911, `r_brush.c` 3803) into two crates that are currently three-line Phase-0 stubs (`rust/quake-tasks`, `rust/quake-render`). It also moves the shader pipeline and embedded pak into `xtask`.

The user asked to "implement phase 8 … then review and validate". Per `CLAUDE.md`/`AGENTS.md` and `docs/ai/FABLE5_WORKFLOW.md`, an XL phase is executed as **one bounded architecture pass (this plan) followed by `/feature-implement` one milestone at a time**, each milestone leaving the tree green. Phase 7 (52k LOC) took eleven milestones across many sessions; Phase 8 is the same order of magnitude and cannot land in one session.

**Scope of the session that follows approval (decision, not a question):** land **M1 (gates, switches, deps, harness) and M2 (`quake-tasks`)**, run their verification, then stop and report — exactly as `CLAUDE.md` prescribes ("when the selected milestone … is complete and verified, stop"). M3 onward are each a separate `/feature-implement docs/ai/plans/rust-conversion-phase-8.md M<n>` invocation. No agent-parallel execution is assumed (Phase 7's was a separate user authorization); subagent use stays within `CLAUDE.md` (repo-researcher, verification-diagnostician, compatibility-reviewer at the STOP points).

Governing: ADR-003 (deps: four new crates, all reviewed here), ADR-004 (unsafe concentrated in `quake-render`/`quake-tasks`/`quake-capi`/`quake-c-sys`; SIMD needs scalar reference impls), ADR-007 (dual-view for `vulkan_globals` and renderer globals during the phase), ADR-009 (no longjmp through Rust frames; worker threads never host a jmp_buf), ADR-010 (per-platform parity; cull decisions exact), ADR-011 (hand-mirrored `repr(C)` for Vulkan-embedding structs, `ash::vk` handle types), ADR-013 (mimalloc across the boundary), ADR-015 (ash; port-then-modernize; SSIM tolerance policy), ADR-016 (crossbeam-deque; API-compatible shims; loom + TSan), ADR-017 (SDL2 + SDL3 both), ADR-019 (gates first; full suite at phase exit).

## Objective

Replace the C task system and the Vulkan renderer with Rust implementations behind two meson switches, verified against the C build by (a) loom/TSan + in-engine stress for the scheduler, (b) a bit-exact property differential for `gl_heap`, (c) exact cull-decision/draw-structure capture and an SSIM screenshot corpus for the renderer, (d) validation layers clean, (e) `timedemo` within threshold — while keeping every existing ADR-019 gate green and the C renderer as the oracle until Phase 9.

## Requirements and non-goals

- R1: `Task_*`/`Tasks_*` ABI and observable semantics preserved (handle = index | epoch<<8, 256 pending slots, 128-byte payload copy, ≤16 dependents, `Task_AddDependency` no-op once `before` has retired, `Task_Join(timeout)` true-on-epoch-advance, `TASKS_MAX_WORKERS 32`, `-pinnedworkers` parsing incl. its digit-validation and `% QThread_NumLogicalCores()` wrap, worker naming `Task_Worker_%d`, `Tasks_GetWorkerIndex()` < num_workers on workers and 0 on the main thread). Also: `INVALID_TASK_HANDLE` (`UINT64_MAX`) is never returned; `Task_Allocate` blocks when all 256 slots are pending (never fails); `Task_AddDependency` on an already-retired `before` is a silent no-op that must **not** decrement `after`'s dependency count (`tasks.c:585-604`); `Task_Join` with a finite timeout returns false without invalidating the handle and true on a stale handle; `Tasks_NumWorkers()` is sim-observable (I1) so its value must match C exactly for the same `-pinnedworkers`/core count; every thread reports a worker index < `TASKS_MAX_WORKERS` (32) — `r_part_fte.rs:6352-6355` relies on this in a SAFETY comment, so the shim clamps/`debug_assert!`s; there is no shutdown path (workers and the table are leaked on exit, as in C); Rust `Mutex` is non-recursive whereas `qmutex_t` is recursive — verify no re-entrant lock path at M2.
- R2: renderer output within ADR-015 tolerance (SSIM ≥ per-scene threshold derived from C run-to-run variance), validation layers clean, cull decisions and draw-call structure **identical** on captured frames, `timedemo` within threshold.
- R3: every existing gate (`run_corpus --check/--compare`, save/config/condump/capture/record/netreplay diffs, interop/physics matrices, formats corpus, sndhash, ctest suite, fmt/clippy/deny/audit, bindgen-smoke, capi-signature parity) stays green after every milestone.
- R4: `gl_heap` port is bit-identical to C on randomized traces (offsets, segment indices, stats).
- R5: build matrix keeps SDL2 and SDL3 (ADR-017), Windows/Linux/macOS, MinGW and arm64 legs.
- NG1: no modernization (no dynamic rendering, VMA, bindless, RT pipelines) — ADR-015.
- NG2: no `sys_sdl_*.c`, `main_sdl.c`, `in_sdl.c` port (Phase 9); no `q_thread_sdl.c` port — Rust threads are `std::thread`, but the C primitives stay for the remaining C.
- NG3: no deletion of `gl_*.c`, `r_*.c`, `tasks.c`, `atomics.h`, `bintoc.c`, `mkpak.c` in this phase — C remains the oracle until Phase 9 (Phase 7 precedent: deletions recorded, deferred). ROADMAP's "switches removed" exit wording is amended accordingly (see D1/AL).
- NG4: no Linux goldens (still deferred from Phase 7); no `c-reference/*` tags.

## Invariants and compatibility surfaces

- I1: headless harness (`-headless`, `no_rendering`) never executes renderer code, so **the corpus gates cannot observe the renderer**; renderer parity needs the new windowed harness (M1). The scheduler, however, **is** exercised headless: the BSP texture/extent fan-outs are `!no_rendering`-guarded (`gl_model.c:1029,1211`) but the alias and MD5/MD3 skin fan-outs (`gl_model.c:2011,2219`, guarded only by `!Tasks_IsWorker()`) run on every map load, which `build-linux.yml:436-448` already relies on. So `run_corpus.py --compare` with a fixed worker count is a real M2 gate, and `Tasks_NumWorkers()` is sim-observable through chunking (`gl_rmain.c:1212,1222`, `r_world.c:1046-1066`) — both sides of any comparison must pass the same `-pinnedworkers` list.
- I2: worker threads never run under a `setjmp` frame; `Sys_Error` on a worker takes the non-longjmp path (`sys_sdl_win.c:656-719`, `sys_sdl_unix.c:566-597` gate on `Tasks_IsWorker()`). Rust worker frames therefore never have C `longjmp` through them (ADR-009). Verify at M2 (landmine L2).
- I3: `vulkan_globals` (`glquake.h`), `gltexture_t`, `cb_context_t`, `vulkan_memory_t`, `glheapstats_t` layouts are shared C/Rust during the phase → ADR-011 mirrors with size/offset asserts in `quake-types`, verified by the ctest ABI probe (`stubs/abi_probe.c` pattern).
- I4: savegames, demos, protocol, config, sound are untouched by this phase; every existing byte-diff gate must stay identical.
- I5: `Tasks_IsWorker` is declared twice in c-sys with different Rust return types (`host.rs:269` `qboolean`, `r_part_fte.rs:928` `bool`); `qboolean` is `bool`/`_Bool` (`q_types.h:122,129`), so both are ABI-equal; the Rust shim exports `bool`. Consolidate the duplicates at M2.
- Platforms/configs: Windows (clang-cl, MSVC, MinGW, arm64), Linux (gcc, ASan leg), macOS (MoltenVK); SDL2 and SDL3; meson configs `build-c`, `build-rs`, `build-rs-c<module>` legs + trace builds.

## Migration authority

- Roadmap phase: **Phase 8** `[ ]` → `[~]` at M1, exit block at M12.
- ADRs: 003, 004, 007, 009, 010, 011, 013, 015, 016, 017, 019 (constraints above).
- C oracle: `build-c` (`-Duse_rust=disabled`) and the per-switch legs `build-rs-ctasks` / `build-rs-crender`.
- Deferred/deletion restrictions: NG2/NG3; ADR-005 formatter gaps (`%e/%g`) untouched; ADR-010 NaN-sign fence (`r_part_fte.c:7262`) becomes Rust when the render half ports at M8 — re-check the fence's observability clause then.

## Repository evidence

| Fact/dependency | Evidence | Confidence |
|---|---|---|
| Phase 8 scope/order/exit/deletes | `ROADMAP.md:225-249` | confirmed |
| tasks.c internals (Vyukov MPMC rings, 256 slots, epoch handles, per-worker indexed counters + steal order, `WAIT_SPIN_COUNT`) | `Quake/tasks.c:41-130, 276-361, 416-455` | confirmed |
| Task API surface + `TASK_TIMEOUT_INFINITE` SDL2/SDL3 split (`SDL_MUTEX_MAXWAIT` vs `(uint32_t)-1`, both 0xFFFFFFFF) | `Quake/tasks.h:82-88` | confirmed |
| Task consumers: `gl_rmain.c` ~20 handles/~35 edges (`:1152-1277`), `gl_screen.c` polling join 10 ms (`:1284`), `gl_model.c` 4 join pairs, `gl_vidsdl.c:3448/3498/4269`, `r_world.c`, `r_brush.c`, `r_part_fte(.c/.rs)` per-worker `deferred_queues` | grep + inventory | confirmed |
| Rust touches only `Host_Glue_Tasks_Init`, `Tasks_IsWorker`, `Tasks_GetWorkerIndex`, `Tasks_NumWorkers` (hand externs; `tasks.h`/`gl_heap.h`/`atomics.h` are **not** bindgen roots) | `quake-c-sys/src/host.rs:166,269`, `r_part_fte.rs:924-928`, `bindings_wrapper.h`, `gen_c_bindings.sh` | confirmed |
| Pinning: `Sys_PinCurrentThread` (`sys_sdl_win.c:859`, `sys_sdl_unix.c:689`; no-op false on macOS/BSD) | read | confirmed |
| `TestTasks_f`/`GL_HeapTest_f` registered via `host.c:1281-1282` and `host_glue.c:184` `host_glue_test_funcs[]` under `_DEBUG` | read | confirmed |
| gl_heap.c external seams: `R_AllocateVulkanMemory`/`R_FreeVulkanMemory` (`gl_rmisc.c:4597/4620`), `GL_SetObjectName` (`gl_vidsdl.c:750`), `Mem_Alloc/Free`, `Sys_Error`, `Con_Printf`, `COM_SeedRand/COM_Rand`, `Q_log2/Q_nextPow2/q_align`; no direct `vulkan_globals` use | `gl_heap.c:159-948` grep | confirmed |
| gl_heap users: `gl_mesh.c` (mesh heap, device address when `ray_query`), `gl_texmgr.c` (texture heap) | grep | confirmed |
| `vulkan_memory_type_t`/`vulkan_memory_t` | `glquake.h:178-190` | confirmed |
| Pattern A swap + option plumbing template | `meson.build:378-385, 399-448, 536-560, 828-861`, `meson_options.txt:18-19` | confirmed |
| capi features/exports; `check_capi_signatures.sh`; ctest oracle TUs (`c_ref_*` rename, `C_SOURCES`, `#include`d TUs) | `quake-capi/Cargo.toml`, `quake-ctest/build.rs:8-150`, `stubs/r_part_fte_ref.c` | confirmed |
| `c_ref_prelude.h` carries compile-only `vulkanglobals_t`/`cb_context_s`/`gltexture_s`/Vk* approximations (Phase 7 carve-out) | `quake-ctest/include/c_ref_prelude.h:1420-1610` | confirmed |
| No TSan/loom/helgrind CI; helgrind only in `.vscode/tasks.json` and `tasks.c` `USE_HELGRIND` annotations | grep | confirmed |
| No screenshot/SSIM/timedemo/validation harness; `harness.c` is headless-only; screenshots via `Image_WritePNG/TGA/JPG` (`gl_vidsdl.c:3895-3899`); `timedemo` prints `%i frames %5.1f seconds %5.1f fps` (`cl_demo.c:840`); `VK_LAYER_KHRONOS_validation` behind `vulkan_globals.validation` (`gl_vidsdl.c:857-902`) | grep | confirmed |
| Linux CI: Vulkan SDK subset (headers, loader, glslang, spirv-opt) installed; no lavapipe/xvfb; existing `-pinnedworkers 0` vs `0..7` corpus step (`build-linux.yml:328-334`) | read | confirmed |
| Harness scripts are stdlib-only Python | grep imports | confirmed |
| `atomics.h` users beyond tasks.c: `gl_rmisc.c` (60), `gl_rmain.c`, `r_world.c`, `r_brush.c`, `host_cmd(_glue).c`, `gl_heap.c`, `r_part*(_glue).c`, `gl_model.c/.h`, `gl_sky.c`, `gl_warp.c`, `model_parse.c`, `glquake.h`, `quakedef.h` | inventory | confirmed |
| MSVC arm of `atomics.h` `Load/StoreUInt32` are compiler barriers only; C11 arm uses `compare_exchange_weak` | inventory | confirmed |
| Shader/pak build: `glslangValidator` + `bintoc` + `mkpak` custom targets (`meson.build:95-125`); `xtask` is a stub | read | confirmed |
| Rust toolchain 1.97.1, `panic=abort`, workspace lints (`unsafe_op_in_unsafe_fn`, `undocumented_unsafe_blocks` deny) | `rust/Cargo.toml`, `rust-toolchain.toml` | confirmed |
| Existing dev/runtime deps: `proptest`, `cc` (dev); `png`, `zune-jpeg`, `miniz_oxide`, `socket2`, `libc`, `windows-sys` | ADR-003 amendments | confirmed |
| macOS CI runners have no usable Vulkan device for MoltenVK smoke; Windows CI has no GPU | assumption from runner specs | **uncertain** — settle at M1 by probing (`vulkaninfo`) in a throwaway CI step |

## Architecture and decisions

### D1: Two switches, sub-slices land as commits under them

- Choice: `use_rust_tasks` (cargo feature `tasks`, `-DUSE_RUST_TASKS`, CI leg `build-rs-ctasks`) and `use_rust_render` (feature `render`, `-DUSE_RUST_RENDER`, leg `build-rs-crender`). Each renderer sub-slice (M3…M10) is its own Pattern-A `if use_rust_render` block and its own landing commit.
- Why: matches the ROADMAP crate boundaries and Phase 7's switch-per-crate rationale (within-phase oracle granularity comes free while unported files stay C in both legs; a switch per sub-slice would multiply the meson/CI matrix ×10 for configurations nobody ships). Bisection unit = landing commit on `build-rs`.
- Amendment recorded against the ROADMAP wording "sub-slices land behind individual switches" and "mixed-build switches removed": switches stay until Phase 9 like Phase 7's (C renderer remains the SSIM/cull oracle).
- Consequence: 2 new meson options, 2 cargo features, 2 CI legs × 3 OS workflows, `common.make` untouched (C-only fallback).

### D2: Dependencies (ADR-003 introduction bar, recorded in the ADR-003 amendment at M1)

| Crate | License → allowlist | Use | Notes |
|---|---|---|---|
| `crossbeam-deque` 0.8 | MIT OR Apache-2.0 → MIT | injector + per-worker deques + stealers (`quake-tasks`) | crossbeam-rs org, maintained, transitives `crossbeam-epoch`, `crossbeam-utils` (same license) |
| `crossbeam-utils` 0.8 | MIT OR Apache-2.0 → MIT | `Parker`/`Backoff`/`CachePadded` | already transitive |
| `loom` 0.7 | MIT | dev-only, `cfg(loom)` model tests | never in the staticlib |
| `ash` 0.38 | MIT OR Apache-2.0 → MIT | Vulkan binding (ADR-015) | `default-features = false, features = ["std", "debug"]` — **no `loaded`** (we load via `SDL_Vulkan_GetVkGetInstanceProcAddr`, so no `libloading`), zero runtime transitives. Introduced at **M3** (handle type for the `vulkan_memory_t` mirror per ADR-011) so the review lands before the big consumers. Design-agent note: on 64-bit C `VkDeviceMemory` is a pointer typedef, on 32-bit a `uint64_t`; `ash::vk::DeviceMemory` is `repr(transparent)` `u64` on both, matching the 64-bit pointer size and the C 32-bit path — the ABI probe row at M3 confirms `size_of == 8`; if the probe disagrees on any CI leg, M3 falls back to a `u64` newtype and defers `ash` to M6 (amendment) |
| `libc` | already present | **not** needed for pinning — pinning calls the existing C `Sys_PinCurrentThread` via c-sys (Phase 9 ports `sys_sdl_*`) | — |

Rejected: `rayon`/`tokio` (not the ADR-016 design), `vulkano`/`wgpu` (ADR-015), `gpu-allocator`/VMA (ADR-015 keeps gl_heap), `image`/`ssim` crates for the harness (Python stdlib TGA decode + in-tree SSIM keeps `scripts/harness` dependency-free).
Each addition: `cargo deny check licenses` over the full tree + `cargo audit`, and a review paragraph in the ADR-003 amendment.

### D3: `quake-tasks` = unsafe-free generic scheduler core; all FFI in `quake-capi`

- Choice: `rust/quake-tasks` is `#![forbid(unsafe_code)]`, generic over a `Job` trait (`fn run(&self, index: Option<u32>)`); it owns the 256-slot table (epoch, `remaining_dependencies`, `remaining_workers`, ≤16 dependents, `indexed_limit`, per-worker counters + steal order), the dependency/epoch state machine, `Join(timeout)` on a per-slot `Mutex<u64>`+`Condvar`, the `Injector`/`Worker`/`Stealer` execution loop with `Parker`-based idling, and the `-pinnedworkers` parser (pure function over `&str`, unit-tested against the C cases). The queue is behind a small `Queue` trait with two impls: `Deque` (crossbeam) and `Mutex<VecDeque>` (loom model). `rust/quake-capi/src/tasks.rs` exports the C names directly (`#[no_mangle] pub extern "C" fn Task_Allocate…`, the `Cvar_FindVar` precedent) with a `CJob { func, indexed_func, payload: [u8; 128] }` whose `run` is the one `unsafe` FFI call; thread-locals `IS_WORKER`/`WORKER_INDEX` live in capi. `Tasks_Init` spawns `std::thread::Builder::new().name("Task_Worker_{i}")`, pins via `Sys_PinCurrentThread` (c-sys hand extern), reads `com_argc/com_argv` through `COM_CheckParm` (already bound).
- Why: ADR-004 wants unsafe concentrated and small; making the core unsafe-free exceeds ADR-004's expectation ("bounded scheduler internals") and makes the loom model exhaustive over the actual state machine. ADR-016 explicitly allows queue mechanics to differ.
- Observable-vs-internal (R1): observable = handle encoding, slot reuse order after free (**not** observable: C returns slots through an MPMC ring; Rust uses a free list — the only visible effect is handle values, which no caller stores across frames; recorded as accepted), dependents cap, payload copy, join semantics, worker index contract. Internal = ring queues, semaphores, spin counts, `ShuffleIndex`.
- `Quake/tasks_glue.c` **is** needed (amended): `TestTasks_f` is referenced from both `host_glue.c:184` (`host_glue_test_funcs[]`) and `host.c:1281` under `_DEBUG`, and `use_rust_tasks` is orthogonal to `use_rust_host`, so the symbol must exist in every configuration. The glue TU keeps `TestTasks_f`/`LotsOfTasks`/`IndexedTasks` (`tasks.c:697-…`) verbatim C under `#ifdef _DEBUG`, which drives the Rust scheduler through the real C ABI and removes the `_DEBUG`⇔`engine-debug` linkage assumption (RA9 closed). tasks.c has no C-visible data symbols and no longjmp-capable callee (`Sys_Error` is not called; `Con_Printf`/`Con_Warning` only), so the glue TU carries no data views. Hand externs (`Sys_PinCurrentThread`, `QThread_NumLogicalCores`) go in a new `rust/quake-c-sys/src/tasks.rs` following the `menu.rs:350-372` precedent; `q_strsplit` is reused from `menu.rs:365`; `loom` is declared as `[target.'cfg(loom)'.dev-dependencies]`. Rust `std::thread` on Windows goes through the CRT-safe spawn path, so there is no `_beginthreadex` concern.
- `atomics.h` is **not** retired at M2 (14 other C users incl. `gl_rmisc.c`×60); it retires with the last C renderer file (deferred with NG3).
- `Tasks_NumWorkers()` is called before `Tasks_Init` returns nowhere; keep C's `CLAMP(1, cores, 32)` and pinned-count override.

### D4: `gl_heap` as pure logic over an injected memory backend, bit-exact vs C

- Choice: `rust/quake-render/src/heap.rs` ports segments/pages/small-alloc buckets/size-class bitfields/dedicated allocs verbatim (same policies, same `MAX_PAGES`, same alignment padding + split + coalesce), generic over `trait DeviceMemoryBackend { fn allocate(&self, size, memory_type_index, device_address, name) -> vk::DeviceMemory; fn free(...); }`. `quake-capi/src/gl_heap.rs` exports the `GL_Heap*` names; its backend calls C `R_AllocateVulkanMemory`/`R_FreeVulkanMemory`/`GL_SetObjectName` (hand externs, since `glquake.h` is not a bindgen root) and threads the `atomic_uint32_t *num_allocations` out-param through (Rust `AtomicU32` via pointer, same ordering as the C11 arm). `glheapallocation_t` is an opaque `Box` behind the C pointer; `glheapstats_t`, `vulkan_memory_t` (`ash::vk::DeviceMemory` + `vulkan_memory_type_t`) are `repr(C)` mirrors in `quake-types::render` with const asserts + ABI-probe rows.
- Verification: `quake-ctest/tests/gl_heap_differential.rs` with oracle TU `stubs/gl_heap_ref.c` (`#include`s `Quake/gl_heap.c` under `c_ref_` renames; supplies `c_ref_R_AllocateVulkanMemory` returning deterministic fake handles, `GL_SetObjectName` no-op; `c_ref_prelude.h` already defines `VkDeviceSize`/`VkDeviceMemory`/`VkMemoryAllocateInfo`). Proptest traces (exponential sizes/alignments like `GL_HeapTest_f`, page sizes 4 KiB–1 MiB, segment sizes up to 256 MiB, ≤ 500 live allocs, ≤ 10k ops) compare per-op (segment handle, offset, dedicated?) and `glheapstats_t`, plus the ported `TestHeapConsistency`/`TestHeapCleanState`. The in-engine `test_gl_heap` is ported and uses the engine's real `COM_Rand`; the ctest oracle's unmasked `COM_Rand` (Phase 7 carry item) is **not** relied on — traces come from proptest.
- Why: ADR-015 requires gl_heap property-tested vs C; the seams are exactly three functions, so the port needs no Vulkan calls.

### D5: Renderer ownership flips follow ADR-007 dual-view, one file family per milestone

- `vulkan_globals` becomes Rust-owned at M5 (`gl_rmisc.c`) with the C-layout view exported (`#[no_mangle] pub static mut vulkan_globals: VulkanGlobals`), ADR-007 table row added; `gltexture_t` list ownership flips at M4; `cb_context_t` arrays at M9. `c_ref_prelude.h`'s approximations of these structs are replaced by the real mirrors as each flips (Phase 7 carve-out closed).
- Function-level carve-outs inside a file are allowed only via the glue TU (Phase 7 `r_part_fte` precedent): RT functions in `gl_mesh.c`/`r_brush.c` stay C in `gl_mesh_glue.c`/`r_brush_glue.c` at M8 and port at M10.

### D6: Renderer verification harness (new, M1, C-only baseline first)

- `scripts/harness/render_corpus.py`: launches a **windowed** engine (not `-headless`) with `-harnesscmds` scripted `map`/`+timedemo`/`screenshot` sequences from `Misc/harness/render_corpus.json` (shareware scenes: start, e1m1 t=0/5/10s demo1 frames, e1m2, sky/water/fog/alpha/particle/viewmodel cases, `r_rtshadows` when supported), `scr_screenshot_format tga` (pure-Python TGA reader), computes global + windowed SSIM (grayscale 8×8 windows, in-tree implementation) between two builds, and enforces per-scene thresholds stored in the corpus (`threshold = min over N C-vs-C runs − margin`).
- `-renderhash` engine flag (C, `harness.c` + `r_world.c`/`gl_rmain.c` hooks, both languages once ported): per-frame hash of cull decisions (visible surface/leaf/entity bitsets) and draw-call structure (pipeline id, index count, instance count, descriptor set ids in submission order) — the **exact** gate ADR-015 requires, and the only parity gate that is bit-exact. Gated exactly like `-parthash` (opt-in, own golden namespace, `run_corpus.py --compare`) but needs a window; lives in `render_corpus.py`.
- Validation: `-validation` (existing `vulkan_globals.validation` path) with `VK_LAYER_KHRONOS_validation`; the harness fails on any validation message in the condump.
- `timedemo`: `render_corpus.py --timedemo demo1` parses the `%i frames … fps` line, N runs, median; threshold = C median × (1 − 0.10) on the same machine (reference hardware = the Windows dev box; CI lavapipe numbers are recorded, not gated).
- CI: Linux `harness-linux` gets `mesa-vulkan-drivers` (lavapipe) + `xvfb-run`, runs the render corpus C-vs-C (stability, sets thresholds) and C-vs-mixed. Windows/macOS CI: a probe step at M1 decides (see evidence table); if no device, the three-OS SSIM exit criterion is satisfied by local runs (Windows dev box, macOS if available) recorded in the amendment log, as Phase 7 did for goldens.

### D7: Scheduler/renderer verification tiers

- **Loom**: `cargo test -p quake-tasks --release` with `RUSTFLAGS="--cfg loom"`; models: single dependency chain, diamond, indexed fan-out with 2–3 workers, join-timeout vs completion race, epoch reuse ABA.
- **TSan**: new `rust.yml` job `tsan` (ubuntu, nightly, `-Zsanitizer=thread`, `-Zbuild-std`, `cargo test -p quake-tasks`); in-engine TSan (mixed build with `-Db_sanitize=thread` on the C side + Rust TSan) is attempted at M2 and recorded as run/not-run — ADR-016 says "replacing the current helgrind workflow", and there is no current helgrind CI to replace.
- **In-engine stress**: `test_tasks` (debug), plus the existing `-pinnedworkers 0` vs `0..7` corpus step now also on `build-rs-ctasks`.
- **Renderer**: per-slice `render_corpus.py` C-vs-mixed (SSIM + `-renderhash` + validation) locally on Windows every milestone, in CI (lavapipe) every push.

### D8: Shader pipeline + embedded pak in `xtask` — Rust build only

- `cargo xtask shaders` runs `glslangValidator`/`spirv-opt` with meson's exact flags (incl. the macOS `spirv-opt` skip) into `OUT_DIR`, and `quake-render` `include_bytes!`s them; `cargo xtask pak` replaces `mkpak`. Meson stops running `bintoc`/`mkpak` **when `use_rust_render` is on**; the C-only build keeps them (C oracle, NG3). Deletion of `bintoc.c`/`mkpak.c`/61 TUs is recorded for Phase 9/10.

## Change boundary

### Expected to change

- `meson.build`, `meson_options.txt`: two options, feature list, Pattern-A blocks per slice, xtask shader path.
- `.github/workflows/{build-linux,build-windows,build-mac,build-mingw,build-msys2-clangarm64,rust}.yml`: new legs, tsan/loom jobs, lavapipe render corpus, ADR-019 steps.
- `rust/quake-tasks/**`, `rust/quake-render/**`, `rust/quake-types/src/render.rs` (new mirrors), `rust/quake-capi/src/{tasks,gl_heap,gl_texmgr,gl_rmisc,gl_vidsdl,gl_draw,…}.rs`, `rust/quake-c-sys/src/{tasks,render}.rs` (hand externs), `rust/quake-ctest/{build.rs,stubs/*_ref.c,tests/*_differential.rs,include/c_ref_prelude.h}`, `rust/xtask/**`, `rust/Cargo.{toml,lock}`, `rust/deny.toml` (no allowlist change expected).
- `Quake/*_glue.c` per slice; `Quake/harness.c` (+ `-renderhash`), `Quake/r_world.c`/`gl_rmain.c` (hash hooks, C side, additive).
- `scripts/harness/render_corpus.py` (new), `Misc/harness/render_corpus.json` (new), `Misc/harness/README.md`.
- `docs/rust-migration/ROADMAP.md` (status), `adr/ADR-003` (amendment), `adr/ADR-007` (rows), `adr/ADR-011` (render mirrors), `adr/ADR-016`/`ADR-015` (amendment notes only if a decision here deviates), `docs/ai/plans/rust-conversion-phase-8.md` (this plan + amendment log).

### Must not change without plan amendment

- `Quake/tasks.h`, `Quake/gl_heap.h`, `Quake/glquake.h` public signatures (shims must match byte-for-byte; `check_capi_signatures.sh`).
- Any existing golden, state-hash definition, savegame/config/protocol format.
- `Quake/common.make` source lists (C-only fallback build).
- Phase ordering; `sys_sdl_*`, `q_thread_sdl.c`, `main_sdl.c` (Phase 9).

## Acceptance matrix

| ID | Acceptance criterion | Verification/gate | Status |
|---|---|---|---|
| AC1 | Switch scaffolding: `build-rs-ctasks`/`build-rs-crender` configure, build, and pass every existing gate identically to `build-rs` | meson three-config builds + `run_corpus.py --compare` | met at M1 (Windows local: four configs build; `--compare` 11/11 identical per leg; CI legs green on PR #33) |
| AC2 | ADR-003 amendment lists the four crates with review notes; `cargo deny check` (both workspaces) + `cargo audit` clean | CI `lint`, `audit` | met at M2 (ADR-003 adoption note; `cargo deny check` clean in both workspaces locally; CI `lint`/`audit` green on PR #33) |
| AC3 | Loom + TSan jobs exist and are green on the scheduler | `rust.yml` `loom`, `tsan` | met at M2 (`loom` 5 models green; `tsan` 23 tests, no reports, in WSL and in CI after the `panic="unwind"` job fix) |
| AC4 | Render harness runs C-vs-C stably (SSIM ≥ threshold, `-renderhash` identical run-to-run, validation clean) on Linux CI (lavapipe) and Windows local | `render_corpus.py --stability` | met, qualified: Windows local (SSIM 1.0, `-renderhash` identical, validation clean) and Linux CI lavapipe (all five entries SSIM 1.0, hashes identical); Windows/macOS CI runners have no usable Vulkan device (RA2, closed) |
| AC5 | `quake-tasks` passes unit/loom/TSan, `test_tasks` stress, corpus 1-vs-8 workers step, and every existing gate under `build-rs` | M2 targeted + broad | met (unit 23/23 debug+release, loom 5/5, TSan clean, `test_tasks` 100k pass, corpus 1-vs-8 identical, every existing gate green on PR #33) |
| AC6 | Windowed engine with Rust tasks under the C renderer: render corpus C-vs-mixed identical `-renderhash`, SSIM ≥ threshold, `timedemo` within 10 % | `render_corpus.py --compare` | met, qualified: `-renderhash` identical and SSIM 1.0 on all five entries (one unreproduced C-only outlier, RA14); `timedemo` within 10 % but display-bound at 60 Hz on the dev box, so only a gross CPU regression is visible |
| AC7 | `gl_heap` differential: 10k proptest traces bit-exact, ported self-test passes in-engine | `cargo test -p quake-ctest --test gl_heap_differential`; `test_gl_heap` | met (M3): 10 000 release traces + the `GL_HeapTest_f`-shaped trace + the device-address trace bit-exact (offsets, memory handle, stats, consistency); `test_gl_heap` (10 000 iterations, the glue's stats-only checks) ran to completion in the debug mixed build |
| AC8 | Each renderer slice (M4–M10): C-vs-mixed `-renderhash` identical, SSIM ≥ threshold, validation clean, all existing gates green | per-milestone | M3 (heap): met on the Windows dev box; C-vs-mixed `-renderhash` identical on all five entries (plain and `--validation` runs), SSIM 1.0, validation clean, headless corpus 11/11 identical; one unreproduced e1m1-map divergence recorded under RA14. M4 (texmgr): met on the Windows dev box; `--validation` compare `-renderhash` identical on all five entries, SSIM 1.0, zero validation messages, headless corpus 11/11 identical, op-trace differential bit-exact. M5 (rmisc): met on the Windows dev box; `--validation` compare `-renderhash` identical on all five entries (demo1/demo2 1925 frames, start/e1m1 285, e1m2 287) for both C-vs-Rust and C-renderer-leg-vs-Rust, SSIM 1.0, zero validation messages, headless corpus 11/11 identical, `P` preamble identical, six-run Rust-side stability; RA14 closed. M6 (vidsdl): met on the Windows dev box; `--validation` compare `-renderhash` identical on all five entries (demo1/demo2 1925 frames, start/e1m1 285, e1m2 287) for both C-vs-Rust and C-renderer-leg-vs-Rust, SSIM 1.0, zero validation messages, headless corpus 11/11 identical, `timedemo` 60.0 vs 60.3 fps (floor 54.3); the `_DEBUG` leg (`build-rs-debug` vs `build-c`, start/e1m1) SSIM 1.0 and validation clean with the expected `P 0 331` vs `P 0 330` preamble (`ray_debug_pipeline` is `_DEBUG`-only); SDL2 leg compile-checked by Linux CI only. M7 (draw layer) and M8 (world/mesh): met on the Windows dev box, see the evidence ledger rows. M9 (frame graph): met on the Windows dev box; `--validation` compare `-renderhash` identical on all five entries (demo1/demo2 1925 frames, start/e1m1 285, e1m2 287) for both C-vs-Rust and C-renderer-leg-vs-Rust, the new graph-shape digest included (`gn=25 ge=51` per steady-state frame, identical), SSIM 1.0, zero validation messages, headless corpus 11/11 identical, `timedemo` 60.3 vs 59.9 fps (floor 54.3, display-bound at 60 Hz), per-worker task counts: 153 816 executed task bodies over 20 workers on every C and Rust run (identical totals; max/median 2.0-3.9 on both sides, i.e. the same steal distribution). M10 (ray tracing): met on the Windows dev box only, where the device supports `VK_KHR_ray_query` (lavapipe lacks it, so no CI leg exercises the RT paths); `--validation` compare `-renderhash` identical on all five entries for C-vs-Rust and C-renderer-leg-vs-Rust at `r_rtshadows 2`, and for C-vs-Rust at `r_rtshadows 1` and `0`, SSIM 1.0 throughout, zero validation messages, identical AS allocation and scratch sizes, headless corpus 11/11 identical (8 skipped for missing mod data; this gate caught the headless `with_ctx` defect, amendment log), `timedemo` 60.4 vs 60.1 fps (floor 54.4, display-bound) |
| AC9 | `vulkan_globals`/`gltexture_t`/`cb_context_t` mirrors pass ABI probe on all CI OSes; ADR-007 rows closed | `progs_abi.rs`-style `render_abi.rs` | M3 (heap part): `render_abi.rs` passes on Windows (`glheapstats_t`, `vulkan_memory_t`, `vulkan_memory_type_t`, `sizeof VkDeviceMemory`); other OSes via CI; `gltexture_t` (M4): 22 field offsets, size, `enum srcformat`, `SRC_*`/`TEXPREF_*` values pass on Windows against the prelude's now-faithful copy, and the real-header check is the `COMPILE_TIME_ASSERT` block in `Quake/gl_texmgr_glue.c`; `vulkan_globals` (M5): `VulkanGlobals` mirror; the probe covers the five embedded structs (`dynbuffer_t`, `vulkan_pipeline_layout_t`, `vulkan_pipeline_t`, `vulkan_desc_set_layout_t`, `buffer_create_info_t`) against the prelude's faithful copies, and the real-header check is the 152-assert `COMPILE_TIME_ASSERT` block in `Quake/gl_rmisc_glue.c` (`vulkanglobals_t` stays compile-only in the prelude, D5 amendment); ADR-007 row closed for storage; `cb_context_t` (M9): `CbContext` and the M8 mirrors `lerpdata_t`, `lm_compute_workgroup_bounds_t`, `glRect_t`, `glMaxUsed_t`, `struct lightmap_s` pass the probe on Windows (54 rows: sizes and every field offset, `frame_graph_mirrors_match_engine_headers`) against the prelude's now-faithful copies, and the real-header check is the 28-row `COMPILE_TIME_ASSERT` block in `Quake/gl_rmain_glue.c`; other OSes via CI; the ADR-007 `vulkan_globals` row's `gl_rmain.c` frame-field writers are Rust from M9 (**met**, subject to CI) |
| AC10 | `xtask` shader/pak output byte-identical to meson's `bintoc`/`mkpak` output | M11 differential | met (2026-09-14, release buildtype): `scripts/harness/xtask_diff.py --build-dir build-c` reports 124 outputs byte-identical (61 `.spv`, 61 bintoc `.c`, `vkquake.pak`, `embedded_pak.c`); `quake-ctest --test xtask_differential` pins the deflate parity against the in-tree miniz (real pak identical; the <=32-byte stored-block divergence is recorded in the amendment log). Debug-buildtype identity is not claimed (`-g` embeds the source path) |
| AC11 | Phase exit: full ADR-019 suite; SSIM corpus on three OSes (or recorded local coverage); `timedemo` thresholds; ROADMAP `[~]`/exit block; `/integration-review` run by the user | M12 | met with recorded coverage (2026-09-14): the full local ADR-019 suite is green on the Windows dev box (M12 evidence row; the only local failures are the three Phase-7 known-stale registered-tier goldens, which fail identically on the C build); the SSIM corpus ran on Windows only (release C-vs-Rust, C-renderer-vs-Rust, C-tasks-vs-Rust, `r_rtshadows` 0/1/2, and the debug-buildtype pair with the validation layer confirmed loaded); Linux lavapipe ran on PR #40 (C-only stability, SSIM 1.0000 run-to-run on all five entries; no ray query, no validation layer, no Linux goldens -- the Phase-7 deferral stands), macOS did not run locally (no box this session; the CI macOS harness ran green); `timedemo` is display-bound on the box (60.4 vs 60.1 fps, floor 54.4) so it is a smoke gate, not a regression gate; ROADMAP Phase 8 has its exit block and stays `[~]`; `/integration-review` is the user's to run |

## Milestones

Graph: `M1 → M2 → M3 → M4 → M5 → M6 → M7 → M8 → M9 → M10 → M11 → M12` (strictly serial: each renderer slice consumes the previous one's Rust-owned state). STOP A (compatibility-reviewer) after M2; STOP B after M6 (device/swapchain); STOP C after M10.

### M1 — Gates first (no porting)

- Scope: D1 switches + cargo features (`tasks`, `render`) + CI legs on all workflows; ADR-003 amendment for `crossbeam-deque`/`crossbeam-utils`/`loom`/`ash` (adoption of each lands with its milestone); `rust.yml` `loom` and `tsan` jobs (initially over an empty `quake-tasks` test target to prove the job shape); `render_corpus.py` + `render_corpus.json` + `-renderhash` C hooks + `-validation` condump check + `timedemo` mode; Linux CI lavapipe/xvfb render-corpus step (C-vs-C stability, threshold generation); one-off CI probe of Vulkan availability on Windows/macOS runners; commit this plan as `docs/ai/plans/rust-conversion-phase-8.md`; ROADMAP Phase 8 → `[~]`.
- Files: `meson.build`, `meson_options.txt`, workflows, `rust/quake-capi/Cargo.toml` (features only), `scripts/harness/render_corpus.py`, `Misc/harness/render_corpus.json`, `Quake/harness.c`, `Quake/gl_rmain.c`/`r_world.c`/`gl_screen.c` (hash hooks under `-renderhash` only), `Misc/harness/README.md`, ADR-003, ROADMAP, plan.
- AC: AC1–AC4.
- Targeted verification: `meson setup build-rs-ctasks …use_rust_tasks=disabled`, `build-rs-crender`, `build-c`, `build-rs` all build; `run_corpus.py --check` (windows-x86_64 goldens) unchanged; `render_corpus.py --stability --vkquake build-c/vkqr-engine` on the dev box; `cargo fmt/clippy/deny/test`.
- Landmines: `-renderhash` must not touch the main state-hash chain (the M10a `-parthash` lesson); `run_corpus.py --extra-args` needs the `=` form for single-token flags.

### M2 — `quake-tasks` (ADR-016), swapped under the C renderer

- Scope: D3 in full. `rust/quake-tasks` (core, parser, loom models, unit tests incl. a single-worker determinism test of dependency ordering and a join-timeout bound test), `rust/quake-capi/src/tasks.rs` (`Tasks_Init`, `Tasks_NumWorkers`, `Tasks_IsWorker`, `Tasks_GetWorkerIndex`, `Task_Allocate`, `Task_AssignFunc`, `Task_AssignIndexedFunc`, `Task_Submit`, `Tasks_Submit`, `Task_AddDependency`, `Task_Join`, `TestTasks_f` under `engine-debug`), `rust/quake-c-sys/src/tasks.rs` (`Sys_PinCurrentThread`, `QThread_NumLogicalCores`; consolidate the two `Tasks_IsWorker` externs), `Quake/tasks_glue.c` (`TestTasks_f` verbatim), meson `if use_rust_tasks` block (`tasks.c` swapped for the glue TU), `Cargo.lock`, ADR-003 adoption note, ADR-016 amendment note (`atomics.h` cannot retire with `tasks.c` — 15 `.c` + 7 headers use it, retires with the last C renderer file; there is no helgrind CI workflow to replace — `helgrind.supp` appears only as a paths exclusion — so TSan is net-new), `tsan`/`loom` jobs now real.
- AC: AC5, AC6.
- Targeted: `cargo test -p quake-tasks` (debug + release), loom, TSan (Linux, via CI or WSL — record which), `test_tasks` in a `-Dbuildtype=debug` `build-rs`, `run_corpus.py --compare` `build-rs` vs `build-rs-ctasks` and vs `build-c` with matching `-pinnedworkers` on both sides (I1) incl. the 1-vs-8 cell, grep that no caller persists a task handle across frames (RA5), `render_corpus.py --compare build-c build-rs` (Windows dev box, C renderer + Rust tasks: `-renderhash` identical, SSIM, validation, `timedemo` within 10 %), `check_capi_signatures.sh` (needs `cc`; note if not runnable locally), all eight meson configs.
- Landmines: L1 `tasks.h:86` `SDL_MUTEX_MAXWAIT` under SDL2 — the C header stays as-is (glue-free swap), the Rust side hard-codes `u32::MAX`; L2 verify `Sys_Error` on a worker never longjmps (I2); L3 `Task_AddDependency` reads `before`'s epoch without a lock in C — preserve the "already retired ⇒ no-op" semantics, not the race; L4 `gl_screen.c:1284` 10 ms polling join must not busy-spin the main thread; L5 `Tasks_Init` runs under `Host_Guard` from Rust host init — it must keep returning normally; L6 worker `Con_Printf` from Rust threads goes through C console locks as today.

### M3 — `gl_heap.c` → `quake-render::heap` (first `use_rust_render` slice)

- Scope: D4. `ash` introduced (no default features), `quake-types::render` mirrors (`vulkan_memory_t`, `vulkan_memory_type_t`, `glheapstats_t`) + `render_abi.rs` probe rows, `quake-capi/src/gl_heap.rs`, `quake-c-sys/src/render.rs` hand externs, `stubs/gl_heap_ref.c` + `tests/gl_heap_differential.rs`, meson `if use_rust_render` first block (`gl_heap.c` dropped), ported `GL_HeapTest_f` (`engine-debug`).
- AC: AC7, AC8, AC9 (heap part).
- Targeted: the differential (10k traces, release), ABI probe on Windows, `test_gl_heap` in-engine debug, `render_corpus.py --compare` (heap offsets are not pixel-visible, but `-renderhash` + validation catch binding errors), `check_ctest_symbols.sh` (note: `#include`d TUs are invisible to it — M10f-2 finding; rely on the MSVC leg for duplicate symbols).
- Landmines: `stubs/r_part_fte_ref.c:1657/1663` and `stubs/r_part_ref.c:331` already define plain `R_AllocateVulkanMemory`/`R_FreeVulkanMemory`/`GL_SetObjectName` doubles, so `gl_heap_ref.c` must `#define` those seams to `c_ref_heap_*` before including `gl_heap.c`; `c_ref_prelude.h` lacks `FindFirstBitNonZero64`, `VkMemoryAllocateFlagsInfo`, `VK_STRUCTURE_TYPE_MEMORY_ALLOCATE_FLAGS_INFO`, `VK_MEMORY_ALLOCATE_DEVICE_ADDRESS_BIT` (add all four); `GL_HeapTest_f` stays verbatim C in `Quake/gl_heap_glue.c` under `_DEBUG` (same reasoning as `tasks_glue.c`); match C defects bit-for-bit — `GL_HeapDestroy` never frees the heap struct and `gl_heap.c:841` uses `=` instead of `==` in `TestHeapCleanState` (the Rust self-test implements the intended `==`; the differential compares allocator state, not the self-test's return value); add a targeted page-saturation unit test (`MAX_PAGES = UINT16_MAX - 1`); proptest distributions mirror `GL_HeapTest_f` (`MAX_ALLOC_SIZE` 64 KB, `MAX_ALIGNMENT_POW2` 14) and never depend on the stub `COM_Rand` (its missing `& COM_RAND_MAX` is a separate carry item).

### M4 — `gl_texmgr.c`

- Scope: texture manager (`gltexture_t` list Rust-owned, C view; upload/staging via the still-C `gl_rmisc.c` staging buffers through hand externs; `GL_Heap*` now Rust). Differential: texture-list ordering and mip/format decisions on the shareware corpus via a ctest driver over the loaders (formats corpus extension), plus render corpus.
- AC: AC8, AC9. STOP-A review covers M1–M3 before this lands.
- Landed (2026-09-07). Files: new `rust/quake-render/src/texmgr.rs` (+ `texmgr/bluenoise.rs`) — the whole of `gl_texmgr.c` (the `gltexture_t` arena and active/free chains written exactly as the C writes them, the two-slot garbage ring, the texture heap over `crate::heap`, the 8-bit loaders, the 32-bit Vulkan image path, the palette builders, the blue-noise tile) generic over `TexMgrBackend` (Vulkan entry points, the `vulkan_globals` slice, cvars, staging ring, `COM_FOpenFile`/`Image_LoadImage`, console, `Sys_Error`, downsampler) and `TexMgrLock` (the engine mutex at C granularity, or `Direct` inside one critical section); new `rust/quake-capi/src/gl_texmgr.rs` (every `TexMgr_*` export of `gl_texmgr.h` except `TexMgr_Init`, plus `quake_rs_texmgr_init` and the `imagelist` command/completion; the list, heap and garbage ring behind one `Mutex`; `EngineBackend` over the hand externs); new `Quake/gl_texmgr_glue.c` (the C-visible data: six `d_8to24table*` palettes, six well-known texture pointers, `gl_max_size`/`gl_picmip`; `TexMgr_Init` = `Host_Reraise (quake_rs_texmgr_init ())`; `Host_Guard` thunks for the cvar/command registrations; `TexMgr_Glue_VulkanEnv`/`OwnerPathId`/`SetNotextureMips`/`Downsample`; `COMPILE_TIME_ASSERT` block on `gltexture_t`, `enum srcformat`, `src_offset_t` and the env struct); `quake-types::render` gains `GlTexture`, `SrcFormat`, the `TEXPREF_*` constants; `quake-c-sys/src/render.rs` gains the M4 seams and the `extern "system"` Vulkan loader entry points; `cbindgen.toml` hand block + `check_capi_signatures.sh` slice for the rest of `gl_texmgr.h`; `meson.build` swap (`gl_texmgr.c` → `gl_texmgr_glue.c` under `use_rust_render`); `quake-ctest`: `stubs/gl_texmgr_ref.c` (oracle under `c_ref_` renames with fake Vulkan/staging/heap/file seams that trace every call), `tests/gl_texmgr_differential.rs`, `render_abi.rs`/`abi_probe.c` rows for the 22 `gltexture_t` fields, `enum srcformat`, the six `SRC_*` and fourteen `TEXPREF_*` values; `c_ref_prelude.h`'s compile-only `gltexture_s`/`TEXPREF_` approximations replaced by the real definitions (D5 closed for this struct).
- Differential design (amends the scope line): the ctest driver is an op-trace differential rather than a loader corpus — both sides run the same proptest-generated `Load` (memory/lump/image sources, every format, random flags, random dims)/`Reload`/`ReloadNobright`/`FreeTexture`/`FreeTextures`/`FreeTexturesForOwner`/`NewGame`/`CollectGarbage`/`DeleteTextureObjects`/`UpdateDescriptorSets`/`LoadPalette` sequence against fake backends that log every Vulkan, staging, heap and file call in one byte-identical text format, and the test compares the traces, an active-list snapshot (name, owner, dims, flags, source fields, crc, handles, in list order) and the fake-handle counters after every op, plus one scripted sequence shaped like the engine's init/newgame/reload path. The real loaders over the shareware files are exercised by the render corpus (`-renderhash` covers texture binding order and descriptor identity).
- Landmines found (details in the amendment log): `TexMgr_Init` stays C (ADR-009); `TexMgr_Downsample` stays C (stb bit-identity); `vulkan_globals` reached through a glue accessor until M5; four C defects replicated or fenced (`CollectGarbage` frees the CS-write set through the wrong layout; cubemap pointer-table UB; `8to32` past a short palette; `fclose` leak on a short read); the C blue-noise tile's alpha bytes are uninitialised stack.

### M5 — `gl_rmisc.c`

- Scope: descriptor layouts, samplers, ~16 `R_Create*Pipelines` families with specialization constants, staging + dynamic ring buffers, `R_AllocateVulkanMemory`; `vulkan_globals` Rust-owned with C-layout view (ADR-007 row). The M3 backend switches from C externs to the Rust allocator. Pipeline-count/handle-order captured in `-renderhash` preamble.
- AC: AC8, AC9.
- Landed (2026-09-10). Files: new `rust/quake-render/src/rmisc/` (`mod.rs` `Engine` trait + `Ctx`; `memory.rs` `R_AllocateVulkanMemory`/`R_FreeVulkanMemory`/`R_CreateBuffer(s)`/`R_FreeBuffer(s)`; `staging.rs` the staging ring and `R_StagingUploadBuffer`; `dynbuf.rs` the dynamic vertex/index/uniform/storage rings, garbage collection and `R_InitGPUBuffers`; `descriptors.rs`; `samplers.rs`; `shaders.rs` the 61 `DECLARE_SHADER_MODULE` entries with their conditions; `pipelines.rs` the pipeline layouts, every `R_Create*Pipelines` family with its specialization constants, `R_DestroyPipelines`; 10 unit tests, Miri-clean); new `rust/quake-capi/src/gl_rmisc.rs` (46 `#[no_mangle]` exports: the `vulkan_globals` storage, the 15 allocation counters, and every function of `glquake.h`'s `gl_rmisc.c` block from `GL_MemoryTypeFromProperties` to `R_StorageAllocate`; the SPIR-V blobs stay the `bintoc` arrays C links, handed over by `Engine::shader_spv`); new `Quake/gl_rmisc_glue.c` (verbatim C: `R_Init`, the cvar callbacks and `r_showbboxes_filter` commands, `GL_WaterAlphaFor*`, player skins, `R_NewGame`/`R_NewMap`/worldspawn parsing, `R_TimeRefresh_f`, `vkmemstats`; plus the 152-assert layout block); `quake-types::render` (`VulkanGlobals`, `DynBuffer`, `VulkanPipelineLayout`, `VulkanPipeline`, `VulkanDescSetLayout`, `BufferCreateInfo`); `quake-c-sys/src/render.rs` (hand externs incl. the shader arrays and the harness hooks); `quake-capi/src/gl_heap.rs` (backend calls the Rust allocator) and `gl_texmgr.rs` (reads `vulkan_globals` directly, the M4 glue accessor retired); `Quake/gl_texmgr_glue.c`; `meson.build` (`gl_rmisc.c` swapped for the glue TU under `use_rust_render`); `cbindgen.toml`/`check_capi_signatures.sh`; `quake-ctest` (`render_abi.rs` + `abi_probe.c` rows, prelude copies of the five embedded structs); `Quake/harness_render.c`/`harness.h`/`gl_rmisc.c` (the `P` preamble and the `Harness_RenderPipelinesDestroyed` hook, both builds); `Misc/harness/README.md`.
- Landmines found (details in the amendment log): `vulkanglobals_t`/`cb_context_t` stay compile-only in the prelude (the glue assert block is the real-header check); RA14 root cause was the harness, not the driver; `r_brush.c:2821` passes `NULL` out-pointers to `R_StorageAllocate`; C `R_CreatePipelines` reads `dstAlphaBlendFactor` from the source table (`// COMPAT:` in `pipelines.rs`).

### M6 — `gl_vidsdl.c` via `ash`

- Scope: instance/device/swapchain/present, extension set (ray-query bundle, present-wait2, full-screen-exclusive on Windows), `SDL_Vulkan_GetVkGetInstanceProcAddr` loading via `quake-platform` (SDL2/SDL3), MoltenVK direct link on macOS (mirror meson's link rule), validation/debug-utils wiring, video modes + `vid_*` cvars + video menu glue, `SCR_ScreenShot_f`. STOP B after landing.
- AC: AC8; render corpus must run on all local/CI devices; SDL2 and SDL3 legs.
- Landed (2026-09-11). Files: new `rust/quake-render/src/vid/` (`mod.rs` `VidEngine` trait, `VidState` (instance/physical device/device/queue/surface/swap chain/render passes/frame buffers/per-frame sync/command buffers/`Procs` extension entry points), the hand-declared `VK_KHR_present_id2`/`VK_KHR_present_wait2` structs and `vkWaitForPresent2KHR` pointer; `instance.rs` `GL_InitInstance`/`GL_InitDevice`/`GL_InitCommandBuffers` incl. the debug-utils messenger, extension/feature negotiation (ray-query bundle, present-wait2, full-screen-exclusive on Windows), physical-device selection and the queue/command-pool setup; `swapchain.rs` surface capabilities, `vid_vsync`/`vid_maxframelatency`/full-screen-exclusive handling, swap-chain (re)creation and destruction; `resources.rs` `GL_CreateRenderResources`/`GL_DestroyRenderResources` (colour/depth/MSAA/screen-effects/UI images, render passes, frame buffers, descriptor sets, `R_CreatePaletteOctreeBuffers`, `GL_UpdateDescriptorSets`); `frame.rs` `GL_BeginRendering` (fences, acquire, the begin-rendering task), `GL_AcquireNextSwapChainImage`, `GL_EndRendering` (`vkWaitForPresent2KHR`, submit, present, `GL_SynchronizeEndRenderingTask`), screenshot readback and the GPU-time/wait counters, `GL_WaitForDeviceIdle`); new `rust/quake-render/src/cb.rs` (command-buffer helpers shared with M9); new `rust/quake-capi/src/gl_vidsdl.rs` (14 `#[no_mangle]` exports: the Vulkan half of `glquake.h`'s `gl_vidsdl.c` block from `GL_BeginRendering` to `GL_SetObjectName`, the five init/resource entry points `gl_vidsdl_glue.c` calls, `R_CreatePaletteOctreeBuffers`, and the `prev_end_rendering_task` storage; `Task_*` reached through c-sys C-ABI externs so `use_rust_render` stays orthogonal to `use_rust_tasks`; `DebugMessageCallback` under `engine-debug`); new `Quake/gl_vidsdl_glue.c` (verbatim C: gamma, the mode list and `vid_*` cvars, `VID_Init`/`VID_Restart`/`VID_Shutdown`, window/fullscreen/focus/cursor handling, the video menu, `SCR_ScreenShot_f`; plus the `VID_Glue_*` accessors — `GetInstanceProcAddr`, `InstanceExtensions`, `CreateSurface`, `WindowMonitor` (Win32), `HasFocus`, `GetFullscreen`, `TakeScreenshot`, `WriteScreenshot`, `SkipRenderResources`, `ClTime`, `ViewOrg`); `quake-capi/src/gl_rmisc.rs`/`gl_texmgr.rs`/`gl_heap.rs` (the `GL_SetObjectName`/`GL_WaitForDeviceIdle` seams now call the Rust exports); `quake-c-sys/src/render.rs` (M6 externs: `VID_Glue_*`, `Harness_RenderInstallHooks`, `Sky_NeedStencil`, `GL_Viewport`, the garbage collectors, `frame_oit_mode`/`bmodel_tlas`/`rs_gputime_us`/`rs_gpuwaitaccum_us`, the five cvars; the two C seam externs removed) and `tasks.rs`; `meson.build` (`gl_vidsdl.c` swapped for the glue TU under `use_rust_render`); `cbindgen.toml`/`check_capi_signatures.sh`.
- Landmines found (details in the amendment log): ash 0.38 predates `VK_KHR_present_id2`/`VK_KHR_present_wait2` (hand-declared, RA8 closed); `prev_end_rendering_task` is a C-visible data symbol (Rust-owned, `glquake.h:701` extern unchanged); `vid_vsync`/`vid_maxframelatency`/`r_raydebug` were `static` in `gl_vidsdl.c` and lose it in the glue so the Rust half can read them; `R_CreatePaletteOctreeBuffers` needs `palette.h` in the cbindgen preamble; the signature-check model TU has no `tasks.h`, so it carries a local `task_handle_t` typedef; the `_DEBUG` leg's `P` preamble legitimately differs from the release leg (`ray_debug_pipeline`).

### M7 — Draw layer

- Scope: `gl_draw.c`, `gl_sky.c`, `gl_warp.c`, `gl_fog.c`, `gl_rlight.c`, `gl_refrag.c`, `r_sprite.c`, `gl_screen.c` 2D orchestration; the Phase 7 `Draw_*` shims in menu/sbar/console retire (carve-out closed).
- AC: AC8.
- Landed (2026-09-12). Files: new `rust/quake-capi/src/gl_draw.rs` (wad/lmp pic cache, `Draw_*` primitives, `GL_SetCanvas`/`GL_SetCanvasColor`, `Draw_String_3D`), `gl_fog.rs` (fog globals + fade, `Fog_*`), `gl_rlight.rs` (`R_MarkLights`, `R_PushDlights`, `R_LightPoint`, `R_UpdateEntityDlights`), `gl_refrag.rs` (`R_AddEfrags`/`R_StoreEfrags` marking), `gl_sky.rs` (skybox/sky-layer/skywind draw, sky texture and `skyfog` state), `gl_warp.rs` + `gl_warp_sin.rs` (warp texture update, turbsin table), `r_sprite.rs` (frame selection incl. angled sprites, `R_DrawSpriteModel(_ShowTris)`), `gl_screen.rs` (`SCR_Draw*` overlays, `SCR_CalcRefdef`, fov/viewsize callbacks, `SCR_TileClear`, `SCR_DrawConsole`); new `rust/quake-types/src/refdef.rs` (`vrect_t`/`refdef_t` mirrors, moved out of `view.rs`); `quake-types::host` (`Entity`, `EntLerp`, `LightCache` now `pub`), `quake-types::render` additions, `quake-c-sys/src/render.rs` seams, `quake-render/src/cb.rs` (the remaining `vkCmd*` wrappers), `quake-capi/src/{lib,view,gl_rmisc,gl_vidsdl}.rs`, `cbindgen.toml`, `meson.build` M7 block; new `Quake/gl_draw_glue.c` (`scr_conalpha` cvar), `gl_fog_glue.c` (`%g`/`%f` command output, ADR-005 gap; command registration), `gl_refrag_glue.c` (the efrag loop's `PScript_RunParticleEffectState`/`R_AllocateEntityBLAS` calls under `Host_Guard`, the Rust loop returns the guard code for `Host_Reraise`, ADR-009), `gl_rlight_glue.c` (note only), `gl_sky_glue.c` (sky cvars, console commands, skywind config I/O, `Sky_Init`), `gl_warp_glue.c` (cvars), `r_sprite_glue.c` (entry points resolve the header through `Mod_Extradata`, which can `Host_Error`, before calling Rust), `gl_screen_glue.c` (cvar/global definitions, `SCR_Init` + key-binding commands, `SCR_DrawGUI` (setjmp target), loading plaque, `SCR_ModalMessage`, `SCR_UpdateScreen` task graph -- M9). The Phase 7 `Draw_*`/`SCR_*` externs in `quake-c-sys` `console.rs`/`menu.rs` resolve to the Rust exports under the `render` feature (carve-out closed). C harness fix: `gl_draw.c` (6 sites), `gl_sky.c`, `gl_warp.c`, `r_sprite.c` (2) now draw through the `vulkan_globals.vk_cmd_draw*` pointers.
- Landmines found (details in the amendment log): headless mode has no `VkDevice`, and `R_UpdateEntityDlights` is reached from `CL_RelinkEntities` headless, so it must read `vulkan_globals` directly rather than through the device context; the C oracle's direct `vkCmdDraw*` calls were invisible to `-renderhash` (a harness blind spot since M1 for the 2D quads, the skybox, the warp update and sprites); every Rust draw must go through `quake_render::cb`.

### M8 — World/mesh

- Scope: `r_world.c` (SIMD culling via `std::arch` SSE/NEON with scalar reference + `-renderhash` exact cull parity), `r_brush.c` (lightmaps, indirect draws), `gl_mesh.c`, `r_alias.c`, particle *render* halves of `r_part.c`/`r_part_fte.c` (glue TUs shrink; ADR-010 NaN-sign fence re-checked), QC polygon builtin rendering (Phase 6 shim retired). RT functions stay in `gl_mesh_glue.c`/`r_brush_glue.c` (D5).
- AC: AC8.
- Landed (2026-09-13). Files: new `rust/quake-capi/src/r_world.rs` (leaf/surface marking with scalar, SSE2 and NEON lanes gated on the C `use_simd`, the parallel mark/cull tasks, texture chains, `R_DrawWorld*`), `r_brush.rs` (brush-model draws, lightmap atlases with the compute and upload update paths, indirect-draw structures, dependency tables, SoA cull data, `GL_BuildLightmaps`/`GL_BuildBModelVertexBuffer`/`GL_PrepareSIMDAndParallelData`), `gl_mesh.rs` (mesh heap, MDL/MD3/MD5 buffer upload, deferred garbage lists, `GL_MakeAliasModelDisplayLists`), `r_alias.rs` (lerp/lighting setup, `R_SetupAliasFrame`, the alias draw bodies), `r_part_render.rs` (classic particle textures, index buffer, `R_DrawParticles(_ShowTris)`); `quake-types::render` mirrors (`aliasmesh_t`, `lightmap_t`, `bmodel_draw_indirect_t`, SoA plane/AABB types, ...), `quake-c-sys` `render.rs`/`tasks.rs` seams, `quake-capi/src/{lib,r_part}.rs`, `cbindgen.toml`, `meson.build` M8 block; new `Quake/gl_mesh_glue.c` and `r_brush_glue.c` (the ray-tracing halves: entity BLAS allocation and animated rebuilds, AS scratch buffer, TLAS allocation/garbage, bmodel BLAS/TLAS builds -- D5, port at M10), `r_alias_glue.c` (`R_DrawAliasModel(_ShowTris)` resolve the alias header through `Mod_Extradata_CheckSkin`, which can `Host_Error`, before entering Rust -- ADR-009), `r_world_glue.c` (`R_MarkSurfaces` re-raises the guard code the Rust marker returns from `R_StoreEfrags` -- ADR-009); `Quake/r_part_glue.c` keeps the pool, cvars and guard trampolines and fences its C render copy under `#ifndef USE_RUST_RENDER` (linked only by `build-rs-crender`).
- Landmines found (details in the amendment log): the parallel back-face cull writes `surfvis[0]` instead of the current word in C (`r_world.c:851`) and is preserved bug-for-bug; the task-path `R_StoreEfrags` raise has no C frame to re-raise into; the SSE2/NEON lightmap store and accumulate lanes truncate `scale` to 16-bit lanes and are mirrored per arch (ADR-010); the FTE particle render half (~2000 lines of `r_part_fte_glue.c`) is deferred to a follow-up landing before M12 and the ADR-010 NaN-sign fence caller therefore stays C; the QC polygon builtin rendering item was already closed by M7 (`pr_ext.c` never had a shim).

### M9 — Frame graph

- Scope: `gl_rmain.c`/`gl_screen.c` task graph (~20 tasks, ~35 edges, 6-way secondary CB recording, identical dependency structure — `-renderhash` gains a graph-shape digest), `cb_context_t` ownership.
- AC: AC8; worker-utilization comparison (`timedemo` + per-worker task counts from `test_tasks`-style counters) recorded.
- Landed (2026-09-13). Files: new `rust/quake-capi/src/gl_rmain.rs` (`R_CullBox`/`R_CullModelForEntity`/`R_RotateForEntity`, the view setup (`R_SetupView*`, frustum, view/projection matrices, `R_SetupContext`), the entity draw loops and the whole `R_RenderView` task graph -- every task, edge and 6-way secondary-command-buffer recording of `gl_rmain.c:1152-1277` in the same allocation order, plus the serial path -- as the status core `RRmain_RenderView`, which returns the first `Host_Guard` code raised by a glue draw thunk or by `R_StoreEfrags`); `gl_screen.rs` gains `quake_rs_scr_update_screen` (the `SCR_UpdateScreen` 5-task graph `begin_rendering`/`setup_frame`/`draw_gui`/`draw_done`/`end_rendering`, `SCR_SetupFrame`, `SCR_DrawDone`, the `in_update_screen`/`scr_disabled_for_loading` gates and the serial path); `r_world.rs`/`r_brush.rs` (graph-digest hooks, `RWorld_MarkSurfaces`'s guard code now returns through `RRmain_RenderView`); `quake-types::render` (`SCBX_*` slot constants, `NUM_WORLD_CBX`/`NUM_ENTITIES_CBX`, the `CbContext` field set completed); `quake-c-sys` `render.rs` (the `gl_rmain.c` globals/cvars the glue keeps, the `V_*`/`R_DrawParticles*`/`PScript_*` seams, the `Harness_RenderGraph*` hooks), `tasks.rs` (`harness_taskcounts`/`Harness_TaskExecuted`), `libm.rs` (`tan`/`atan`/`tanf`); `quake-capi/src/{lib,tasks}.rs`, `cbindgen.toml`, `meson.build` (`gl_rmain.c` swapped for the glue TU under `use_rust_render`; `r_world_glue.c` dropped); new `Quake/gl_rmain_glue.c` (the renderer globals and cvars the engine reads by name, `R_PrintStats` (`%g`, ADR-005), the `r_showbboxes` edict walk, the `RRmain_Glue_*` `Host_Guard` thunks over the `Host_Error`-capable alias/sprite draws, `R_RenderView` = `Host_Reraise (RRmain_RenderView (...))`, and a 28-row `COMPILE_TIME_ASSERT` block on `cb_context_t`, `lerpdata_t`, `lm_compute_workgroup_bounds_t`, `glRect_t`, `glMaxUsed_t`, `struct lightmap_s` against the real headers); `Quake/gl_screen_glue.c` (`SCR_UpdateScreen` = `Host_Reraise (quake_rs_scr_update_screen (...))`; `SCR_Glue_RenderView`/`SCR_Glue_DrawGUI` thunks; `SCR_DrawGUI` exported as the setjmp target); `Quake/r_world_glue.c` deleted (its only job was the M8 re-raise); `Quake/gl_rmain.c`/`gl_screen.c`/`r_world.c` (C side: `R_GraphTask`/`R_GraphEdge` hooks, additive); `Quake/harness_render.c`/`harness.h` (`-renderhash` graph-shape digest: `Harness_RenderGraphTask (handle, name, limit)`/`Harness_RenderGraphEdge` fold names, indexed limits and edge name pairs into the chain at `Harness_RenderDrawDone`; `-renderhashdetail` prints `graph=/gn=/ge=`), `Quake/harness.c`/`tasks.c` + `quake-capi/src/tasks.rs` (`-taskcounts`: per-worker executed-task counters from both schedulers, printed at `Harness_Shutdown`); `quake-ctest` (`render_abi.rs` `frame_graph_mirrors_match_engine_headers` + 54 `abi_probe.c` rows for the six structs, closing PR #38 F3; `c_ref_prelude.h` `cb_context_t` now faithful, D5 closed for this struct); ROADMAP, ADR-007, this plan.
- Landmines found (details in the amendment log): the real graph is 25 tasks/51 edges per steady-state frame, not the ~20/~35 the scope estimated (the `r_world.c` mark/cull/chain tasks and the five `gl_screen.c` tasks count); the glue draw thunks may only call `Host_Guard` from the main thread (plan I2), on workers a raise is `Sys_Error` as in M8; `in_update_screen` stays set after a raise (the C `longjmp` never cleared it); cbindgen also emits `extern` *block* declarations whose parameters name `CbContext`/`Entity`, so the glue thunk declarations join the exclusion list.

### M10 — Ray tracing

- Scope: BLAS build, TLAS + scratch, ray-query lightmap shadows, `r_rtshadows` paths; glue TUs from M8 removed. STOP C.
- AC: AC8 (RT scenes gated only where a device supports ray query — the dev box; lavapipe lacks it, recorded).
- Landed (2026-09-13). Files: `rust/quake-capi/src/gl_mesh.rs` (the entity-BLAS half of `gl_mesh.c`: `EntityBlas` (64-byte `#[repr(C)]` mirror of `entity_blas_t`), `AsProcs` over the `VK_KHR_acceleration_structure` entry points in `vulkan_globals`, `R_AllocateEntityBLAS`/`R_FreeEntityBLAS`/`R_FreeAllEntityBLASes`, the deferred-build queue (`PendingBlasBuild`/`flush_pending_blas_builds`) and `R_UpdateAnimatedBLASes` with its memory barrier; `mesh_buffer_heap` is a private static again like the C `static glheap_t *`), `rust/quake-capi/src/r_brush.rs` (the TLAS/scratch half of `r_brush.c`: the `bmodel_tlas`/`as_scratch_buffer`/`as_scratch_buffer_size` exports the engine declares in `glquake.h`/`gl_vidsdl.c`, `R_EnsureASScratchBufferSize`/`R_FreeASScratchBuffer`, `R_CollectTLASGarbage`, `GL_DeleteBModelAccelerationStructures`/`GL_BuildBModelAccelerationStructures` (world + inline bmodel BLAS builds), `R_BuildTopLevelAccelerationStructure` (instance gather, TLAS regrow at >1024 instances, host-visible instance ring, scratch sizing) and the private `create_addressable_buffer`/`allocate_tlas`/`query_tlas_sizes` helpers; the M8 ray-query lightmap binding now reads the Rust-owned `bmodel_tlas`), `rust/quake-c-sys/src/render.rs` (`Mod_Extradata` extern; the `bmodel_tlas`/`R_CollectTLASGarbage`/`R_BuildTopLevelAccelerationStructure`/`*EntityBLAS` externs stay for the C-renderer leg and resolve to the Rust exports under `use_rust_render`), `rust/quake-capi/cbindgen.toml` (the 13 M10 names join the exclusion block; the M8 `mesh_buffer_heap`/`GLMesh_Glue_AddBLASGarbage` entries go), `meson.build` (`use_rust_render` now swaps only `r_alias_glue.c` in), `Quake/gl_mesh_glue.c` and `Quake/r_brush_glue.c` deleted (plan D5 closed), `rust/quake-capi/src/model_parse.rs` (M6 comment updated), ROADMAP, ADR-007 rows, this plan.
- Landmines found (details in the amendment log): the C `R_AllocateEntityBLAS` has no `needload` guard before `Mod_Extradata`, so a worker could reach `Mod_LoadModel (mod, true)` (the M6-recorded hole) -- the port guards it (`COMPAT`, ADR-009); cbindgen would have emitted `extern AccelerationStructureKHR bmodel_tlas;`/`extern DynBuffer as_scratch_buffer;` and mirror-typed prototypes for the exports `glquake.h` already declares; `CMDLINE_LENGTH` (256) silently drops `+cvar` `--extra-args` behind the long worktree exe path, so the `r_rtshadows 0`/`1` legs were driven through harness `cmds`; `quake-ctest` does not link under `CC=clang-cl` (`key_dest` LNK2005, `keys_ref.c` relies on `cl` merging tentative definitions), so the symbol gate ran with `cl`; the headless corpus caught the first cut of `R_AllocateEntityBLAS` reading `ray_query` through `with_ctx` (which loads the `VkDevice` and `Sys_Error`s headless) ahead of the C's early return -- the three RT entry points reachable without a device now read the guard fields raw.

### M11 — `xtask` shader pipeline + embedded pak

- Scope: D8. Byte-identity differential vs meson's `bintoc`/`mkpak` outputs; meson uses `xtask` under `use_rust_render`.
- AC: AC10.
- Landed (2026-09-14). Files: `rust/xtask` becomes lib+bin (`src/lib.rs` `repo_root`; `src/shaders.rs` the 61-job table transcribed from `meson.build` (21 sources + 40 variants), `Options::for_build` (glslang `-V --quiet` + `-g` on debug, `spirv-opt -Os --canonicalize-ids --strip-debug` skipped on debug/macOS, `--target-env vulkan1.1` for the `sops` shaders), `Tools::find` (`QUAKE_GLSLANG`/`QUAKE_SPIRV_OPT`, then `PATH`, then `$VULKAN_SDK/bin`), `compile_all`; `src/bintoc.rs` the `bintoc` text format and `deflate_raw` (`miniz_oxide` `CompressorOxide::new(0xFFF)`, the `TDEFL_MAX_PROBES_MASK` flags of `bintoc.c`); `src/pak.rs` the `mkpak` PACK writer + depfile; `src/main.rs` `shaders`/`pak`/`mkpak`/`bintoc` commands), `rust/.cargo/config.toml` (`cargo xtask` alias), `rust/quake-capi/build.rs` (new; under `render` compiles the shaders into `OUT_DIR/shaders`, writes `shaders_gen.rs` (a `Shader::name()` -> `include_bytes!` table) and builds/deflates the pak into `embedded_pak_gen.rs`), `rust/quake-capi/src/shaders.rs` + `src/embedded_pak.rs` (new; the latter exports `vkquake_pak`/`vkquake_pak_size`/`vkquake_pak_decompressed_size` for `common_fs.c`), `quake-capi/src/gl_rmisc.rs` (`shader_spv` reads the embedded table instead of 122 C externs), `quake-c-sys/src/render.rs` (the `*_spv` extern block removed), `quake-capi/Cargo.toml` (build-dep `xtask`), `cbindgen.toml` (pak symbols excluded), `meson.build` (`use_rust_render` computed right after the tool lookups; the shader jobs, `mkpak` and `bintoc -c` custom targets are skipped under it, the `quake_rs` target passes `QUAKE_GLSLANG`/`QUAKE_SPIRV_OPT`; the C-only build and the `bintoc`/`mkpak` native executables are unchanged), `scripts/harness/xtask_diff.py` (new; the AC10 differential), `rust/quake-ctest/tests/xtask_differential.rs` (new; `deflate_raw` vs the in-tree `tdefl_compress_mem_to_heap`), `xtask/src/shaders.rs` `job_table_covers_every_shader` (the table names exactly `quake_render::rmisc::Shader::ALL`), `.github/workflows/rust.yml` (the lint jobs install `glslangValidator`/`spirv-opt` and run the xtask tests), `build-linux.yml` (harness job runs `xtask_diff.py`), this plan.
- Landmines found (details in the amendment log): `-g` makes the debug-buildtype SPIR-V path-dependent, so identity is a release-buildtype claim; `miniz_oxide` never emits a stored block for an incompressible input of <=32 bytes where the C `tdefl` does (pak unaffected, pinned by test); `Shader::name()` is the bintoc symbol without `_spv`, and keying the table by the job symbol crashed `build-rs` at startup; the blobs live in `quake-capi`, not `quake-render` as D8 said, so `quake-render` stays tool-free for `miri`.

### M12 — Phase exit

- Scope: full ADR-019 suite re-run; SSIM corpus per OS (CI lavapipe + local Windows + macOS if available; record coverage honestly); validation clean; `timedemo` thresholds; loom/TSan green; unsafe inventory count per crate; ROADMAP Phase 8 status block; deletions recorded (deferred to Phase 9); `/integration-review` (user-invoked; `disable-model-invocation: true`).
- AC: AC11.
- Landed (2026-09-14). Files: `rust/quake-capi/src/r_brush.rs` (`build_surface_display_list`: signed size arithmetic for the C `(lnumverts - 4) * VERTEXSIZE * sizeof(float)` term, which the debug render leg found panicking with `attempt to add with overflow` on 3-edge surfaces), `scripts/harness/render_corpus.py` (`--validation` now fails when the run did not print `Using VK_LAYER_KHRONOS_validation`, so the flag cannot pass vacuously on a release buildtype), this plan, `docs/rust-migration/ROADMAP.md` (Phase 8 status/exit block, unsafe inventory, deletions deferred).
- Two findings that change what earlier evidence rows mean (amendment log): `-validation` is a no-op outside `_DEBUG`/`engine-debug` builds (`gl_vidsdl.c:855`, `instance.rs:122`), so every release-buildtype "zero validation messages" claim from M2 to M11 only says the harness regex saw nothing, not that the layer ran; the phase-exit validation-clean evidence is the debug-buildtype pair. And the debug windowed leg was last run at M6/M7, which let an M8 regression (`r_brush.rs` overflow, release-only wrapping) survive to M12; the debug leg is part of the per-milestone render gate from here on.
- Unsafe inventory ([ADR-004](../../rust-migration/adr/ADR-004-unsafe-policy.md), first recorded count; `grep -rhoE '\bunsafe\b' --include='*.rs' <crate>/src | wc -l`, comment-line tokens subtracted): `quake-capi` 5147, `quake-render` 323, `quake-c-sys` 284, `quake-ctest` 207, `quake-progs` 139, `quake-types` 36 (all `unsafe extern "C" fn` pointer types under `#![forbid(unsafe_code)]`), `quake-net` 18, `quake-platform` 16, and 0 in `quake-tasks`, `quake-host`, `quake-cvar`, `quake-formats`, `quake-fs`, `quake-image`, `quake-math`, `quake-snd`, `quake-util`, `xtask` (`forbid(unsafe_code)` in 9 crates + 2 `quake-c-sys` files). The ADR-004 review pass that minimises `quake-capi`'s count is Phase 10 work.

## Final verification

- Formatting: `cargo fmt --all -- --check`; `./format.sh` for touched C.
- Rust: `cargo clippy --workspace --all-targets --locked -- -D warnings`; `cargo test --workspace --locked --release` and debug; `cargo deny check` (both workspaces); `cargo audit`; loom (`RUSTFLAGS=--cfg loom cargo test -p quake-tasks --release`); TSan (nightly, Linux).
- Meson (Windows dev box, PowerShell for MSVC per memory note): `build-c`, `build-rs`, `build-rs-ctasks`, `build-rs-crender`, `build-rs-chost`, `build-rs-cprogs`, `build-c-trace`, `build-rs-trace` (+ clang-cl pair).
- Harness: `run_corpus.py --check` (4 combos) and `--compare` (C vs each leg, `-parthash`, `-sndhash`), `save_diff`, `config_diff`, `condump_diff`, `capture_diff`, `record_diff`, `netreplay_diff`, `interop_matrix`, `physics_matrix`, `builtin_diff`, `trace_diff`, `run_formats_corpus`, `check_capi_signatures.sh`, `check_headers.sh`, `check_ctest_symbols.sh`, **new** `render_corpus.py --stability/--compare/--timedemo`.
- CI workflows affected: all six build workflows + `rust.yml`.
- Cannot run locally: Linux/macOS/MinGW/arm64 legs, TSan (unless WSL), lavapipe — push after each milestone and read CI (Phase 7 M11 lesson: M3–M11 unpushed hid two Linux-only defects).
- Debug buildtype (M12 lesson): `render_corpus.py --vkquake build-c-debug --compare build-rs-debug --validation` is the only leg on which `--validation` means anything (the layer is `_DEBUG`-only) and the only one with overflow checks on; run it every milestone, against the C *debug* build (`_DEBUG` changes `freeknownstrings` and `MI_DEBUG`, so release C is not its oracle). Windows notes: `check_headers.sh` needs vcvars + `CC=clang`; `check_ctest_symbols.sh` needs MSVC `link.exe` ahead of Git's on `PATH`.

## Risks, assumptions, and open questions

| ID | Type | Item | Mitigation/decision | Status |
|---|---|---|---|---|
| RA1 | risk | Windowed harness on CI: lavapipe + xvfb + SDL2 video may not initialise, or SSIM noise floor on lavapipe differs from GPUs | C-vs-C stability run sets per-platform thresholds; Windows dev box is the reference; CI gates `-renderhash` (exact) even if SSIM is advisory there | open |
| RA2 | risk | No Vulkan on Windows/macOS CI runners → three-OS SSIM exit criterion unsatisfiable in CI | Probe at M1; fall back to recorded local runs (Phase 7 goldens precedent), amendment-logged. PR #33 probes: Windows runner has no usable Vulkan device (engine exits -1, harness timeout, 0 screenshots); macOS runner's MoltenVK aborts in `vkCreateInstance` (`-[AppleParavirtDevice newArgumentEncoderWithLayout:]`, exit -6). Linux lavapipe stability step is green (five entries, SSIM 1.0, hashes identical). The SSIM gate is Linux CI + Windows local; macOS coverage is local-only if a machine is available at M12 | closed |
| RA3 | risk | `crossbeam-deque` isn't loom-instrumented | `Queue` trait; loom covers the state machine with a mutex queue; TSan covers the real deque | decided (D3) |
| RA4 | risk | Rust worker threads are not SDL threads: SDL-side per-thread state (`SDL_GetError`), thread naming on macOS/Windows | `std::thread::Builder::name` sets OS thread names; no worker calls SDL today (`tasks.c` never does); verify at M2 | open |
| RA5 | risk | Handle value/slot-reuse order differs from C's ring (D3) | Documented as internal; no caller persists handles across frames (grep at M2) | decided |
| RA6 | risk | `gl_rmisc.c` is 4.9k lines of pipeline creation with specialization constants — transliteration errors surface only as validation errors or visual diffs | `-renderhash` pipeline preamble, validation-fail gate, per-scene SSIM. M5: the `P` line (creation-order chain over pipeline names, count) is identical C/Rust (`P 0 330`, `P 1 660` after the frame-1 `vid_restart`), every entry is `-renderhash`-identical with validation clean | closed at M5 |
| RA7 | risk | SIMD cull parity across SSE/NEON/scalar and `-ffp-contract=off` | Scalar reference impl, `-renderhash` exact cull bitsets, ADR-010 per-platform parity | open |
| RA8 | risk | `ash` API surface vs the engine's 1.4-era extension use (present-wait2, full-screen-exclusive) | Check `ash` 0.38 coverage at M6; raw `vk::` function pointers via `ash::vk::*Fn` for anything missing (still ash types, no new crate) | closed at M6: ash 0.38 covers `VK_EXT_full_screen_exclusive`, `VK_KHR_present_id`/`present_wait` and the ray-query bundle but not `VK_KHR_present_id2`/`present_wait2`; the four `repr(C)` structs, the `p_next` chain markers and the `vkWaitForPresent2KHR` pointer type are declared by hand in `quake-render::vid` from `vulkan_core.h`, loaded through the same `vkGetDeviceProcAddr` path as C; no new crate |
| RA9 | assumption | Debug-build `_DEBUG` ⇔ cargo `engine-debug` for `TestTasks_f`/`GL_HeapTest_f` linkage | Closed by amendment: both test commands stay verbatim C in glue TUs under `#ifdef _DEBUG`, so no cross-language linkage assumption remains | closed |
| RA10 | risk | Multi-session phase; context loss | Plan + amendment log are the durable state; one milestone per commit; push after each | accepted |
| RA11 | scope | ROADMAP exit wording "switches removed" vs C-oracle-until-Phase-9 | Amendment (D1); Phase 7 precedent | decided, flag to owner |
| RA12 | scope | "Individual switches" per sub-slice interpreted as per-crate switches + per-slice commits | Amendment (D1) | decided, flag to owner |
| RA13 | risk | `atomics.h` MSVC arm has weaker ordering than C11 arm; Rust uses `SeqCst`/`AcqRel` per the C11 arm | Per-platform parity is about sim state, not memory ordering; stronger ordering is safe; TSan on Linux | accepted |
| RA14 | risk | `-renderhash` draw records key pipelines by `VkPipeline` handle, which the dev-box driver aliases across identical pipeline state (M2 amendment). Second C-only outlier at the post-review re-validation: `e1m1-map` frame 15 onward in one `build-c` run, while nine further runs (three C-vs-C stability, three C-vs-Rust, one validation compare, both builds) agree on the stable value and the Rust build never diverged | Key on pipeline identity (name hash in `vulkan_pipeline_t`) with the pipeline port at M5, earlier if the outlier recurs. M3 evidence: `e1m1-map` is bimodal at frame 15 on the dev box (two C values, each matched by the Rust build in paired runs; one C-vs-C run paired the two), and one C-vs-Rust run differed from frame 1 on the Rust side, unreproduced in the validation run and the dedicated repeats, with identical stdout. The heap cannot move a `VkPipeline` handle; keying on pipeline identity is the fix. PR #34 review follow-up (2026-09-06): the frame-1 outlier appears in exactly one of 207 archived e1m1-map runs (`a1907d674a1223e1` vs the batch-stable values), that run had no `-renderhashdetail` output, and a dedicated re-run with detail on (six C-only stability runs, three C-vs-Rust compares, 283 frames each) is identical throughout; so it is unreproduced, uncharacterised beyond "one Rust-side run, identical stdout", and stays under this row for the M5 fix. **M5 root cause (2026-09-10):** not driver aliasing — `harness_render.c` never cleared its handle table on `R_DestroyPipelines`, so the frame-1 `vid_restart` recreation (`gl_vidsdl.c` Destroy/Create) registered 660 pipelines into 512 slots: reused handles were replaced in place, new ones appended until the cap, the rest dropped (name hash 0) and stale entries kept; which *used* pipelines lost their name depended on driver handle-reuse order — deterministic in C, run-to-run variable in the Rust build (demo1 `c28`/`ent` from frame 11, exactly the M2 outlier shape). The "151 aliased handles" measurement was this recreation reuse. Fix: `Harness_RenderPipelinesDestroyed` clears the table at the start of `R_DestroyPipelines` in both builds; 330 < 512 so the table never overflows and handle keys are sound. Evidence: three C-vs-Rust demo1 runs identical, `--stability --runs 6` on the Rust build identical over 1925 frames, the C chain unchanged by the fix | closed at M5 |
| RA15 | risk | `unsafe impl Sync for CJob` (`quake-capi/src/tasks.rs`) hands the same mutable 128-byte payload to every worker of an indexed task; loom and TSan run `-p quake-tasks` only, so this impl and the C task functions behind it are outside the modelled/sanitized surface | Same contract as `tasks.c` (`task->payload` shared by design); covered by the in-engine gates (`test_tasks`, corpus compares, render corpus). An in-engine TSan run of the mixed build is the Linux-only follow-up already recorded in the ADR-016 amendment | accepted |
| RA16 | risk | `new_job` zero-fills the payload when `payload` is null, where `tasks.c` leaves the slot's stale bytes | Scan of every `Task_Assign(Indexed)?Func` caller in `Quake/*.c` and `rust/quake-capi/src/*.rs`: none passes NULL, so the difference is unobservable; recorded so a future null-payload caller is not assumed to see stale bytes | accepted |
| RA14 | risk | `-renderhash` names draws by `VkPipeline` handle; drivers that dedupe identical pipeline state alias handles across differently named pipelines (151 of 465 distinct handles on the dev box), so attribution is "last registered name" and one unreproduced C-only outlier run differed from frame 11 | Deterministic on the dev box (identical registration pattern run to run) and never seen on the Rust side; key records on a pipeline-identity hash in `vulkan_pipeline_t` at M5, sooner if it recurs; see the M2 amendment. Closed at M5 by the root cause above (the table-clear hook, not the planned struct change) | closed at M5 |

## Plan amendment log

| Date | Repository contradiction/evidence | Smallest amendment | Acceptance impact | Approval |
|---|---|---|---|---|
| 2026-09-05 | ROADMAP:248 "switches removed; renderer fully Rust" conflicts with ADR-019/Phase 7 practice of C staying the oracle until Phase 9 | Keep `use_rust_tasks`/`use_rust_render` and the C sources; record deletions | AC11 wording | approved with plan |
| 2026-09-05 | ROADMAP:227 "sub-slices land behind individual switches" | Two crate-level switches; sub-slices are landing commits | none | approved with plan |
| 2026-09-05 | `TestTasks_f` is referenced from `host_glue.c:184` and `host.c:1281`; `use_rust_tasks` is orthogonal to `use_rust_host` | D3: add `Quake/tasks_glue.c` carrying `TestTasks_f` verbatim C; RA9 closed | none | approved (design-agent finding, verified by grep) |
| 2026-09-05 | Alias/MD5/MD3 skin fan-outs (`gl_model.c:2011,2219`) are not `no_rendering`-guarded and run headless; `Tasks_NumWorkers()` chunking is sim-observable | I1 rewritten: `run_corpus.py --compare` is a real M2 gate; both sides need matching `-pinnedworkers` | AC5 strengthened | approved |
| 2026-09-05 | ADR-016 claims `atomics.h` retires with `tasks.c` (15 C + 7 header users remain) and that TSan replaces a helgrind workflow (none exists in CI) | ADR-016 amendment note at M2; TSan recorded as net-new | none | approved |
| 2026-09-05 | Existing ctest stub TUs already define the three gl_heap seams as plain symbols; prelude lacks four gl_heap declarations; two C defects in gl_heap.c | M3 landmine list added | AC7 wording | approved |
| 2026-09-05 (M1) | Draw-call structure is only observable at the `vk_cmd_draw*` dispatch pointers `gl_vidsdl.c` resolves; `r_world.c` has no single seam and cull decisions are made per entity in `gl_rmain.c` | `-renderhash` lives in a new `Quake/harness_render.c` (also in `common.make`): trampolines installed on the dispatch pointers after device creation (`gl_vidsdl.c`), pipeline registry (`gl_rmisc.c`), entity cull fold (`gl_rmain.c`), per-frame fold at `SCR_DrawDone` (`gl_screen.c`); `r_world.c` untouched | none (D6 file list) | applied |
| 2026-09-05 (M1) | `R_DrawEntitiesOnList` hands entities to whichever recording task asks next, so the entity contexts' order and partition vary run to run | entity-context draws are folded commutatively (`ent=`/`en=`); every other context stays an ordered chain; draws recorded after `SCR_DrawDone` (GUI, post-process) fold into the next frame's line | AC4 unchanged (still exact) | applied |
| 2026-09-05 (M1) | `take_screenshot` was consumed by whichever end-rendering task ran next — the previous frame's, still in flight, in ~1 of 3 runs — and that task cleared a request made after its own check, dropping screenshots | M1 engine change in `gl_vidsdl.c`: the flag is handed over on the main thread in `GL_EndRendering` via `end_rendering_parms_t.screenshot`, so a screenshot always captures the frame issued after the command. Windowed-only path, no sim state touched (state-hash goldens unaffected) | AC4 | applied |
| 2026-09-05 (M1) | Windowed runs were non-deterministic for reasons outside the renderer: mouse-grab view delta, `S_StartSound`'s mixer-clock-dependent `COM_Rand` consumption shifting particle lifetimes, the asynchronous "Wrote …" notify landing on a wall-clock frame | `render_corpus.py` launches with `-nomouse -nosound` and a `0 con_notifytime 0` prelude; `timedemo` entries run without `-renderhash` (fixed dt would void the fps) | none | applied |
| 2026-09-05 (M1) | `main_sdl.c` floors every non-fixed-dt harness frame at `sys_ticrate` (0.025 s, a Phase 7 two-process networking safeguard), so `--timedemo` measured 38.6 fps on every build — a pacing artifact, not renderer throughput | the floor exempts `cls.timedemo` (no server, no peer); M1 engine change in `main_sdl.c`, harness-only path | AC6/AC11 timedemo threshold now measures rendering | applied |
| 2026-09-05 (M1) | The Linux CI Vulkan SDK subset has no validation layer; Windows/macOS runners' Vulkan availability unknown (RA2) | `--validation` is a local Windows gate; Linux CI runs `--stability` on lavapipe without it; non-fatal `render_corpus.py --runs 1` probe steps added to the Windows and macOS harness jobs — their first result decides RA2 | AC4 wording | applied |
| 2026-09-05 (M2) | `tasks.h` includes `q_stdinc.h`, which pulls in `SDL.h`; `check_capi_signatures.sh` compiles its probe without SDL include paths | The script claims `__TASKS_H` and carries a hand-copied slice of the three typedefs and eleven declarations (the `gl_texmgr`/`glquake` precedent in the same script); drift is still caught because `host.c` compiles the real `tasks.h` next to `quake_rs.h` in every engine build | none | applied |
| 2026-09-05 (M2) | RA5: C recycles slots through an MPMC ring, Rust through a free list, so handle values after reuse differ | Accepted as internal: the only cross-frame handle is `prev_end_rendering_task` (`gl_vidsdl.c:228`), used through `Task_AddDependency` (`gl_screen.c:1266-1288`, no-op once retired) and `Task_Join` (`gl_vidsdl.c:3452-3455`, true on a stale handle), both epoch-safe; recorded in the ADR-016 amendment | none | applied |
| 2026-09-05 (M2) | `-Dbuildtype=debug` mixed builds fail to link on MSVC (`cl` and `clang-cl` alike): `quake_rs_host_shutdown` (`quake-capi/src/host.rs:1994`, Phase 7, present on `master`) references `printf`, which the UCRT only provides inline; release builds link, debug builds need `legacy_stdio_definitions.lib` | Pre-existing Phase 7 defect, outside M2 scope: `test_tasks` was verified with a local-only `-Dc_link_args=legacy_stdio_definitions.lib` configuration of `build-rs-debug`. The fix (link that library under `use_rust` on MSVC, or route the message through `Sys_Printf`) is a separate scoped change | AC5 evidence notes the workaround | applied |
| 2026-09-05 (M2) | M1 harness defect found by the M2 render corpus: C-vs-C `-renderhash` flaked on demo1 screenshot frames (chain `c116`, the `SCBX_GUI` context; three of four runs) while Rust-vs-C, ctasks-vs-Rust and validation runs were identical. Cause: `con_notifytime 0` leaves a one-frame notify window because `con_times` is a float and `realtime` a double (`Con_NotifyAlpha`), and the "Wrote …" line prints from the asynchronous end-rendering task on a wall-clock frame | `render_corpus.py` prelude becomes `0 con_notifytime -1` (notify lines never hashed; the console proper still is); README updated; no engine change | AC4/AC6 evidence re-run after the fix | applied |
| 2026-09-05 (M2) | `TestTasks_f` is `#ifdef _DEBUG` C in `tasks_glue.c`, so the plan wording "`TestTasks_f` under `engine-debug`" in the M2 scope is superseded by the D3 amendment; the Rust shim exports only the eleven ABI entry points | M2 scope text read with D3: no Rust `TestTasks_f` | none | applied |
| 2026-09-05 (M2) | M1 harness weakness found while triaging one C-side outlier: `-renderhash` draw records name a pipeline by looking its `VkPipeline` handle up in `renderhash_pipelines[]`, but the driver on the dev box dedupes identical pipeline state, so registrations alias (temporary instrumentation, C-vs-C `start-map`: 1320 registrations, 390 duplicate handles, 465 distinct, 151 handles carrying two different names, e.g. `particles_post_oit` sharing a handle with `fte_particles_blend_color_tris_post_oit`; the pattern is identical run to run). A record therefore carries "the last name registered for that handle", not the pipeline the draw bound. One C-only `--validation` run of `demo1-frames` attributed the view-model (`c28`) and entity-fold records differently from frame 11 on; the run was not reproducible (three C-vs-C stability runs, three further C-vs-Rust runs and the final validation compare all agree), the Rust side never diverged, and no root cause for that single run was established | Recorded as RA14; no engine change at M2. The robust fix keys records on pipeline identity (a name hash carried in `vulkan_pipeline_t`, registered at creation) instead of the driver handle, which is an engine struct change that lands with the pipeline port (M5) or earlier if the outlier recurs | AC6 evidence notes the outlier and the clean re-run | applied (superseded at M5: the root cause was the harness table never being cleared on `R_DestroyPipelines`; fixed with a clear hook, no `vulkan_pipeline_t` change) |
| 2026-09-06 (M2, PR #33) | The `rust.yml` `tsan` job failed on its first real run: the workspace profiles set `panic = "abort"` while `cargo test` builds the harness with unwinding, and under `-Zbuild-std` the rebuilt `panic_abort` then fails ("does not have the panic strategy `abort`"); reproduced and fixed in WSL Ubuntu with the same command | The job passes `--config 'profile.release.panic="unwind"'` (job-only; the staticlib keeps `abort`) | AC3: TSan now has a local run too (23 tests, no reports) | applied |
| 2026-09-06 (M2, PR #33) | Integration review (compatibility-reviewer, STOP A on M1+M2): F1 (medium) `indexed_task_counters` was laid out `slot * workers + worker`, inverting `tasks.c`'s `MAX_PENDING_TASKS * worker + slot` (`IndexedTaskCounterIndex`), so the counters two workers touch during one fan-out shared a cache line; F2 (medium) AC1-AC6 still read `pending` and the completion gate was unticked after M2; F3 (low) `CJob`'s `Sync` impl is outside loom/TSan; F4 (low) null-payload zero-fill differs from C; F5 RA14 already recorded | F1: `scheduler.rs` indexes counters `worker * table_size + slot` (unit/loom/clippy re-run; harness re-validated against the rebuilt `build-rs`, see the M2 evidence row); F2: AC rows and gate updated with the PR #33 CI evidence, RA2 closed; F3/F4: RA15/RA16 recorded | AC1-AC6 statuses set; AC6 re-measured | applied |
| 2026-09-06 (M2, PR #33) | Copilot review on PR #33: `Scheduler::with_table` accepted `table_size == 1` and then always panicked on the `free_slots` range; `ssim()` divided by zero on captures smaller than the 8-pixel window; `render_corpus.py` crashed with a traceback on a `cmds` entry lacking the `<frame> ` prefix | `with_table` asserts `table_size >= 2`; `ssim()` returns the global value for mean/min when no window fits; `entry_cmds()` validates every entry once and reports the entry name (both `want` sites use `screenshot_count()`). All three are edge-case hardening with no behavior change on the corpus | none | applied |
| 2026-09-06 (M3) | cbindgen cannot spell the types of the seven `GL_Heap*` entry points (`VkDeviceSize`/`VkDeviceMemory` from `vulkan_core.h`, `vulkan_memory_type_t`/`glheapstats_t` from `glquake.h`/`gl_heap.h`, opaque `glheap_t`/`glheapallocation_t`), and the Rust signatures use `ash::vk::DeviceMemory`/`VulkanMemoryType` | The declarations are hand-written in `cbindgen.toml`'s preamble under `gl_heap.h`'s `__HEAP__` guard, the Rust items are excluded from export, and `check_capi_signatures.sh` compiles `gl_heap.h` next to `quake_rs.h` with a hand `VkDeviceSize`/`VkDeviceMemory`/`vulkan_memory_type_t` slice (the M2 `tasks.h` precedent) | none | applied |
| 2026-09-06 (M3) | `glheap_t` is opaque across the ABI, so `Quake/gl_heap_glue.c` cannot carry `TestHeapCleanState`/`TestHeapConsistency` verbatim (they walk `heap->segments[]`) | The glue's `test_gl_heap` keeps `GL_HeapTest_f` verbatim and reduces the two checks to their `GL_HeapGetStats` parts; the structural checks are `Heap::check_consistency`/`check_clean_state` in Rust, exercised per operation by the differential and the `quake-render` unit tests | AC7 wording | applied |
| 2026-09-06 (M3) | The differential found that `TestHeapCleanState` holds only for segments of at least 2^7 pages: `GL_HeapCreate` sets bit 0 in all eight size-class bitfields (`gl_heap.c:227`) but a coalesced free block only re-sets classes up to `log2(size_in_pages)` (`:159-162`). Both `GL_HeapTest_f` (256 pages) and the engine heaps (mesh/texture segments ≫ 128 pages) are above the bound; the Rust reproduces the C bit for bit | The differential applies the clean-state check only to configurations with ≥ 128 pages per segment and compares `glheapstats_t` on every configuration; no engine change | none | applied |
| 2026-09-06 (M3) | `quake-ctest` needs the `ash` handle type to compare `GL_HeapGetAllocationMemory` across the two sides; `quake-render`'s `Counter` for the fake backend is `()` and the C `num_allocations` pointer is a pure pass-through in both implementations | `ash` is a `quake-ctest` dependency with the same feature set; the differential passes `NULL` for the C counter (the oracle's fake seams accept it), and the C fake handles are the same sequential values the Rust fake produces, so segment identity and dedicated-ness are compared through the handle | none | applied |
| 2026-09-06 (M3) | `GL_HeapDestroy` in C never frees the heap struct (the only caller is the `_DEBUG` self-test) | The Rust shim leaks the box the same way (`Heap::destroy` releases the segments); documented on the shim | none | applied |
| 2026-09-06 (M3, PR #34 review) | `ash` was an unconditional dependency of `quake-types`, so it reached every build (incl. the fuzz workspace and a `use_rust_render=disabled` staticlib) while ADR-003 said it reaches the staticlib only under `render` | `quake-types` gains a `render` feature (`dep:ash` + the `render` module); `quake-render`/`quake-ctest` enable it, `quake-capi`'s `render` feature forwards it; ADR-003 text corrected; fuzz `Cargo.lock` drops `ash` | none | applied |
| 2026-09-06 (M3, PR #34 review) | The render ABI probe's `vulkan_memory_t`/`vulkan_memory_type_t`/`VkDeviceMemory` rows measure the prelude's hand copies, not `glquake.h`/`vulkan_core.h`, so ADR-011's "compiled from the engine's own headers" and the 32-bit claim were overstated | Probe/ADR-011/mirror docs say what is measured; the real-header check is a `COMPILE_TIME_ASSERT` block in `Quake/gl_heap_glue.c` (compiled under `use_rust_render`); the prelude's `VkDeviceMemory` follows `VK_USE_64_BIT_PTR_DEFINES`; 32-bit recorded as unchecked with the D2 fallback | AC9 (heap part) wording | applied |
| 2026-09-06 (M3, PR #34 review) | `GL_HeapCreate` took the `repr(C)` enum by value across the ABI (an out-of-range `int` from C would be UB on the Rust side); `GL_HeapGetStats` derived its `*mut` from a `&mut` | The shim takes `c_int` and maps 0/1/2, anything else is `Sys_Error`; `Heap::stats_ptr` (the crate's one documented `unsafe fn`) derives the pointer from the raw heap pointer. Kept as-is with a note: the shim's `assert!`s on zero size/alignment stay on in release (C's are `NDEBUG`-gated; `panic = "abort"`, no caller passes 0) | none | applied |
| 2026-09-06 (M3, PR #34 review) | `Heap::destroy` clears the segment list where the C leaves `heap->segments`/`num_segments` dangling; `check_consistency`'s zero-size guard is an addition (the C loops forever) and its counter sum was non-wrapping; the differential capped segments at 4096 pages so the outer `mask_offset` loop and the `q_align` `uint16_t` wrap never reached the oracle | Divergences documented on the functions (`wrapping_add` for the sum, `debug_assert!` on `Q_log2 (0)`); `config_strategy` gains 8192 pages and a fixed `MAX_PAGES` trace (`large_segment_trace_matches_c`) drives past page 4096 and through the wrap on both sides | AC7 strengthened | applied |
| 2026-09-06/07 (STOP A extended to M3) | The plan scheduled the STOP A review after M2; PR #34 landed M3 with its own PR review, so the compatibility-reviewer pass was extended to M3 before M4 started | Verdict NON-BLOCKING: every allocation-path construct matched `gl_heap.c` line by line (alignment padding/split, coalesce order, size-class bitfields, dedicated path without `pNext`, small-alloc bucket math, segment-growth loop). Three minor findings, none acted on: `GL_HeapDestroy` leaves C with stale segments while Rust starts empty (only the `_DEBUG` self-test destroys a heap); `GL_HeapGetAllocationMemory` returns a snapshot rather than a live segment deref (identical while the segment lives); `GL_HeapAllocate`'s size/alignment asserts are unconditional where C's `assert` compiles out under `NDEBUG` (no caller passes 0). Not checked by the reviewer: `GL_HeapDestroy` callers beyond the two `GL_HeapCreate` sites | none | recorded |
| 2026-09-07 (M4) | `TexMgr_Init` registers two cvars and one command; `Cvar_RegisterVariable`/`Cmd_AddCommand` can `Host_Error` (longjmp), which must not cross a Rust frame (ADR-009) | `void TexMgr_Init (void)` stays C in `Quake/gl_texmgr_glue.c` and calls `Host_Reraise (quake_rs_texmgr_init ())`; the registrations run in `Host_Guard` thunks (`TexMgr_Glue_RegisterVariables`/`RegisterCommands`) whose status the Rust init returns. The other entry points only `Sys_Error` | none | applied |
| 2026-09-07 (M4) | `TexMgr_Downsample` is the `stb_image_resize` implementation (`STB_IMAGE_RESIZE_IMPLEMENTATION` lives in `gl_texmgr.c`); porting the resampler would not be bit-identical | Function-level carve-out (D5 allows it via the glue TU): `TexMgr_Glue_Downsample` keeps the stb implementation in C, the Rust `TexMgrBackend::downsample` calls it | none | applied |
| 2026-09-07 (M4) | `gl_texmgr.c` reads eleven `vulkan_globals` members (device, image limits, colour format, four samplers, warp render pass, two descriptor-set layouts); `vulkan_globals` is C-owned until M5 and `glquake.h` cannot be a bindgen root (ADR-011) | `TexMgr_Glue_VulkanEnv` copies them into `texmgr_glue_env_t`, layout-asserted on both sides, sampled fresh at each use (`vid_restart`/`vid_anisotropic` recreate the samplers). The palettes, the six well-known texture pointers and the two cvars keep C storage in the glue TU for their direct C readers (ADR-007 row added) | none; the accessor retires at M5 | applied |
| 2026-09-07 (M4) | C defects in `gl_texmgr.c` the port had to decide on: (a) `TexMgr_CollectGarbage` (`:1743`) frees `garbage->descriptor_set` through the CS-write layout when a `storage_descriptor_set` exists; (b) cubemap loads take `data` as a table of six face pointers, a mipmapped cubemap passes `num_mips * 6` regions from a `MAX_MIPS` (16) array (`:1305`, out-of-bounds read), and the `TEXPREF_PREMULTIPLY`/`TEXPREF_ALPHA` fixups and the lump/image sources would treat the pointer table as pixels; (c) `TexMgr_8to32` indexes a short miptex palette out of bounds; (d) a short `fread` in `TexMgr_ReloadImage` jumps to `invalid` without `fclose`; (e) the RGBA blue-noise tile's alpha bytes come from an uninitialised `TEMP_ALLOC` buffer | (a) replicated (`// COMPAT:` at the free); (b) unreachable in the engine — `gl_sky.c` loads cubemaps with `TEXPREF_NONE` from a face table — so the Rust clamps the region count to `MAX_MIPS` (the in-bounds reading, commented) and the differential excludes mipmap/premultiply/alpha and lump/image sources for cubemaps; (c) an abort in Rust (comment); (d) the Rust closes the file; (e) zero in Rust (doc comment). None is pixel-visible on the corpus (`-renderhash` and SSIM identical) | none | applied |
| 2026-09-07 (M4) | In the differential, `TEXPREF_PERSIST` memory-sourced textures survive `TexMgr_NewGame` while the test frees its pixel buffers between cases | The test strips `PERSIST` from memory-sourced loads (file-sourced loads keep it: their reload path re-reads the file), which is a test constraint, not a semantic gap — the engine's persistent memory textures are the six builtins, covered by the scripted sequence | none | applied |
| 2026-09-07 (M4) | `TexMgr_ReloadImages` has no caller outside `gl_texmgr.c` (the engine reloads through `TexMgr_ReloadNobrightImages` and per-texture `TexMgr_ReloadImage`) | Not exported; `gl_texmgr.h` unchanged, so the C-only build keeps it and `check_capi_signatures.sh` is unaffected | none | applied |
| 2026-09-07 (M4, PR #35 review) | `gl_texmgr.h:110` declared `TexMgr_InitHeap ()` old-style; an empty `()` is compatible with any prototype, so neither the header nor the `check_capi_signatures.sh` model could flag drift for that export | Declaration and definition spelled `(void)` (no ABI change; every caller passes no arguments); the script model follows the header again | none | applied |
| 2026-09-07 (M4, PR #35 review) | Seven findings from the owner's review, all accepted: (1) `TexMgr` kept the texture arena as `Box<[GlTexture]>` while the free/active lists and every C caller hold raw pointers into it, and `upload_with` held a `&mut GlTexture` across the `delete_texture` under the lock while the same slot was written through the list -- both undefined under Stacked and Tree Borrows even though the differential could not see it; (2) the `PREMULTIPLY`/`ALPHA` passes in `load_image32_prepare` walked `width*height` pixels behind the six-pointer face table for `SRC_RGBA_CUBEMAP`; (3) `TexMgr_8to32` indexed the palette without a bound, so a Valve miptex with a short palette and an index past it aborted where the C reads garbage; (4) the picmip saturating cast, `wrapping_shr` and the 8to32 bound lacked `// COMPAT:` markers; (5) `EngineBackend` fetched `texmgr_glue_env_t` on every Vulkan call inside one entry point; (6) the glue's `COMPILE_TIME_ASSERT`s were unconditional while the Rust asserts are 64-bit-guarded, so a 32-bit C-only build would have failed to compile; (7) the `TexMgr_LoadImage` safety doc said `data` may be null for every format; the reviewer also asked for a Miri job with a `TexMgr` smoke test | (1) the arena is `*mut [GlTexture]` from `Box::into_raw`, freed in `Drop` and on `init_list`, so no `&mut` to the allocation exists after the first slot pointer is derived; `upload_with`/`load_image32_with` re-derive `&mut GlTexture` after the delete and never hold one across a `TexMgrLock::with`; (2) both passes skip cubemaps (no caller reaches them: `gl_sky.c` loads cubemaps with `TEXPREF_NONE`); (3) an index past the palette yields transparent black (`// COMPAT:` note: C reads out of bounds); (4) markers added; (5) `EngineBackend { glue: GlueEnv }` is built once per exported entry point; (6) `TEXMGR_LAYOUT_64(expr)` guards the ten layout asserts and the env size assert, and `quake-types::render` states the 32-bit layout is unverified; (7) doc says null only for `TEXPREF_WARPIMAGE`; new `rust.yml` `miri` job (nightly, `components: miri`, `quake-render` tests under Stacked Borrows and `-Zmiri-tree-borrows`), with `texmgr_smoke` over a fake backend (`SmokeBackend`/`SmokeHeap`) covering the arena, indexed/RGBA/Valve-palette uploads, the overwrite cache hit, an in-place `TexMgr_LoadImage32`, descriptor refresh, the garbage ring and teardown; `page_saturation_at_max_pages` is `#[cfg_attr(miri, ignore)]` (65534 allocations, minutes under Miri). Miri then found two more: the RGBA path reads `data` as `unsigned *`, and `init` built the bluenoise pixels in a `Vec<u8>` (aligned only by the allocator's habit) -- now a `Vec<u32>` with the same bytes, and the 4-byte contract is in the `load_image32_prepare`/`load_image_with`/`TexMgr_LoadImage` safety docs | AC8/AC9 unchanged (ADR-004 unsafe inventory: the arena is one more raw pointer, one more `SAFETY` block in `release_arena`) | applied |
| 2026-09-10 (M5) | D5 says the prelude's `vulkanglobals_t`/`cb_context_t` approximations are replaced by the real mirrors as each flips, but `c_ref_prelude.h` cannot spell the real members (Vulkan handles, `PFN_vk*` function pointers, SDK enums) without the SDK headers the ctest build deliberately avoids | `vulkanglobals_t` (and later `cb_context_t`) stay compile-only in the prelude; the five embedded structs it *can* spell (`dynbuffer_t`, `vulkan_pipeline_layout_t`, `vulkan_pipeline_t`, `vulkan_desc_set_layout_t`, `buffer_create_info_t`) are faithful copies with probe rows, and the real-header check for `vulkanglobals_t` is the 152-line `COMPILE_TIME_ASSERT` block in `Quake/gl_rmisc_glue.c` (the `gl_texmgr_glue.c` pattern) | AC9 wording | applied |
| 2026-09-10 (M5) | RA14 root cause: the M1 harness never cleared `renderhash_pipelines[]` on `R_DestroyPipelines`; the frame-1 `vid_restart` re-registers all 330 pipelines (660 > the 512 cap), so dropped/stale entries depended on driver handle reuse, deterministic in C and not in the Rust build | `Harness_RenderPipelinesDestroyed` hook at the start of `R_DestroyPipelines` (C and `quake-render::rmisc::pipelines::destroy_pipelines` via `Engine::pipelines_destroyed`) instead of the planned name hash in `vulkan_pipeline_t` (no engine struct change); the `P` preamble line (RA6) records count and creation-order name chain per (re)creation | RA6/RA14 closed; AC8 M5 evidence | applied |
| 2026-09-10 (M5) | `r_brush.c:2821` passes `NULL` for the buffer/offset out-pointers of `R_StorageAllocate` (the C `R_DynBufferAllocate` null-checks every out-parameter) | The Rust `R_Vertex/Index/Uniform/StorageAllocate` exports write through a null-tolerant `write_opt` helper; safety docs say "null or valid" | none | applied |
| 2026-09-10 (M5) | The M4 `texmgr_glue_env_t`/`GlueEnv` accessor for the eleven `vulkan_globals` members was a stand-in for the M5 ownership flip | Retired: `gl_texmgr.rs` reads the Rust-owned `vulkan_globals` directly; `Quake/gl_texmgr_glue.c` loses the accessor | none | applied |
| 2026-09-11 (M5, PR #36 review) | Twelve findings (Copilot and the owner), eleven accepted: (1) `with_ctx` materialised a `&mut vulkan_globals` for every export, but `R_StagingAllocate`/`R_StagingEndCopy`, `R_AllocateVulkanMemory` and the `R_*Allocate` rings run on task workers (`gl_model.c` miptex loads, `r_alias.c` uniform allocations) while the main thread touches the struct, and init-time exports re-enter through C (`R_InitSamplers` → `GL_WaitForDeviceIdle` → `R_SubmitStagingBuffers`), so the doc comment's "single-threaded" claim was false and the reference aliased on both axes (ADR-004); (2) `staging.rs:379` wrote `device_idle` non-atomically from workers while `GL_WaitForDeviceIdle` reads/writes it on the main thread; (3)+(4) `R_CreateStagingBuffers` and `R_InitDynamicBuffers` aligned the *requested* size where the C (`gl_rmisc.c:527`, `:868`) aligns `memoryRequirements.size`, under-allocating when a driver reports a larger requirement; (5) the staging lock has no owner notion, so a same-thread nesting SDL's recursive `QMutex` would allow hangs silently; (6) `set_fragment_spec` cast a `SpecializationInfo<'_>` to `'static`; (7) push-constant sizes `40`/`44` were hand-transcribed; (8) `chunks_exact(4)` silently drops a trailing partial SPIR-V word; (9) `submit_buffer` `Sys_Error`s where the C ignores three `VkResult`s; (10) the README described the `P` line's `<count>` as per-line where it is cumulative and `render_corpus.py` counts `P`/`END` as "frames"; the twelfth (the `harness_render.c` cumulative count itself) was judged a reasonable design and left as is. Pinning (7) exposed a real divergence: `sizeof (skinning_push_constants_t)` is 48 (three 8-byte addresses pad the struct), not 44, so the Rust `skinning`/`skinning_8` push ranges were 4 bytes short of what `gl_mesh.c` pushes (`sizeof (pc)`); the corpus never reached it (no MD5 model under `ray_query`) | (1) `Ctx::vg` is a raw-pointer `VgPtr` (`NonNull` + `PhantomData<&UnsafeCell<_>>`); every access goes through the `vg!`/`vg_mut!` macros (one field, one statement, right-hand side evaluated before the field `&mut`), `model_slot`/`model_oit_slots` became `set_model_slot`/`set_model_oit_slots` stores, and the three whole-struct references left (`Env::new`+`PipelineInfos::defaults`, `destroy_pipelines`) are scoped with `// SAFETY:` to callback-free main-thread regions; the `with_ctx` doc and the ADR-007 row state the threading facts; (2) `AtomicBool::from_ptr` + `Relaxed` store with a `// COMPAT:` note; (3)+(4) `q_align (requirements.size, ...)`; (5) `Shared::owner: Option<ThreadId>`, a same-thread re-`acquire` asserts (panic → abort) and the module doc records the no-nesting invariant and the callers checked; (6) `PipelineInfos<'a>` carries the spec borrow on `shader_stages`/`graphics`, so the borrow checker rejects a copy outliving `spec`; (7) `#[repr(C)]` `MeshInterpolatePushConstants`/`SkinningPushConstants` mirrors in `quake-types::render` with `const` size asserts (40, 48) and `size_of` at the layouts — not probe rows, because the ctest prelude cannot include `render.h`; (8) `debug_assert_eq!(len % 4, 0)` naming the shader; (9) a divergence comment at the site; (10) README row and script wording | AC8/AC9 unchanged; the skinning range is now 48 as in C | applied |
| 2026-09-11 (M6) | RA8: ash 0.38 ships no `VK_KHR_present_id2`/`VK_KHR_present_wait2` types, which `gl_vidsdl.c` uses for `vid_maxframelatency` (`vkWaitForPresent2KHR`, `VkPresentId2KHR`) | `quake-render::vid` declares the four structs, their `Extends*` chain markers and the `PFN_vkWaitForPresent2KHR` type by hand with the `vulkan_core.h` enumerant values; everything else is the ash binding | RA8 closed | applied |
| 2026-09-11 (M6) | The plan's M6 scope said `SDL_Vulkan_GetVkGetInstanceProcAddr` loading "via `quake-platform`"; the SDL calls are C-only until Phase 9 (NG2) and `quake-platform` has no SDL binding | The glue TU exports `VID_Glue_GetInstanceProcAddr`/`VID_Glue_InstanceExtensions`/`VID_Glue_CreateSurface`/`VID_Glue_WindowMonitor` (SDL2 and SDL3 branches verbatim from `gl_vidsdl.c`); `ash::Entry::from_static_fn` wraps the pointer, so the loading path (and the macOS MoltenVK link) is unchanged | none | applied |
| 2026-09-11 (M6) | `GL_SetObjectName` and `GL_WaitForDeviceIdle` were the M3-M5 C seams (hand externs in `quake-c-sys`); both live in `gl_vidsdl.c` | They are Rust exports in `quake-capi/src/gl_vidsdl.rs`; the `gl_heap.rs`/`gl_texmgr.rs`/`gl_rmisc.rs` backends call them in-crate and the two c-sys externs are removed (the ADR-007 dual-view note in `gl_rmisc.rs` still applies: they touch `vulkan_globals` through the exported symbol) | none | applied |
| 2026-09-11 (M6) | `vid_vsync`, `vid_maxframelatency` and `r_raydebug` are `static cvar_t` in `gl_vidsdl.c`, but the Rust swap-chain/frame code reads them | Non-static in `gl_vidsdl_glue.c` (registration, callbacks and defaults unchanged; the names were file-local only, no other TU defines them); read through c-sys externs | none | applied |
| 2026-09-11 (M6) | `prev_end_rendering_task` is declared `extern` in `glquake.h:701` and written by `gl_screen.c`; the pattern-A swap must keep the data symbol | `#[no_mangle] pub static mut prev_end_rendering_task: u64` in `gl_vidsdl.rs` (ADR-007 dual view, plain scalar, main thread only); `cbindgen.toml` excludes it and the header keeps the `glquake.h` declaration | none | applied |
| 2026-09-11 (M6) | `build-rs-debug` vs `build-c` `-renderhash` preambles differ (`P 0 331` vs `P 0 330`) | Expected: `ray_debug_pipeline` is created under `_DEBUG` only (`gl_rmisc.c:2108-2133`, same in `rmisc::pipelines`), and the C reference leg was the release build; the frame chains and SSIM are identical, so no gate change — compare debug against debug when a debug-leg preamble is needed | none | applied |
| 2026-09-11 (STOP B on M4-M6, PR #37) | Integration review (compatibility-reviewer, NON-BLOCKING): F1 the `VID` doc comment claimed every entry point is serialised against the end-rendering task, but `GL_BeginRendering(use_tasks = true)` overlaps the previous frame's task (`gl_screen.c` orders it before the *begin task* only, as `gl_vidsdl.c:3473-3509` does) and materialised a `&mut VidState` to read `render_resources_created` while the task held its own; F2 the `vkCreateDevice` pNext chain is re-linked from unlinked structs in Rust (ash `push_next`) whereas C's `CHAIN_PNEXT` keeps stale tails from the feature query; F3 the `P 0 331` debug-leg row was read without its explanation (the previous row explains it). Parity confirmed by evidence for the present-id2/wait2 hand declarations, instance/device/command-buffer init, swap-chain create, acquire, submit/present tail, `GL_EndRendering` sampling and the glue partition | F1: `gl_vidsdl.rs` reads the flag through a raw field read (`render_resources_created()`, no `&mut`) and the `VID` comment states the exception; F2: the deviation is documented at the null-out in `vid/instance.rs` (not simulation- or `-renderhash`-observable; the C behaviour hands the driver stale feature structs only when an extension is present and its feature is turned off between the query and the create); F3: no change. Not compared line by line by the review: `resources.rs`, `cb.rs`, the head of `end_rendering_task`, `write_screenshot`, swap-chain views/semaphores/destroy | none | applied |
| 2026-09-11 (M6, PR #37 review + master merge) | Four Copilot findings: (1)+(2) the `dynbuf.rs`/`staging.rs` alignment of the requested size instead of `requirements.size` -- already fixed on master by the PR #36 review (f18a6e55), so the merge resolves them; (3) `GL_EndRendering` cast `render_scale` to the 4-bit field unclamped where `gl_vidsdl.c:4240` does `CLAMP (0, render_scale, 8)`, so a negative cvar value selected the 8x path; (4) `init_instance` looked up `vkEnumerateInstanceVersion` through the fatal `instance_proc`, where the C (`gl_vidsdl.c:826-836`) tolerates its absence on a Vulkan 1.0 loader. Merging master also exposed that M6 predated the PR #36 `VgPtr` discipline: `init_instance`/`init_device` took `&mut VulkanGlobals`, and `init_device` calls `Harness_RenderInstallHooks` (`Quake/harness_render.c`) from C, which rewrites `vulkan_globals.vk_cmd_draw*` while that `&mut` was live (ADR-004 aliasing); the vid modules and `cb.rs` read `ctx.vg.<field>` through the reference | (3) `.clamp(0, 8)` before the cast with a comment citing the C; (4) optional `get_instance_proc_addr` lookup, missing means 1.0; the vid layer is on the raw-pointer view: `init_instance`/`init_device` take `VgPtr<'_>` and use `vgp!`/`vgp_mut!` (the `vid` module's field-level macros over a bare `VgPtr`), `frame.rs`/`resources.rs`/`swapchain.rs` use `vg!`/`vg_mut!`, `CmdProcs::new` and `scbx_mut` take a `VgPtr`, `scbx_ptr` a raw pointer, and `GL_InitInstance`/`GL_InitDevice`/`GL_EndRendering` in `gl_vidsdl.rs` never form a `&`/`&mut vulkan_globals` (`VgPtr::from_raw`, `addr_of!` reads); reads are hoisted out of `unsafe` blocks so clippy's `unused_unsafe` holds under `engine-debug` too | AC8 unchanged; ADR-004 unsafe inventory: the vid `unsafe` blocks are field-level pointer accesses now | applied |
| 2026-09-12 (M7) | `R_UpdateEntityDlights` (`gl_rlight.c`) runs from `CL_RelinkEntities` in headless mode, where no `VkDevice` exists; the first Rust port reached it through the M5 device context and the headless corpus hung on `map` | Headless-reachable renderer paths read `vulkan_globals` fields directly (no `with_ctx`/`device()`); rule carried into M8 (world/mesh marking runs headless too) | none | applied |
| 2026-09-12 (M7) | `-renderhash` wraps only the `vulkan_globals.vk_cmd_draw*` pointers, but `gl_draw.c` (six `Draw_*` sites), `gl_sky.c`, `gl_warp.c` and `r_sprite.c` call `vkCmdDraw`/`vkCmdDrawIndexed` directly, so the C oracle under-recorded the 2D quads, the skybox, the warp update and every sprite (chain 116 and the entity fold differed against the Rust port, which routes all draws through `quake_render::cb`); a harness blind spot since M1 | The C sites draw through the pointers (identical output; the pointers resolve to the same entry points outside `-renderhash`); the OIT-resolve/postprocess `vkCmdDraw` in `gl_vidsdl.c` stays (C in both builds, M9). Rule for M8-M10: every Rust draw goes through `cb::draw*` and every C draw through `vulkan_globals` | AC8 evidence now covers those draws | applied |
| 2026-09-12 (M7) | `R_StoreEfrags` calls `PScript_RunParticleEffectState` and `R_AllocateEntityBLAS`, both `Host_Error`-capable, from inside the efrag walk; `Fog_FogCommand_f`/`Sky_SkyCommand_f` print `%g`/`%f` (ADR-005 gap); `SCR_DrawGUI` is a `setjmp` target and the loading plaque/`SCR_ModalMessage` re-enter `SCR_UpdateScreen` | The two callbacks run under `Host_Guard` in `gl_refrag_glue.c` and the Rust loop returns the guard code for `Host_Reraise` (ADR-009); the `%g` output and `SCR_DrawGUI`/plaque/modal stay C in the glue TUs; `vrect_t`/`refdef_t` mirrors move from `view.rs` to `quake-types::refdef` so the render-only build types `r_refdef` | none | applied |
| 2026-09-13 (M8) | `r_world.c:851` (`R_BackfaceCullSurfacesParallel`, scalar path) clears the culled bit in `surfvis[0]` rather than in the current 32-surface word, so the draw path still chains the back-facing surface; the SIMD path (`:577`) masks the current word | Preserved bug-for-bug in `r_world.rs` with a `COMPAT` comment (`-renderhash` cull parity would otherwise differ on the scalar leg); fixing it is a C-and-Rust behaviour change for a later phase | none | applied |
| 2026-09-13 (M8) | `R_MarkSurfaces` reaches `R_StoreEfrags`, whose `Host_Guard`ed callees can `Host_Error`; the synchronous call has a C frame to re-raise into but the parallel mark tasks run on workers, where the C original would have `longjmp`ed a main-thread `jmp_buf` from a worker thread | Synchronous path: `RWorld_MarkSurfaces` returns the guard code and `r_world_glue.c` calls `Host_Reraise` (ADR-009); task path: a raised code becomes `Sys_Error` (the worker path was already unsound in C, I2) | none | applied |
| 2026-09-13 (M8) | The lightmap accumulate/store lanes (`R_BuildLightMap`, `R_AccumulateLightmap*`) truncate the light scale to 16-bit lanes in SSE2 (`_mm_set1_epi16 (scale)`) and NEON (`vmlal_n_u16`, `vshrn_n_u32`), while the scalar tail multiplies by the full `unsigned` scale | Each lane is transcribed per arch behind the same `use_simd` gate with `COMPAT (ADR-010)` comments; the scalar reference remains the fallback for other targets | AC8 evidence is per-arch (x86_64 SSE2 locally; NEON only via the arm64 CI leg build, not executed) | applied |
| 2026-09-13 (M8) | The FTE particle *render* half (`r_part_fte_glue.c` `PScript_DrawParticleBatches`, batch/vertex emission, `PScript_DrawParticles(_ShowTris)`, `P_LoadTexture`, ~2000 lines) is Vulkan-typed like the classic half but is entangled with the C-owned ADR-007 shared state (`cl_stris`, deferred queues, per-type emit metadata) that the Rust simulation half writes; porting it in M8 alongside 8k lines of world/mesh code exceeds the milestone's bisection unit | The classic `r_part.c` render half ports now (`r_part_render.rs`); the FTE render half is deferred to a follow-up Phase 8 landing before M12 (same switch, own commit). ADR-010's `r_part_fte.c:7262` NaN-sign caller (`RotatePointAroundVector` in `PScript_UpdateParticleTypes`, glue `:2351`) therefore stays C, unchanged; its observability clause is re-checked when that half ports | AC8 for FTE particle draws is covered by the C copy in both builds (identical by construction); Phase-exit scope unchanged | applied |
| 2026-09-13 (M8) | "QC polygon builtin rendering (Phase 6 shim retired)": `pr_ext.c` draws through `Draw_*`/`GL_SetCanvas`, which M7 already resolves to the Rust exports under `render`; there is no separate polygon shim left | No M8 work; item closed by M7 | none | applied |
| 2026-09-13 (M8) | `R_GetEntityLerpedTransform` and the other `r_alias.rs`/`gl_mesh.rs` exports are declared in `glquake.h`/`gl_model.h` with `entity_t`/`aliashdr_t` parameters that cbindgen would emit under different type names, colliding with the C headers | Every M8 export is listed in the `cbindgen.toml` exclusion block (the M7 rule) | none | applied |
| 2026-09-13 (review of M7-M8, PR #38) | Integration review (compatibility-reviewer, NON-BLOCKING): F1 the synchronous marking loops in `r_world.rs` kept running after `R_StoreEfrags` raised through `Host_Guard` (the C original `longjmp`s out of `R_MarkSurfaces` at that point; `Host_Error` disconnects and nulls `cl.worldmodel`), so the remaining leaves re-ran guarded callees against a disconnected client and `setup_world_cbx_tex_ranges` re-read the nulled `cl.worldmodel`; F2 whether the NEON `vmlaq_f32` cull lanes match the clang-built C oracle depends on floating-point contraction; F3 the M8 `quake-types::render` mirrors have Rust size asserts but no C-side `abi_probe.c` rows; F4 the SoA cull gate is `target_arch` in Rust and `USE_SIMD` in C (a 32-bit GCC build without SSE2 would differ); F5 the preserved `surfvis[0]` race (amendment row above) is a non-atomic read-modify-write that TSan may flag on the scalar parallel path | F1: `store_efrags` returns whether a raise occurred and every marking loop returns at once (skipping the stat add and `setup_world_cbx_tex_ranges`; the task path breaks before `abort_on_pending_raise`), re-verified (four configs, render corpus C-vs-Rust and C-renderer-vs-Rust identical, headless corpus identical, fmt/clippy/tests); F2: the C build compiles with `-ffp-contract=off` (`meson.build`) and `core::arch` lowers `vmlaq_f32` to an unfused `simd_mul`/`simd_add`, so both sides stay unfused -- recorded as a `COMPAT (ADR-010)` comment on the NEON lanes (arm64 CI builds them, no arm64 device executes them); F3: probe rows are added when M9 flips `cb_context_t` (AC9 row); F4/F5: no change (no 32-bit CI leg; the race is the C original's, ADR-010) | none | applied |
| 2026-09-13 (PR #38 review, round 2) | Four human review comments on the M7/M8 ports: G1 `r_alias.rs` `R_DrawAliasModel` re-read `e->skinnum` per surface where the C (`r_alias.c:488`) reads it once before the surface loop, so a clamp on one surface no longer stuck for the rest (multi-surface MD3/MD5, not `-renderhash`-visible); G2 `R_SetupAliasFrame`'s MD5 pose clamp copied the `PV_QUAKE1` branch (`Con_DPrintf` + `pose1 = pose2`) where the C (`r_alias.c:303-310`) is silent and clamps both poses to 0; G3 the out-of-range frame clamp printed a `Con_DPrintf` the C (`r_alias.c:246-247`) does not; G4 `gl_refrag.rs` `R_StoreEfrags` advances `ppefrag` before the `MOD_EMITREPLACE` `continue`, where the C (`gl_refrag.c:274`) `continue`s without advancing and re-processes the same efrag forever (a `MOD_EMITREPLACE` static hangs the C renderer) -- an undocumented deviation | G1: `skinnum` hoisted above the surface loop; G2: MD5 branch silent, both poses clamp to 0; G3: the `Con_DPrintf` removed; G4: kept as a deliberate fix (a hang has no observable state to preserve) with a `COMPAT (ADR-010)` comment; the C side is left as is (C stays the oracle, NG3) and is a candidate for a separate C fix | none (no corpus scene reaches any of the four paths; `-renderhash` cannot observe G1-G3) | applied |
| 2026-09-13 (M9) | The M8 `R_StoreEfrags` guard code was re-raised by `r_world_glue.c`'s `R_MarkSurfaces` wrapper, but M9 moves the caller (`R_RenderView`) into Rust, so a `Host_Reraise` there would sit under Rust frames | `RWorld_MarkSurfaces` returns its code to `RRmain_RenderView`, which folds it with the draw-thunk codes and returns the first raised one for `gl_rmain_glue.c`'s `R_RenderView` to `Host_Reraise`; `r_world_glue.c` is deleted (its only content was the wrapper) | none | applied |
| 2026-09-13 (M9) | `R_DrawAliasModel`/`R_DrawSpriteModel` (via `Mod_Extradata*`) and `R_ShowBoundingBoxes` can `Host_Error`; the C graph runs them on worker threads (`R_DrawEntitiesTask`), where the original would `longjmp` a main-thread `jmp_buf` | The `RRmain_Glue_*` thunks call `Host_Guard` only when `!Tasks_IsWorker ()` (plan I2); on a worker the callee runs bare and a raise becomes `Sys_Error`, exactly the M8 rule for the mark tasks; `gl_screen.rs`'s serial path runs `V_RenderView`/`SCR_DrawGUI` under the `SCR_Glue_*` guards and `SCR_UpdateScreen` re-raises after the Rust frames return (ADR-009) | none (the corpus never raises inside a frame) | applied |
| 2026-09-13 (M9) | The C `SCR_UpdateScreen` sets `in_update_screen = true` and a `Host_Error` `longjmp` skips the reset, so the next frame's early-return gate stays armed until `SCR_DrawGUI`'s setjmp path clears it | Preserved: the Rust port returns the guard code with `in_update_screen` still set (`COMPAT` comment, ADR-009/ADR-010) | none | applied |
| 2026-09-13 (M9) | The scope estimated the frame graph at ~20 tasks/~35 edges; the graph-shape digest counts 25 tasks/51 edges per steady-state frame once the `r_world.c` mark/cull/chain tasks and `gl_screen.c`'s five frame tasks are included (`gn=0 ge=0` for the ten pre-world frames, `gn=5 ge=4` for the first frame without a world graph, `gn=25 ge=50` on the first world frame with no `prev_end_rendering_task` edge) | Numbers recorded; the digest hashes task names, indexed limits and edge name pairs in allocation order so any reordering or missing edge differs before a draw does; identical C vs Rust on all five entries | AC8 evidence gains the graph digest | applied |
| 2026-09-13 (M9) | `c_ref_prelude.h`'s `cb_context_s` approximation lacked the fields `gl_rmain.c` touches and listed `canvastype_t` after the struct that embeds it | The prelude now carries the faithful `cb_context_t` (and the M8 `lerpdata_t`, `lm_compute_workgroup_bounds_t`, `glRect_t`, `glMaxUsed_t`, `struct lightmap_s`) so `abi_probe.c` can size them (PR #38 F3 closed); `vulkanglobals_t` stays compile-only (M5 amendment) | AC9 | applied |
| 2026-09-13 (M9) | cbindgen emits not only Rust exports but also `extern "C"` *block* declarations whose parameter types it knows (`CbContext *`, `Entity *`), so the `RRmain_Glue_*`/`SCR_Glue_*`/`V_*` seams appeared in `quake_rs.h` under mirror type names and broke the MSVC build | The glue-thunk and engine-seam names join the `cbindgen.toml` exclusion block (extends the M7 rule to extern declarations) | none | applied |
| 2026-09-13 (M9) | The plan asked for per-worker task counts from `test_tasks`-style counters, but `test_tasks` is `_DEBUG`-only and counts its own synthetic tasks | New `-taskcounts` harness flag: `tasks.c` and `quake-capi/src/tasks.rs` bump a per-worker atomic on every executed task body (scalar once, indexed once per index) and `Harness_Shutdown` prints `Harness: taskcounts=...` on stdout, captured by `render_corpus.py`'s `stdout.txt`; independent of `harness_active`, so `timedemo` pacing is unaffected | AC (M9) worker-utilization evidence | applied |
| 2026-09-13 (M10) | The C `R_AllocateEntityBLAS` (`gl_mesh.c:657`) calls `Mod_Extradata` with no `needload` guard, unlike its `gl_mesh.c` siblings, so on a task worker (`R_StoreLeafEFrags` -> `R_StoreEfrags` -> `R_AllocateEntityBLAS`) a model whose `Mod_EnhancedModels_f` reload failed would parse -- or `Host_Error` -- off the host thread (the hole M6 recorded at `model_parse.rs`/`gl_model.c:485-504`) | The Rust `R_AllocateEntityBLAS` returns early when `needload` is set (`COMPAT (ADR-009)` comment); the entity gets no BLAS, which only matters for a model the alias draw already skips. Hole closed for the Rust render build; the C oracle keeps its `Con_Warning` | none in the corpus (`needload` is never set at draw time there) | applied |
| 2026-09-13 (M10) | The M8 `Host_Guard` seams for the BLAS calls (`Refrag_Glue_AllocateEntityBLAS` in `gl_refrag_glue.c`, `ClMain_Glue_*EntityBLAS` in `cl_main_glue.c`) and the `quake-c-sys` externs for `bmodel_tlas`, `R_CollectTLASGarbage`, `R_BuildTopLevelAccelerationStructure`, `Mod_Extradata` and the `*EntityBLAS` family are still needed by the `use_rust_render=disabled` leg, which links the C `gl_mesh.c`/`r_brush.c` | Kept: under `use_rust_render` the externs resolve to the `#[no_mangle]` Rust exports and the guard thunks wrap a function that no longer raises (its only `Host_Error` path is the guarded `Mod_Extradata`); they retire with the C-renderer leg at Phase 9 | none | applied |
| 2026-09-13 (M10) | `entity_blas_t` is `Mem_Alloc`ed in C and freed through `Mem_Free` from `R_FreeEntityBLAS` only; nothing else touches the allocation | `EntityBlas` is a `Box` behind `(*e).blas_data` (`Box::into_raw`/`Box::from_raw`, 64-byte layout assert), so the Rust build never mixes allocators for it; `mesh_buffer_heap` and `add_blas_garbage` are private (the M8 `GLMesh_Glue_AddBLASGarbage` export is gone with the glue TU) | none (allocation-side only; the heap traffic through `GL_HeapAllocate` is unchanged) | applied |
| 2026-09-13 (M10) | cbindgen emitted `extern AccelerationStructureKHR bmodel_tlas;`, `extern DynBuffer as_scratch_buffer;` and `R_AllocateEntityBLAS(Entity *)`/`R_UpdateAnimatedBLASes(CbContext *)` prototypes into `quake_rs.h`, breaking the MSVC build against the `glquake.h`/`gl_vidsdl.c` declarations of the same names | The 13 M10 exports join the `cbindgen.toml` exclusion block (M9 rule) | none | applied |
| 2026-09-13 (M10) | `render_corpus.py --extra-args="+r_rtshadows N"` did nothing: `Cmd_StuffCmds_f` reads the `cmdline` cvar clamped to `CMDLINE_LENGTH` (256, `quakedef.h:101`) and the long worktree exe path pushes the `+cvar` tail off the end, so the three modes produced byte-identical screenshots | The `r_rtshadows 0` and `1` legs ran from scratchpad corpus copies with `"0 r_rtshadows N", "0 r_rtshadows"` prepended to each entry's `cmds` (the echo confirms the value on stdout). Carry item: a `--cvar`/`--cmds` option in `render_corpus.py` | none | applied (harness use only) |
| 2026-09-13 (M10) | The headless corpus (`run_corpus.py --compare`) hung on `id1-demo1` behind a `Quake Error` box: the first Rust `R_AllocateEntityBLAS` read `vulkan_globals.ray_query` through `with_ctx`, which eagerly loads the `VkDevice` and `Sys_Error`s (`gl_rmisc: vulkan_globals.device is not created yet`) under `-headless`, where the C returns on `!vulkan_globals.ray_query` before touching the device (`CL_RelinkEntities` -> `R_AllocateEntityBLAS` runs every client frame headless too) | `R_AllocateEntityBLAS`, `R_UpdateAnimatedBLASes` and `GL_BuildBModelAccelerationStructures` (reachable through the `r_rtshadows` callback) now read the guard fields raw (`(*addr_of!(vulkan_globals)).ray_query`, the `GLMesh_UploadBuffers`/`gl_rlight.rs` idiom) ahead of any `with_ctx`; the other M10 entry points already gate on `bmodel_tlas`/`as_scratch_buffer` before loading the device. Caught by the harness, not by the windowed corpus (which always has a device) | none in the windowed build; the headless Rust build errored out instead of running | applied |
| 2026-09-13 (M10) | `check_ctest_symbols.sh` builds `quake-ctest` and under `CC=clang-cl` the link fails with `LNK2005: key_dest already defined` (`keys_ref.o` vs `stubs.o`): `keys_ref.c:155-172` relies on MSVC `cl` merging tentative definitions, and clang-cl defaults to `-fno-common` | Built `quake-ctest` with `cl` for the gate and the tests (both pass); pre-existing, not M10 code. Carry item: give `keys_ref.c` an explicit `extern` or move the definition | none | recorded |
| 2026-09-13 (STOP C on M9-M10) | Integration review (`compatibility-reviewer`, fresh context, read-only over `298d5b39` + `b3d806a2`; NON-BLOCKING, PASS-with-fixes): F1 (should-fix) the `GL_BuildBModelAccelerationStructures` build loop flushed against `as_scratch_buffer_size` (the power-of-two, >= 8 MB buffer) where `r_brush.c:2703` compares against the *local* maximum of the per-BLAS and TLAS scratch sizes, so the C emits an `AS_BUILD -> AS_BUILD` barrier and restarts the scratch offset for nearly every inline bmodel while the Rust packed them at distinct offsets with no barrier -- valid Vulkan and pixel-identical, but a different command stream that `-renderhash` cannot see (`harness_render.c` hooks only the draw calls); F2 (nit) six private `unsafe fn` helpers (`cmd_memory_barrier`, `entity_at`, `create_addressable_buffer`, `allocate_tlas`, `bmodel_traced_surfaces`, `tlas_instance_address`) lacked `# Safety` sections (every `unsafe {}` block had its SAFETY comment); F3 (nit, inherited) `R_AllocateEntityBLAS` runs on the `R_StoreLeafEFrags` worker and calls the unlocked `GL_HeapAllocate` on the shared mesh heap exactly as `gl_mesh.c:657-750` does. Parity confirmed by evidence for the entity BLAS allocate/free/update paths, the AS scratch and TLAS lifetimes and garbage slots, the TLAS build and regrow, the 13 exports vs `glquake.h`, the cbindgen exclusions, the `meson.build` swap, the D5 deletions and the docs rows | F1: the shadowing `let scratch_buffer_size = as_scratch_buffer_size` is gone and the loop compares against the local maximum sized above, with a `// COMPAT:` note (ADR-010); F2: `# Safety` contracts added; F3: recorded in the `R_AllocateEntityBLAS` Safety section as a kept C data race (Phase 9). Not compared line by line by the review: `gl_rmain.rs` view setup and secondary-CB recording vs `gl_rmain.c`, `gl_screen.rs`, `gl_rmain_glue.c`, the M5 `create_buffers`/`storage_allocate`/`DYN.add_garbage` seams; the TLAS regrow path and the `Sys_Error` branches have no runtime evidence anywhere | none | applied |
| 2026-09-13 (PR #39 review on M9-M10) | Human review (NON-BLOCKING, no correctness blockers) plus a Copilot pass: R1 the `contentscache_origin` compare in `gl_rmain.rs` used `[f32; 3] ==` where `gl_rmain.c` uses `memcmp`, so `-0.0 == 0.0` matched and a NaN origin never did (ADR-010); R2 the `prev_end_rendering -> begin_rendering` graph edge hashed `"?" -> "?"` on both legs because `begin_rendering_task` was named after the edge and the previous frame's end task was never named, so the edge carried no shape information; R3 `size_of::<bool>()` for the `use_tasks` payload was tied to `sizeof (qboolean)` only by coincidence; R4 the `MOD_SPRITE` draw ran inside a match guard; R5 `R_BuildTopLevelAccelerationStructure` re-implemented `gl_mesh::entity_at`; R6 the M10 `needload` guard in `R_AllocateEntityBLAS` was Rust-only, leaving the C oracle different. Copilot's claim that `R_CollectTLASGarbage` `Sys_Error`s on non-ray-query devices is wrong: `AsProcs::load()` runs only for a non-null garbage slot, which only a built TLAS (ray query on) can fill -- same shape as `r_brush.c:198-206` | R1: bitwise compare with a `// COMPAT:` note; R2: both legs name `begin_rendering` first and the carried-over end task as `prev_end_rendering` before the edge, so the steady-state digest counts 26 nodes / 51 edges (was 25 / 51) and still compares identical C vs Rust; R3: `size_of::<c::qboolean>()` with the `q_types.h` note; R4: explicit arm (with a scoped `clippy::collapsible_match` allow, which wants the guard form back); R5: `entity_at` is `pub(crate)` and shared; R6: `gl_mesh.c` mirrors the guard, so the M6 `Con_Warning` in `Mod_LoadModel` is unreachable from this path on both legs (the M10 COMPAT note and `model_parse.rs` updated). Recorded, not changed: the TLAS regrow path and the `Sys_Error` branches stay untested on both legs | none | applied |
| 2026-09-13 (PR #39 Linux CI on M9-M10) | The Linux `build-rs-chost` leg (Rust render, `-Duse_rust_host=disabled`) failed to link: the M9 serial frame in `gl_rmain.rs` called `quake_rs_ftepart_update_particles_setup_task` directly (to keep the `Host_Reraise` out of the Rust frames, ADR-009), but that status core is `r_part_fte.rs`'s and only exists under `host`; with the C `r_part_fte.c` in the link the entry is `PScript_UpdateParticlesSetupTask`, which `Host_Error`s directly. The Windows dev box only builds the all-Rust, `crender` and `debug` legs, so the mixed config was never linked locally | `gl_rmain_glue.c` gains `RRmain_Glue_UpdateParticlesSetup`, `Host_Guard` around `PScript_UpdateParticlesSetupTask (NULL)` like the sibling draw thunks, and the Rust serial frame calls it in both configs; the `quake-c-sys` extern for the status core is gone. Re-verified with `build-rs-chost`, `build-rs-ctasks` and `build-rs-trace` added to the local legs: all seven link, `check_capi_signatures.sh` OK on `chost`/`rs`/`crender`, headless corpus C vs `chost` and C vs Rust 11 ran / 0 failed, render corpus C vs Rust and `chost` vs Rust identical `-renderhash` on all five entries (gn 26 / ge 51), SSIM 1.0, zero validation messages, fmt/clippy/tests clean | none | applied |
| 2026-09-14 (M11) | D8 says `quake-render` `include_bytes!`s the shaders, but `quake-render` must stay tool-free (its unit tests run under `miri` and on every `cargo test --workspace` box, with no Vulkan SDK) and the blobs are only needed where the C engine reads them (`gl_rmisc.rs` `shader_spv`, `common_fs.c` `vkquake_pak`) | The build script and the embedded blobs live in `quake-capi` under `render`; `quake-render` keeps `Shader::ALL` as the contract and `xtask`'s `job_table_covers_every_shader` (dev-dep on `quake-render`) pins the two tables to each other. `cargo test --workspace` needs no SDK tools because no workspace member enables `quake-capi/render` | AC10 unchanged (the differential compares `xtask` output, not the crate that embeds it) | Claude (2026-09-14), scoped |
| 2026-09-14 (M11) | `glslangValidator -g` (every meson debug buildtype) embeds the shader source path (`OpSource`/`OpString`) in the SPIR-V, so the debug output depends on the working directory the tool ran from (`meson` runs it in the build dir with a relative `../Shaders/...` path, `build.rs` with the repository path) | AC10 byte identity is claimed and checked for the release buildtype (no `-g`, `spirv-opt --strip-debug`); the debug pipeline runs the same commands with the same flags and is verified only by building and running `build-rs-debug`. Making the paths equal would mean re-creating meson's cwd in `build.rs` for no runtime gain | AC10 scoped to the release buildtype | Claude (2026-09-14), scoped |
| 2026-09-14 (M11) | `miniz_oxide` 0.8.9 `flush_block` (`deflate/core.rs`) only falls back to a stored block once `total_bytes > 32`, where the in-tree `tdefl_compress_mem_to_heap` (miniz 3.x `tdefl_flush_block`) does so for any block, so an incompressible input of 1-32 bytes deflates to a static block in Rust (shorter) and a stored block (`n+5` bytes) in C; every larger input tried and the real 432082-byte pak are byte-identical | Kept `miniz_oxide` (MIT/Zlib, ADR-003; the alternative is an in-tree port of `tdefl`). `quake-ctest --test xtask_differential` pins the boundary (`n<=32` must differ with the C length `n+5`, `n>32` and the real pak must match), `deflate_raw`'s doc records it, and the pak differential in `xtask_diff.py` would fail the day the pak ever hit it | AC10 unchanged for the real inputs | Claude (2026-09-14), scoped |
| 2026-09-14 (M11) | The first embedded table was keyed by the job symbol (`basic_vert_spv`) while `Shader::name()` yields the stem (`basic_vert`), so `build-rs` panicked at startup (`no SPIR-V embedded for "basic_vert"`) after every unit test passed | `build.rs` keys the table by `xtask::bintoc::symbol(job.name)` (the same function that names the bintoc symbol) and `xtask` gained `job_table_covers_every_shader`, which fails if any `Shader::ALL` name has no job or a duplicate one | none | Claude (2026-09-14), scoped |
| 2026-09-14 (M11) | `rust.yml`'s lint jobs build `quake-capi` with the `render` feature set (`progs,render` and `progs,host,cvar,render`), which now runs `build.rs` and needs `glslangValidator`/`spirv-opt` on the runner; the Linux job had neither | The Linux lint job installs the same LunarG SDK subset the build workflows use (cached by version), the macOS job `brew install`s `glslang spirv-tools`, and both run `cargo test -p xtask` and the ctest differential; `build-linux.yml`'s harness job runs `xtask_diff.py` against `build-c` | none | Claude (2026-09-14), scoped |
| 2026-09-14 (M12) | The M8 amendment deferred the FTE particle render half (`r_part_fte_glue.c`, 2714 lines) "to a follow-up Phase 8 landing before M12"; that landing did not happen (M9-M11 took the phase's remaining budget) and porting ~2000 Vulkan-typed lines entangled with C-owned ADR-007 state is not a phase-exit change | The FTE render half, ADR-010's `r_part_fte.c:7262` NaN-sign caller and the observability re-check carry over to Phase 9 alongside the switches (ROADMAP Phase 8 "still C" list). Both builds run the same C code for it, so the SSIM/`-renderhash` legs cover it as a same-source path, not as a port | AC8 unchanged for FTE particle draws (C oracle on both sides); AC11's "renderer fully Rust" exit criterion is recorded as not met (it was already scoped to Phase 9 at M1) | Claude (2026-09-14), scoped |
| 2026-09-14 (M12) | `-validation` only enables `VK_LAYER_KHRONOS_validation` under `#ifdef _DEBUG` (`gl_vidsdl.c:855`; upstream behaviour, mirrored by `engine-debug` in `quake-render/src/vid/instance.rs:122`). The release-buildtype `--validation` legs recorded at M2-M11 therefore ran with no layer loaded (their stdout has no `Using VK_LAYER_KHRONOS_validation` line and no `VK_EXT_debug_utils`), and "zero validation messages" on them was vacuous | `render_corpus.py --validation` now fails unless the run printed the layer line (verified: the release pair fails with "validation layer not loaded", the debug pair passes). The phase's validation-clean evidence is the `build-c-debug` vs `build-rs-debug` leg: layer loaded on both sides, zero messages, all five entries. The release-buildtype SSIM/`-renderhash` identity claims are unaffected | AC8/AC11 "validation clean" is a debug-buildtype claim on Windows; the CI lavapipe leg has no layer (recorded in `build-linux.yml`) | Claude (2026-09-14), scoped |
| 2026-09-14 (M12) | The debug windowed leg (`build-rs-debug`) crashed at startup on `start`: `r_brush.rs:1845 attempt to add with overflow` in `build_surface_display_list`. The C computes `sizeof(glpoly_t) + (lnumverts - 4) * VERTEXSIZE * sizeof(float)` in `size_t`, which wraps for 3-edge surfaces and unwraps on the add; the M8 port cast the negative product to `usize` and added, which is the same bits in release (wrapping) and a panic in debug. The leg was last run at M6/M7, so M8-M11 never exercised it | Signed `isize` arithmetic, cast once at the end (`r_brush.rs`); `build-rs-debug` rebuilt, the debug pair is 5/5 identical `-renderhash` and SSIM 1.0 with the layer loaded, headless `build-c-debug` vs `build-rs-debug` 11/11 identical. The debug leg joins the per-milestone render gate (Final verification) | none (release output was already correct) | Claude (2026-09-14), scoped |
| 2026-09-14 (M12) | Headless `run_corpus.py --compare` release C vs `build-rs-debug` differs on 7 server entries. Cause: `_DEBUG` changes C behaviour, not the port: `pr_edict_arena.c:425` skips the known-string slot reuse (`#ifndef _DEBUG freeknownstrings = progsstrings`), mirrored by `arena.rs:630` under `ENGINE_DEBUG`, and `mem.c` sets `MI_DEBUG 3`/`MI_GUARDED 1`; release C vs debug C differs on the same 7 entries | A C-only debug build (`build-c-debug`, `-Dbuildtype=debug -Duse_rust=disabled`) is the debug legs' oracle: 11/11 identical against `build-rs-debug`. The M6 note "compare debug against debug" (written for the `-renderhash` preamble) now covers the headless corpus too (Final verification) | none | Claude (2026-09-14), scoped |
| 2026-09-14 (M12) | Phase-exit coverage gaps, recorded rather than glossed: the three Phase-7 known-stale `windows-x86_64` registered-tier goldens (`e3m6-trains`, `save-e2m1`, `music-wav`, `MANIFEST.json` "KNOWN STALE (2026-09-04)") still fail `--check` identically on C and Rust and are not regenerated here (user-owned decision); macOS did not run (no box this session); Linux lavapipe, TSan, `miri`, `cargo audit` and the fuzz smoke are CI-only and unverified until the push; `timedemo` on the dev box is display-bound (~60 fps both builds) so the threshold is a smoke gate; the exit criteria "switches removed" and "renderer fully Rust" are Phase 9 by the M1 amendment (C-side remnants under `use_rust_render`: `gl_vidsdl_glue.c` SDL/video-mode half, `gl_rmisc_glue.c` `R_Init`/cvars/worldspawn, `gl_texmgr_glue.c`, `r_part_fte_glue.c`, `r_alias_glue.c`, `gl_heap_glue.c`/`tasks_glue.c` self-tests, the 61 shader TUs and `bintoc`/`mkpak` for the C-only build) | Recorded in the AC11 row and the ROADMAP exit block; no code change | AC11 met with recorded coverage | Claude (2026-09-14), scoped |
| 2026-09-14 (M12, CI) | PR #40 (2026-09-14, all 25 running checks green on the first run, no re-runs): Harness Linux 38m11s (`xtask_diff.py` 124/124 on `build-c`, the C-only lavapipe render-corpus stability leg SSIM 1.0000 on every run-0-vs-run-1 pair, the 8 harness gates 0 failed), Harness Windows 12m36s, Harness macOS 7m1s (the mixed `use_rust` macOS build lives there; the "Build macOS" matrix jobs are `-Duse_rust=disabled`), Windows clangarm64 5m30s, mingw64 2m34s, fmt+clippy+deny 2m22s (the new Vulkan-SDK-tools cache hit, `xtask` pipeline tests pass), clippy aarch64 49s (`brew install glslang spirv-tools`), miri 2m0s, TSan 48s, loom 34s, fuzz smoke 18m25s, unit+differential tests on ubuntu/macos/windows, ASan mixed, the 7 Build Linux/Windows/macOS jobs and the two formatting jobs. `cargo audit` and the exhaustive f32 sweep are schedule-only and did not run on the PR. That run required no code change; the M12 evidence row's "not run" list shrinks to the macOS local SSIM run, the two schedule-only jobs and `/integration-review`. The second run failed one job -- see the next row. |
| 2026-09-14 (M12, PR #40 review + CI) | **Review (Copilot, four findings; three accepted).** (1) `xtask_diff.py` resolved `--out` inside the `cargo run` working directory (`rust/`), so a relative `--out` landed under `rust/` and `--out <build-dir>` would have overwritten the oracle: `build_dir` and `out` are now made absolute in `main()` (`os.path.abspath`; the `tempfile.mkdtemp` default was already absolute) and `--out == --build-dir` is rejected with rc 2. (2) Its `cargo run` lacked `--locked`: added, as every other cargo invocation in the harness scripts and CI. (3) This plan's M11 entry misstated `Tools::find`'s precedence: corrected to `QUAKE_GLSLANG`/`QUAKE_SPIRV_OPT`, then `PATH`, then `$VULKAN_SDK/bin` (`rust/xtask/src/shaders.rs`). (4) "`rust.yml` should set `LD_LIBRARY_PATH` for the extracted `libvulkan.so`": declined -- `glslangValidator` and `spirv-opt` do not load the Vulkan loader (the lint job already runs them without it), and `libvulkan.so` is only extracted because the SDK cache key is shared with `build-linux.yml`. **CI.** The first `rust.yml` run (`79a29d0a`) was green; the second (`d99801b7`, the docs commit -- `pull_request` `paths` filters are evaluated against the whole PR diff, so the workflow re-ran) failed "unit + differential tests (ubuntu-latest)": `sv_phys_differential` died with `Host_Error: NUM_FOR_EDICT: bad pointer` (SIGABRT). Root cause (fixture, not port): `run_raise_case` drives both sides through a raise inside `SV_Physics`; the `Host_Error` longjmp skips the `SV_Physics` epilogue (`sv_phys.c:2456`, mirrored at `sv_phys.rs:3989`), leaving `push_grid_active`/`push_cache_active` set, `pushable_ent_cache` pointing into that test's edict arena and `SV_Physics_Alloc_Hook` installed (both sides, since both raise). The next test's `ctest_world_reset` frees that arena, and its `sv_fastpushmove 0` tick takes the cached list (`sv_phys.c:1392`) and walks the stale pointers: `NUM_FOR_EDICT` "bad pointer" outside `ctest_try_host`, so the stub aborts. Whether it manifests depends on the libtest thread schedule and on allocator address reuse -- green on the first run, red on the second, intermittent locally. Fix (`rust/quake-ctest/tests/sv_phys_differential.rs`, `run_raise_case`): after the raise both sides run one complete fast-pushers tick, which is the only path that resets those statics, and the shared alloc-hook slot is cleared. The engine's raise-past-epilogue behaviour is upstream QuakeSpasm-Spiked and is unchanged (C stays the oracle; noted for Phase 9's host inversion, where `Host_Error` unwinding becomes Rust's). Confirmation: with the fixture's edict arenas and globals temporarily retired by `VirtualProtect(PAGE_NOACCESS)` instead of `Mem_Free` (a local diagnostic, not committed), the unfixed test faulted deterministically at an address inside the retired arena in the order arena_change -> bad_movetype -> fastpushmove, and the fixed test passed the full 28-test suite with the guard on (5/5 full, 20/20 subset, 2/2 single-threaded), so no stale-arena access remains in the suite. Clean fixed file under `cargo test` on the Windows box: 10/10 subset, 10/10 full, 3/3 single-threaded. Residual, recorded rather than claimed fixed: before the guard was added, the fixed file under `cargo test` in the vcvars environment still hit a `0xc0000005` in a minority of runs (4/20, 1/8) that the same binary launched directly (180/180) and every run with a diagnostic present (50+) never showed; the guard proves it is not a stale-arena or globals access, and its cause is undetermined (heap-layout dependent). Debugger tooling on the box (lldb without its Python runtime, no cdb/procdump) could not capture it; if CI shows it again, the next step is a Linux `-Zsanitizer=address` run of this test binary. | AC10 evidence re-run after the script change: `xtask_diff.py` with a relative `--out` 124 identical, no output under `rust/`, `--out build-c` rejected (rc 2); the M12 evidence row's CI wording corrected below | none |
| 2026-09-15 (M12, PR #40 second review) | Owner's adversarial pass over M11-M12 (seven ranked findings, all accepted; none blocks the merge). (1) `xtask_diff.py` was one-directional: a job present in `meson.build`'s tables but missing from the xtask table passed with one shader fewer. The script now compares the `.spv` name sets of both directories (`.raw.spv` intermediates excluded on both sides) and names each side's extras. (2) The `sv_phys_differential.rs` pusher-statics reset ran only after every assertion passed, so a genuine raise-case divergence would have surfaced as the same unarmed `NUM_FOR_EDICT` abort in whichever test ran next, with the real assertion message scrolled past. The reset is now a `Drop` guard created before the raise loop (the test profile unwinds; the file mutex is still held and `lock()` recovers from poisoning); a deliberately broken needle now yields one failed test and 27 passed, no abort. (3) `compile_all` sized its thread pool from `available_parallelism()` even under `build.rs`, on top of the rustc jobs; Cargo's `NUM_JOBS` now bounds it (no jobserver crate, so no ADR-003 addition). The design consequence that `cargo check`/`clippy -p quake-capi --features render` needs glslang + spirv-opt is recorded in `readme.md`'s Rust section. (4) `xtask_diff.py` now refuses a debug-buildtype build dir up front (`meson-info/intro-buildoptions.json`), documents that both sides must resolve the same tool binaries (`QUAKE_GLSLANG`/`QUAKE_SPIRV_OPT` from `meson-logs/meson-log.txt`), removes its default temp `--out` on success and names it on failure. (5) `deflate_raw` doubles the output buffer on every `Okay`, as `tdefl_compress_mem_to_heap` does, instead of the `< 30` free-space heuristic whose termination depended on `miniz_oxide` never returning `Okay` with zero progress. (6) The `embedded_pak.c` failure path of `xtask_diff.py` points at the `deflate_raw` note when `vkquake.pak` itself is identical (a final block of <= 32 coded bytes is the pinned `miniz_oxide`/`tdefl` divergence; accept it, both inflate the same). (7) `rust.yml`'s flattened `tar --wildcards` line restored to the multi-line form of `build-linux.yml`. | AC10 gate strengthened (both directions, buildtype guard); no acceptance impact otherwise | none |

## Verification evidence and handoff

| Milestone | Changed files/behavior | Check and result | Acceptance IDs | Remaining risk/next action |
|---|---|---|---|---|
| M1 (2026-09-05) | `meson_options.txt`/`meson.build` (`use_rust_tasks`, `use_rust_render`), `quake-capi/Cargo.toml` (features `tasks`, `render`), `common.make` + new `Quake/harness_render.c` (`-renderhash`, `-renderhashdetail`), hooks in `harness.c/.h`, `gl_vidsdl.c` (trampolines, screenshot handover), `gl_rmisc.c`, `gl_rmain.c`, `gl_screen.c`; `main_sdl.c` timedemo pacing exemption; new `scripts/harness/render_corpus.py` + `Misc/harness/render_corpus.json`; workflows `build-linux/windows/mac.yml` (ctasks/crender legs, compares, lavapipe stability step, RA2 probes), `rust.yml` (`loom`, `tsan`); ADR-003 amendment; README; ROADMAP `[~]`; this plan | **Targeted (Windows dev box, MSVC cl):** `build-c`, `build-rs`, `build-rs-ctasks`, `build-rs-crender` configure and build; `check_capi_signatures.sh` OK on all three mixed headers (run inside vcvars64 with `CC=clang-cl`); `run_corpus.py --compare` C-only vs `build-rs`, `build-rs-ctasks` (both sides `-pinnedworkers 0,1,2,3`) and `build-rs-crender`: 11/11 identical each incl. the registered-tier entries; `run_corpus.py --check`: 8 ok, 3 mismatches = the KNOWN STALE registered-tier goldens recorded in `goldens/windows-x86_64/MANIFEST.json` at Phase 7 M11 (pre-existing, frame 0 differs; the mixed builds agree with C on the same three); `render_corpus.py --stability --runs 3` (with `-renderhashdetail`) and `--runs 2 --validation`: every screenshot SSIM exactly 1.0000 (28 + 16 comparisons), `-renderhash` identical run to run on all five entries (demo1/demo2 962 frames, start/e1m1 142, e1m2 143), zero validation-layer messages; `render_corpus.py --timedemo --timedemo-compare build-rs` (3 runs each): C median 237.2 fps, mixed 237.2 fps, floor 213.5 — before the `main_sdl.c` exemption both read 38.6 fps (the `sys_ticrate` cap); `cargo fmt --check`, `cargo clippy --workspace --all-targets --locked -D warnings`, `cargo test --workspace --locked --release` (118 suites, 1491 tests, 0 failed), `RUSTFLAGS=--cfg loom cargo test -p quake-tasks --release --locked` (job shape, 0 tests) all rc=0; `clang-format --dry-run -Werror` clean on the eight touched C files. **Not run locally:** `cargo deny check` (not installed here; no `Cargo.lock` change at M1 — CI `lint`), `tsan` job (nightly + `-Zbuild-std`, Linux only), Linux/macOS/MinGW/arm64 legs, lavapipe stability step, the Windows/macOS Vulkan probes — all read from CI after push. | AC1 (targeted), AC2 (ADR text; deny via CI), AC3 (job shapes; real at M2), AC4 (Windows local exact; Linux lavapipe pending CI) | RA1/RA2 settle on the first CI run (lavapipe threshold, probe outcomes). Timedemo fps is identical to 0.1 fps across six runs and may be present-wait/display bound (~240 Hz) rather than CPU bound at 640x480 — still a valid regression floor, but a CPU regression under that bound would be invisible; revisit with a larger resolution or `vid_vsync`-free config at M2 if the Rust scheduler needs a sharper measure. The three stale registered goldens remain the Phase 7 hand-off to the owner. Next: M2 (`quake-tasks`). |
| M2 (2026-09-05) | New `rust/quake-tasks` (`#![forbid(unsafe_code)]`: `scheduler.rs` 256-slot epoch table, dependency/indexed state machine, free list, per-slot `Mutex`/`Condvar`; `queue.rs` `crossbeam-deque` vs `cfg(loom)` `Mutex<VecDeque>`; `sync.rs`; `pinned.rs` parser; `tests.rs` 16 + `pinned` 7 unit tests; `loom_tests.rs` 5 models; `build.rs` `cfg(loom)` declaration), new `rust/quake-capi/src/tasks.rs` (eleven exported `Task_*`/`Tasks_*`, `CJob` 128-byte payload, `-pinnedworkers` via `COM_CheckParm`, `std::thread` workers `Task_Worker_<i>` pinned through C `Sys_PinCurrentThread`), new `rust/quake-c-sys/src/tasks.rs` (hand externs; the duplicate `Tasks_IsWorker` externs in `host.rs`/`r_part_fte.rs` consolidated), `quake-capi` `tasks` feature wired, `Cargo.lock`, new `Quake/tasks_glue.c` (`_DEBUG` `TestTasks_f` verbatim), `meson.build` swap (`tasks.c` → `tasks_glue.c` under `use_rust_tasks`), `check_capi_signatures.sh` (hand `tasks.h` slice), ADR-016 amendment, ADR-003 adoption note, `render_corpus.py` prelude `con_notifytime -1` + README, ROADMAP status, this plan (five M2 amendments, RA14) | **Targeted (Windows dev box, MSVC cl, current run):** `cargo test -p quake-tasks --locked` debug and release: 23 passed; `RUSTFLAGS=--cfg loom cargo test -p quake-tasks --release --locked`: 5 passed; `cargo deny check` in `rust/` and `rust/fuzz`: advisories/bans/licenses/sources ok; `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` rc=0; `cargo test --workspace --locked --release`: 1514 passed, 0 failed; `clang-format --dry-run -Werror Quake/tasks_glue.c` clean; `check_capi_signatures.sh` OK (vcvars64, `CC=clang-cl`); release builds of `build-c`, `build-rs`, `build-rs-ctasks`, `build-rs-crender` succeed; `test_tasks` (`LotsOfTasks` 100k + `IndexedTasks`) passes in `-Dbuildtype=debug` `build-rs` with the local-only `legacy_stdio_definitions.lib` link workaround (amendment row). `run_corpus.py --compare` (headless, 11 entries ran, 8 skipped for missing add-on data, 0 failed, every entry identical) on five combos: `build-rs` vs `build-rs-ctasks` and `build-c` vs `build-rs` both pinned `0,1,2,3`; `build-c -pinnedworkers 0` vs `build-rs -pinnedworkers 0..7` and `build-rs` 0 vs 0..7 (the CI step, 1-vs-8 cell); `build-c` vs `build-rs` auto workers. `render_corpus.py --compare build-c build-rs` (C renderer over Rust tasks): all five entries `-renderhash` identical (demo1/demo2 962 frames, start/e1m1 142, e1m2 143), every screenshot SSIM 1.0000; same with both sides `-pinnedworkers 0,1,2,3`; `--validation` on demo1/demo2 identical with zero validation-layer messages (final run after the `con_notifytime -1` fix and after reverting triage instrumentation); C-vs-C `--stability --runs 3` demo1 identical after the fix; `--timedemo --timedemo-compare build-rs` 3 runs each: C median 60.1 fps, mixed 59.9 fps, floor 54.1 (display-bound at 60 Hz in this session where M1 read 237 fps, so a CPU-side regression stays invisible under the bound — same caveat as M1). **Manual:** grep for persisted task handles (RA5: only `prev_end_rendering_task`, epoch-safe); `Sys_Error` on a worker takes the non-`longjmp` path (I2/L2). **TSan (WSL Ubuntu, nightly, `-Zsanitizer=thread -Zbuild-std`, after the PR #33 job fix):** 23 passed, no ThreadSanitizer reports. **Post-review re-validation (2026-09-06, after the F1 counter-layout fix, `build-rs` rebuilt with MSVC cl 19.44):** `cargo test -p quake-tasks` debug and release 23 passed, loom 5 passed, `cargo fmt --check` and `cargo clippy --workspace --all-targets --locked -- -D warnings` rc=0; `render_corpus.py --compare build-c build-rs`: demo1/demo2 (962 frames, 4 shots), start (142), e1m2 (143) `-renderhash` identical, every SSIM 1.0000; `e1m1-map` differed from frame 15 in that one run with SSIM 1.0 on both shots — the `build-c` side was the outlier (RA14 pattern: its frame-15 hash matches none of nine further runs; `--stability --runs 3` C-vs-C, three C-vs-Rust `--compare` runs and the `--validation` compare all agree, `-renderhashdetail` on); `--validation` compare: all five entries identical, zero validation messages; `--timedemo --timedemo-compare build-rs` 3 runs: C median 60.0 fps over [237.2, 60.0, 60.0], mixed 59.9 over [59.9, 60.0, 59.9], floor 54.0 — the first C run confirms the ~240 fps CPU-side rate and the rest are display-bound, so the gate holds only against a gross regression; headless `run_corpus.py --compare` `build-c` vs `build-rs` both `-pinnedworkers 0,1,2,3`, and `build-c -pinnedworkers 0` vs `build-rs -pinnedworkers 0..7`: 11 ran, 0 failed, every entry identical. **Not run locally:** Linux/macOS/MinGW/arm64 legs incl. the Linux `build-rs-ctasks` `-pinnedworkers` step and the lavapipe render step, `cargo audit` (CI `audit`) — PR #33 first run: every leg green except `tsan` (fixed, see the amendment); Copilot review findings resolved; the post-review push re-runs every workflow. | AC2 (deny clean locally), AC3 (loom real; TSan local + CI), AC5 (targeted + headless broad), AC6 (Windows local exact; timedemo display-bound); AC1-AC6 statuses set in the acceptance matrix after the integration review | RA14 open (pipeline-handle aliasing in `-renderhash`, two unreproduced C-only outliers; fix at M5). Integration review (STOP A) run on M1+M2: F1 fixed, F2 applied, F3/F4 recorded as RA15/RA16. Phase 7 debug-link defect (`printf` in `quake_rs_host_shutdown`) needs its own fix before a debug mixed build links without the workaround. STOP A (compatibility-reviewer / `/integration-review`, user-invoked) before M3 lands. Next: M3 (`gl_heap` under `use_rust_render`). |
| M3 (2026-09-06) | New `rust/quake-render/src/heap.rs` (the `gl_heap.c` suballocator, generic over `DeviceMemoryBackend`: segments/pages/small-alloc buckets/size-class bitfields/dedicated allocations, `check_consistency`/`check_clean_state`, unit tests incl. page saturation), new `rust/quake-types/src/render.rs` (`GlHeapStats`, `VulkanMemory` with `ash::vk::DeviceMemory`, `VulkanMemoryType` mirrors), new `rust/quake-capi/src/gl_heap.rs` (the seven `GL_Heap*` exports, boxed opaque `glheap_t`/`glheapallocation_t`, `CBackend` over `R_AllocateVulkanMemory`/`R_FreeVulkanMemory`/`GL_SetObjectName`, `Sys_Error` failure exit), new `rust/quake-c-sys/src/render.rs` (hand externs), `cbindgen.toml` preamble declarations under `__HEAP__`, `check_capi_signatures.sh` `gl_heap.h` slice, `meson.build` first `use_rust_render` block (`gl_heap.c` swapped for new `Quake/gl_heap_glue.c`: `_DEBUG` `test_gl_heap` only), `quake-ctest`: new `stubs/gl_heap_ref.c` (oracle with `c_ref_heap_*` fake seams, `_DEBUG` undefined), `tests/gl_heap_differential.rs` (proptest traces, `GL_HeapTest_f`-shaped trace, device-address trace), `tests/render_abi.rs` + `abi_probe.c` rows, prelude gains the four gl_heap declarations, `ash` dev dep; `rust.yml` clippy runs with `--features progs,render`; ADR-003 adoption note for `ash`, ADR-011 amendment; both `Cargo.lock`s (+`ash` 0.38.0+1.3.281, no transitives) | Targeted: `gl_heap_differential` 3/3 in debug (500 cases) and release (10 000 cases); `render_abi` 2/2; `test_gl_heap` in `build-rs-debug` ran to completion (frame-scripted, no abort); `check_capi_signatures.sh` OK (clang-cl); `check_ctest_symbols.sh` OK. Broad: `cargo fmt --check`, `clippy --workspace --all-targets` and `-p quake-capi --features progs,render` clean, `cargo test --workspace --release` all ok, `cargo deny check` clean in both workspaces; `build-c`/`build-rs`/`build-rs-ctasks`/`build-rs-crender` (release MSVC) build and each compiles exactly one of `gl_heap.c`/`gl_heap_glue.c`; `render_corpus.py --compare build-c build-rs`: SSIM 1.0 on every shot, `-renderhash` identical on all five entries in the `--validation` run (validation clean) and on four of five in the plain run (e1m1-map: see RA14, identical in three dedicated repeats and on `build-rs-crender`); `run_corpus.py --compare build-c build-rs` 11/11 identical. Not run: Linux/macOS/MinGW/arm64 legs (CI after push), lavapipe render corpus (CI), `timedemo` (heap not on the frame path; M2 numbers stand) | AC7, AC8 (heap), AC9 (heap part) | RA14 outlier (pipeline-handle aliasing) remains open until M5 keys draws on pipeline identity; `TestHeapCleanState`'s structural bitfield check is Rust-only now; next: M4 (`gl_texmgr.c`) after the STOP A review covers M1–M3 |
| M3 review fixes (2026-09-06, PR #34) | `ash` gated behind `quake-types`'s new `render` feature (`quake-render`, `quake-ctest`, `quake-capi/render` enable it; fuzz `Cargo.lock` drops `ash`); `GL_HeapCreate` takes `c_int` and validates the enum; `GL_HeapGetStats` via `Heap::stats_ptr`; `heap.rs` docs on `destroy`/`check_clean_state`/`check_consistency`/`q_log2`, wrapping counter sum; differential: 8192-page configs, `large_segment_trace_matches_c` (65534 pages, `mask_offset >= 64`, `q_align` wrap), size-class citations; prelude `FindFirstBitNonZero64` initialised, `VkDeviceMemory` follows `VK_USE_64_BIT_PTR_DEFINES`; `Quake/gl_heap_glue.c` `COMPILE_TIME_ASSERT` block; `render.rs` offset asserts; probe/`render_abi.rs`/ADR-003/ADR-011 wording; RA14 note | Rust (1.97.1, Windows): `cargo fmt --check` ok; `cargo clippy --workspace --all-targets --locked -D warnings` and `-p quake-capi --features progs,render` clean; `cargo deny check` clean in `rust/` and `rust/fuzz`; `cargo check -p quake-types` / `-p quake-capi` without `render` ok; `cargo test --workspace --locked --release` all ok (`gl_heap_differential` 4 passed incl. `large_segment_trace_matches_c`, 7.58 s; `render_abi` 2 passed); debug differential 4 passed. C (MSVC): `clang-format --dry-run -Werror Quake/gl_heap_glue.c` ok; `build-rs`, `build-rs-crender`, `build-rs-debug` rebuild and link (the `COMPILE_TIME_ASSERT` block compiles); `check_capi_signatures.sh` OK for `build-rs` and `build-rs-crender` headers; `test_gl_heap` in `build-rs-debug` reports no failure. Harness: `render_corpus.py --compare build-c build-rs` full corpus OK (`-renderhash` identical: demo1/demo2 962 frames, start 142, e1m1 142, e1m2 143; SSIM 1.0000 everywhere); `--validation` compare on start-map/e1m1-map OK; RA14 diagnostic: six C-only e1m1-map stability runs and three C-vs-Rust compares with `-renderhashdetail`, 283 frames each, all identical. Not run locally: Linux/macOS/MinGW/arm64 legs (CI) | AC2, AC7, AC9 (heap part) | RA14 still open (unreproduced); 32-bit render ABI unchecked (D2 fallback); next: M4 after STOP A |
| M4 (2026-09-07) | See the M4 milestone entry for the file list: `quake-render::texmgr` (+ `bluenoise`), `quake-capi/src/gl_texmgr.rs`, `Quake/gl_texmgr_glue.c`, `quake-types::render` (`GlTexture`, `SrcFormat`, `TEXPREF_*`), `quake-c-sys/src/render.rs`, `cbindgen.toml`, `check_capi_signatures.sh`, `meson.build`, `quake-ctest` (`stubs/gl_texmgr_ref.c`, `tests/gl_texmgr_differential.rs`, `render_abi.rs`, `abi_probe.c`, `build.rs`, `c_ref_prelude.h` real `gltexture_s`/`textureflags_t`), `quake-capi/src/gl_heap.rs` (`CBackend::new`, `Send`), `Cargo.lock` (`quake-render` → `quake-util`, no new crates), ROADMAP, ADR-007 row, ADR-011 note, this plan | **Targeted (Windows dev box, MSVC, Rust 1.97.1):** `gl_texmgr_differential` 2/2 in debug and release (proptest traces + scripted sequence), every op's trace, list snapshot and handle counters identical; `render_abi` 4/4 (22 `gltexture_t` offsets + size, `enum srcformat` size, six `SRC_*`, fourteen `TEXPREF_*`); `gl_heap_differential` 4/4 debug still green; `check_capi_signatures.sh` OK on `build-rs` and `build-rs-crender` headers (vcvars64, `CC=clang-cl`); `check_ctest_symbols.sh` OK (43 oracle sources); `clang-format --dry-run -Werror` clean on `Quake/gl_texmgr_glue.c` (the ctest stub was `clang-format -i`'d to match the other stubs; CI only checks `Quake/`). **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` rc=0; `cargo test --workspace --locked` debug and `--release` all suites ok (0 failures); `cargo deny check` in `rust/` clean, `rust/fuzz` lockfile unchanged (`--locked` metadata ok); `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link (each compiles exactly one of `gl_texmgr.c`/`gl_texmgr_glue.c`); `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on all five entries (demo1/demo2 1923 frames each, start 283, e1m1 283, e1m2 285), every screenshot SSIM 1.0000, zero validation-layer messages; headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped (missing add-on data), 0 failed, every entry identical. **Not run:** `timedemo` (texture uploads are load-time; the M2 display-bound caveat stands), Linux/macOS/MinGW/arm64 legs and the lavapipe render step (CI after push), `cargo audit` (CI), `build-rs-ctasks` rebuild (CI) | AC8 (texmgr), AC9 (`gltexture_t` part) | RA14 unchanged (no outlier seen in this run); `vulkan_globals` accessor and the C-stored palettes/pointers retire at M5/M9; the differential's cubemap exclusions and `PERSIST` stripping are recorded limits; next: M5 (`gl_rmisc.c`, `vulkan_globals` ownership flip) |
| M4 review fixes (2026-09-07, PR #35) | `quake-render::texmgr` (raw arena + `Drop`, cubemap skip, 8to32 bound, `// COMPAT:` markers, `Vec<u32>` bluenoise pixels, alignment in the safety docs, `SmokeBackend`/`SmokeHeap` + `texmgr_smoke`), `quake-render::heap` (`page_saturation_at_max_pages` ignored under Miri), `quake-capi/src/gl_texmgr.rs` (`EngineBackend { glue }` snapshot per entry point, `Send`, `TexMgr_LoadImage` doc), `Quake/gl_texmgr_glue.c` (`TEXMGR_LAYOUT_64`), `quake-types::render` (32-bit wording), `rust.yml` (`miri` job), this plan | **Targeted (Windows dev box):** `cargo +nightly miri test -p quake-render --locked` 8 passed / 1 ignored under Stacked Borrows and again with `MIRIFLAGS=-Zmiri-tree-borrows` (flag honoured: a bogus `-Zmiri-` flag errors); before the fixes Miri flagged the `Vec<u8>` RGBA pixels (unaligned `&[u32]`) and an 8x8-vs-4x4 reload in the first draft of the smoke test; `gl_texmgr_differential` 2/2, `render_abi` 4/4; `check_capi_signatures.sh` OK on `build-rs` and `build-rs-crender`; `clang-format --dry-run -Werror` clean on the glue. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` (plus `-p quake-capi --features render`) rc=0; `cargo test --workspace --locked` debug and `--release`: 238 suite results, none failing; `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` link (the first `build-rs` attempt failed: the snapshot backend is no longer a ZST and `TexMgr<B>: Send` needs `B: Send`, fixed with a documented `unsafe impl Send`); `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on all five entries, every screenshot SSIM 1.0000, no validation messages; headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed. **Not run:** the `miri` CI job itself on Linux (CI after push), `timedemo`, other OS legs | AC8, AC9 unchanged | The per-entry-point env snapshot is unmeasured (structural fix only); Miri covers `quake-render`'s tests, not the capi shim; next: M5 |
| M5 (2026-09-10) | See the M5 milestone entry for the file list: `quake-render::rmisc` (8 modules), `quake-capi/src/gl_rmisc.rs`, `Quake/gl_rmisc_glue.c`, `quake-types::render` mirrors, `quake-c-sys/src/render.rs`, `gl_heap.rs`/`gl_texmgr.rs` backend switches, `Quake/gl_texmgr_glue.c`, `meson.build`, `cbindgen.toml`, `check_capi_signatures.sh`, `quake-ctest` (`render_abi.rs`, `abi_probe.c`, `c_ref_prelude.h`, `gl_texmgr_ref.c`), `Quake/harness_render.c`/`harness.h`/`gl_rmisc.c` (`P` preamble, `Harness_RenderPipelinesDestroyed`), `Misc/harness/README.md`, ROADMAP, ADR-007 row, ADR-011 amendment, this plan | **Targeted (Windows dev box, MSVC 19.44, Rust 1.97.1):** `render_abi` passes (the five embedded-struct rows added; `VulkanDescSetLayout` `num_acceleration_structures` at offset 36); `build-rs`/`build-rs-crender`/`build-rs-debug` link with the 152 `COMPILE_TIME_ASSERT`s (both `_DEBUG` sizes); `check_capi_signatures.sh` OK on `build-rs` and `build-rs-crender` headers (vcvars64, `CC=clang-cl`); `check_ctest_symbols.sh` OK (43 oracle sources); `quake-render` under Miri 18 passed/1 ignored (default and `-Zmiri-tree-borrows`); three C-vs-Rust `demo1-frames` runs identical after the RA14 fix (md5 `EBB786D0` both sides each run), `--stability --runs 6` on `build-rs` "stable over 1925 frames x 6 runs". **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` clean; `cargo test --workspace --locked --release` all 119 test binaries ok (0 failures), the debug suite (1544 tests) before the final harness-hook edits; `cbindgen` header builds; `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link; `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on all five entries (demo1/demo2 1925 frames each, start 285, e1m1 285, e1m2 287), `P` lines identical, every screenshot SSIM 1.0000 (global/mean/min), zero validation-layer messages; `render_corpus.py --compare build-rs-crender build-rs` identical on all five; headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed, every entry identical. **Not run:** `timedemo` (M2 display-bound caveat), `test_gl_heap`/`test_tasks` debug smoke after the final edits (the debug build links), Linux/macOS/MinGW/arm64 legs and the lavapipe render step (CI after push), `cargo audit`/`cargo deny` (CI; no new crates), TSan | AC8 (rmisc), AC9 (`vulkan_globals` part) | RA6/RA14 closed; `cb_context_t` row at M9; the C writers of `vulkan_globals` (`gl_vidsdl.c`, `gl_rmain.c`) retire with M6-M9; next: M6 (`gl_vidsdl.c` via `ash`, STOP B after) |
| M5 review fixes (2026-09-11, PR #36) | `quake-render::rmisc` (`mod.rs` `VgPtr` + `vg!`/`vg_mut!`, `has_buffer_device_address`; `staging.rs` owner-tracked lock, atomic `device_idle`, `requirements.size`, divergence note; `dynbuf.rs` `requirements.size`; `pipelines.rs` `PipelineInfos<'a>`, `set_model_slot`/`set_model_oit_slots`, mirror-derived push ranges; `shaders.rs` size guard; `descriptors.rs`/`memory.rs`/`samplers.rs` field-level accesses), `quake-types::render` (push-constant mirrors), `quake-capi/src/gl_rmisc.rs` (`VgPtr::from_raw`), `Misc/harness/README.md`, `scripts/harness/render_corpus.py` (wording), ADR-007 row, this plan | **Targeted (macOS dev box, Rust stable + nightly Miri 2026-08-17):** `cargo +nightly miri test -p quake-render --locked` 18 passed / 1 ignored; `cargo test -p quake-render -p quake-types` 19 passed; `render_abi` 5/5; the `SkinningPushConstants == 44` first draft of the const assert failed at compile time and a C `sizeof` probe confirmed 48, which is what the fix pins. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` rc=0; `cargo test --workspace --locked` (debug) every suite ok, none failing; `build-c` and `build-rs` (mixed build, `gl_texmgr_glue.c`/`gl_rmisc_glue.c` TUs) build and link; `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail` on macOS (MoltenVK): `-renderhash` identical on all five entries (demo1/demo2 1925 lines, start/e1m1 285, e1m2 287), every screenshot SSIM 1.0000, no validation-layer messages; demo1 + e1m1 re-run identical under `--validation` after the final (atomic `staging_buffer_size`) rebuild. **Not run:** headless `run_corpus.py` (no simulation/VM change), `check_capi_signatures.sh` (no exported signature changed), Windows/Linux legs and the CI `miri` job (CI after push) | AC8, AC9 unchanged | The `skinning` push-range change (44 → 48) is unexercised by the corpus (needs an MD5 model under `ray_query`); the `VgPtr` discipline is enforced by convention (macros + scoped `// SAFETY:` blocks), not the type system; next: M6 |
| M6 (2026-09-11) | See the M6 milestone entry for the file list: `quake-render::vid` (5 modules) + `cb.rs`, `quake-capi/src/gl_vidsdl.rs`, `Quake/gl_vidsdl_glue.c`, `gl_rmisc.rs`/`gl_texmgr.rs`/`gl_heap.rs` seam retargets, `quake-c-sys/src/render.rs`/`tasks.rs`, `meson.build`, `cbindgen.toml`, `check_capi_signatures.sh`, ROADMAP, ADR-007 row, ADR-015 amendment, this plan | **Targeted (Windows dev box, MSVC 19.44, Rust 1.97.1):** `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link, `gl_vidsdl_glue.c` warning-free; the `build-rs` binary carries one `vkGetDeviceProcAddr failed to find` string against the C build's 26 (the C Vulkan half is out of the link); `check_capi_signatures.sh` OK on `build-rs` and `build-rs-crender` headers (vcvars64, `CC=clang-cl`); `quake-render` under Miri 18 passed/1 ignored; `render_corpus.py --compare build-c build-rs --validation`: `-renderhash` identical on all five entries (demo1/demo2 1925 frames, start/e1m1 285, e1m2 287), `P` lines identical, every screenshot SSIM 1.0000, zero validation messages, "render corpus OK"; `--compare build-rs-crender build-rs` identical on all five; `--compare build-c build-rs-debug --validation --entry start-map,e1m1-map`: SSIM 1.0, validation clean, frame chains identical, `P` count 330 vs 331 (expected, see the amendment log); `--timedemo demo1` reference median 60.3 fps, candidate 60.0 fps (floor 54.3) OK. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` and the `render,tasks,host,engine-debug` feature set clean; `cargo test --workspace --locked` all ok; `cbindgen` header builds (`quake_rs.h:188-208` M6 block); headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed, every entry identical. **Not run:** `check_headers.sh` (no `cc` locally; CI), the SDL2 branches of the glue (Windows has no SDL2 — Linux CI `-Duse_sdl3=disabled` legs compile them), Linux/macOS/MinGW/arm64 legs and the lavapipe render step (CI after push), `cargo audit`/`cargo deny` (CI; no new crates), TSan, `test_gl_heap`/`test_tasks` debug smoke (the debug build links and runs the corpus) | AC8 (vidsdl) | RA8 closed; STOP B ran on M4-M6 (NON-BLOCKING, see the amendment log; the two fixes re-verified: four configs link, `render_corpus.py --compare build-c build-rs --validation` identical `-renderhash` on all five entries, SSIM 1.0, validation clean, fmt/clippy clean); next: M7 (draw layer) |
| M7 (2026-09-12) | See the M7 milestone entry for the file list: eight `quake-capi` modules + `gl_warp_sin.rs`, `quake-types::refdef`, eight `Quake/*_glue.c`, `quake-c-sys` `render.rs`/`console.rs`, `quake-render::cb`, `meson.build`, `cbindgen.toml`, the C harness-hook fix in `gl_draw.c`/`gl_sky.c`/`gl_warp.c`/`r_sprite.c`, ROADMAP, this plan | **Targeted (Windows dev box, MSVC 19.44, Rust 1.97.1):** `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link; `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on all five entries (demo1/demo2 1925 lines, start/e1m1 285, e1m2 287), every screenshot SSIM 1.0000, zero validation messages, "render corpus OK"; `--compare build-rs-crender build-rs` identical on all five. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` and `-p quake-capi --features progs,render` clean; `cargo test --workspace --locked` all ok; headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed. **Not run:** `check_capi_signatures.sh`/`check_headers.sh` (CI), `timedemo` (no draw-layer hot path changed; M6 baseline stands), Linux/macOS/MinGW/arm64 legs and the lavapipe render step (CI after push), `cargo audit`/`cargo deny` (no new crates), `test_gl_heap`/`test_tasks` debug smoke | AC8 (draw layer) | next: M8 (world/mesh) |
| M8 (2026-09-13) | See the M8 milestone entry for the file list: five `quake-capi` modules (`r_world`, `r_brush`, `gl_mesh`, `r_alias`, `r_part_render`), `quake-types::render` mirrors, `quake-c-sys` `render.rs`/`tasks.rs`, four new `Quake/*_glue.c`, `r_part_glue.c` fenced, `meson.build`, `cbindgen.toml`, ROADMAP, this plan | **Targeted (Windows dev box, MSVC 19.44, Rust 1.97.1):** `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link; `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on all five entries (demo1/demo2 1925 lines, start/e1m1 285, e1m2 287), every screenshot SSIM 1.0000, zero validation messages, "render corpus OK"; `--compare build-rs-crender build-rs` identical on all five. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `-p quake-capi --features progs,render` and `--features progs,host,cvar,render` clean; `cargo test --workspace --locked` all ok; headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed. **Not run:** NEON lanes (arm64 CI builds them, no arm64 device executes them), `r_rtshadows` scenes (RT halves are the unchanged C, M10), `r_particles 2`/`r_quadparticles 0` visual spot-check, `check_capi_signatures.sh`/`check_headers.sh` (CI), `timedemo` (M6 baseline stands), Linux/macOS/MinGW/arm64 legs and the lavapipe render step (CI after push), `cargo audit`/`cargo deny` (no new crates) | AC8 (world/mesh) | FTE particle render half deferred (amendment log); next: M9 (frame graph) |
| M9 (2026-09-13) | See the M9 milestone entry for the file list: `quake-capi/src/gl_rmain.rs` (new), `gl_screen.rs`, `r_world.rs`, `r_brush.rs`, `tasks.rs`, `quake-types::render`, `quake-c-sys` `render.rs`/`tasks.rs`/`libm.rs`, `cbindgen.toml`, `meson.build`, new `Quake/gl_rmain_glue.c`, `gl_screen_glue.c`, `r_world_glue.c` deleted, C hooks in `gl_rmain.c`/`gl_screen.c`/`r_world.c`, `harness.c`/`harness.h`/`harness_render.c`/`tasks.c` (`-renderhash` graph digest, `-taskcounts`), `quake-ctest` probe rows/prelude, ROADMAP, ADR-007, this plan | **Targeted (Windows dev box, MSVC cl 19.44, Rust 1.97.1):** `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link (the 28 `COMPILE_TIME_ASSERT` rows hold); `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on all five entries (demo1/demo2 1925 lines, start/e1m1 285, e1m2 287) including the graph digest (`gn=25 ge=51` per steady-state frame), every screenshot SSIM 1.0000, zero validation messages, "render corpus OK"; `--compare build-rs-crender build-rs` identical on all five; `--timedemo --timedemo-compare build-rs --extra-args=-taskcounts` 3 runs each: C median 60.3 fps over [60.3, 60.3, 60.2], Rust 59.9 over [59.9, 59.9, 59.9], floor 54.3 (display-bound at 60 Hz, M6 caveat); per-worker task counts: 153 816 executed task bodies over 20 workers on every run of both builds (identical totals; per-worker min/max 3575/13928 .. 2887/19700 C and 3833/14050 .. 2289/19322 Rust, max/median 2.0-3.9 on both sides); `cargo test -p quake-ctest --test render_abi` 6 passed. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `-p quake-capi --features progs,render` and `--features progs,host,cvar,render` clean; `cargo test --workspace --locked` debug and release all ok; `cargo deny check` ok; `check_ctest_symbols.sh` OK (43 sources); `check_capi_signatures.sh build-rs/quake_rs.h` OK (vcvars64, `CC=clang-cl`); headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed, every entry identical. **Not run:** `r_rtshadows` scenes (RT halves are the unchanged C, M10), NEON lanes, Linux/macOS/MinGW/arm64 legs and the lavapipe render step (CI after push), `cargo audit` (no new crates) | AC8 (frame graph), AC9 (`cb_context_t` + M8 mirrors, Windows; CI for the other OSes), M9 worker-utilization AC | Next: M10 (ray tracing), then STOP C |
| M10 (2026-09-13) | See the M10 milestone entry for the file list: `quake-capi/src/gl_mesh.rs` (entity BLAS), `r_brush.rs` (TLAS + AS scratch), `quake-c-sys/src/render.rs`, `cbindgen.toml`, `meson.build`, `Quake/gl_mesh_glue.c`/`r_brush_glue.c` deleted, `model_parse.rs` comment, ROADMAP, ADR-007, this plan | **Targeted (Windows dev box, MSVC 19.44, Rust 1.97.1, RTX-class device with `VK_KHR_ray_query`):** `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link; `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail` at the default `r_rtshadows 2`: `-renderhash` identical on all five entries (demo1/demo2 1925 lines, start/e1m1 285, e1m2 287), every screenshot SSIM 1.0000, zero validation messages, both builds log `Using ray queries` and identical AS allocation sizes (demo1 1320 KB, e1m1 1210 KB, 1238/1239/1420 KB for the others) and the same `Reallocating dynamic AS scratch buffer (8192 KB)`, the RT console lines diff-identical; `--vkquake build-rs-crender --compare build-rs`: identical on all five; `r_rtshadows 1` (harness `cmds`, `--validation`): identical on all five, SSIM 1.0, AS allocated; `r_rtshadows 0`: identical on all five, SSIM 1.0, zero AS allocations, and the demo1 `shot-02` differs across the three modes (so each mode really ran); `--timedemo --timedemo-compare`: C 60.4 vs Rust 60.1 fps median (floor 54.4, display-bound at 60 Hz); headless corpus 11/11 identical (8 skipped for missing mod data; this gate caught the headless `with_ctx` defect, amendment log); `check_capi_signatures.sh` OK; `check_ctest_symbols.sh` OK (43 sources, `quake-ctest` built with `cl`); `cargo test --workspace --locked` debug and `--release` all green; `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings` (plus the `progs,host,cvar,render` and `progs,render` feature sets) clean; `cargo deny check` ok (one pre-existing `Unicode-3.0` allowance warning). **Not run:** lavapipe (no ray query, so the RT paths are exercised only on the dev box -- AC8 note), NEON lanes, the Linux/macOS/MinGW/arm64 CI legs, the TLAS regrow path (>1024 instances; no corpus scene reaches it), the `Sys_Error` failure branches | AC8 (ray tracing) | STOP C ran on M9-M10 (NON-BLOCKING, PASS-with-fixes, see the amendment log; the F1 scratch-bound fix and the F2/F3 doc fixes re-verified: `build-rs`/`build-rs-crender`/`build-rs-debug` link, `render_corpus.py --vkquake build-c --compare build-rs --validation` identical `-renderhash` on all five entries, SSIM 1.0, zero validation messages, `--vkquake build-rs-crender --compare build-rs` identical on all five, headless corpus 11 ran / 8 skipped / 0 failed, fmt/clippy (all three feature sets) clean, `cargo test --workspace` all ok). The PR #39 review fixes (six findings, amendment log) re-verified the same way: four configs link, both render-corpus legs identical `-renderhash` on all five entries (graph digest now 26 nodes), SSIM 1.0, zero validation messages, headless corpus 11 ran / 0 failed, fmt/clippy/tests clean; next: M11 |
| M11 (2026-09-14) | See the M11 milestone entry for the file list: `rust/xtask` (lib+bin: `shaders.rs`, `bintoc.rs`, `pak.rs`, `main.rs`), `rust/.cargo/config.toml`, `quake-capi/build.rs` + `src/shaders.rs` + `src/embedded_pak.rs`, `gl_rmisc.rs`, `quake-c-sys/src/render.rs`, `quake-capi/Cargo.toml`/`cbindgen.toml`, `quake-ctest` dev-dep + `tests/xtask_differential.rs`, `meson.build`, `scripts/harness/xtask_diff.py`, `rust.yml`, `build-linux.yml`, this plan | **Targeted (Windows dev box, MSVC 19.44 + clang-cl, Rust 1.97.1, Vulkan SDK 1.4):** `xtask_diff.py --build-dir build-c` (release buildtype): 124 outputs byte-identical (61 `.spv`, 61 `.c`, `vkquake.pak` 432082 bytes / 13 entries, `embedded_pak.c`); `cargo test -p xtask` 6 passed (incl. `job_table_covers_every_shader`, 61 jobs = `Shader::ALL`); `cargo test -p quake-ctest --test xtask_differential` 3 passed (synthetic corpus, the 1..=48-byte boundary, the real pak vs the in-tree `tdefl`); `build-rs` (`use_rust_render`) configures without the 63 shader/pak custom targets and links (build.rs compiled 122 `.spv`, embedded pak 121912 bytes deflated); `check_capi_signatures.sh build-rs/quake_rs.h` OK (no `*_spv`/`vkquake_pak` in the header); `run_corpus.py --compare build-rs` 8 ran / 0 failed identical; `render_corpus.py --vkquake build-c --compare build-rs --validation --tier shareware`: `-renderhash` identical on all five entries (demo1/demo2 964 lines, start/e1m1 144, e1m2 145), every SSIM 1.0000, zero validation messages; `cargo fmt --check`, `cargo clippy --workspace --all-targets --locked -D warnings` and `-p quake-capi --features progs,render` clean; `cargo deny check licenses` ok (`miniz_oxide` MIT OR Zlib OR Apache-2.0; the pre-existing `Unicode-3.0` allowance warning). **Not run:** `cargo test --workspace` (broad, deferred to M12), the debug-buildtype identity (not claimed, amendment log), the Linux/macOS/MinGW/arm64 CI legs incl. the new SDK-tool installs in `rust.yml` and the `xtask_diff.py` harness step, `timedemo` | AC10 | The C `bintoc`/`mkpak` tools and the 61 shader TUs stay in the C build until Phase 9/10 (NG3); the CI-side tool installs are unverified until the push; next: M12 |
| M12 (2026-09-14) | `rust/quake-capi/src/r_brush.rs` (overflow fix), `scripts/harness/render_corpus.py` (`--validation` layer-loaded check), this plan, ROADMAP | **Broad (Windows dev box, MSVC 19.44 + clang-cl 22.1.8, Rust 1.97.1, RTX 5080 driver 616.64, SDL 3.4.12, full id1 + registered data).** Builds: `build-c`, `build-rs`, `build-rs-ctasks`, `build-rs-crender`, `build-rs-chost`, `build-rs-cprogs`, `build-c-trace`, `build-rs-trace`, `build-rs-debug`, `build-c-debug` all link. Cargo: `fmt --check`; `clippy --workspace --all-targets --locked -D warnings` + `-p quake-capi --features progs,render` + `progs,host,cvar,render` clean (re-run after the `r_brush.rs` fix); `test --workspace --locked` release and debug 122 suites / 1554 passed; loom 5 passed; `cargo deny check` ok in `rust/` and `rust/fuzz` (pre-existing `Unicode-3.0` warning); `check_capi_signatures.sh` OK on 7 headers; `check_ctest_symbols.sh` OK (43 oracle sources); `check_headers.sh` 12/12 + bindgen 12/12. Headless: `xtask_diff.py` 124 identical; `run_corpus.py --stability` C stable; `--check` C and Rust (state and `--sndhash`) green except the three known-stale registered goldens (identical failures on C); `--compare` 11/11 identical for C vs Rust (plain, `-parthash`, `--sndhash`), C vs `cprogs`/`chost`/`ctasks` (`-pinnedworkers 0,1,2,3`)/`crender`, Rust workers 1 vs 8, `build-c-debug` vs `build-rs-debug`, and C vs Rust again after the fix; `trace_diff.py` identical (C self e1m1 117672 records; C vs Rust e1m1 148206 / e1m2 180567 / start 95358); `builtin_diff.py` 235 identical (e1m1, start); `save_diff.py` identical (95091/95075 bytes) C vs Rust and C vs `chost`; `condump_diff.py` identical (340 bytes); `config_diff.py` identical (989 bytes); `capture_diff.py` PASS (2385 records, 1200-frame captures, two C windows); `record_diff.py` identical (20239 bytes, 600 frames) C self and C vs Rust; `netreplay_diff.py` PASS x2 (1402 frames); `interop_matrix.py` PASS 24 cells; soak C/C 5000 frames PASS (Base-666 hash-identical, FTE+999 diagnostic fork at frame 128 as Phase 7 recorded); `physics_matrix.py` 84/84; `run_formats_corpus.py` 190 ok / 0 reject. Windowed (`render_corpus.py`): C stability 2 runs SSIM 1.0; release C vs Rust `-renderhash` identical on all five entries (demo1/demo2 964 lines, start/e1m1 144, e1m2 145), 14/14 SSIM 1.0000 (threshold 0.99), at `r_rtshadows` 2 (default), 1 and 0; C-renderer vs Rust and C-tasks vs Rust identical / SSIM 1.0; **debug pair `build-c-debug` vs `build-rs-debug` `--validation`: layer loaded on both sides (`Using VK_LAYER_KHRONOS_validation`, `VK_EXT_debug_utils`), 5/5 identical `-renderhash`, SSIM 1.0, zero validation messages**; the new layer-loaded check fails the release pair as intended; `timedemo` C median 60.4 fps vs Rust 60.1 (floor 54.4; display-bound). **CI (PR #40, 2026-09-14):** every running check green on the first run -- Linux/MinGW/arm64 builds, the Linux `xtask_diff.py` and lavapipe render-corpus stability legs, TSan, `miri`, loom, fuzz smoke, the three-OS unit+differential tests, the mixed ASan leg and the Windows/macOS harnesses (details in the status paragraph below); the second run's ubuntu unit+differential job failed on the `sv_phys_differential` fixture flake fixed in the review/CI amendment row. **Not run:** macOS local SSIM (no box; the CI macOS harness ran); `cargo audit` and the exhaustive f32 sweep (schedule-only); `/integration-review` (user-invoked) | AC11 (with the recorded coverage), AC8 validation-clean re-scoped to the debug buildtype | Phase 9 carries the switches, the FTE render half, the C glue remnants, the deletions and the Linux goldens; Phase 10 the ADR-004 review of `quake-capi`'s 5147 tokens; the Phase-7 stale goldens stay the user's call |
| M7-M8 review fixes (2026-09-13, PR #38) | `quake-capi/src/r_world.rs` (`store_efrags` returns the raise state, every marking loop stops at once, `COMPAT (ADR-010)` note on the NEON lanes), this plan, ROADMAP | **Targeted (Windows dev box, MSVC 19.44, Rust 1.97.1):** `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link; `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on all five entries, every screenshot SSIM 1.0000, zero validation messages, "render corpus OK"; `--compare build-rs-crender build-rs` identical on all five. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `-p quake-capi --features progs,render` and `--features progs,host,cvar,render` clean; `cargo test --workspace --locked` all ok; headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed. **Not run:** the raise path itself (no corpus entry makes `R_StoreEfrags` `Host_Error`; the early return is checked by reading against `r_world.c`), NEON lanes, other OS legs (CI after push) | AC8 unchanged | F3 probe rows due at M9; next: M9 (frame graph) |
| PR #38 review round 2 (2026-09-13) | `quake-capi/src/r_alias.rs` (`skinnum` read once per model; MD5 pose clamp silent and to 0; no frame-clamp `Con_DPrintf`), `quake-capi/src/gl_refrag.rs` (`COMPAT (ADR-010)` comment on the `MOD_EMITREPLACE` advance), this plan, ROADMAP | **Targeted (Windows dev box, MSVC 19.44, Rust 1.97.1):** `build-c`, `build-rs`, `build-rs-crender`, `build-rs-debug` build and link; `render_corpus.py --compare build-c build-rs --validation --extra-args=-renderhashdetail`: `-renderhash` identical on demo1/demo2/start/e1m1, SSIM 1.0000, zero validation messages; the first run flagged `e1m2-map` on its pre-map console frames only (frames 1-12, `en=0 n=0`, the same chain-116 values appearing at shifted frame numbers), and two `--entry e1m2-map` re-runs plus a C-vs-C run were identical over all 287 lines -- timing-dependent loading-screen frames, harness noise (carry item: exclude or stabilise pre-`cl.worldmodel` frames); `--compare build-rs-crender build-rs` identical on all five. **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`, `-p quake-capi --features progs,render` and `--features progs,host,cvar,render` clean; `cargo test --workspace --locked` all ok; headless `run_corpus.py --compare build-c build-rs`: 11 ran, 8 skipped, 0 failed. **Not run:** the four changed paths themselves (no shareware scene has a multi-surface model with a bad skin, an MD5 with an out-of-range pose, an invalid frame, or a `MOD_EMITREPLACE` static); each fix is checked by reading against `r_alias.c`/`gl_refrag.c` | AC8 unchanged | next: M9 (frame graph) |
| M6 review fixes + master merge (2026-09-11, PR #37) | `quake-render::vid` (`mod.rs` `vgp!`/`vgp_mut!`, `scbx_mut`/`scbx_ptr`; `instance.rs` `VgPtr` parameters, optional `vkEnumerateInstanceVersion`; `frame.rs`/`resources.rs`/`swapchain.rs` field-level accesses), `cb.rs` (`CmdProcs::new(VgPtr)`), `rmisc/mod.rs` (`pub(crate) use` of the macros), `quake-capi/src/gl_vidsdl.rs` (`VgPtr::from_raw` init, raw `GL_EndRendering` sampling, `render_scale` clamp), `gl_rmisc.rs` (`with_ctx` `pub(crate)` on master's body), ADR-007 row, this plan (merge conflicts in the last three) | **Targeted (Windows dev box, Rust 1.97.1):** `cargo check -p quake-capi --features render,tasks,host` clean after the migration (168 field-access errors from the merge worked down to zero), `cargo clippy` with `render,tasks,host,engine-debug` clean (one `unused_unsafe` per hoisted read fixed). `quake-render` under Miri 18 passed/1 ignored (Stacked Borrows and `-Zmiri-tree-borrows`); `build-c`, `build-rs` (clang-cl) build and link, `build-rs-debug` links only with `-Dc_link_args=legacy_stdio_definitions.lib` (pre-existing Phase 7 `printf` reference in `host.rs`, clang-cl/lld debug-only, same local workaround as the M6 row); `check_capi_signatures.sh` OK on the `build-rs` header; `render_corpus.py --compare build-c build-rs --validation`: `-renderhash` identical on all five entries (demo1/demo2 964 lines, start/e1m1 144, e1m2 145), every screenshot SSIM 1.0000, zero validation messages, "render corpus OK". **Broad:** `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings` (default and the `render,tasks,host,engine-debug` set) rc=0; `cargo test --workspace --locked` every suite ok. **Not run:** headless `run_corpus.py` (no simulation change), `build-rs-crender`, `timedemo`, Linux/macOS/MinGW/arm64 legs and the lavapipe render step (CI after push); the `render_scale` clamp and the 1.0-loader path are unexercised by the corpus (`r_scale` default is in range; the dev box loader is 1.3) | AC8 unchanged | Behaviour-neutral on the corpus (field-level reads/writes of the same fields in the same order); CI legs pending the push |

## Completion gate

- [x] Requirements map to acceptance criteria (R1→AC5/6, R2→AC4/6/8/11, R3→AC1/5/8, R4→AC7, R5→AC1/6).
- [x] ADR-003/004/007/009/010/011/015/016/017/019 constraints satisfied and cited in commits.
- [x] Each milestone landed as one commit with the tree green; evidence rows filled with current tool output (targeted / broad / manual / not-run distinguished).
- [ ] ROADMAP status and amendment log updated (done at M12); `/integration-review` run by the user at M12 (pending the user).

Status after M6 (2026-09-11): the first three items hold for M1–M6 (evidence rows above, one commit per milestone plus the PR #33/#34/#35/#36 review fixes; M5 and M6 verified on the Windows dev box and the PR #36 fixes on the macOS dev box, CI legs pending the pushes); ROADMAP and the amendment log are current; the STOP A review ran as the `compatibility-reviewer` agent on M1+M2 and was extended to M3 (NON-BLOCKING) before M4 started; the STOP B review ran on M4-M6 (NON-BLOCKING; its two code-level findings are fixed on the M6 branch, PR #37). A review of M7-M8 ran on PR #38 (NON-BLOCKING; F1 fixed on the branch, F2 recorded as a COMPAT note, F3 due at M9). A second, human review round on PR #38 found four alias/efrag transcription divergences (G1-G4, amendment log); three are fixed to match the C and the `MOD_EMITREPLACE` advance is recorded as a deliberate fix. M9 and M10 landed on 2026-09-13 as one commit each (evidence rows above; Windows dev box only, CI legs pending the push); the STOP C review ran on M9-M10 (NON-BLOCKING, PASS-with-fixes; its one code-level finding, the brush-BLAS scratch flush bound, is fixed and re-verified on the branch, amendment log); the PR #39 review round (six code-level findings incl. the ADR-010 `memcmp` compare, the `?`-edge in the graph digest and the C-side `needload` mirror) is fixed and re-verified on the branch, as is the Linux `build-rs-chost` link failure it surfaced (the serial-frame particle setup now goes through a C `Host_Guard` thunk in both host configs, amendment log; the mixed `chost`/`ctasks`/`trace` legs are part of the local verification from here on). M11 landed on 2026-09-14 as one commit (evidence row above; Windows dev box only, CI legs pending the push; AC10 met for the release buildtype). M12 (phase exit, 2026-09-14): the full local suite ran on the Windows dev box (M12 evidence row), the two findings it produced (the debug-only `r_brush.rs` overflow and the inert release `-validation`) are fixed/gated and recorded, the unsafe inventory is in the M12 entry, ROADMAP Phase 8 has its exit block and stays `[~]` (switches, FTE render half, glue remnants and deletions carry to Phase 9). The first three boxes hold for M1-M12; the fourth ticks when the user runs `/integration-review` on the branch. Phase 8 M11+M12 CI legs ran on PR #40 (2026-09-14, all 25 running checks green on the first run, no re-runs): Harness Linux 38m11s (`xtask_diff.py` 124/124 on `build-c`, the C-only lavapipe render-corpus stability leg SSIM 1.0000 on every run-0-vs-run-1 pair, the 8 harness gates 0 failed), Harness Windows 12m36s, Harness macOS 7m1s (the mixed `use_rust` macOS build lives there; the "Build macOS" matrix jobs are `-Duse_rust=disabled`), Windows clangarm64 5m30s, mingw64 2m34s, fmt+clippy+deny 2m22s (the new Vulkan-SDK-tools cache hit, `xtask` pipeline tests pass), clippy aarch64 49s (`brew install glslang spirv-tools`), miri 2m0s, TSan 48s, loom 34s, fuzz smoke 18m25s, unit+differential tests on ubuntu/macos/windows, ASan mixed, the 7 Build Linux/Windows/macOS jobs and the two formatting jobs. `cargo audit` and the exhaustive f32 sweep are schedule-only and did not run on the PR.
