# Rust migration Phase 7 — Client & server simulation (`quake-host`, `quake-cvar`, `quake-net` remainder) — agent-executed

## Context

Phases 0–6 are complete on `master` (`27a5fe89`); this branch is `feature/rust-conversion-phase-7-a3ee42`. Phase 7 (`docs/rust-migration/ROADMAP.md:181`) ports client & server simulation to Rust: ~52k LOC across 26 C files, plus the Phase 5 carry-ins (net_main.c dispatch funnels, net_dgrm.c orchestration half) and Phase 6 carve-outs (43 server-coupled pr_cmds builtins, pr_ext strzone/knownzone, the pr_edict_arena flip, ED_ParseEdict/ED_ParseGlobals dispatchers, CSQC load chain). It is the phase that closes four ADR-007 dual-view windows: `sv`/`svs`, `cl`/`cls`, `net_message`/`msg_readcount`/`msg_badread`, and `qcvm`/`pr_global_struct`/`qcvm->edicts`.

**What makes this plan different from Phases 5/6:** the user has authorized agent execution with parallelism. Discrete tasks are assigned to subagents with explicit model (Opus/Sonnet/Haiku) and effort levels; the main session (Fable, high effort) orchestrates, integrates, and is the sole writer of shared build files. Priority is accuracy/correctness/quality; cheaper models are used only for genuinely mechanical or read-only work, and only where a bit-exact differential gate exists to catch divergence.

**User decisions (recorded):**
- Agent-parallel execution authorized (the planning request). Parallelism is in *development* (isolated worktrees, ctest-only work); **landing stays strictly serialized** — one commit per milestone, tree green (all meson configs + gates) after each.
- Compat policy unchanged from Phases 5/6: bug-for-bug preservation, deletions recorded but deferred (C stays the oracle), `(compat exception)` ADRs followed exactly with `// COMPAT:` markers.
- **Golden platform coverage:** Phase 7 exits with darwin-arm64 (existing) + windows-x64 goldens (generated locally in M11 from the c-reference build, CI `--check` leg added). **Linux goldens are deferred to a later phase** — recorded as an amendment against the ROADMAP's "per platform" exit wording; Linux CI keeps `--compare`/`--stability` coverage meanwhile.
- **Soak budget: 100k server frames per cell × 8 cells** for the full soaks at M9 and M11. Smoke cells at every milestone from M4. If a desync ever reproduces only beyond 100k frames, extend that cell ad hoc.

Governing: ADR-003 (deps), ADR-004 (unsafe), ADR-005 (formatter; %g/%e panic — specifier audits required), ADR-006 (edict arena view→owner flip), ADR-007 (dual-view table updates are review-blocking), ADR-008 (ambient qcvm; `_Host_Frame` VM-switch ordering; no QcVmGuard until Phase 9), ADR-009 (Host_Guard/Host_Reraise; no longjmp through Rust frames; post-guard invariant; hot-path guard cost must be re-measured), ADR-010 (per-platform determinism; libm call-through; -ffp-contract=off project-wide; NaN-sign exception takedown when r_part_fte sim callers port), ADR-011 (FFI mirrors), ADR-016 (tasks.c stays C — hard non-goal), ADR-019 (gate-first verification; full-suite re-run at phase exit). Template: `docs/ai/plans/rust-conversion-phase-6.md` — one milestone per commit, tree green after each, amendment log.

## Milestone graph

```
M1 gates-first ─┬─▶ M2 cvar+cmd ─┬─▶ M3 world ─▶ M4 sv_move+sv_phys ─▶ M5 pr builtins+ED_Parse*  [server track]
                │                │        ══ STOP A: compat review M1–M5 ══
                │                └─▶ M7 client stratum (ctest/worktree dev ∥ M3–M6; LANDS after M6) [client track]
                │                    M6 sv_user + sv_main (after M5)
                └───────────────────▶ M8 host.c + host_cmd.c (after M6 and M7)
                                          ══ STOP B: compat review M6–M8 ══
                     M9 NET funnels + pr_ext/strzone + arena flip (after M8) ─┐
                     M10 UI stratum + r_part sim (dev ∥ M9, lands after) ─────┴─▶ M11 phase exit
```

Rationale for ordering (deviations from naive file order):
- **M2 before M3**: no hard dependency either way, but the physics cvar-matrix oracle should not have a registry flip landing mid-physics-port, and M2 proves the new Pattern A + meson-switch machinery on the cheapest subject (cvar.c has one global, `cvar_vars` at cvar.c:26).
- **Client track is independent of the server track** (needs only M1 gates + M2 registry; sits on the already-Rust MSG layer) — the biggest parallelism win. It develops concurrently with M3–M6 but lands after M6.
- **M9 (NET funnels) before M10 (UI)**: funnel flips become legal once M6/M7/M8 statusize the strata beneath; landing them right after M8 gives the phase-exit soak maximum bake time in the final NET configuration. UI doesn't affect the soak.

## Switch granularity (decision)

Two new meson switches: **`use_rust_cvar`** and **`use_rust_host`** (+ CI leg `build-rs-chost`). NET carry-ins ride the existing `use_rust_net`; pr_cmds/pr_ext/arena work rides `use_rust_progs` Pattern C. Rationale: matches ROADMAP crate boundaries (quake-cvar, quake-host, quake-net remainder); within-phase oracle granularity comes free (unported files remain C in both legs); a sv/cl switch split would double the meson/CI matrix to enable only mixed-language single-process configs the interop matrix already covers at process level. No dedicated `build-rs-ccvar` leg (small enough to bisect locally).

## Agent execution rules

- Main session (Fable, high): orchestration, all landing commits, sole writer of `meson.build`, `meson_options.txt`, `Quake/common.make`, `rust/quake-ctest/build.rs`, CI workflows. Parallel agents deliver source files + tests only (worktree isolation).
- Compat-critical porting → **Opus high**. Mechanical transliteration behind strong differential gates → **Sonnet medium/high**. Read-only discovery/audit/inventory → **Haiku low** (Sonnet low where judgment is needed). Existing project agents keep their roles: repo-researcher (Haiku low), verification-diagnostician (Sonnet medium), compatibility-reviewer (Opus high, fresh context) at STOP A/B and M11.
- Every port task's prompt includes: the exact C source range, the governing ADRs, the flip pattern to use, the differential-test contract, and "return status codes, never panic across FFI; no longjmp-reachable C callee without a trampoline."
- After two failed attempts on the same problem: stop, run a `/loop-breaker`-style read-only diagnosis (Sonnet/Opus), don't iterate blind.

## Milestones and tasks

### M1 — Gates first (verification infrastructure; no porting)

All tasks parallel except T1.8 (integration, last).

| # | Task | Model/effort |
|---|---|---|
| T1.1 | New longer server-exercising corpus entries (e1m1-long, pusher/elevator-heavy segments; registered-tier → local-only where needed) | Sonnet medium |
| T1.2 | `scripts/harness/physics_matrix.py`: cvar-matrix sweep over `--compare-extra-args` — `sv_fte_recursivehullckeck 0/1` × `sv_gameplayfix_elevators 0–3` × `sv_smoothplatformlerps 0/1` (12 cells; document CI vs local split) | Sonnet high |
| T1.3 | Soak driver: `interop_matrix.py --soak` — frame-count target, periodic server state-hash exchange, desync = hash mismatch vs same-seed C/C reference OR Host_Error/protocol error/drop; on failure auto-dump hash window + repro demo (see Soak definition below) | Sonnet high (design reviewed by main session) |
| T1.4 | **Extend the existing `Harness_HashClient` (`Quake/harness.c:236`)** to cover `cl.qcvm` globals when active — closes the CSQC hash gap; client sim vars are already hashed | Opus medium |
| T1.5 | ADR-011 ABI mirrors for `sv`/`svs`/`cl`/`cls`/`client_t`/`client_state_t` + inventory of big statics (`sv_pusher_support[MAX_EDICTS]` etc.) | Sonnet medium |
| T1.6 | Meson switches `use_rust_cvar`/`use_rust_host` (boilerplate per `meson.build:386-390`) + cargo features + `build-rs-chost` CI leg | Sonnet low |
| T1.7 | Three read-only audits, parallel: (a) ADR-005 specifier audit of host_cmd.c savegame writer + `Host_WriteConfiguration` + `Key_WriteBindings`; (b) cl_parse.c inventory of all 39 `Host_*` sites with per-svc context; (c) cvar/cmd external-linkage surface | Haiku low ×3 |
| T1.8 | Integration: C-vs-C self-compare of T1.2/T1.3/T1.4 **plus fault-injection red-test of each** (a detector that has never fired is not a gate); wire CI; open ADR-007 rows annotated with closing milestones | Main session |

- Gates: `physics_matrix.py` C-vs-C all cells green, injected fault red; `interop_matrix.py --soak --frames 20000` C-vs-C clean, injected fault detected at the right frame; `run_corpus.py --stability` over new entries; `check_headers.sh`; both new meson legs compile.

### M2 — cvar.c + cmd.c → quake-cvar (first flip)

- T2.1 (Sonnet medium, first): add cvar.c/cmd.c to quake-ctest `C_SOURCES` (+`common.make` same commit); differentials: registration order, `cvar_vars` walk order, completion, alias expansion, tokenizer edge cases, config-dump ordering.
- T2.2 (Opus medium) port cvar.c ∥ T2.3 (Sonnet high) port cmd.c — Pattern A + glue.
- T2.4 (main session): flip under `use_rust_cvar`; verify no double-registration (Rust code already registers through the C registry — direction inverts here).
- Gates: new ctest differentials; `run_corpus.py --compare build-c build-rs`; config.cfg byte-diff C-vs-Rust (writer still C, registry Rust); `check_ctest_symbols.sh`.

### M3 — world.c

- T3.1 (Opus medium, first): world.c into ctest; differentials for **both** hullchecks (`sv_fte_recursivehullckeck`, `world.c:33`), areanode insertion order, `SV_LinkEdict`/`SV_TouchLinks` ordering, `SV_Move` pipeline vs captured fixtures; hullcheck fuzz target.
- T3.2 (**Opus high**): port world.c — both hullcheck impls, areanodes, `SV_TouchLinks` re-entrancy into `PR_ExecuteProgram` with ADR-009 guard placement **and cost measurement** (guard economics were validated at ~13 calls/frame; this is per-link per-move).
- T3.3 (main session): flip under `use_rust_host`; physics matrix goes C-vs-Rust.
- Gates: ctest world differentials both hullcheck settings; `physics_matrix.py --compare` all 12 cells; corpus compare incl. server-hash entries; guard-overhead measurement recorded.

### M4 — sv_move.c + sv_phys.c

- T4.1 (Opus medium, first): ctest differentials — pusher modes 0–3, `sv_pusher_support` frame states, gravity half-step, `sv_analyticphysics`, checkbottom/movestep fixtures.
- T4.2 (Sonnet high) port sv_move.c ∥ T4.3 (**Opus high**) port sv_phys.c (pusher-support-frame subsystem + integrator; float-operation-order exact per ADR-010; 12 cvars; `sv_pusher_support[MAX_EDICTS]` ownership per T1.5).
- T4.4 (main session): flip; full physics matrix.
- Gates: physics matrix all cells C-vs-Rust; long-entry server-hash parity; `--stability`; soak smoke cell.

### M5 — server-coupled pr_cmds builtins + ED_Parse* dispatchers

- T5.1: the 43 builtins via existing Pattern C (`PF_RS`/`RUST_PF`), in parallel groups: link group setorigin/setsize/setmodel (**Opus high** — SV_LinkEdict→SV_TouchLinks→PR_ExecuteProgram re-entrancy) ∥ trace/movement group traceline/tracebox/checkpos/walkmove/droptofloor/checkbottom/pointcontents/aim/findradius (**Opus high**) ∥ PVS group checkclient + `checkpvs` global (**Opus high**) ∥ message group 8× PF_Write*, stuffcmd/bprint/sprint/centerprint (Sonnet high) ∥ world-effects group sound/particle/lightstyle/makestatic/ambientsound/changelevel/setspawnparms/precaches (Sonnet high) ∥ `PF_cl_*` set (Sonnet high). `PF_break` stays C permanently.
- T5.2 (Sonnet high, ∥): ED_ParseEdict/ED_ParseGlobals key dispatchers (ctest infra exists from Phase 6); pr_edict.c residue onto the deletion list. Needed before M6 (`SV_SpawnServer`) and M8 (`Host_Loadgame_f`).
- T5.3 (main session): integrate; re-baseline `builtin_diff.py` / `trace_diff.py` floors.
- Gates: `builtin_diff.py`; `trace_diff.py` (min-records floor); `save_diff.py`; full corpus compare; interop smoke.

**══ STOP A — fresh-context compatibility review (compatibility-reviewer, Opus high) of M1–M5 ══**

### M6 — sv_user.c + sv_main.c

- T6.1 (Sonnet medium, first): verbatim behaviour-neutral C split of sv_main.c into send half (FTE delta writer, `SVFTE_*`, baselines, `MSG_WriteStaticOrBaseLine`, PVS culling, `SV_WriteEntitiesToClient`) and connection half (`SV_ConnectClient`, `SV_SpawnServer`, protocol negotiation, sv/svs storage); corpus-verified neutral; `common.make` same commit.
- T6.2 (Opus medium): ctest differentials for the delta writer vs record fixtures.
- T6.3 (**Opus high**) port send half ∥ T6.4 (Sonnet high) port sv_user.c (libm trig call-through, ADR-010) ∥ T6.5 (**Opus high**) port connection half with **statusized** `SV_ConnectClient` (the ADR-009 unlock for M9).
- T6.6 (main session): flip; sv/svs storage → Rust; **close ADR-007 sv/svs row**.
- Gates: `record_diff.py` + `netreplay_diff.py` C-vs-Rust; `interop_matrix.py` full 6×4; physics matrix re-run; soak smoke.

### M7 — client stratum (dev ∥ M3–M6; lands after M6)

- T7.1 (Opus medium): ctest differentials — cl_parse svc dispatch fixtures from recorded server streams, lerp/relink, tent tables.
- T7.2 (parallel leaf ports against the still-C cl/cls dual-view, flipped individually): view.c+chase.c (Sonnet high) ∥ cl_input.c (Sonnet medium) ∥ cl_tent.c (Sonnet medium).
- T7.3 (**Opus high**): cl_parse.c — svc dispatch, statusization of all 39 `Host_*` sites (input: T1.7b inventory), CSQC load chain. The phase's largest ADR-009 surface.
- T7.4 (Opus high): cl_main.c (lerp/relink, cl/cls storage) + cl_demo.c playback logic; **close ADR-007 cl/cls row**.
- Gates: client-hash parity (incl. the new cl.qcvm coverage) on client corpus entries; `capture_diff.py`/`record_diff.py`; demo-playback corpus compare; interop full matrix.

### M8 — host.c + host_cmd.c

- T8.1 (Opus medium): ctest differentials — `Host_FilterTime` accumulator, savegame writer/reader byte fixtures.
- T8.2 (**main session, Fable high**): host.c orchestration behind the C setjmp shell (`Host_Guard` host.c:302 / `Host_Reraise` host.c:339 stay C until Phase 9; `_Host_Frame` VM-switch ordering per ADR-008). The ADR-008/009 center of the phase.
- T8.3: host_cmd.c — `Host_Savegame_f`:1554/`Host_Loadgame_f`:1797 (**Opus high**; byte-diff subject; T1.7a audit as input) ∥ game commands (Sonnet high) ∥ `ExtraMaps_*` background-thread boundary (Opus medium — thread stays C or the Rust side is Send-audited).
- T8.4 (Sonnet high): `Host_WriteConfiguration`:486 config.cfg writer via the ADR-005 formatter (Key_WriteBindings still C through capi until M10; end-to-end config byte-diff completes there).
- Gates: `save_diff.py` byte-clean in all three directions (C-write/Rust-read, Rust-write/C-read, Rust/Rust); config.cfg byte-diff; full corpus; soak smoke.

**══ STOP B — fresh-context compatibility review (Opus high) of M6–M8 ══**

### M9 — NET funnel flips + pr_ext/arena (after M8; dev ∥ M10)

- T9.1 (Sonnet high): flip the net_main.c Pattern B funnels (NET_Connect:548, NET_GetMessage:703, NET_GetServerMessage:761, NET_Send*:814/840, NET_CanSendMessage:874, NET_SendToAll:890, NET_Init:974/Shutdown:1054, NET_Poll:1078/SchedulePollProcedure:1093, slist trio) — legal now that all strata beneath are statusized.
- T9.2 (**Opus high**): net_dgrm.c orchestration half (`_Datagram_ServerControlPacket`:689 → Rust SV_ConnectClient, hostcache/slist, heartbeats, rcon).
- T9.3 (Opus medium): pr_ext strzone/strunzone + knownzone with the **pr_edict_arena flip** (`qcvm->edicts` Rust-owned; ADR-006 view→owner); remaining pr_ext Phase-7 builtins; **close ADR-007 qcvm/pr_global_struct/edicts row and the net_message/msg_readcount/msg_badread row** (storage leaves net_msg_glue.c:38).
- Gates: `netreplay_diff.py`; interop full matrix; **first full-length soak in final NET config**; **save byte-diff suite re-run post-arena-flip** (M8's verification predates the flip).

### M10 — UI stratum + particle sim (dev ∥ M9; lands after)

- T10.1 (**Opus high**) console.c — `con_mutex`, non-leaf `Con_Printf` (reaches SCR_UpdateScreen; capi shim + re-entrancy story) ∥ T10.2 (Sonnet high) keys.c incl. `Key_WriteBindings` (completes config byte-diff end-to-end) ∥ T10.3 (Sonnet medium) sbar.c (98 Draw_* capi shims) ∥ T10.4 (Sonnet high, may split by menu groups) menu.c (4950 LOC, 108 Draw_* shims; drawing stays shimmed until Phase 8).
- T10.5 (Opus medium): r_part.c + r_part_fte.c **simulation halves**; **ADR-010 NaN-sign exception takedown** (PerpendicularVector/RotatePointAroundVector sim callers port; ADR amendment removes the fence).
- Sub-milestones (**added 2026-09-02**, mirroring the M9a-M9g split): **M10a** gates-first (no porting) -> **M10b** keys.c -> **M10c** console.c -> **M10d** sbar.c -> **M10e** menu.c -> **M10f** r_part.c + r_part_fte.c simulation halves + the ADR-010 NaN-sign takedown.
- Gates (**corrected 2026-09-02**, see the amendment log -- the original line's first and third clauses named gates that cannot observe their subjects): `condump_diff.py` byte-diff (console.c + keys.c `bindlist`); config.cfg byte-diff fully Rust (keys.c `Key_WriteBindings`); `Harness_HashClient`'s new particle term through `run_corpus.py --compare` and the soak (r_part.c simulator); ctest differentials as the **sole** gate for sbar.c, menu.c and the r_part_fte.c simulator; ADR-010 amendment landed.

### M11 — Phase exit

- Full netplay soak (100k × 8 cells); **full ADR-019 suite re-run** (corpus, physics matrix, interop, all byte-diffs, fuzz soak, Miri on new unsafe islands); **windows-x64 golden generation** from the c-reference build + CI `--check` leg (Linux deferred per recorded decision, ROADMAP amendment noted); fresh-context `/integration-review` (Opus high); ROADMAP status + deletion lists recorded (deletion itself stays deferred — C remains the oracle); ADR-007 table shows zero open Phase-7 rows; amendment log finalized.

## Soak definition ("hours-long netplay soak")

Local-only gate (registered-tier precedent), via `interop_matrix.py --soak`:
- 4 build combos (C/C, C/R, R/C, R/R) × 2 protocols (666, FTE+999) = 8 cells; dedicated server + scripted deterministic client input; faster-than-realtime where the fixed timestep allows.
- Duration is frame-count based: **100k server frames/cell** (user decision; extend a cell ad hoc if a failure needs longer to reproduce). CI keeps existing short cells + one 20k-frame nightly smoke.
- Pass = zero desyncs; desync = periodic (every 64 frames) server state-hash mismatch vs the same-seed C/C reference stream, or any Host_Error/protocol error/drop. **Never byte identity** (live timing is nondeterministic — Phase 5 finding).
- On failure: auto-dump last-N hash window + server demo + frame index, sufficient for offline replay.
- Proven by fault injection in M1; smoke cell in every Gates line from M4; full runs at M9 and M11, results recorded in the amendment log (CI cannot attest them).

## Verified ground truth (landmines)

1. `world.c:33` — two hullcheck impls behind `sv_fte_recursivehullckeck` (engine's misspelling); nothing in current tooling toggles it; both port, both in the M1 matrix.
2. `harness.c:195` — `Harness_HashServer` returns early unless `sv.active && vm->progs`; only `save-e1m1` + `map-e1m2` exercise it in CI today; demos start no server (zero progs trace records — Phase 6 finding).
3. `Harness_HashClient` exists (`harness.c:236`) and covers client sim vars — but **cl.qcvm/CSQC globals are not hashed**; T1.4 closes exactly that gap, not more.
4. host_cmd.c `ExtraMaps_*` runs a background parsing thread — port around the thread boundary, not through it (ADR-016: no Rust thread-local assumptions on C workers).
5. cl_parse.c has 39 `Host_*` sites — the phase's largest ADR-009 statusization surface; per-svc status returns; post-guard invariant per message.
6. ADR-009 guard cost validated only at ~13 calls/frame; `SV_TouchLinks`→`PR_ExecuteProgram` is per-link per-move — measure before the M3 flip.
7. setorigin/setsize/setmodel → `SV_LinkEdict` → `SV_TouchLinks` → `PR_ExecuteProgram` → possibly the same builtins again: Rust world must tolerate recursive VM entry; ADR-006's tightened rule ("no reference lives across a builtin dispatch") exists for exactly this path; Miri model it.
8. sv_phys.c: `sv_pusher_support[MAX_EDICTS]` static, 12 cvars, pusher modes 0–3, gravity half-step, `sv_analyticphysics` — float-operation-order exact (ADR-010), no reassociation.
9. sv_user.c trig = libm call-through per ADR-010 (`quake_c_sys::libm`, `// COMPAT: ADR-010` per site), never `f32::` methods.
10. `Con_Printf` is not a leaf — reaches `SCR_UpdateScreen`, holds `con_mutex`; console flip needs a capi shim and a re-entrancy story.
11. menu.c (108) + sbar.c (98) `Draw_*` calls — renderer stays C until Phase 8; the shim surface is a scoped deliverable, deliberately throwaway.
12. ADR-010's NaN-sign exception (PerpendicularVector/RotatePointAroundVector) is fenced only because all callers are in r_part_fte.c — M10 must take it down with an ADR amendment.
13. ADR-007 rows close at specific milestones: sv/svs (M6), cl/cls (M7), net_message trio (M9), qcvm/pr_global_struct/edicts (M9). "An unlisted dual view is a review error."
14. host.c setjmp shell (`Host_Guard`:302/`Host_Reraise`:339) stays C until Phase 9; `_Host_Frame` VM-switch ordering (ADR-008) and `Host_FilterTime` are behavior-bearing.
15. `Host_WriteConfiguration`:486 + `Host_Savegame_f`:1554/`Host_Loadgame_f`:1797 are byte-diff subjects; the ADR-005 specifier audit (T1.7a) is a prerequisite (%g/%e panic if reached).
16. quake-ctest `C_SOURCES` has zero Phase 7 files; every differential requires `c_ref_*` additions, and `Quake/common.make` must change in the same commit as any C split (real MinGW link failure precedent).
17. The cvar flip inverts registration direction (Rust currently registers into the C registry); double-registration and callback-direction bugs are the M2 failure mode. `cmd_text` already sits on Rust SZ buffers.
18. NET funnel flips are illegal until M6 (SV_ConnectClient), M7 (cl_parse), and M8 (host) statusize the strata beneath — M9 must not be pulled earlier.
19. Live captures are timing-nondeterministic — soak pass criterion is state-hash desync detection, never byte identity.
20. Goldens exist for darwin-arm64 only; the dev machine is Windows, so local gates are `--compare`/`--stability` until the M11 windows-x64 goldens land; Linux goldens deferred (recorded user decision).
21. `PF_changeyaw` precedent (`pr_cmds.c:1573`): a builtin with a direct non-table caller flips in place via `#ifdef`, not slot-only — applies to any M5 builtin `sv_move.c`/others call directly.

## Risks

| Risk | Mitigation |
|---|---|
| Physics float divergence invisible to short corpus entries | M1 long entries + 12-cell matrix + fault-injection-proven detectors before any port |
| Missed Host_Error path longjmps through a Rust frame (UB) | T1.7b inventory; per-svc statusization; STOP A/B reviews check reachability |
| Delta-writer mismatch only visible in live netplay | record/netreplay diffs at M6; soak smoke every milestone from M4; full soak M9/M11 |
| Guard overhead in SV_TouchLinks/SV_Move hot paths | Measure at M3 pre-flip; batch guards at funnel level if needed |
| Parallel worktree conflicts on build files | Main session sole writer of meson/common.make/build.rs/CI; agents deliver source+tests |
| cvar registry flip silently breaks Rust-registered cvars | M2 registration-order differential + double-registration assert |
| Savegame byte-diff blocked by specifier drift | T1.7a ADR-005 audit first; fixtures before port |
| Arena flip invalidates M8 savegame verification | Save suite re-run in M9 Gates line |
| Soak flakiness erodes trust | Deterministic scripted input; hash-based criterion; auto-captured repro artifacts |
| Sonnet-tier transliteration introduces subtle divergence | Only used where a bit-exact differential gate exists; Opus/main-session review at landing; STOP reviews |

## Non-goals

Renderer halves of r_part*/menu/sbar/console drawing internals (Phase 8); `tasks.c` (ADR-016, Phase 8); setjmp shell removal and `QcVmGuard` (Phase 9); C deletions (recorded, deferred — C stays the oracle); CSQC feature work beyond load-chain parity; performance work beyond guard-overhead neutrality; net_dgrm_rel.c/net_loop.c/wire layer/demo format (already Rust); `net_wins.c`/`net_bsd.c`/`net_win.c` (Phase 9); gameplay behavior changes; `PF_break` (C forever); `PF_Fixme`/extension machinery (follows the builtins, stays C this phase unless the last slot flips).

## Verification summary (three tiers)

- **Inner loop (per task):** `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, the task's ctest differentials (release + debug), `check_ctest_symbols.sh` when C_SOURCES changed.
- **Milestone boundary:** full ctest suite; `run_corpus.py --compare build-c build-rs` (+ `build-rs-chost` oracle); `save_diff.py`; `physics_matrix.py` (from M3); `builtin_diff.py`/`trace_diff.py` (progs-touching milestones); interop/soak smoke (from M4); `check_headers.sh`/`check_capi_signatures.sh`; all meson configs build.
- **Phase exit (M11):** full ADR-019 suite; full soak (100k × 8 cells); fuzz soak; Miri; fresh-context `/integration-review`; windows-x64 goldens + CI leg, Linux deferred.

## Amendment log

- **2026-08-28 plan committed** (pre-M1). Planning was agent-executed: three parallel exploration agents (code inventory, conventions/ADRs, harness/CI), one Plan agent for milestone design, cross-checked by the main session. One Plan-agent error caught in review and corrected before approval: T1.4 originally proposed *creating* a client state-hash; `Harness_HashClient` already exists (`harness.c:236`), so T1.4 targets only the cl.qcvm/CSQC gap. User decisions recorded above: Linux goldens deferred; soak budget 100k frames/cell.
- **2026-08-29 M1 gate evidence (all measured, Windows x64, this checkout).**
  - `check_headers.sh`: 12/12 headers compile standalone, 12/12 bindgen ok, exit 0.
  - Both new meson legs configure and build clean: `-Duse_rust_cvar=enabled` (146/146) and `-Duse_rust_host=enabled` (345/345).
  - `run_corpus.py --compare` (C-only vs mixed, shareware tier): **8 ran, 0 skipped, 0 failed**, every entry `identical` — including the three new pusher entries `e1m1-long`, `e1m5-trains`, `e1m1-plat-crush`.
  - `run_corpus.py --stability` (shareware tier): **8 ran, 0 failed**, all `stable`.
  - `physics_matrix.py` 12-cell CI trim × 7 entries (`save-e1m1`, `map-e1m2`, `e1m1-long`, `e1m5-trains`, `e1m1-plat-crush`, `e3m6-trains`, `save-e2m1`), C-vs-C: **84 ok, 0 skipped, 0 failed** (re-measured 2026-08-29 after the review pass; the first M1 measurement read *72 ok, 12 skipped* because it predated `e1m1-plat-crush` landing and still selected `gamedir-switch`, which now opts out via `"physics": false`); red-tested on the elevators axis via `e1m1-plat-crush` (hash chains fork at `F 56` between `sv_gameplayfix_elevators` 0 and 3, same-cvar rerun byte-identical). Lerps axis still vacuous per the KNOWN GAP above.
  - `interop_matrix.py --soak --frames 20000 --combos C/C`, with the C/C short-circuit removed so this is a genuine two-process comparison: **PASS both protocols** — `Base-666` 313 checkpoints reached, traffic in band, hash-identical; `FTE+999` 313 checkpoints reached, traffic in band, `hash forks @128` reported as the expected diagnostic. Reference streams: rel 3/4 unrel 19910/19986 (Base-666) and rel 4/5 unrel 19904/19983 (FTE+999). Red-tested separately via `--inject-desync-at` (`net_messagetimeout 0`), which fails both protocols on the reliable-traffic condition.
  - T1.4's `cl.qcvm` hash branch red-tested by two-stage fault injection: an injected build that loads id1 `progs.dat` into `cl.qcvm`, compared against a control build carrying the *same* injection with only the T1.4 branch disabled — **all 8 entries differ**, so the new hash line is the sole cause. Dormant on id1 data (no csprogs), hence injection rather than a corpus entry.
  - `cargo fmt --check` and `cargo clippy --workspace --all-targets -- -D warnings`: clean. `clang-format` clean on all four touched C files.
  - Not run in M1 (out of scope, no Phase 7 code ported yet): `save_diff.py`, `builtin_diff.py`, `trace_diff.py`, `record_diff.py`, `netreplay_diff.py`, Miri, the full 100k×8 soak (M9/M11), and any Linux/macOS leg — CI covers the last on push.
- **2026-08-29 PR #26 review pass.** Findings triaged individually rather than accepted wholesale; accepted ones fixed in a follow-up commit on the same branch.
  - *Accepted, code:* `Quake/sys_sdl_win.c` now keys the console-input suppression on the actual failing call (`GetNumberOfConsoleInputEvents` returning 0 degrades to "no console input, ever", printed once) instead of on `harness_active` — the old predicate was a proxy for the cause and also silenced console input for a legitimate `-dedicated -harnesscmds` operator. `Quake/net_wins.c`'s `WSAECONNRESET` diagnostic no longer prints an address: `recvfrom()` returned `SOCKET_ERROR`, so `addr` may still hold the caller's previous sender. `Quake/main_sdl.c`'s two near-identical pacing branches deduped. `Quake/harness.c` gained `_Static_assert`s tying the seven constants `rust/quake-ctest/stubs/abi_probe.c` hardcodes to the real headers, and `Quake/render.h` gained the file's existing "must be changed in … too" markers on the three shadowed structs.
  - *Accepted, harness:* `physics_matrix.py` keeps both hash chains on failure (`--results-dir`) and reports the first diverging record line instead of only "DIFFERS"; corpus entries can opt out of the physics selector with `"physics": false` (used by `gamedir-switch`); `interop_matrix.py` gained a minimum-checkpoint floor on the reference stream, frame-derived client/server timeouts, and three corrected docstring claims (a dedicated server *is* fixed-dt under `-demohash`; a missing checkpoint means the server died, not that the client was dropped; both processes are now paced).
  - *Accepted, docs:* the CI soak smoke drops from 20000 to 5000 frames (~30 min serial at 20k because both processes pace to real time; both bugs it exists to catch surfaced by ~frame 128 — 20000 stays the local budget); `meson.build` and `build-linux.yml` now say plainly that `USE_RUST_CVAR`/`USE_RUST_HOST` and the `build-rs-chost` leg have no consumer until M2 and so cannot fail yet; `Misc/harness/README.md` records that the three new pusher entries have **no goldens on any platform**, so `--check` skips them, and carries the `e1m1-plat-crush` derivation the corpus note used to hold; `abi_probe.c`'s "not circular" claim replaced with an explicit KNOWN GAP (the `entity_t`/`entlerp_t`/`lightcache_t` shadow is transcribed, not header-derived, and only the constants are gated).
  - *Rejected:* "`run_corpus.py --check` silently skips missing goldens" — it already prints `skipped <name>: no golden for <plat>` per entry plus a summary count (`run_corpus.py:219-221`); the real gap was that this was undocumented, which the README change fixes. The remaining review asks about darwin-arm64 rerelease/mod-tier goldens cannot be answered from this machine (Windows, no macOS host) and stay open.
  - *Re-verified after the fixes:* full C build via meson/ninja (so the new `_Static_assert`s and all four C edits compile); `physics_matrix.py` C-vs-C **84 ok, 0 skipped, 0 failed**; `clang-format` clean on every touched C file; `python -m py_compile` clean on both touched scripts; `corpus.json` parses. Not re-run (unchanged inputs): the soak, corpus `--compare`/`--stability`, `check_headers.sh`.
- **2026-08-29 M2 landed** (`use_rust_cvar` first flip: cvar.c + cmd.c → `quake-cvar` helpers + `quake-capi` cores + `Quake/cvar_cmd_glue.c`).
  - *Task-shape amendment:* T2.2 (cvar.c) and T2.3 (cmd.c) were executed as **one Opus-high port agent**, not two parallel agents — the two files share the raise topology, the glue file, the cbindgen exclusions, and the `cvar_vars`/`cmd_functions` registry interlock (`Cvar_Create` checks `Cmd_Exists`; `Cmd_AddCommand2` checks `Cvar_FindVar`); splitting them would have serialized on a shared contract anyway. The ctest differential suite (T2.1) ran as a second parallel agent against a frozen contract (`m2-contract.md`).
  - *Architecture as landed:* ADR-009 raise topology — glue C owns the reraising ABI wrappers (`Cbuf_Execute`, `Cmd_ExecuteString`, `Cvar_Set*`, …) over `quake_rs_*` status cores; Rust-internal calls use cores only; Rust `xcommand_t` handlers propagate raises via a `PENDING_RAISE` side-channel drained after guarded dispatch. Glue C also owns `cmd_text`/`cmd_source`/`cmd_functions`/`cmd_alias`/`cl_nopext`/`cmd_warncmd` storage and the serverinfo/userinfo replication blocks (incl. the `&cl_name`/`&cl_topcolor`/`&cl_bottomcolor` pointer-identity hacks) so no ADR-011 svs/cls mirrors were needed in M2; those port with M6/M7. `cvar_vars` is Rust-private.
  - *Contract deviations (recorded, accepted):* (1) `Cbuf_InsertText` and `Cmd_ForwardToServer` are glue wrappers, not direct Rust exports — `SZ_GetSpace` (net_msg.c:481) can Host_Error on overflow, so both are raise-capable. (2) `Cvar_Set_f` returns on NULL from `Cvar_Create` instead of C's NULL-deref; `CvarCmd_Glue_HostClientName` returns `""` for NULL `host_client` — both replace C crash paths, unreachable in normal operation. (3) The port agent edited `rust/quake-capi/cbindgen.toml` (hand decls + ~40 excludes) — needed for `check_capi_signatures.sh`, no other owner existed.
  - *Pre-existing exposure recorded (not fixed in M2):* Rust modules from earlier phases call the now-reraising wrappers via `quake-c-sys` (`bgmusic.rs:137,523,525`; `cfgfile.rs:154,217`; `fs.rs:1323-1324`; `net_main.rs:317,325` — `c::Cvar_Set`/`c::Cvar_SetQuick`/`c::Cvar_RegisterVariable`/`c::Cbuf_AddText`). A caught raise would longjmp through those Rust frames — but this exposure predates M2 (the same calls previously hit the C functions with identical longjmp behavior). Later milestones statusize those strata; no new exposure added.
  - *Drive-by fix (required to build):* `Quake/pr_cmds.c:41` and `Quake/pr_ext.c:261` carried unguarded `#pragma GCC diagnostic` from Phase 6 commit 0911af5a → C4068 under MSVC `/W2 /WX`. Invisible until now because only clang-cl/MinGW CI ever compiled the `use_rust_progs` leg; the M2 local MSVC `build-rs` config exposed it. Wrapped in `#if defined(__GNUC__)` per the `gl_texmgr.c` pattern.
  - *Gate evidence (all measured, Windows x64, this checkout):*
    - All three meson configs build clean: `build-rs` (flip live), `build-rs-chost` (C oracle leg), `build-c`.
    - `check_capi_signatures.sh`: OK.
    - `run_corpus.py --compare build-c build-rs` (registered tier): **11 ran, 8 skipped (missing mod/rerelease data — normal for this machine), 0 failed**, every entry `identical`.
    - Config byte-diff (writer still C, registry Rust): `+seta _m2test 3.14 +set gamma 0.6 +quit` headless run of both builds → `id1/vkQuake.cfg` **byte-identical** (104 lines; `seta` archived, plain `set` correctly not). Side finding: with shareware data `COM_CheckRegistered` never sets the `cmdline` cvar so `stuffcmds`/`+`-commands are inert — stock quirk, needed registered PAKs for this gate.
    - ctest differential suite: new `cvar_cmd_differential.rs` (20 tests: registration order/dup/collision, FindVarAfter flag filter, completion, alias define/redefine/unalias/expansion, 4 tokenizer edge-case sets, Cbuf ordering, WriteVariables byte-compare, SetQuick ROM/LOCKED/CHANGED/default_string semantics, SetValue formatting, Cvar_Create matrix, ExecuteString srctype filter, CheckParm) — **20/20 pass debug AND release**, deterministic across 3 runs; full quake-ctest suite green including `cfgfile_differential`, which the flip broke (its old plain-named Cvar_Set capture stubs had to go) and which was re-seamed via a callback-based capture (`ctest_register_logged_cvar_pair` registers the tested cvars into BOTH registries with a change-logging callback — Cvar_SetQuick's no-change early return makes only real changes log, identically on both sides). Documented gap: the ADR-009 raise/longjmp paths are not differentially tested (see module doc).
    - `check_ctest_symbols.sh`: OK — 30 oracle sources (now incl. cvar.c, cmd.c) export only `c_ref_*` and allowlisted symbols.
    - `cargo fmt --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean (post-agent fixes: 23 `// SAFETY:` comments per repo convention + 3 misc lints in the new test file; no behavior change, tests re-run green after).
    - `clang-format` clean on `cvar_cmd_glue.c`, `pr_cmds.c`, `pr_ext.c`.
    - Not run (out of scope for M2): `save_diff.py` (savegame writer untouched), `physics_matrix.py` C-vs-Rust (starts M3), `builtin_diff.py`/`trace_diff.py` (no progs change), interop/soak (starts M4), Linux/macOS legs (CI on push).
- **2026-08-29 PR #28 review pass (M2).** Six findings, triaged individually; five accepted, one accepted with its scope narrowed.
  - *Accepted, blocking, code:* `cvar_inc_f` (`rust/quake-capi/src/cvar.rs`) narrowed to `c_float` **before** the `+ 1`, so `inc` rounded twice where C rounds once. C is `Cvar_SetValue (name, Cvar_VariableValue (name) + 1)` with `double Cvar_VariableValue` (`Quake/cvar.c:83-98`, `Quake/cvar.h:128`), i.e. a `f64` add and one narrowing at the call. Divergence is real above f32 integer precision: `"16777217"` + 1 gives `16777218` in C and `16777216` (unchanged) two-step. Fixed and marked `// COMPAT: ADR-010`.
  - *Accepted, docs:* the `Cbuf_InsertText`/`Cmd_ForwardToServer` comment in `Quake/cvar_cmd_glue.c` claimed the glue wrapper is the `"cmd"` `xcommand_t`; it is not — `Cmd_Init` registers the private Rust `cmd_forward_to_server_handler`, which parks its raise in `PENDING_RAISE`. Comment corrected.
  - *Accepted, docs:* `PENDING_RAISE` (`rust/quake-capi/src/cmd.rs`) gained an explicit INVARIANT paragraph naming `cmd_execute_string_core` as the sole drain site, the public `Cmd_FindCommand` as the pointer-leak vector, and misattribution as the failure mode. The dispatcher now also *clears* any stale value before each guarded handler call (`debug_assert_eq!` in debug), so a leak cannot be re-issued as a `Host_Error` blamed on the next command in release builds.
  - *Accepted, code + docs:* `cmd_stuffcmds_f`'s `j` is unbounded exactly as in C; the no-overflow argument rests entirely on `Quake/common.c:1396` clamping `com_cmdline` to `CMDLINE_LENGTH - 1`. Recorded as a `// COMPAT:` note, and `Quake/harness.c` now `_Static_assert`s `CMDLINE_LENGTH == 256` (copied into `cmd.rs` *and* `c_ref_prelude.h`) and `sizeof (CONFIG_NAME) == sizeof ("vkQuake.cfg")`. Only the length of `CONFIG_NAME` is checked: a compile-time string compare needs `__builtin_strcmp`, which MSVC `cl` lacks; the spelling is covered by the config byte-diff gate, which reads the file back by name.
  - *Accepted, test, scope narrowed:* the review said `cfgfile_differential`'s capture cannot see sets to unregistered names **and** that its second `CFG_ReadCvars` pass is invisible. The first half is largely theoretical — `CFG_ReadCvarOverrides` iterates the caller-supplied `vars` array and cannot set a name outside it. The second half is real and was fixed: pass 2 re-set identical values, so `Cvar_SetQuick`'s no-change early return logged nothing. `stubs.c` gained `ctest_reset_logged_cvars(side)` (side-selective, so the shared log stays symmetric), called between the passes, plus an assertion that a `vid_width` set actually appears after the sentinel writes. **Red-tested:** stubbing out the Rust `FS_rewind` now fails the differential; before the fix it passed.
  - *Accepted, test, blocking:* `cvar_cmd_differential.rs` covered zero console-command handlers. Added `console_command_handlers_match` — 48 command lines fed to both sides through `Cbuf_AddText`/`Cbuf_Execute`, comparing the captured `ctest_con_log` stream **and** the resulting cvar state after every line: `inc` (usage/unit/amount/missing), `toggle` (usage/missing/numeric/explicit pair), `cycle` (usage/no-match/mid-list/last), `set`/`seta` (usage/extra-args/create/archive), `cvarlist`/`cmdlist`/`apropos` (prefix-scoped), `echo`, `alias`/`unalias`/`unaliasall` through the buffer, `exec` (missing file/usage), `stuffcmds` (driven off a real `cmdline` string), `wait`, `reset`/`resetcfg`/`resetall`. **Red-tested:** reintroducing the `inc` double-narrowing makes it fail with `left: 16777218 / right: 16777216`. Incidental finding while writing it: `Cbuf_Execute` never clears `cmd_wait` (host.c does, via `Cbuf_Waited`), so a test that issues `wait` must emulate a frame boundary or every later `Cbuf_Execute` in the process is a no-op.
  - *Re-verified after the fixes:* `cargo fmt --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean; full `cargo test --workspace` green **debug and release** (21/21 in `cvar_cmd_differential`, 1/1 in `cfgfile_differential`); all three meson configs (`build-c`, `build-rs`, `build-rs-chost`) build under MSVC/clang-cl, so the new `_Static_assert`s compile; `run_corpus.py --compare build-c build-rs` **11 ran, 8 skipped, 0 failed**, every entry `identical`; `check_ctest_symbols.sh` OK (30 oracle sources); `check_capi_signatures.sh` OK; `check_headers.sh` 12/12 compile + 12/12 bindgen. Not re-run (inputs unchanged): the config byte-diff (no writer or `Cvar_WriteVariables` change; the ctest byte-compare covers the writer), physics/interop/soak (start at M3/M4), Linux/macOS legs (CI on push).
- **2026-08-30 M3 landed** (`use_rust_host` first flip: world.c -> `rust/quake-capi/src/world.rs` + `Quake/world_glue.c`).
  - *Contract amendment (the milestone's one substantive finding).* The frozen M3 contract asserted **exactly one** raise-capable entry point, `SV_LinkEdict`. That is wrong. `PR_GetString` reaches `Host_Error` (`Quake/pr_edict_arena.c:315`), so the two `Con_Warning` sites in `SV_HullForEntity` (`world.c:145,152`) are raise-capable, as is `assert_always` at `SV_Move`'s tail (`world.c:1306`); by call chain that makes `SV_ClipMoveToEntity` (`world.c:928` -> `942`), `SV_Move` (`world.c:1260`), `SV_TestEntityPosition` (`world.c:605`) and `SV_PointContentsAllBsps` (`world.c:588`) raise-capable too. **Six** entry points are therefore ADR-009 status cores (`quake_rs_sv_link_edict`, `..._hull_for_entity`, `..._clip_move_to_entity`, `..._move`, `..._test_entity_position`, `..._point_contents_all_bsps`) wrapped by plain-named C in `world_glue.c`. The port agent initially proposed calling `Host_Reraise` from the Rust frame of `SV_PointContentsAllBsps` (arguing the frame holds only plain data with no `Drop`, under `panic = "abort"`); **rejected** -- the longjmp would still unwind a Rust frame, which ADR-009 prohibits structurally, with no exception for frames that happen to be trivially destructible. The `Host_Reraise` declaration was removed from `quake-c-sys` so the rule is enforced by the absence of the symbol: `grep` finds no `Host_Reraise` declaration or call site anywhere under `rust/`.
  - *Architecture as landed:* 18 `#[no_mangle]` exports from `world.rs` (2139 lines, 29 `// COMPAT:` markers) -- 12 plain non-raising names plus the six cores. Both hull-check implementations ported bit-exactly (FTE `Q1BSP_RecursiveHullTrace` and QuakeSpasm `SV_SlowRecursiveHullCheck`), including `DoublePrecisionDotProduct` narrowing, the `DIST_EPSILON` double-literal promotions, and the `frac -= 0.1` double promotion in the solid back-off loop (each marked ADR-010). `box_hull`/`box_clipnodes`/`box_planes` became Rust-private statics; the two `sv_fte_*` cvar objects stay C-owned in `world_glue.c` because `sv_main.c` registers them. `World_ClipToNetwork` reads `cl.entities` through `World_Glue_ClNumEntities`/`World_Glue_ClEntity`/`World_Glue_QcvmIsClient` accessors rather than an ADR-011 `cl` mirror -- mirrors for `cl`/`cls` are M7 work and adding one here would open an ADR-007 row this milestone cannot close. **No ADR-007 or ADR-011 mirror changed in M3.**
  - *ADR-006 re-entrancy:* `sv_touch_links` holds no Rust reference across `World_Glue_CallTouch`; the touched set is a `Vec<u16>` of edict *numbers* (the ADR-006 substitution for C's `TEMP_ALLOC` list), every pointer is re-derived through a guarded `World_Glue_EdictNum` after each dispatch, and the full C re-validation triple (`free`/identity, `touch`/`solid`, 6-way bbox) is preserved. The deliberate **non**-restore of `pr_global_struct->self`/`other` on the raising path is kept: `world.c`'s longjmp skips that restore, so returning the status early is the faithful behaviour, and it is commented as such.
  - *Signature gate extended:* `check_capi_signatures.sh`'s model TU now includes `protocol.h`, `progs.h` and `world.h` (plus the `@PER_LEVEL_LIMITS@` substitution) so the wrapper signatures are diffed against the engine headers. That required prototypes `world.h` never had -- `SV_CreateAreaNode`, `SV_InitBoxHull`, `SV_HullForBox`, `SV_HullForEntity` (+6 lines). Coverage is now **17 of 18** exports; `SV_FindTouchedLeafs` and `SV_MoveBounds` remain header-less and are covered only by the differential tests. **Red-tested four ways:** mutating `SV_CreateAreaNode`'s signature fails the gate; mutating `SV_InitBoxHull`/`SV_HullForBox` fails with two errors; mutating the still-header-less `SV_MoveBounds` **passes**, proving the gate is declaration-driven and the 17/18 figure is honest; mutating a wrapper parameter in a throwaway `world_glue.c` copy fails `clang-cl -fsyntax-only`.
  - *Guard-cost gate (ROADMAP landmine #6, now closed).* `Host_Guard` measured at **16.805 ns/call** vs **1.744 ns** for the same call direct (clang-cl `/O2`, 20M iterations; `sizeof (jmp_buf)` is 256, so each guard is ~1 KB of `memcpy` plus two `setjmp`s). Guards-per-server-frame measured on three server-exercising corpus entries with temporary counters in `world_glue.c` (applied, measured, reverted -- not committed): `e1m1-long` 2730 guards / 6001 frames = **0.45/frame**; `e1m1-plat-crush` 7605 / 4001 = **1.90/frame**; `e3m6-trains` 16338 / 6001 = **2.72/frame**. Worst case is ~46 ns/frame, ~0.0003% of a 14 ms frame. The fear that motivated the landmine -- a guard per link per move -- does not materialise: the only guard sites are `World_Glue_CallTouch`, `World_Glue_EdictNum` and `World_Glue_NumForEdict`, all inside `SV_TouchLinks`, so guard count tracks trigger *overlaps*, not moves. The `SV_Move`/hull-check hot paths take zero guards, and the two `World_Glue_WarnSolidBsp*` guards never fired on any entry. No guard batching needed.
  - *Gate evidence (all measured, Windows x64, this checkout):*
    - All three meson configs build clean, each with the right world TU: `build-rs` links `Quake_world_glue.c.obj` and **no** `Quake_world.c.obj`; `build-c` and `build-rs-chost` link `Quake_world.c.obj`.
    - `run_corpus.py --compare build-c build-rs` (registered tier): **11 ran, 8 skipped (missing mod/rerelease data -- normal for this machine), 0 failed**, every entry `identical`.
    - `physics_matrix.py --vkquake-a build-c --vkquake-b build-rs`, 12 cells x 7 entries, C-vs-Rust: **84 ok, 0 skipped, 0 failed** -- both `sv_fte_recursivehullckeck` settings, so both hull-check implementations are exercised against the C oracle.
    - New `rust/quake-ctest/tests/world_differential.rs`: **31/31 pass**. Both hull checks, areanode insertion order under both `sv_fte_createareanode` settings, `SV_LinkEdict`/`SV_TouchLinks` ordering incl. re-entrant relink, the `SV_Move` pipeline, `SV_PointContentsAllBsps`, and two raise tests driving the `SV_HullForEntity` `Con_Warning` paths through `ctest_try_host` (asserting the raise actually fired -- `c.0 == 1` -- before comparing the `PR_GetString` message, otherwise the test would prove nothing about propagation). **Red-tested by mutation across three rounds** (13/29 failures on the first sweep); all mutations reverted, zero `MUTATION` markers remain.
    - Harness topology mirrors the engine: `stubs.c` owns plain-named wrappers over all six cores, and the fixture's re-entrant link hook is installed from C (`ctest_world_set_rust_link_fns`) rather than from a Rust function pointer, so no longjmp unwinds a Rust frame in the tests either.
    - `check_ctest_symbols.sh`: OK -- 31 oracle sources (now incl. `world.c`).
    - `check_capi_signatures.sh`: OK against a freshly regenerated cbindgen header. `check_headers.sh`: exit 0, 12/12 compile + 12/12 bindgen. (Note: `check_headers.sh:17` hardcodes `cc`, which Git Bash does not provide; a `cc -> clang` shim was needed to run it locally. Not an M3 regression -- the checked list is 12 fixed core headers, none of them `world.h` -- and left unfixed as out of scope.)
    - `cargo fmt --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --workspace` **83 test binaries, 619 tests passed, 0 failed**.
    - `clang-format` clean on `Quake/world_glue.c` and `Quake/world.h`. (`format.sh` runs clang-format 18 in docker and could **not** run -- docker daemon down; local clang-format 22.1.8 was used and agrees with the checked-in formatting of both files.)
    - `Quake/common.make` deliberately unchanged: it has `world.o` but zero `USE_RUST`/`RUST` references (it never gained M2's `cvar_cmd_glue.o` either), so it is a pure-C path, and M3 splits no C file.
    - Not run (out of scope for M3): `save_diff.py`, `builtin_diff.py`/`trace_diff.py` (no progs or savegame change), `record_diff.py`/`netreplay_diff.py`, interop/soak (start at M4), Miri, Linux/macOS legs (CI on push).
- **2026-08-30 M4 landed** (`use_rust_host` extended: sv_move.c + sv_phys.c -> `rust/quake-capi/src/sv_move.rs` (832 lines) + `sv_phys.rs` (3996 lines) + `Quake/sv_move_glue.c` + `Quake/sv_phys_glue.c`).
  - *Execution shape:* three parallel agents against a frozen contract, with the main session as sole writer of every shared file (`meson.build`, `rust/quake-ctest/build.rs`, `rust/quake-capi/src/lib.rs`, `rust/quake-capi/cbindgen.toml`, `Quake/server.h`) -- T4.1 ctest differentials (Opus), T4.2 sv_move port (Sonnet high), T4.3 sv_phys port (Opus high). The one cross-boundary need (adding `sv_move.c`/`sv_phys.c` to ctest `C_SOURCES`) was routed to the main session rather than edited in place, and the split held: no agent touched another's files.
  - *Architecture as landed:* 12 `#[no_mangle]` exports -- 3 plain non-raising names (`SV_FixCheckBottom`, `SV_CloseEnough`, `SV_PushGridEntityLinked`) and 9 ADR-009 status cores (`quake_rs_sv_check_bottom`, `..._movestep`, `..._step_direction`, `..._new_chase_dir`, `..._move_to_goal`, `..._check_all_ents`, `..._check_velocity`, `..._check_water_transition`, `..._physics`) wrapped by plain-named reraising C. **The M4-specific rule:** because M3 split six world.c entry points the same way, Rust sv_move/sv_phys call the *cores* (`crate::world::quake_rs_*`, same crate, no FFI round trip) and never the plain reraising wrappers -- otherwise a caught jump would be re-issued through a Rust frame. 28 `// COMPAT:` markers (3 + 25).
  - `sv_phys_glue.c` (389 lines) owns the C-visible storage `sv_phys.c` defined -- the twelve physics cvar objects, `sv_analyticphysics_frame` and the seven `sv_speeds_*` counters, initialisers byte-identical to `sv_phys.c:44-56,:345-346,:705-707` (sv_main.c registers the cvars by address; host.c reads and zeroes the counters) -- plus 20 `SvPhys_Glue_*` entry points: 12 `Host_Guard` callbacks (both NaN warnings, the two think dispatches, both `SV_Impact` touches, the un-embed `Con_DPrintf2`, `blocked`, PlayerPre/PostThink, StartFrame, `SV_StartSound`, the two `Host_EndGame` bad-movetype sites) and 8 non-raising shims (three console prints, `qcvm == &sv.qcvm`, `svs.maxclients`, two `svs.clients[]` reads, `sv_player`). `sv_move_glue.c` (79 lines) is five reraising wrappers and nothing else -- every raising path inside sv_move.c already funnels through world.c's cores.
  - All five pusher/move-frame struct types are file-local to `sv_phys.c`, so they became Rust-private: **no ADR-007 or ADR-011 mirror changed in M4**.
  - *The milestone's one substantive compatibility finding* (surfaced by the test agent, verified against the C before the fix): `SV_StepDirection` (`sv_move.c:244`) computes `yaw = yaw * M_PI * 2 / 360` in `double` -- `M_PI` is a double macro -- but **assigns back into the `float` parameter `yaw`**, so the angle is narrowed to f32 before `cos`/`sin` promote it again. The first port kept the angle in `f64`. The narrowing is observable: at 90 degrees C's `cos` sees `1.5707964f` and yields `-4.371139e-08`, where an unnarrowed f64 angle yields `6.1232e-17`. Fixed at `sv_move.rs:528`, marked `// COMPAT: ADR-010`.
  - *Contract deviations (recorded, accepted):* (1) `SvPhys_Glue_ImpactTouch` deliberately does **not** reuse M3's `World_Glue_CallTouch`: the world glue stamps `pr_global_struct->time` before its dispatch, whereas `SV_Impact` (`sv_phys.c:424-436`) sets `time` **once** before **two** dispatches, and QC may change it in between. Pinned by `impact_touch_stamps_one_time_across_both_dispatches`. (2) `SV_PusherBlockIsPersistentRiderContact`'s `robust_push` and `riding` parameters are folded into one `robust_riding: bool` to stay under clippy's 7-argument limit; C only ever evaluates `robust_push && riding`.
  - *Coverage traded, recorded:* with `Quake/sv_phys.c` now in ctest `C_SOURCES`, `SV_PushGridEntityLinked` resolves to the real `c_ref_*` symbol, so M3's interceptable `ctest_world_pushgrid_log` seam no longer exists. `world_differential` (still 31/31) no longer asserts link-time push-grid ordering; the new `sv_phys_differential` grid tests cover it instead.
  - *A vacuous-gate trap, caught twice.* `quake_rs_sv_physics` needs `ED_AllocSetHook`, and the obvious ctest stub -- store the hook, return the previous one, never call it -- would have made all Rust-side `pushable_ent_cache` coverage vacuous while staying green. Instead the harness forwards into `c_ref_ED_AllocSetHook`'s real hook slot, and a new `CTEST_WORLD_TOUCH_SPAWN` QC builtin allocates an edict mid-tick from `PlayerPreThink` (entity 1, before the pusher at entity 2), landing exactly where `SV_Physics_Alloc_Hook` is armed (`sv_phys.c:2397`). The **first** version of that test still passed under a dead-stub mutation, because with `sv_pushgrid 1` a second route (`SV_LinkEdict -> SV_PushGridEntityLinked -> PushGrid_Insert`, `sv_phys.c:202`) carries the new edict anyway; it now runs with `sv_pushgrid 0` **and** `sv_gameplayfix_elevators 0`, with `sv_pushgrid 1` / `sv_fastpushmove 0` controls.
  - *Signature gate extended (M4's second gate finding).* The three plain exports were first excluded from `quake_rs.h` and left "declared by server.h / world.h", which gave them **zero** gate coverage: a red-test mutating `SV_CloseEnough`'s prototype **passed**. They are now hand-declared in the `#ifdef _QUAKE_WORLD_H` block exactly as M3's plain exports are, and `check_capi_signatures.sh`'s model TU includes `net.h` + `server.h` behind a new `@SERVER_LIMITS@` substitution that *extracts* the six quakedef.h limits `server.h` sizes its buffers with (`MAX_MSGLEN`, `MAX_DATAGRAM`, `MAX_MODELS`, `MAX_SOUNDS`, `MAX_PARTICLETYPES` as `#define`s, plus the `MAX_CL_STATS` enumerator) rather than hand-copying values, with a count assertion so a rename fails loudly. **Red-tested:** the same `SV_CloseEnough` mutation now fails the gate with `conflicting types for 'SV_CloseEnough'`. The nine `quake_rs_*` cores stay declaration-only -- like M3's six, they have no C original to diff against.
  - *Corrected diagnosis (recorded so it is not rediscovered).* The sv_phys port agent reported `check_capi_signatures.sh` blocked by a "pre-existing local toolchain/cbindgen-version mismatch" on `expected a boolean or an integer for key profile.release.debug`. That is wrong: `debug = "line-tables-only"` has been in `rust/Cargo.toml` since Phase 0 (`d3f0b482`) and is valid. The message appears when cbindgen's `cargo metadata` runs under a stale toolchain -- `rust-toolchain.toml` only resolves from inside `rust/`, and the gate runs from the repo root. `RUSTUP_TOOLCHAIN=1.97.1`, plus the `cc -> clang` shim Git Bash needs, runs the gate clean.
  - *Also corrected:* the same agent flagged that `sv_phys.rs` might escape CI clippy because no workflow passes `-F host`. It does not: `rust/quake-ctest/Cargo.toml:23` enables `host` on its `quake-capi` dependency, so `cargo clippy --workspace` lints it. (The dedicated `progs` clippy line at `.github/workflows/rust.yml:52` exists precisely because no member enables *that* feature.) No workflow change made.
  - *Build fix:* `sv_move_glue.c` needed `#include "steam.h"` before `quake_rs.h` (the generated header declares the Phase 2 Steam shims in terms of `steamgame_t`) -- the line `world_glue.c` and `sv_phys_glue.c` already carried. Caught by the first `build-rs` link, by no earlier gate.
  - *`meson.build`:* `Quake/sv_move.c` and `Quake/sv_phys.c` were removed from the unconditional `srcs` list and moved into the `use_rust_host` if/else, matching how world.c/cvar.c/cmd.c were handled at M2/M3 -- without that they would have been double-listed in the C leg and mixed C+glue in the Rust leg. `Quake/common.make` deliberately unchanged: it lists `sv_move.o`/`sv_phys.o` but has zero `USE_RUST`/glue references, so it is a pure-C path and M4 splits no C file.
  - *Gate evidence (all measured, Windows x64, this checkout):*
    - All three meson configs build clean, each with the right TUs: `build-rs` links `Quake_sv_move_glue.c.obj` + `Quake_sv_phys_glue.c.obj` and **no** `Quake_sv_move.c.obj`/`Quake_sv_phys.c.obj`; `build-c` and `build-rs-chost` link the C originals.
    - `physics_matrix.py --vkquake-a build-c --vkquake-b build-rs`, 12 cells x 7 entries, C-vs-Rust: **84 ok, 0 skipped, 0 failed** -- both `sv_fte_recursivehullckeck` settings, all four `sv_gameplayfix_elevators` values, both `sv_smoothplatformlerps` values.
    - `run_corpus.py --compare build-c build-rs` (registered tier): **11 ran, 8 skipped (missing mod/rerelease data -- normal for this machine), 0 failed**, every entry `identical`.
    - `run_corpus.py --stability build-rs`: **11 ran, 8 skipped, 0 failed**, all `stable`.
    - `interop_matrix.py --soak --frames 20000 --combos R/R Rsv/Ccl --protocols FTE+999`: **PASS** on both cells (`R/R`, `Rsv/Ccl`, protocol FTE+999), 313 checkpoints each against a 20001-frame same-seed C/C reference stream, traffic in band. Both cells report `hash forks @128 (diagnostic)` -- expected and non-failing: live sessions are timing-nondeterministic, so the pass criterion is checkpoint reachability plus traffic band, never byte identity (M1 finding).
    - New ctest suites: `sv_move_differential` **17/17**, `sv_phys_differential` **28/28**; `world_differential` unchanged at **31/31**. Full `cargo test --workspace`: **85 test binaries, 664 tests passed, 0 failed** (debug); the three physics/world suites re-run green in release. Red-tested by mutation by the test agent across three rounds; all mutations reverted, zero `MUTATION`/`TEMPLINK` markers remain.
    - `check_ctest_symbols.sh`: OK -- 33 oracle sources (now incl. `sv_move.c`, `sv_phys.c`).
    - `check_capi_signatures.sh`: OK against a freshly regenerated cbindgen header, plus the red-test above. `check_headers.sh`: exit 0, 12/12 compile + 12/12 bindgen.
    - `cargo fmt --all --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` clean.
    - `clang-format` clean on `Quake/sv_move_glue.c` and `Quake/sv_phys_glue.c`. (`format.sh` runs clang-format 18 in docker and could not run -- docker daemon down; local clang-format 22.1.8 was used and agrees with the checked-in formatting.)
    - Not run (out of scope for M4): `save_diff.py`, `builtin_diff.py`/`trace_diff.py` (no progs or savegame change), `record_diff.py`/`netreplay_diff.py` (M6/M7), the full 4-combo interop matrix and the 100k x 8 soak (M9/M11), Miri, Linux/macOS legs (CI on push).
- **2026-08-31 M5 landed** (`use_rust_progs` Pattern C extended to the host-coupled slots: 41 builtins + the two `ED_Parse*` dispatchers -> `rust/quake-capi/src/progs_builtins_sv.rs` (1126 lines) + `progs_builtins_sv_msg.rs` (477) + `progs_builtins_sv_fx.rs` (388) + `progs_builtins_cl.rs` (317) + `progs_edict_dispatch.rs` (585), with `Quake/pr_cmds_sv_glue.c`, `pr_cmds_sv_msg_glue.c`, `pr_cmds_sv_fx_glue.c`, `pr_cmds_cl_glue.c` and `pr_edict_dispatch_glue.c`). Executed as agent waves -- wave 1 the link/trace/PVS group (Opus high), wave 2 the message, world-effects and client groups in parallel (Sonnet high), plus the `ED_Parse*` dispatchers -- with T5.3 integration and all shared-file writes in the main session.
  - **The plan's "43 builtins" reconciles to 41, exactly.** `PF_changeyaw` was already flipped in Phase 6 M7 (in-file `#ifdef`, because `sv_move.c`'s `SV_MoveToGoal` calls it directly), and `PF_break` stays C permanently by plan; 43 - 2 = 41. A name-by-name cross-check confirms the 41 `RUST_PF` rows in `pr_cmds_glue.c` cover exactly the 41 `quake_rs_pf_*` exports across the four modules, none left over on either side. Separately, the plan's "8x `PF_Write*`" is a stale count: the classic eight (`WriteByte/Char/Short/Long/Coord/Angle/String/Entity`) landed in Phase 6 and their rows are already `PF_RS`; group D's six are the FTE extension writes (`WriteFloat`, `WriteDouble`, `WriteInt`, `WriteInt64`, `WriteUInt64`, `WriteString2`) only.
  - **`PF_RSH` is new, and requires both switches.** `use_rust_progs` (`meson.build:393`) and `use_rust_host` (:404) are independent options, and `Quake/pr_cmds_glue.c` ships under `use_rust_progs` **alone**. A `PF_RS` flip would therefore point the table at `rust_pf_*` in the progs=on/host=off cell, where neither the `RUST_PF` wrapper nor the `quake_rs_pf_*` core is compiled. All 41 rows use a new `PF_RSH (name)` macro (`pr_cmds.c:31-82`, `pr_ext.c:252-309`) expanding to `rust_pf_##name` only under `#if defined(USE_RUST_PROGS) && defined(USE_RUST_HOST)`; the 41 `RUST_PF` rows sit in `pr_cmds_glue.c`'s first-ever `#ifdef USE_RUST_HOST` block; and the Rust modules are gated on a `progs-host` Cargo feature Meson sets only for `use_rust_progs and use_rust_host` (the ctest-only `progs-m5` feature the agents proposed was retired in favour of it). The five glue files ride `if use_rust_host` alone, matching M3/M4 -- verified safe because none of them calls a `quake_rs_*` symbol and each self-gates on `USE_RUST_HOST`.
  - **A fourth configuration cell was built to prove this, and it found a real bug.** `-Duse_rust_progs=disabled -Duse_rust_host=enabled` is a live CI leg (`build-linux.yml:137`, `build-rs-cprogs`) that no local build had exercised this phase. Configuring it locally caught the `ED_Parse*` flip in `pr_edict.c` gating on `USE_RUST_HOST` alone, which left that leg with an unresolved `quake_rs_ed_parse_globals`. All four cells -- `build-c`, `build-rs`, `build-rs-chost`, `build-rs-cprogs` -- now build and link.
  - **`ED_ParseGlobals`/`ED_ParseEdict` flip in place, not through a table** -- the `PF_changeyaw` precedent (landmine 21): `host_cmd.c:2071/2097`, `pr_ext.c:3988` and `pr_edict.c:947` call them directly. `pr_edict_dispatch_glue.c` deliberately defines only the four `PREdictDispatch_Glue_*` leaf callbacks, so `pr_edict.c` stays unconditional in `srcs` and there is no symbol collision. The edict crosses FFI **by number**, not by pointer (`quake_rs_ed_parse_edict (data, edict_num, out, detail)`), the C wrapper doing the `NUM_FOR_EDICT_NO_CHECK` conversion -- matching `PRParse_Glue_UnlinkEdict`, and avoiding a raise the C original does not have. The nine `PREDD_*` status codes are private to `progs_edict_dispatch.rs:34-53` and are **not** the `PRBI_ERR_*` set; `pr_edict.c` carries a mirror plus a raise switch, with a keep-in-sync comment on both sides. One upstream bug preserved verbatim: `ED_ParseGlobals`'s two `Host_Error` strings say `ED_ParseEntity`, an upstream copy-paste.
  - *Cross-milestone fix (an M3 defect, Linux-only, that would have broken CI).* `rust/quake-ctest/stubs/stubs.c:3438` carried tentative definitions of `sv_fte_recursivehullckeck`/`sv_fte_createareanode` while `Quake/world.c:33/35` defines the same pair, which the ctest prelude renames to `c_ref_sv_fte_*` on both sides -- two strong definitions under `-fno-common` (GCC 10+, and rust-lld). MSVC merges them silently; a WSL `cargo build --workspace --all-targets` reproduces `duplicate symbol: c_ref_sv_fte_recursivehullckeck`. Confirmed byte-identical at `HEAD`, so it predates M5, and `gh run list` shows the newest CI run on this branch is from the **M2** commit -- M3 and M4 have never been through a Linux leg. The first attempt, deleting the definitions, broke Linux *compilation* instead, because no header declares either cvar and the tentative definition was also the declaration; they are now `extern cvar_t` declarations, clean on both toolchains (WSL workspace build green, exit 0).
  - *A gate that was measuring the linker, not the engine (`builtin_diff.py`).* The first C-vs-Rust run went red on three entries -- `cvar_description`, `ex_CheckPlayerEXFlags`, `ex_walkpathtogoal` -- each reporting bound ordinal `79` in `build-c` and `-1` in `build-rs`. Root cause: `PF_sv_finalefinished` (`pr_cmds.c` row #79), `PF_cvar_description`, `PF_sv_CheckPlayerEXFlags` and `PF_sv_walkpathtogoal` all have the identical body `G_FLOAT (OFS_RETURN) = 0;`, so `/OPT:ICF` folds them to **one address** and `PR_DumpBuiltinTable_f`'s whole-table pointer scan reported a spurious binding for the three that are not in the table at all. Flipping row 79 to a distinct `rust_pf_` wrapper un-folds the group and the probe correctly returned `-1`; `build-rs-chost` (where `PF_RSH` yields the C originals) matched `build-c` exactly, isolating the cause to the flip. Fixed at the probe: it now checks the entry's **own number** (`qcvm->builtins[number] == want`) instead of scanning. That loses no binding -- `PR_EnableExtensions` writes `builtins[documentednumber]`, and `PF_Fixme`'s lazy path writes `builtins[binum]` only when `extensionbuiltins[].number == binum`, so every binding path installs at the entry's own number -- and it is toolchain-independent. What it gives up is the incidental observation that an entry's function *also* occupies a fixed core slot under a different number (`ex_finaleFinished` aliasing `#79`): an aliasing fact, not a numbering one, and unrecoverable under folding either way. Verified non-vacuous after the change -- `vectoangles2 51 51` and `infokey 80 80` still report real bindings in both builds.
  - *`pr_trace.c` opened the trace sink in text mode.* `Sys_fopen (..., "w")` produced the header `PRTRACE 1\r\n` on Windows, and `trace_diff.py` -- which byte-compares -- rejected **both** builds with `trace header mismatch`, so this gate had never actually run on this platform. Changed to `"wb"`: the trace is a byte-compared artifact, and Windows traces now agree with every other platform's. Diagnostic output only (no goldens), and both sides of every diff change identically.
  - *Coverage gaps carried to STOP A (recorded, not closed):* (1) `rust/quake-ctest/stubs/pf_fx_ref.c` leaves **5 of group E's 12 builtins as abort-on-call stubs** -- `PF_sv_ambientsound`, `PF_sv_lightstyle`, `PF_sv_makestatic`, `PF_sv_setspawnparms`, `PF_sv_localsound` -- so they have no differential oracle and rest on trace/corpus parity alone. (2) The precache-overflow test uses `CTEST_FX_MODEL_SLOTS = 8` rather than the real `MAX_MODELS`. (3) Group E's `PR_GetString`-raise and `SV_Precache_Sound`-overflow paths are untested. (4) Group F excludes sound-argument marshaling from its differential suite by design, and one mutation survives there for that reason. (5) `tracebox` and `checkpvs` are flipped in **both** the SSQC and CSQC columns, so the CSQC side rides the same core.
  - *Deliberate non-change:* `rust/quake-capi/cbindgen.toml` still has no `[defines]` entry for `progs-host`, so cbindgen "fails open" and emits the prototypes into `quake_rs.h` in every config with a `WARN: Missing [defines] entry` line. Verified cosmetic: the generated header is byte-identical in `build-rs` and `build-rs-chost`, declarations without definitions are harmless while nothing calls them, and `host`/`progs`/`fs`/`snd` have carried the same gap since Phase 2.
  - *Gate evidence (all measured, Windows x64, this checkout, unless noted):*
    - All four meson cells build and link: `build-c`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`.
    - `builtin_diff.py` `build-c` vs `build-rs`: **identical, 235 entries** over both `e1m1` and `start`; `build-c` vs `build-rs-chost` identical as well.
    - `trace_diff.py` `build-c-trace` vs `build-rs-trace`: **byte-identical**, **148206** records over `e1m1`, **186175** over `e2m1`, **217042** over `e3m1` (`--exitafter 400`).
    - `save_diff.py` C-vs-Rust: both savegames **identical** (95091 and 95075 bytes).
    - `run_corpus.py --compare build-c build-rs` (registered tier): **11 ran, 8 skipped** (missing mod/rerelease data -- normal for this machine), **0 failed**, every entry `identical`. `run_corpus.py --stability build-rs`: **11 ran, 8 skipped, 0 failed**, all `stable`.
    - `physics_matrix.py --vkquake-a build-c --vkquake-b build-rs`, 12 cells x 7 entries: **84 ok, 0 skipped, 0 failed**.
    - `interop_matrix.py --soak --frames 20000 --combos R/R Rsv/Ccl --protocols FTE+999`: **PASS** on both cells, **313 checkpoints** each against a 20001-frame same-seed C/C reference stream, traffic in band. Both report `hash forks @128 (diagnostic)` -- expected and non-failing per the M1 criterion: live sessions are timing-nondeterministic, so the pass criterion is checkpoint reachability plus traffic band, never byte identity.
    - New ctest suites: `progs_builtins_sv_differential` **25**, `progs_builtins_sv_msg_differential` **19**, `progs_builtins_sv_fx_differential` **15**, `progs_builtins_cl_differential` **11**, `progs_edict_dispatch_synthetic` **22** -- 92 new tests. Full `cargo test --workspace`: **90 test binaries, 756 passed, 0 failed** (up from M4's 664). Each group red-tested by mutation by its own agent; all mutations reverted.
    - `check_headers.sh`: exit 0, **12/12 compile + 12/12 bindgen**. `check_capi_signatures.sh`: exit 0. `check_ctest_symbols.sh`: exit 0, 33 oracle sources export only `c_ref_*` and allowlisted symbols.
    - `cargo fmt --all --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` exit 0. `clang-format --dry-run -Werror` clean on every touched `Quake/*.c` (`format.sh`'s dockerised clang-format 18 could not run -- docker daemon down; local clang-format 22.1.8 agrees with the checked-in formatting).
    - Linux cross-check: WSL `cargo build --workspace --all-targets` exit 0 after the `stubs.c` fix (a real GCC/rust-lld leg, not a substitute for CI).
    - Not run (out of scope for M5): `record_diff.py`/`netreplay_diff.py` (M6/M7), the full 4-combo interop matrix and the 100k x 8 soak (M9/M11), Miri, the macOS leg and the Linux *meson* legs (CI on push).
- **2026-08-31 STOP A** (fresh-context `compatibility-reviewer`, Opus high, M1-M5) returned two findings; both were independently verified against source before action, and only one survived.
  - **Accepted, with the scope narrowed (commit `0e461901`).** `PREdictDispatch_Raise`'s `PREDD_ERR_GUARD` case (`pr_edict.c:778`) called `Host_Reraise (detail)` and then `return`ed, unlike its sibling `PRBI_Raise` (`pr_cmds_glue.c:322-325`) which follows the reraise with a `Sys_Error` backstop. The reviewer proposed fixing every bare `Host_Reraise` call site. That is wrong: grepping all of them found ~30 in `cvar_cmd_glue.c`, `sv_move_glue.c`, `sv_phys_glue.c`, `world_glue.c` and `pr_exec_glue.c` with no backstop, and `host.c:335`'s documented contract -- *"HOST_GUARD_OK is a no-op so call sites can pass the guard's return value through unconditionally"* -- makes those correct **by design**. The distinction is the call pattern, not the call: a bare `Host_Reraise (r)` at a wrapper tail passes OK through and must return; a `case ERR_GUARD:` arm inside a status switch has already established the value names a real jump, so a return from it is a silently-swallowed `Host_Error`. Exactly one site matches the second pattern. Confirmed inert before landing: the sole `PREDD_ERR_GUARD` producer (`progs_edict_dispatch.rs:425-434`) only sets it when `Host_Guard` returned non-zero, and `HOST_GUARD_OK` is `0`, so the backstop cannot fire on a real guard result. `Sys_Error` is `FUNC_NORETURN` (`sys.h:149`), so falling into the `default:` arm is not a hazard.
  - **Rejected.** The reviewer read M5's `PR_DumpBuiltinTable_f` probe narrowing (`qcvm->builtins[number] == want` replacing the whole-table pointer scan) as dropping bindings whose `.number` is 0 or is assigned by the descending `--g` allocator, and proposed a `num == 0` fallback. `PR_InitExtensions` (`pr_ext.c:6224-6237`) refutes it: `if (documentednumber) number = documentednumber; else number = --g;` -- the allocator's value is written **back into** `.number`, so no entry ever reaches the probe with a stale or zero number. The proposed fallback would be dead code that reintroduced exactly the `/OPT:ICF` fragility the narrowing removed.
  - **Coverage gap closed separately.** STOP A did not reach the M3/M4 physics bodies (~7000 lines, the phase's most determinism-sensitive surface). A dedicated follow-up review of that surface was run rather than proceeding to M6 on the gap.
- **2026-08-31 M3/M4 physics arithmetic fixes** (commit `18936190`), from that follow-up review.
  - **Float width drift at three `(a + b) * 0.5` midpoints** -- `sv_move.rs` (two, vs `sv_move.c:73-74`) and `world.rs:575` in `SV_CreateAreaNode` (vs `world.c:235`). All three widened *both* operands to `f64` before the add. C adds two `float` operands in `float` and promotes only that rounded sum for the `* 0.5`, so the widened form skips a rounding step. The in-code comment's defence -- a power-of-two multiplier commutes with rounding -- holds across the normal range, but not when the `float` sum overflows: C yields an infinite midpoint, a `double` add a finite one near 3e38. ADR-010 forbids the width change on its own terms regardless of reachability. Fixed to add in `f32` and promote only for the multiply, matching C's shape exactly.
  - **Two unmarked float-to-int casts** in `quake_rs_sv_new_chase_dir` (`sv_move.rs`, vs `sv_move.c:292` and `:323`) now carry `// COMPAT: ADR-010`. The reviewer suggested routing them through a truncating helper reproducing `cvttss2si`; **declined, and the reasoning is the point.** ADR-010 is *per-platform*, and C's own float-to-int is not portable here: x86-64 lowers to `cvttss2si` (NaN/out-of-range -> `INT_MIN`), arm64 to `fcvtzs` (saturates, NaN -> 0). Rust's `as` matches `fcvtzs` exactly, so emulating x86-64 semantics would **break** arm64 parity to fix x86-64's. The two disagree on x86-64 only for a NaN or `|yaw| >= 45 * 2^31`, which an `anglemod`-fed yaw does not reach; the deltas are entity-origin differences bounded far inside `i32`. Documented rather than changed.
  - **Debug-profile overflow panics: assessed, no change.** The reviewer flagged `(num_edicts - 1) * edict_size` and the PUSH_GRID index arithmetic as panicking under `[profile.dev]`'s default `overflow-checks` where C wraps. Not reachable: `PushGrid_Insert` (`sv_phys.rs:536-548`) already caps `cells` at `4*4*4` with an explicit per-axis span check *before* the multiply, and the edict expression is bounded by `MAX_EDICTS * edict_size`. Meson selects the cargo `debug` profile only for `buildtype=debug` (`meson.build:415`), and CI's only debug leg (`build-windows.yml:38-68`) sets `-Duse_rust=disabled`, so the profile is not exercised with Rust at all. Adding `wrapping_*` here would obscure genuine overflows for an unreachable one.
  - *Gate evidence (Windows x64, this checkout):* `physics_matrix.py` C-vs-Rust **84 ok, 0 skipped, 0 failed** over 12 cells x 7 entries; `run_corpus.py --compare` **11 ran, 8 skipped, 0 failed**, every entry `identical`; `save_diff.py` both savegames identical (95091 / 95075 bytes); `cargo test --workspace` **90 binaries, 756 passed, 0 failed** (unchanged from M5); `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean; `build-c` and `build-rs` both relink.
- **Open, carried into M6:** neither M3, M4 nor M5 has been through CI -- the newest run on `feature/rust-conversion-phase-7-a3ee42` is from the M2 commit, so the Linux/macOS/MinGW/arm64 meson legs, `cargo deny`, and the CI differential-harness jobs are unexercised for three milestones. Pushing is an outward-facing action and is awaiting the user's decision.
- **2026-08-31 M3/M4 follow-up compatibility review** (fresh-context `compatibility-reviewer`, Opus high) covering the surface STOP A could not reach. Coverage full on the pusher-support frame-state subsystem (`sv_phys.c:738-1130` + `SV_EndClientMoveFrame`/`SV_PushEntityToIgnoringPusher` vs `sv_phys.rs:1248-2075` and `:2375-2470`), the PUSH_GRID bodies, `SV_WalkMove`/`SV_Physics_Client*`/`SV_CheckWaterTransition`/`SV_CheckStuck`, the `SV_Physics_*` dispatch family, and `sv_move.c`'s chase-direction half; partial on `world.c`'s area/link half.
  - **One finding, accepted and fixed (commit `0feb1c1f`).** `SV_Physics_Client`'s movetype dispatch (`sv_phys.rs:3408`) compared the **raw** `(*ent).v.movetype` entvar against `MOVETYPE_NOCLIP` while its five sibling arms compared the truncated `movetype` local. C dispatches through `switch ((int)ent->v.movetype)` (`sv_phys.c:2023`), so a client edict with a movetype in `(8.0, 9.0)` truncates to `8` and takes the NOCLIP path in C, while Rust fell through to `else` and raised `Host_EndGame ("SV_Physics_client: bad movetype 8")` -- a server drop where C keeps simulating. Verified isolated before fixing: `switch ((int)` appears exactly once across `sv_phys.c`, `world.c` and `sv_move.c`, and the top-level `SV_Physics` chain (`sv_phys.c:2416-2429`) compares raw floats in C, which the Rust chain (`sv_phys.rs:3914-3940`) matches correctly. The `else` arm already passed `as_int(...)` to the glue, matching C's `default:`. One-token fix.
  - **Two verification gaps the reviewer flagged as unread were closed in the main session; neither yielded a finding.** `SvPhys_Glue_ClientActive`/`ClientKnownToQc` (`sv_phys_glue.c:372/379`) do apply C's `num - 1` client-slot index, matching `sv_phys.c:1996/1999`. `SV_PointContents`'s Rust body (`world.rs:1021-1032`) expresses C's `cont <= CONTENTS_CURRENT_0 && cont >= CONTENTS_CURRENT_DOWN` as `(-14..=-9).contains(&cont)` -- the same closed interval.
  - *Notable non-findings the reviewer checked and confirmed equivalent:* `PushGrid_Cell`'s NaN catch and `+-2^23` clamp; `PushGrid_GatherCandidates`' pointer-address insertion sort, dedup and large/tail append order; `SV_TracePusherFloorAtOrigin`'s aliasing case where C passes `pusher->v.origin` as its own `pusher_origin`; the pusher frame-counter `wrapping_add` and `== 0` reset vs C's unsigned overflow; `SV_TouchLinks` re-entrancy (the list carries edict *numbers*, `EDICT_NUM` re-resolves per iteration, and the `self`/`other` restore is correctly skipped on the raising path, matching C's longjmp); `SV_AreaTriggerEdicts`' `listcount == listspace` bailout returning without recursing; `SV_WalkMove`'s shared `steptrace` zero-init; and `SV_MoveToGoal`'s preserved `(COM_Rand () & 3) == 1 ||` short-circuit.
  - *Still unread, and not asserted correct:* `World_Glue_CallTouch` and `World_Glue_PushGridEntityLinked` bodies; the `q_max_f`/`q_max_i`/`anglemod`/`angle_vectors`/`box_on_plane_side` helpers; and `QHashMap`'s insert/lookup semantics vs C's `HashMap_*` (a probe-order difference would reorder `push_grid_entries` but not the gather output, which is sorted and deduped).
  - *Gate evidence after the fix (Windows x64):* `physics_matrix.py` C-vs-Rust **84 ok, 0 failed**; `run_corpus.py --compare` **11 ran, 8 skipped, 0 failed**; `save_diff.py` both savegames identical; `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean; `build-rs` relinks. Note the harness could not have caught this finding: no corpus entry assigns a fractional client `movetype`.
- **2026-09-01 M6 landed** (`use_rust_host` Pattern A extended to the whole server stratum: `Quake/sv_main.c` -> `rust/quake-capi/src/sv_main.rs` (1732 lines), `Quake/sv_send.c` -> `sv_send.rs` (4035), `Quake/sv_user.c` -> `sv_user.rs` (969), with `Quake/sv_main_glue.c` (515), `sv_send_glue.c` (336) and `sv_user_glue.c` (136), and `rust/quake-c-sys/src/sv_main.rs`/`sv_send.rs`/`sv_user.rs` (262/203/109) as the C-side extern surface). Landed as three commits: T6.1 the C split (`f8b5ae18`), T6.0 the ctest oracle foundation (`1970506c`), and this one carrying T6.2-T6.6. Executed as an agent wave -- T6.2 delta-writer oracle and T6.5 connection half (Opus high), T6.3 send half (Opus high), T6.4 sv_user (Sonnet high) -- with T6.6 integration and every shared-file write in the main session.
  - **T6.0 was not in the plan, and M6 could not start without it.** `rust/quake-ctest/include/c_ref_prelude.h` declared five hand-cut stand-ins where the real server state belongs (`ctest_server_stub_t sv`, a 4-field `struct client_s`, `ctest_svs_t svs`, `ctest_cl_t cl`, `ctest_cls_stub_t cls`), so adding the three subjects to `C_SOURCES` did not compile (`error C2039: 'limit_entities': is not a member of 'client_s'`). The prelude now includes the real `Quake/server.h` and `Quake/client.h` (`:1278-1279`), and `abi_probe.c`'s `#define`/`#undef` rename dodge -- which existed only because the prelude occupied those names -- was deleted, so the probe finally measures the same declarations under the same names every other oracle TU sees. New `rust/quake-ctest/tests/sv_stratum_linkproof.rs` calls one real entry point per new oracle TU (`SV_ModelIndex`, `SV_CreateBaseline`, `SV_ClientThink`): MSVC pulls only *referenced* archive members, so a green `cargo build --tests` proves nothing by itself, and this test is what proves all three link with every transitive symbol resolved. T6.0 also retired a fixture comment that claimed `SV_StartSound` raises on an unprecached sample -- `sv_main.c:307-311` prints `"not precacheed"` and returns; its only raise arms are `volume < 0` (:284), `attenuation` out of range (:292) and `channel < 0` (:295), and all three `sv_phys.c` call sites pass literals -- so the test now asserts the real observable (the console line) instead of a stub artifact.
  - *Architecture as landed:* ADR-009 topology unchanged from M3/M4 -- glue C owns the reraising wrappers over `quake_rs_*` status cores, Rust calls cores only, `Host_Reraise` appears nowhere under `rust/`. `sv_send.c` has exactly **one** raise-class site (`Sys_Error` at `:1096`, the FatPVS realloc), and `Sys_Error` aborts rather than longjmps, so it is called directly per the `world.rs:32`/`sv_phys.rs:33` precedent; `sv_user.c` has **zero** direct sites and needs two `Host_Guard` trampolines for its transitive surface; `sv_main.c` has six. **`SV_ConnectClient` is statusized on exactly `Host_Guard`'s 0/1/2 set, propagated verbatim with no remapping** -- the ADR-009 unlock M9's NET funnel flips depend on -- fed by three raise sources (`EDICT_NUM`->`Host_Error`, `PR_ExecuteProgram(SetNewParms)`, and the tail call into `quake_rs_sv_send_serverinfo`), with `Host_Reraise` called from `sv_main_glue.c:495` only. `SV_FatPVS` and `MSG_WriteStaticOrBaseLine` keep their plain names and exact signatures because the renderer (`r_world.c:962`) and `cl_demo.c:395,407` call them in every configuration. 43 `// COMPAT:` markers across the three modules (10 + 21 + 12), seven of them in `sv_main.rs` for preserved upstream bugs each checked against the C: the `n = 3` `q_strncasecmp` on a one-char needle (`sv_main.c:75`); an `int` count compared against `255.0f` (`:253`); the `"not precacheed"` misspelling (`:309`, spelled correctly at `:399`); `attenuation == 4` reaching `256` and truncating in a byte (`:349`); three sites operating on `host_client` rather than the `client` argument (`:534-540`, `:591`, `:618`/`:621`); a 64-float read from `&pr_global_struct->parm1` where `globalvars_t` declares only `parm1..parm16` (`:750`); and a `Sys_Error` naming `"Host_CheckForNewClients"` from inside `SV_CheckForNewClients` (`:784`). `sv_user.rs` routes all seven trig/`sqrt` sites through `quake_c_sys::libm` per ADR-010, never `f32::` methods. ADR-005 audit: `sv_main.c` has zero `%g`/`%e` and one `%f` (`Host_Error ("SV_StartSound: attenuation = %f")`, `:293`) emitted from C glue, so the Rust formatter is never reached.
  - **ADR-007 `sv`/`svs` row closed.** `rust/quake-capi/src/sv_main.rs` now defines `#[no_mangle] pub static mut sv: Server` / `svs: ServerStatic` over the ADR-011 mirrors, and `server.h`'s `extern server_t sv;` is the C view of the same storage. This is the migration's **first C-visible Rust storage whose type has no C spelling**, and it broke `quake_rs.h`: cbindgen emitted `extern Server sv;`, failing every C translation unit with `error C2061: syntax error: identifier 'sv'`, and even spelled correctly it would have redeclared `server.h`. Fixed with a two-name `[export].exclude` block in `rust/quake-capi/cbindgen.toml` -- trimmed from six after empirically confirming the four type names were never emitted. The stale `rust/quake-c-sys/src/sv_phys.rs:117-121` comment claiming `server_t` "has no ADR-011 mirror in Phase 7" was corrected in the same pass.
  - *Integration defect the tests could not see.* `rust/quake-ctest/stubs/sv_main_ref.c:67-68` still **defined** plain `server_t sv; server_static_t svs;` after the storage moved into Rust -- two definitions of one symbol, with the winner decided by how rustc happened to split codegen units. The suite stayed green either way, so this was found by reading the file's own duplicate-symbol warning, not by a failing test; it is the same resolution-order hazard that let `precache` reach CI and break only the Windows job. Both are now `extern` declarations, which makes the Rust statics the single definition and makes the ctest link itself the proof.
  - *Two vacuous gates the port agents found by mutation, not by failure.* (1) `c_ref_prelude.h:1143-1146` renames `SV_ClearDatagram`, `SV_ModelIndex` and `SV_ModelForIndex`, so without `#undef`s the *"Rust" branch* of those three drivers called `c_ref_*` -- three differentials that passed while comparing a function with itself. This is the inverse of the M5 `#undef` hazard and the worse direction: M5's form makes a test wrong, this form makes it empty. Fixed at `sv_main_ref.c:113-118`; a prelude grep confirmed the hole is confined to those three (none of the 20 sv_send exports and only `SV_ReadClientMessage` of the 13 sv_user exports are renamed, and that one is a documented non-driven function). (2) `ctest_phys_reset` never called `ctest_phys_set_cvars`, so every sv_phys cvar sat at `.value == 0`; with `sv_analyticphysics_frame` defaulting true, `friction == 0` makes `r0 == 1.0` exactly and `log(stopspeed/ns)/log(r0)` sends every velocity component to NaN **on both sides**, which a bit-exact `assert_eq!` accepts. Everything downstream of `SV_UserFriction` was untestable -- deleting `SV_Accelerate`'s clamp was confirmed *unkillable* before the fix. Fixed by setting the real default block in `ctest_svuser_reset`; of the seven `ctest_phys_reset` callers only `progs_builtins_sv_msg_differential.rs` still omits the cvars, and it has zero references to any physics path.
  - *Two harness gates repaired so they could run at all.* `scripts/harness/check_ctest_symbols.sh:44` now runs `(cd rust && cargo build -p quake-ctest)` instead of `--manifest-path` from the repo root -- rustup's upward search never reached `rust/rust-toolchain.toml` and fell back to a stale default, dying on `expected a boolean or an integer for key profile.release.debug`; the gate previously only worked if the caller knew to export `RUSTUP_TOOLCHAIN`. `scripts/harness/check_headers.sh` now honours `$CC` (`CC="${CC:-cc}"`, matching `check_capi_signatures.sh`), because this dev box has clang but no `cc` and the gate was being reported "not run" for a missing alias.
  - *A gate that could only compare sizes (`stubs.c` recorder).* `NET_SendMessage`/`NET_SendUnreliableMessage` discarded their buffers, so section I of `sv_send_differential.rs` compared only the byte *count* a side claimed to send -- **a port emitting the right number of wrong bytes passed all three tests**. Both now append to a recorder retaining call count, per-call `(len, reliable)` and concatenated payload, with an explicit `truncated` flag (1 MiB / 1024-call caps) so two sides that both overflow cannot compare equal on a prefix, and `ctest_net_send_call_len` returning -1 past the retained range so no test can read truncation as "no data". Deliberately not `c_ref_*`-prefixed: `stubs.c` is not in `build.rs`'s `C_SOURCES`, so `check_ctest_symbols.sh` does not police it, and both sides write through the same recorder by design.
  - *New ADR-010 finding, pinned by T6.2:* `ENTALPHA_ENCODE` runs in `float`, and the ULP below 0.25 rounds back onto 64.5 and encodes as 65 -- a port doing it in `f64` diverges. T6.2's fixtures are hand-constructed rather than recorded, declared explicitly with its reasoning: bit-isolated `UF_*` masks and ULP boundaries are unreachable in a dense recorded id1 stream, and the recorder does not capture BSP node/leaf topology, so the worldmodel would be synthetic either way.
  - **Systemic risk raised for STOP B, not fixed here.** Both vacuous gates above, and M5's `/OPT:ICF` probe, share one class: **a bit-exact differential passes whenever both sides degenerate identically** -- zeroed cvars, NaN propagation, an un-initialised fixture, a stub returning a constant. None of the three was found by a failing test; all were found by mutation. The durable lesson for the rest of the phase is that mutation testing is the only thing that has caught this class, and the harness would benefit from a cheap NaN/sentinel assertion on fixture outputs.
  - *Coverage gaps carried to STOP B (recorded, not closed):* (1) **`SV_SpawnServer` is not drivable in ctest** -- `Mod_ForName`, `PR_LoadProgs` and `ED_LoadFromFile` are `Sys_Error` stubs, `Host_ClearMemory` is a no-op, `SV_Precache_Model` is a test double. It is covered *through* the corpus and interop harness (`map-e1m2`, `e3m6-trains`, `save-e2m1` and every interop cell spawn real servers) but has no differential of its own, and M6 was deliberately not signed off on ctest evidence alone. (2) `Sys_Error` longjmps in ctest but terminates in the shipping engine, so `sv_main.c:213`, `:784`, `:835` are uncovered by construction; `NET_CheckNewConnections` returns NULL, so `:478` and `:505` are unreachable there too. (3) The registration-order gate observes only the relative order of the three `CVAR_SERVERINFO` cvars through `svs.serverinfo` -- `Cvar_FindVarAfter` walks alphabetically and cannot see order at all -- so reordering two non-SERVERINFO cvars would not be caught. (4) `client->limit_entities` is clamped to `qcvm->max_edicts` (16 in the fixture) by `sv_main.c:519`, so the per-protocol 600/32000 limits are asserted post-clamp. (5) `SvPhys_Glue_StartSound`'s trampoline has no raise-propagation test after T6.0's fixture correction; it is unreachable-by-construction from `SV_CheckWaterTransition` and still covers `NUM_FOR_EDICT`'s raise on a corrupt edict.
  - *Deliberate non-change:* `check_capi_signatures.sh`'s no-argument path (regenerating the header itself) still fails on this box with the `profile.release.debug` toolchain error. CI always passes a pre-generated header (`build-linux.yml:149-156`), so that path is never exercised there; the gate was run by passing `build-rs/quake_rs.h` explicitly. `Quake/common.make` needed no M6 change -- T6.1 already added `sv_send.o` in the split commit, and the file has zero `glue` and zero `USE_RUST` references.
  - *Gate evidence (all measured, Windows x64, this checkout):*
    - All four meson cells build and link: `build-c`, `build-rs` (**first link of `sv_main_glue.c`/`sv_send_glue.c`/`sv_user_glue.c`**), `build-rs-chost`, `build-rs-cprogs`.
    - `cargo test --workspace`: **907 passed, 0 failed**. New/extended ctest suites: `sv_main_differential` **55**, `sv_send_differential` **74** (14/14 mutations killed, none survived), `sv_user_differential` **21** (4 mutations killed with real non-NaN physics in play), `sv_stratum_linkproof` **1**.
    - `netreplay_diff.py` C-self and C-vs-Rust: **1191 records delivered**, state-hash chains **identical over 1402 frames**, recorded demos identical at **39370 bytes**. This is M6's load-bearing gate -- it drives a real recorded session through `SV_ReadClientMessage`, `SV_ClientThink` and the whole FTE delta writer on the Rust build and demands byte identity with C.
    - `record_diff.py` C-self and C-vs-Rust: **identical, 20239 bytes**, `e1m1`, 600 frames.
    - `interop_matrix.py` full **6x4 IPv4 matrix: PASS, 24 cells, 0 failures**.
    - `interop_matrix.py --soak --frames 20000 --combos R/R Rsv/Ccl --protocols FTE+999`: **PASS** on both cells, **313 checkpoints** each, traffic in band, `hash forks @128 (diagnostic)` -- identical to the M4/M5 record and non-failing per the M1 criterion.
    - `physics_matrix.py` C-vs-Rust, 12 cells x 7 entries: **84 ok, 0 skipped, 0 failed**.
    - `run_corpus.py --compare build-c build-rs`: **11 identical, 8 skipped, 0 failed**; the same against `build-rs-chost` (oracle leg): **11 identical, 8 skipped, 0 failed**.
    - `save_diff.py` C-vs-Rust: both savegames **identical** (95091 / 95075 bytes).
    - `check_ctest_symbols.sh` PASS; `check_headers.sh` **12/12 compile + 12/12 bindgen** (`CC=clang`); `check_capi_signatures.sh` OK on `build-rs`, `build-rs-chost` and `build-rs-cprogs`; `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` both exit 0.
    - Not run (out of scope for M6): `builtin_diff.py`/`trace_diff.py` (no progs or builtin-table change), the 100k x 8 full soak (M9/M11), Miri, `capture_diff.py` (client stratum, M7), the `--ipv6` interop leg (local-only, and this machine's IPv6 loopback fails for **C/C** too, so it attests nothing either way), and every Linux/macOS/MinGW/arm64 leg.
- **Open, carried into M7:** M3, M4, M5 and now M6 have still not been through CI -- the newest run on `feature/rust-conversion-phase-7-a3ee42` remains the M2 commit, so the Linux/macOS/MinGW/arm64 meson legs, `cargo deny`, and the CI differential-harness jobs are unexercised for four milestones. M5 found a real Linux-only duplicate-symbol defect that MSVC merges silently, which is precisely the class this gap hides. Pushing is an outward-facing action and is awaiting the user's decision.

**2026-09-01 M6 evidence correction -- the `check_ctest_symbols.sh` PASS above was vacuous.**

- The M6 entry lists `check_ctest_symbols.sh PASS` as gate evidence. It was not evidence. The script resolved its symbol reader as a bare `nm`, and read each object with `leaked=$(nm -g "$obj" 2>/dev/null | ... || true)`. `nm` is not installed on this machine (LLVM ships `llvm-nm`; there is no binutils), so every invocation was a "command not found" whose message went to `/dev/null` and whose non-zero status was swallowed by `|| true`. `leaked` came back empty for every source, the script printed `ok:` for each and `OK: N oracle sources export only c_ref_* and allowlisted symbols` at the end, having inspected **zero bytes**. Found by T7.0 while it was re-implementing the check by hand; verified independently in the main session (`command -v nm` finds nothing, and the pipeline is unguarded).
- This does not invalidate M6's other gates -- `record_diff.py`, `netreplay_diff.py`, the interop matrix, the physics matrix, the soak smoke cell, `save_diff.py`, `check_headers.sh`, `check_capi_signatures.sh`, the ctest suite, fmt and clippy were all really run and really passed. What it invalidates is the specific claim that M6's oracle sources were proven not to leak un-renamed globals into the ctest link.
- Repaired in the T7.0 commit: the script now resolves `$NM` up front (explicit `$NM`, then `nm`, then `llvm-nm`) and **exits 1 with a diagnostic if none exists**, and each per-object read is `if ! syms=$("$NM" -g "$obj" 2>&1); then` -- a reader that fails is a FAIL, not a silent ok. `llvm-nm` on MSVC objects reports COMDAT float/vector constants (`__real@...`, `__xmm@...`), the C++-mangled `__local_stdio_*_options` guard objects and the `<stdio.h>` inline wrappers (`fprintf`, `sscanf`, `vfprintf_l`, ...) as *defined* globals; those are toolchain artifacts, not engine symbols, and the filter list (`NOISE_RE`/`NOISE_EXACT`) was built by enumerating the actual symbol set across all 43 oracle objects rather than guessed.
- Red-tested three ways before being trusted: with no reader on `PATH` -> exit 1; with `NM=/definitely/not/a/tool` -> exit 1; with `#define v_blend c_ref_v_blend` disabled in the prelude -> `FAIL: Quake/view.c exports un-renamed global(s): v_blend`. The prelude was then restored byte-exactly. With the repair in place the gate genuinely measures, and reports `OK: 43 oracle sources ...` -- and it immediately earned its keep: it is what caught `cshift_water`/`cshift_slime`/`cshift_lava` (`Quake/view.c:258-260`, `const cshift_t`, declared in no header) as real un-renamed leaks in T7.0.
- **This is the fourth instance of the degenerate-gate hazard** (after the M5 `/OPT:ICF` probe, the T6.5 prelude-rename vacuity and the T6.4 zeroed-cvar NaN), and the first where the degeneracy was in a *harness script* rather than a fixture. All four were found by mutation testing or by reading, never by a failing test. Carried to STOP B as a systemic finding, with the added rule: **a gate that shells out to an external tool must fail when the tool is missing.**

**2026-09-01 M7 T7.2 -- the client stratum's leaves: `view.c`, `chase.c`, `cl_input.c`, `cl_tent.c`.**

- Four Pattern A whole-file swaps under the existing `use_rust_host` switch, developed as two parallel Opus-high agents with disjoint file ownership (A: `view.c`+`chase.c`, B: `cl_input.c`+`cl_tent.c`). To remove any shared-file race the main session pre-created every shared slot before launching them -- the eight placeholder `.rs` modules, the four `stubs/*_ref.c` files, both `lib.rs` module declarations and the `build.rs` `C_SOURCES` entries -- and barred both agents from `meson.build`, `meson_options.txt`, `Quake/common.make`, `build.rs`, both `lib.rs` files, `stubs/stubs.c` and `include/c_ref_prelude.h`, with instructions to *report* needed edits instead of making them. Both complied; the only file either touched outside its allowlist was `stubs/pf_cl_ref.c`, which agent B flagged and whose change is purely additive (three new `ctest_cl_*` accessors).
- **Model deviation from the plan, recorded.** The plan assigns T7.2 to Sonnet high (view+chase) and Sonnet medium (cl_input, cl_tent). Both were raised to Opus high: `view.c` carries ~30 cvars plus the float-order-sensitive bob/roll/kick/cshift math, and `cl_tent.c` carries the `TE_*` dispatch and the beam/temp-entity tables. The standing instruction to pick the model that best suits the work outranks the plan's cost-shaped default. The three subjects were also folded into two agents so no two agents could own the same file.
- **An agent recommendation was checked and rejected.** Agent B asked for a `USE_RUST_HOST` conditional around `cl_input.o`/`cl_tent.o` in `Quake/common.make`. `Quake/common.make` contains no `USE_RUST`, no `RUST` and no `_glue.o` anywhere: the MinGW/Makefile build is C-only and changes only when a C file is *split* (T6.1 added `sv_send.o`), never for a Pattern A flip. Following the recommendation would have broken the MinGW CI leg.
- ADR-009: `chase.c` has no raise site of its own; `view.c:922` and `cl_tent.c`'s `Sys_Error` abort rather than longjmp (the `world.rs:32` / `sv_phys.rs:33` / M6 `sv_send.c:1096` precedent) and are called directly. Every other reachable raiser -- `Cvar_RegisterVariable` x43, `Cmd_AddCommand`, `CL_RelinkEntities`, `R_RenderView`, `Mod_ForName`, the particle system -- goes through a `Host_Guard` trampoline in the owning `_glue.c`. `Host_Reraise` appears in no `.rs` file.
- **Registration-order finding, retiring a risk carried since M2.** Cvar registration order is *not* observable: `Quake/cvar.c:642` `Cvar_RegisterVariable` inserts **alphabetically** (`cvar.c:663-679`), so `cvarlist`, the registry walk and `config.cfg` are order-independent for differently-named cvars. This also explains why T6.5's registration-order gate could only ever observe the three `CVAR_SERVERINFO` cvars.
- **The meson flip earned its keep immediately.** Both agents listed "the `_glue.c` files have never been compiled under `USE_RUST_HOST`" as residual risk, and the first real `ninja -C build-rs` found three defects no ctest mirror could see: (1) cbindgen emitted `CL_BaseMove`/`CL_FinishMove`/`quake_rs_cl_send_move`/`CL_NewTempEntity`/`KeyDown`/`KeyUp`/`CL_KeyState` using the ADR-011 mirror names `UserCmd`/`EntityOpaque`/`kbutton_t`, which are syntax errors in C; (2) it typed `CL_UpdateBeam`'s first parameter `void *` against `client.h:393`'s `struct qmodel_s *`; (3) `view_glue.c` referenced `V_cshift_f`/`V_BonusFlash_f` above their definitions, and no header declares them. Fixed by the established M3/M4/M6 pattern -- a hand-written `#ifdef _CLIENT_H_` block in `cbindgen.toml`'s `after_includes` plus the matching `[export].exclude` entries -- and a forward declaration in `view_glue.c`. Note the recurring shape: `V_cshift_f`, `V_BonusFlash_f`, `KeyDown`, `KeyUp`, `CL_KeyState` and `CL_NewTempEntity` are all *external-linkage symbols declared in no header*, the same class as T7.0's `cshift_water`/`cshift_slime`/`cshift_lava` leak.
- **`stubs.c`'s hand-transcribed `V_CalcRoll` was retired.** T6.0 had to transcribe `view.c:87-108` by hand so the M6 `sv_user` differential could reach `SV_ClientThink`; with `view.c` ported it now forwards to `quake_rs_v_calc_roll`, which is exactly what the shipped `-Duse_rust_host` build does, and removes a copy that could silently drift from the port. Red-tested: adding `+ 0.125` to the core fails 14 tests, so the forwarding is genuinely observable and not a sixth degenerate fixture.
- Mutation testing by the port agents: 31 mutations on view/chase (28 killed first attempt; two survivors exposed genuinely weak cases and were killed after the case table was rewritten; the third, cvar registration order, is a true equivalent mutant per the finding above) and a comparable pass on cl_input/cl_tent. The `V_CalcBob` reassociation mutation (`bob*0.3 + bob*0.7*sin` -> `bob*(0.3+0.7*sin)`) killed 9 cases -- exactly the ADR-010 class the gate exists for.
- Evidence, all re-measured by the main session after both agents landed and after the meson flip: `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets -- -D warnings` exit 0; `cargo test --workspace` **963 passed / 0 failed** (908 baseline + 14 chase/view + 41 cl_input/cl_tent, an exact match); `check_ctest_symbols.sh` -> `OK: 43 oracle sources ...`; `check_headers.sh` -> 12 ok + 12 bindgen ok; `check_capi_signatures.sh` -> `OK: quake_rs.h declarations are compatible with the engine headers`; **all six meson configs build** (`build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace`); `run_corpus.py --compare` **11 ran / 0 failed** both `build-c` vs `build-rs` and `build-rs-chost` vs `build-rs`; `save_diff.py` C-vs-Rust both saves byte-identical; `record_diff.py` identical (20239 bytes); `physics_matrix.py` **84 ok / 0 failed** (12 cells x 7 entries) C-vs-Rust; `interop_matrix.py` full 6x4 **PASS**.
- Two harness gates are environment-sensitive on this Windows box and must be run from an MSVC environment with the pinned toolchain (`vcvars64` for `INCLUDE`, `RUSTUP_TOOLCHAIN=1.97.1`, `CC=clang`): in a bare Git Bash shell `check_headers.sh` fails with `cc: command not found` (then, with `CC=clang`, with a missing libc sysroot) and `check_capi_signatures.sh` fails with `expected a boolean or an integer for key profile.release.debug` because the PATH cargo is 1.68.2. Neither is a code defect, but both are silent-looking failures worth knowing about; recorded here so a future run does not mistake them for regressions.
- Carry to STOP B (new): `Cmd_AddCommand` has no plain twin in the ctest link (single shared command table), so the three view commands are checked by handler-pointer identity only, weaker than the cvar side; `r_refdef` is a single shared object, so the view fixtures work by set -> run one side -> snapshot -> set again -> run the other, and a future edit that ran both sides before snapshotting would silently make them vacuous (**fifth degenerate-fixture exposure**); the `TE_LIGHTNING2` mutant survives because the beam model paths are unobservable (the `Mod_ForName` abort message carries no path); `init_tents_status_parity` is acknowledged weak; and `CL_AngleLocked` returns `qboolean`, which the Rust side spells `bool` -- a 1-byte-vs-4-byte ABI trap if a later TU ever redeclares it as `int`.
- **Soak-smoke addendum (completed after the T7.2 commit landed).** `interop_matrix.py --soak --frames 20000` was re-run over the **full 8-cell matrix** rather than the usual two cells: 4 build combos (`C/C`, `Csv/Rcl`, `Rsv/Ccl`, `R/R`) x 2 protocols (`Base-666`, `FTE+999`). **Result: PASS, 8/8 cells**, 313 checkpoints reached per cell against a 20001-frame same-seed C/C reference stream (rel 4/5, unrel 19908/19988), traffic in band in every cell. Six cells were hash-identical; `Base-666 Rsv/Ccl` and `Base-666 R/R` report `hash forks @128 (diagnostic)`, which matches the M4/M5/M6/T7.2 record exactly and is non-failing per the M1 criterion -- live sessions are timing-nondeterministic, so the pass condition is checkpoint reachability plus traffic band, never byte identity. This is the widest soak coverage taken in the phase so far; the 100k x 8 full soaks remain scheduled for M9 and M11.

**2026-09-01 M7 T7.3 -- `cl_parse.c`: the server-message parser, and the phase's largest ADR-009 surface.**

- Pattern A whole-file swap on the existing `use_rust_host` switch. `Quake/cl_parse.c` (2224 lines) -> `rust/quake-capi/src/cl_parse.rs` (3098) + `Quake/cl_parse_glue.c` (643) + `rust/quake-c-sys/src/cl_parse.rs` (260), with `rust/quake-ctest/stubs/cl_parse_ref.c` (1477) and `rust/quake-ctest/tests/cl_parse_differential.rs` (2450, 40 tests) as the oracle. `cl`/`cls` stay C-owned here; that ADR-007 row closes in T7.4 with `cl_main.c`.
- **Agent recovery: the glue became the contract.** The first T7.3 agent died mid-task having written only `cl_parse_glue.c`, and was unresumable (absent from `ListAgents`). Rather than discard the work the main session **audited the orphaned glue independently and then froze it as the interface contract** for a fresh Opus agent, which wrote the Rust side against it and made zero glue edits. The audit diffed all 38 `Host_Error`/`Host_EndGame` sites in `cl_parse.c` against `ClParse_Raise`'s switch: every format string, argument order and argument type matches, including the two sites (`:979`, `:1868`) that correctly *share* `CLPARSE_ERR_VERSION` because their messages are byte-identical.
- **Four raise sites are dead, and the proof is recorded so a later reader does not treat them as a gap.** `cl_parse.c:886/:890/:894` sit inside the `#if 0` at `:852`/`#endif` at `:915` (`CL_KeepaliveMessage`, which has no callers anywhere in `Quake/`). The `UF_UNUSED2` `Host_EndGame` at `:439` is unreachable because `Quake/protocol.h:33` defines `LERP_BANDAID` unconditionally, so the live branch is always `news->lerp = MSG_ReadShort()` at `:437`. That leaves **34 live sites**, all mirrored as `CLPARSE_*` status codes and re-issued verbatim from `ClParse_Raise` -- the `PRBI_Raise` pattern from `pr_cmds_glue.c:309`. The Rust core returns status and never jumps.
- **A UB hazard that no test could have caught, found by reading and sent to the agent mid-flight.** `ClParse_Raise` dereferences its `const char *s` payload *after* the Rust core has returned, so `s` must never point into a Rust stack frame. Confirmed there are exactly two producers, both with static storage: `MODEL_PRECACHE` (`cl_parse.rs:438`, a module-level `static mut`, read at `:1656`) and the glue's own `svc_strings` (read at `:2986`). No third producer exists. This is a lifetime invariant the type system cannot express across FFI and the differential suite cannot observe -- recorded here as a standing rule for every future status-code glue that carries a string payload.
- Glue ownership: `cl_parse_glue.c` owns `svc_strings[128]` (transcribed from `cl_parse.c:30-88`), the 34 `CLPARSE_*` codes, `ClParse_Raise`, 21 `ClParse_Glue_*` `Host_Guard` trampolines and 5 entry-point forwarders. The five entry points (`CL_EntityNum`, `CL_ParseLocalSound`, `CL_NewTranslation`, `CL_RegisterParticles`, `CL_ParseServerMessage`) keep their exact C signatures.
- ADR-005: the parser's 25 format strings use only `%3i %4i %i %d %s %c %x` (the glue adds `%u` and `%#x`). No `%g`/`%e`/`%f`, so the Rust formatter's panic path is unreachable from this file. ADR-010: exactly one libm site, `libm::fabs` at `cl_parse.rs:1032` (the `CL_EntitiesDeltaed` 100-unit lerp-reset check), plus 14 further `// COMPAT: ADR-010` order-preservation markers.
- **The meson flip earned its keep for the second milestone running.** The first `ninja -C build-rs` failed all three Rust configs with `../Quake/cl_parse_glue.c(337): error C2065: 'CL_GenerateRandomParticlePrecache': undeclared identifier`. `CL_GenerateRandomParticlePrecache` (`cl_main.c:939`) is external-linkage but declared in no header; `cl_parse.c:1657` re-declared it locally at the point of use, and the glue frame has to do the same. Fixed with a forward declaration in the glue. **The ctest mirror could not have caught it**: `cl_parse_ref.c:257` already carried the prototype, so the oracle link was clean while the real build was broken. This is the argument for flipping meson as part of the task rather than deferring it to the milestone boundary.
- **This is the seventh instance of the header-less-external defect class** (after T7.0's `cshift_water`/`cshift_slime`/`cshift_lava` and T7.2's `V_cshift_f`/`V_BonusFlash_f`/`KeyDown`/`KeyUp`/`CL_KeyState`/`CL_NewTempEntity`). `CL_EntityNum`, `CL_ParseLocalSound` and `svc_strings` are the same shape and the glue already handled them; `r_part_fte.c:156` re-declares `CL_EntityNum` locally and calls it at `:4458`, `:5961` and `:7251`, so the plain name must stay a real C function. **Standing rule adopted: every Pattern A port must enumerate the file's non-static file-scope symbols and check each against `Quake/*.h` before the flip.**
- No cbindgen `after_includes` block was needed. The five cores take only `c_int`, `*mut c_int`, `*mut *mut c_void` and `*mut *const c_char` and return a private `type Raise = c_int`, the same shape as T7.2's already-working `quake_rs_cl_parse_tent`.
- **An agent recommendation to edit `Quake/common.make:237` was checked and rejected -- the second time in two milestones.** `grep -c "USE_RUST\|RUST\|_glue" Quake/common.make` is **0**: the MinGW/Makefile build is C-only and changes only when a C file is *split*, never for a Pattern A flip. Following it would have broken the MinGW CI leg.
- **Two more degenerate fixtures found and closed, bringing the systemic count to seven.** `rust/quake-ctest/stubs/stubs.c`'s `R_FreeEntityBLAS` was an *abort* stub, which made every model change in `CL_ParseUpdate`/`CL_ParseBaseline` unreachable; and `PScript_UpdateModelEffects` was `{ (void)mod; }`, which made `CL_RegisterParticles`'s entire second loop unobservable. Both are now counting stubs (`ctest_blas_free_reset/count`, `ctest_pscript_model_effects_reset/count`, `ctest_pscript_last_model_name`). As with the previous five, **both were found by reading and mutation testing, never by a failing test.**
- Mutation testing by the port agent: 9 mutations, 3 of which survived the green 40-test suite before the case table was extended to kill them; the fixture was then restored and verified byte-identical by sha256.
- **Four environment traps hit and diagnosed during verification; none was a code defect.** (1) `cargo fmt --check` and `cargo clippy` **fail when `INCLUDE` is set** -- they must be run from a plain Bash shell with `env -u INCLUDE -u LIB -u LIBPATH`, never from a vcvars PowerShell. (2) cmd's `set RUSTUP_TOOLCHAIN=1.97.1 && ...` appends a trailing space, producing `override toolchain '1.97.1 ' is not installed`; export inside bash instead. (3) **NEW:** `check_ctest_symbols.sh` fails under `bash -lc` from cmd with `/usr/bin/link: missing operand` because Git Bash puts `C:\Program Files\Git\usr\bin` ahead of MSVC on `PATH` and rustc picks up **coreutils `link`** instead of MSVC's `link.exe`; it must be run from the plain Bash tool. `CC=clang` additionally breaks it (`pf_fx_ref.c` implicit-declaration errors). (4) Conversely `check_headers.sh` and `check_capi_signatures.sh` *do* need vcvars plus `RUSTUP_TOOLCHAIN=1.97.1` and `CC=clang`.
- **A harness gate reported FAIL for a reason that was not a regression, and the control run is what proved it.** `capture_diff.py cap_c.cap cap_rs.cap` failed at recv byte 4708. A C-vs-C control failed at the *same* offset with a third value, and the diverging bytes decode as `svc_time` (`protocol.h:252`) followed by a float -- the documented run-to-run timing noise. The gate is designed to be called with `--window-from` (a second run of the reference build), exactly as `.github/workflows/build-linux.yml:246` does; the bare invocation was the error. Recalibrated it **PASSES**: recv window 4708 bytes (well above the `--min-window` 1024 degeneracy floor), send 98 bytes, both directions byte-identical, per-kind counts within tolerance.
- Evidence, all measured by the main session after the flip: `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets -- -D warnings` exit 0; `cargo test --workspace` **1003 passed / 0 failed** (963 baseline + 40 new, an exact match); `check_ctest_symbols.sh` -> `OK: 43 oracle sources ...`; `check_headers.sh` -> 24 ok + 12 bindgen ok; `check_capi_signatures.sh` -> `OK: quake_rs.h declarations are compatible with the engine headers`; **all six meson configs build**; `run_corpus.py --compare` **11 ran / 8 skipped / 0 failed** both `build-c` vs `build-rs` and `build-rs-chost` vs `build-rs`; `save_diff.py` both saves byte-identical (95091 and 95075 bytes); `record_diff.py` identical (20239 bytes, e1m1, 600 frames); `physics_matrix.py` **84 ok / 0 failed**; `interop_matrix.py` full 6x4 **PASS** (24 runs, exit 0 -- the per-cell progress lines were lost to a `tail` pipe, but the script's `ok` accumulator spans every cell and the visible tail is the last three *planned* cells, so the verdict covers the whole matrix); `capture_diff.py --window-from` **PASS**.
- Carry to STOP B (new, and the largest single gap in the phase so far): **`CL_ParseServerInfo` is blind past `CL_ClearState`** because `PR_ClearProgs` is an abort stub in the ctest link -- 6 status codes and roughly 180 lines are compiled and read-verified but never executed differentially. Also: `CLPARSE_ERR_TOOMANYSTATICS` needs allocator-failure injection to reach; guard status 1 is never exercised (all 9 seams only ever produce `a == 2`, which is a value rather than a branch); the CSQC, particle and sky/fog arms are stop-point-only; and `cl_parse.c:1566` has a **pre-existing unguarded `cl.model_precache[modelindex]` out-of-bounds read**, transliterated as-is per the bug-for-bug rule, with the suite steering around it via `BASELINE_SPAWNERS`.
- **Independent structural validation by the main session, beyond the agent's own testing.** (a) svc dispatch completeness: all 54 `case` labels in `CL_ParseServerMessage`, including the 13 `svcfte_*`/`svcdp_*` extension arms, have a matching arm in the Rust `match`; no Rust-only arm exists. (b) Status contract: 34 `CLPARSE_*` `#define`s in the glue against 34 `const`s in the Rust, **identical values, zero orphans in either direction, and all 34 actually returned from some Rust path** -- so no code is declared-but-unreachable and none is consumed-but-never-produced. (c) ADR-010: zero uses of any `f32`/`f64` math method anywhere in the file, one `libm` site, 15 `// COMPAT: ADR-010` markers; the libm site was read against `cl_parse.c:500` and the float-subtraction-then-promote-to-double order matches exactly. (d) ADR-009: only 13 distinct direct `c::` calls (`Con_*` printers, `Sys_Error`, `Mem_*`, `COM_Rand`, `Cmd_Argv`, `COM_StripExtension`, `COM_SkipPath`) and **none of them is a `Host_Error`/`Host_EndGame` raiser**; every raiser goes through one of the 21 `ClParse_Glue_*` trampolines. (e) Test-suite non-vacuity: 40 tests, 202 assertions, **zero tests without an assertion**, and the `side` dispatch was read to confirm the plain-name arm calls the real `quake_rs_*` core rather than the renamed oracle. (f) The generated `build-rs/quake_rs.h` declares all five cores with the expected types; its 12 repeated `typedef int Raise;` are legal under the project's `c_std=gnu11,c11` and are the established pattern, not new.
- Carry to STOP B (also new): `cl_parse_ref.c:806-830` **hand-mirrors** `cl_parse_glue.c:580-637`'s five entry-point forwarders rather than compiling the real glue, so the oracle's copy can silently drift from the shipped one. This is the same class T7.2 *retired* for `stubs.c`'s transcribed `V_CalcRoll`; it is accepted here because the glue is not in the ctest link, but it should be revisited when the ctest link gains the ability to compile `_glue.c` directly.
- Not run at commit time, stated so the ledger is not read as wider than it is: `interop_matrix.py --soak --frames 20000` was **in flight** when this entry was written and is recorded as a separate addendum below, exactly as T7.2's soak was; `builtin_diff.py`/`trace_diff.py` (no progs or builtin-table change in T7.3); the 100k x 8 full soak (M9/M11); Miri; the `--ipv6` interop leg; and **every Linux, macOS, MinGW and arm64 leg** -- CI's newest run on this branch is still the M2 commit, so M3-M7 have never been through a non-Windows compiler.

- **Soak-smoke addendum (completed after the T7.3 commit landed).** `interop_matrix.py --soak --frames 20000` was re-run over the **full 8-cell matrix** -- 4 build combos (`C/C`, `Csv/Rcl`, `Rsv/Ccl`, `R/R`) x 2 protocols (`Base-666`, `FTE+999`). **Result: PASS, 8/8 cells**, exit 0, 313 checkpoints reached per cell, traffic in band in every cell. Reference streams: `Base-666` 20001 frames hashed, rel 3/4, unrel 19911/19988; `FTE+999` 20001 frames hashed, rel 4/5, unrel 19908/19988 (the `FTE+999` reference matches T7.2's figures exactly).
- **This run strengthens the `hash forks @128 (diagnostic)` classification from an assumption into a demonstrated control, which is worth more than the PASS itself.** Since M4 the `@128` fork has been recorded as non-failing on the M1 criterion -- live sessions are timing-nondeterministic, so the pass condition is checkpoint reachability plus traffic band, never byte identity -- but every prior observation of it happened to be in a cell containing Rust, so the classification rested on the criterion rather than on evidence. In this run the fork appears in **both pure `C/C` cells** (`Base-666 C/C` and `FTE+999 C/C`). A `C/C` cell contains no Rust code by construction, so the fork cannot be a port divergence. Hash-identical this run: `Base-666 Csv/Rcl`, `Base-666 R/R`, `FTE+999 R/R`.
- Second corroborating observation: **the fork set is not stable across runs.** T7.2's 8-cell run forked in exactly `Base-666 Rsv/Ccl` and `Base-666 R/R`; this run forks in `Base-666 C/C`, `Base-666 Rsv/Ccl`, `FTE+999 C/C`, `FTE+999 Csv/Rcl` and `FTE+999 Rsv/Ccl`, and `Base-666 R/R` -- which forked in T7.2 -- is hash-identical here. A structural port defect would be reproducible in a fixed set of cells; a timing artifact would not be. Both facts point the same way.
- Scope limits, so the ledger is not read as wider than it is: this is a **20k-frame** smoke soak, not the 100k x 8 full soaks, which remain scheduled for M9 and M11; it ran on Windows/MSVC only; and it attests the T7.3 tree, not T7.4.

**2026-09-01 M7 T7.4 -- `cl_main.c` + `cl_demo.c`: the client entry points, and the ADR-007 `cl`/`cls` row closes.**

- Two Pattern A whole-file swaps on the existing `use_rust_host` switch, completing M7. `Quake/cl_main.c` (1441 lines) -> `rust/quake-capi/src/cl_main.rs` (2266) + `Quake/cl_main_glue.c` (985) + `rust/quake-c-sys/src/cl_main.rs` (317); `Quake/cl_demo.c` (871) -> `rust/quake-capi/src/cl_demo.rs` (1497) + `Quake/cl_demo_glue.c` (407) + `rust/quake-c-sys/src/cl_demo.rs` (140). Oracles: `stubs/cl_main_ref.c` (1994) + `tests/cl_main_differential.rs` (61 tests, 181 assertions) and `stubs/cl_demo_ref.c` (814) + `tests/cl_demo_differential.rs` (27 tests, 92 assertions).
- **ADR-007: the `cl`/`cls` row is closed.** `client_static_t cls` and `client_state_t cl` are now Rust-owned storage at `rust/quake-capi/src/cl_main.rs:61` and `:67`, typed as the ADR-011 mirrors `ClientStatic`/`ClientState`. Two Phase-7 rows remain open: the `net_message`/`msg_readcount`/`msg_badread` trio and the `qcvm`/`pr_global_struct`/`qcvm->edicts` group, both scheduled for M9.
- ADR-009: `cl_main.c` has three live raise sites (`:232` connect-failed, `:983` lost-read, `:1108` lost-send), mirrored as `CLMAIN_RAISE_CONNECT_FAILED` (-101) / `LOST_READ` (-102) / `LOST_SEND` (-103) and re-issued from `ClMain_Raise` (`cl_main_glue.c:814`), with 40 `Host_Guard` trampolines and 26 plain-named re-raising wrappers. `cl_demo.c` has **no** raise site of its own -- its only failure exits are `Sys_Error` at `:175`/`:196`, which terminate rather than jump -- so `cl_demo_glue.c` needs no status-code translation table at all: its 15 trampolines guard callees whose transitive reach was not proven raise-free (`S_StopAllSounds`, `S_LoadSound`, `Fog_GetFogCommand`, `Sky_GetSkyCommand`, `DemoList_Rebuild`, `COM_FOpenFile`), and its 7 plain wrappers pass the `Host_Guard` result straight to `Host_Reraise`. The reasoning is written out at `cl_demo_glue.c:38-51`. No Rust source calls `Host_Reraise` anywhere in the workspace -- the only occurrences of the name in `.rs` files are comments explaining its absence.
- **A cbindgen defect the port agent explicitly reported as absent.** The agent's handoff stated no `cbindgen.toml` change was needed. The first `ninja` disproved it: once `cl`/`cls` became `#[no_mangle] pub static mut` with ADR-011 mirror types, cbindgen emitted `extern ClientStatic cls;` / `extern ClientState cl;` into `quake_rs.h`, and neither mirror has a C spelling, so the configs that include `client.h` failed to compile. Fixed by adding `"cl", "cls"` to `[export].exclude`, exactly the `"sv", "svs"` block T6.6 added for the same reason -- C already gets both declarations from `client.h:154`/`:332`. **The rule generalises: every ADR-007 row closure needs a `cbindgen.toml` exclusion, because the symbol that changes owner is by definition already declared in an engine header.**
- **Header-less externals, instances eight and nine -- and the first ones the standing rule caught *before* the flip.** T7.3 adopted the rule that every Pattern A port must enumerate the file's non-static file-scope symbols and check each against `Quake/*.h` first. Applied here it found five cross-TU symbols declared in no header (`cl_maxpitch`, `cl_minpitch`, `cl_confirmquit`, `needs_relink`, `CL_GenerateRandomParticlePrecache`); all five stay real C objects in the glue. Three of them (`cl_maxpitch`, `cl_minpitch`, `needs_relink`) are *already* named in `extern "C"` blocks by ported modules (`cl_input.rs:103-104`, `view.rs:91`), so converting them to Rust statics would have produced a duplicate definition MSVC can merge silently -- precisely the class M5 found to be a hard error only on Linux. Instance nine was found by the compiler rather than the rule: `CL_SendInitialUserinfo` and `CL_Viewpos_Completion_f` are used above their definitions *within* `cl_main_glue.c`, needing forward declarations.
- **A linkage rule that only the linker could teach, recorded because the obvious reading is wrong.** A systematic audit found seven functions the glue had promoted from `static` to external. Citing the M5 Linux duplicate-symbol precedent the main session made all seven `static` again -- and the rebuild failed with six `LNK2019 unresolved external symbol ... referenced in function quake_rs_cl_init`. Rather than iterate, `rust/quake-capi/src/cl_main.rs:2095-2104` was read to derive the required set from the port itself: **a `cl_main.c` file-static must keep `static` in the glue unless the code that takes its address moved to Rust.** `CL_Init` did move, and it registers six console-command handlers, so those six must have external linkage even though `cl_main.c:1188+` makes them static. `CL_Viewpos_Completion_f` stays `static` because its address is taken only inside the glue. This is an unavoidable consequence of the flip, not a transliteration slip, and it is now commented at the declaration site.
- **A dropped initializer in `stubs.c` that turned out to be the *fix*, not the defect.** The agent (working outside its file allowlist, fully disclosed, because it could not link otherwise) replaced the harness's `client_static_t cls = {ca_disconnected};` with a plain `extern`. Checked rather than taken on trust: `Quake/client.h:103-107` puts `ca_dedicated` first in `cactive_t`, so C's tentative definition of `cls` in the real engine zero-initialises `state` to `ca_dedicated`, and the Rust `core::mem::zeroed()` matches it exactly. The harness's `{ca_disconnected}` (= 1) was itself a divergence from production C; removing it brings the oracle *closer* to the engine.
- **The largest finding of the milestone: a four-layer degenerate fixture hiding all of `CL_RelinkEntities`.** The port agent reported two surviving mutants -- the teleport threshold (`cl_main.rs:908`, `100.0`) and the frametime clamp (`cl_main.rs:1086`, `0.1`) -- and attributed both to `SCR_UpdateZoom` being a `Sys_Error` abort stub. That was the first layer of four, and each was only visible after the one above it was removed:
    1. `SCR_UpdateZoom` (`stubs.c`, called from `cl_main.c:681`) aborted **before** the entity loop, so both sides stopped at the same message and every test past that point compared identical untouched state. Converted to a counting stub.
    2. Execution then reached `R_UpdateEntityDlights` (`cl_main.c:935`, *after* the loop -- proof the loop now ran). The real `gl_rlight.c:421` body early-returns unless `vulkan_globals.ray_query` and `r_rtshadows >= 2` and `r_gpulightmapupdate` are all set, none of which the ctest link sets, so counting is behaviourally faithful.
    3. With the loop live, the mutants **still** survived: `ctest_clmain_set_entity` writes `origin`/`angles` but never `msg_origins`/`msg_angles`, so `CL_LerpEntity` always saw a zero delta. The same degenerate shape, one level down. Added `ctest_clmain_set_entity_msg`.
    4. Even seeded, no entity reached the lerp: `cl_main.c:664` drops any entity whose `msgtime` differs from `cl.mtime[0]`, and an unseeded `0` never matches -- so the loop had been nulling every model and lerping nothing, which also means the *pre-existing* `relink_entities_over_a_short_entity_array_matches` test was itself vacuous. `msgtime` is now a parameter of the new setter. Reaching the lerp then exposed the two `PScript_*` abort stubs, which are also the **only** place the clamped frametime escapes to anything observable; both became recording stubs.
- **Both mutants are now dead, and that is measured, not asserted.** With the fixture repaired: baseline 61/61 pass; teleport threshold `100.0` -> `1000.0` fails exactly one test; frametime clamp `0.1` -> `0.9` fails exactly one test; reverting restores 61/61. Three tests were added to do it (`relink_entities_lerps_below_the_teleport_threshold`, `..._snaps_above_the_teleport_threshold`, `relink_entities_clamps_frametime_before_the_particle_trail`), the first two straddling the threshold on one axis because a single case cannot observe a constant's value. The suite also stopped asserting the abort it used to depend on -- `relink_entities_interpolates_the_player_velocity` previously required `guard == GUARD_SYS_ERROR` and a `SCR_UpdateZoom` message, i.e. the degeneracy was written into the test as an expectation.
- **This is the eighth through eleventh instance of the degenerate-fixture hazard, and the first found as a *chain*.** Every previous instance (the M5 `/OPT:ICF` probe, T6.5 prelude-rename vacuity, T6.4 zeroed-cvar NaN, `check_ctest_symbols.sh` missing-tool vacuity, T7.2's `r_refdef` ordering, T7.3's `PScript_UpdateModelEffects` and `R_FreeEntityBLAS`) was a single layer. Here removing each layer revealed the next, and the mutation score did not move until the fourth was gone -- so a mutation run that stops at "still survives, must be unreachable" reports the *first* cause and hides the rest. **Rule adopted: when a mutant survives because a stub blocked the path, re-run the mutation after unblocking it; do not record the first blocker as the explanation.** `R_AllocateEntityBLAS` and `R_RocketTrail` remain abort stubs and still bound this suite. Every converted stub counts or records rather than no-ops, per the `R_FreeEntityBLAS` precedent, so the "reached a module that is not an oracle source" tripwire survives the conversion.
- `Quake/common.make` was correctly left alone -- a Pattern A flip never touches it (`grep -c "USE_RUST\|RUST\|_glue"` is still 0). Third milestone running in which that change was *not* needed; here the agent did not propose it.
- Verification, all on Windows/MSVC unless stated. **Targeted:** `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets -- -D warnings` exit 0, zero diagnostics; `cargo test --workspace` **1091 passed / 0 failed** (1088 before this milestone's three new tests, an exact match); the four-run mutation check described above. **Broad:** all six meson configs build (`build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace`); `check_ctest_symbols.sh` exit 0 over 43 oracle sources; `check_headers.sh` exit 0 with bindgen clean on `sys.h`/`mem.h`/`steam.h`; `check_capi_signatures.sh` exit 0; `run_corpus.py --compare` 11 ran / 8 skipped / 0 failed in **both** directions (`build-c` vs `build-rs` and the `build-rs-chost` oracle vs `build-rs`); `save_diff.py` identical on both saves (95091 and 95075 bytes); `record_diff.py` identical (20239 bytes, e1m1, 600 frames); `physics_matrix.py` **84 ok / 0 failed** -- all 12 cvar cells x 7 entries; `interop_matrix.py --combos --protocols` **PASS over all 24 cells** (6 protocol configs x 4 build combos); `capture_diff.py` **PASS**, reliable prefixes identical in both directions.
- The capture gate is recorded with its window because that number is the difference between a gate and a formality: the recv window calibrated to **4708 bytes** from the reference build's own second run, four and a half times the 1024-byte `--min-window` degeneracy floor, so the byte-identical prefix is a real comparison rather than a window that collapsed to nothing.
- Not run at commit time, stated so the ledger is not read as wider than it is: `interop_matrix.py --soak --frames 20000` was **in flight** when this entry was written and is recorded as a separate addendum below, as T7.2's and T7.3's were; `builtin_diff.py`/`trace_diff.py` (T7.4 changes no progs code and no builtin table); the 100k x 8 full soaks (M9/M11); Miri; the `--ipv6` interop leg; and **every Linux, macOS, MinGW and arm64 leg** -- CI's newest run on this branch is still the M2 commit, so M3 through M7 have never been through a non-MSVC compiler. That gap is not hypothetical: M5's duplicate-symbol defect was a hard error only on Linux and a silent merge on MSVC, and this milestone's five header-less externals are the same class.
- Carried to STOP B, new from T7.4: `R_AllocateEntityBLAS` and `R_RocketTrail` are still `Sys_Error` abort stubs and bound the `cl_main` suite; the four converted stubs (`SCR_UpdateZoom`, `R_UpdateEntityDlights`, `PScript_ParticleTrail`, `PScript_RunParticleEffectState`) are shared by both sides of the differential rather than renamed by the `c_ref_` prelude, because `gl_pscript.c` and `gl_rlight.c` are not oracle sources -- so their counters are reset per side by the fixtures and attest the **last** side to run.

- **T7.4 soak-smoke addendum (completed after the T7.4 commit landed).** `interop_matrix.py --soak --frames 20000` over the full 8-cell matrix -- 4 build combos (`C/C`, `Csv/Rcl`, `Rsv/Ccl`, `R/R`) x 2 protocols (`Base-666`, `FTE+999`). **Result: PASS, 8/8 cells**, exit 0, 313 checkpoints reached in every cell, traffic in band in every cell. Reference streams: `Base-666` 20001 frames hashed (rel 3/4, unrel 19911/19988), `FTE+999` 20001 frames hashed (rel 4/5, unrel 19906/19985).
- **The `hash forks @128 (diagnostic)` control held again, and this run is the cleanest demonstration of it yet: seven of the eight cells fork, and one of the seven is `FTE+999 C/C` -- pure C against pure C.** A fork in a cell with no Rust in it cannot be evidence of a port defect, which is exactly why the M1 pass criterion is checkpoint reachability plus traffic band and never byte identity. The one hash-identical cell this run is `Base-666 Csv/Rcl` -- a *mixed* cell, C server against Rust client. If the fork tracked language boundaries, that is the last cell that would come out identical.
- Third consecutive observation that **the fork set is not stable across runs**: T7.2 forked in exactly `Base-666 Rsv/Ccl` and `Base-666 R/R`; T7.3 forked in five cells and `Base-666 R/R` was identical; T7.4 forks in seven and `Base-666 Csv/Rcl` is the identical one. Three runs, three different partitions, and `Base-666 R/R` has now been on both sides twice. A structural port defect would be reproducible in a fixed set of cells; run-to-run timing nondeterminism is the only explanation consistent with all three.
- Scope limits, so the ledger is not read as wider than it is: this is a **20k-frame** smoke soak, not the 100k x 8 full soaks scheduled for M9 and M11; it ran on Windows/MSVC only; and it attests the T7.4 tree at commit `3ccabc5c`.

**2026-09-01 M8 T8.1 -- `host.c` + `host_cmd.c` in the ctest oracle (fixtures only, no port).**

- Oracle sources for the M8 port subjects, landed ahead of the port: `stubs/host_ref.c` (604 lines) + `tests/host_differential.rs` (947, 29 tests) and `stubs/host_cmd_ref.c` (314) + `tests/host_cmd_differential.rs` (546, 20 tests). `c_ref_prelude.h` +149, `stubs.c` +315/-11, `build.rs` +9. No `Quake/` file, `meson.build`, `meson_options.txt` or CI workflow was touched -- verified by an empty `git diff -- Quake/ meson.build meson_options.txt .github/`, which is also why the six meson configs and the harness gates are not re-run for this commit: nothing they compile or execute changed.
- **A new oracle shape, adopted because the existing one is actively wrong here: translation-unit composition.** Every other oracle source is listed in `build.rs`'s `C_SOURCES` and renamed to `c_ref_*` by the force-included `c_ref_prelude.h`, whose macros are TU-wide by construction -- one `#define` rewrites the definition *and* every call in all 43 oracle sources. That is right for the `sv_`/`cl_` strata and wrong for `host.c`, because four of the symbols `host.c` owns are the ADR-009 trap machinery this harness *substitutes for*: `Host_Error`, `Host_EndGame`, `Host_Guard` and `Host_Reraise`, all owned by `stubs.c`. Measured, not assumed: **9 oracle sources hold 68 calls to `Host_Error`/`Host_EndGame`** (`cl_parse.c` alone has 39) and **9 test files assert on `GUARD_HOST_ERROR`/`GUARD_END_GAME`**, 25 reference the trap machinery at all. A prelude-wide rename would silently repoint every one of them at the real `Host_Error`, which walks a stack trace, disconnects the client, shuts the server down and longjmps to a `host_abortserver` no test has set. So `stubs/host_ref.c` and `stubs/host_cmd_ref.c` `#include` the C file behind a **per-TU** rename block: `host.c`'s own definitions and internal calls are renamed, every other TU keeps the harness trap under the plain names. It is also the only way to reach the subjects at all -- `Host_SavegameComment` (`host_cmd.c:1519`), `Host_Savegame_f` (`:1554`) and `Host_Loadgame_f` (`:1797`) are all file-static.
- **The defect this shape introduces, and the one it introduced before being caught.** Because the `C_SOURCES` loop never sees the two files, `build.rs` emitted no `rerun-if-changed` for them, so a mutation edit to `Quake/host.c` did not rebuild the oracle -- the agent's first mutation sweep finished in 1.2 seconds against a stale archive and would have reported a perfect score for tests that never ran against the mutant. Fixed with an explicit watch loop (`build.rs:137-142`). The permanent cost is recorded rather than left to be discovered: `check_ctest_symbols.sh` reads `C_SOURCES`, so `host_ref.o` and `host_cmd_ref.o` are **outside its coverage**. The gate still passes (43 sources, exit 0) but it is now attesting less than its name suggests. Closing that hole is deliberately deferred to the end of M8 rather than done here, because T8.2/T8.3 will add more TU-composed content and the allowlist should be written once against the final set.
- **Three cvar doubles deleted from `stubs.c`, and the deletion is forced rather than chosen.** `max_edicts` (`host.c:76`), `deathmatch` (`:88`) and `coop` (`:89`) cannot be renamed: each is *also* the spelling of a struct member `host.c` reaches (`qcvm->max_edicts` at `:888`/`:963`/`:964`, `pr_global_struct->deathmatch`/`->coop` at `:990`/`:991`), and an object-like `#define` rewrites member accesses too. They therefore stay plain, `host_ref.o` supplies all three for the whole link, and `stubs.c`'s fakes had to go or the link would break. Net fidelity gain -- the other TUs now read the real `cvar_t` objects with the real `"32000"`/`"0"`/`"0"` defaults -- but `max_edicts.value` moves from `stubs.c`'s pre-seeded `8192.0f` to `0` until something registers it, which is the exact shape of the degenerate-fixture hazard this phase has now hit eleven times.
- **That degeneracy risk was discharged by evidence rather than by the suite passing.** The only consumer that could have gone vacuous is `cl_parse_ref.c:315`, `cl.max_edicts = CLAMP (MIN_EDICTS, (int)max_edicts.value, MAX_EDICTS)` -- previously `8192`, now `256`. It does not matter, and the proof is already in the suite: `cl_parse_differential.rs:1361-1379` drives entity index **64** expecting it to pass (`cl_parse.c:826` is `>`, not `>=`) and index **65** expecting the exact message `"CL_ParseStartSoundPacket: ent = 65"`. Both assertions are only satisfiable if `cl.max_edicts` is still the fixture's 64 at that point, so a silent drift to 256 -- or to 8192 -- would fail the second one. `deathmatch`/`coop` are value-neutral: the deleted doubles were `0.0f`/`"0"` and the real definitions are `"0"`.
- **The reported ADR-005 blocker is real in its facts and wrong in its conclusion; recorded corrected so T8.3 is not planned around a phantom.** The agent's handoff called `%g` a blocking prerequisite for a Rust `Host_Savegame_f`. The facts check out: `Fog_GetFogCommand` (`gl_fog.c:281`) emits four `%g` and `Sky_GetSkyCommand` (`gl_sky.c:541`) emits one, both are called with `always = true` from `host_cmd.c:1689`/`:1693`, and `always` forces `need_skyfog`, so **every savegame does contain five `%g`-formatted numbers**. But a specifier audit of the three byte-diff subjects' own bodies finds **no `%g` or `%e` in any of them**: `Host_Savegame_f` (`host_cmd.c:1554-1712`) uses only `%d`/`%f`/`%i`/`%s`, `Host_Loadgame_f` (`:1797-1990`) only `%d`/`%i`/`%s`, and `Host_WriteConfiguration` (`host.c:486`) has no specifiers at all -- it delegates to `Key_WriteBindings` and `Cvar_WriteVariables`, which use `%s` only. The five `%g` are formatted **inside `gl_fog.c` and `gl_sky.c`**, renderer files out of Phase 7 scope entirely, and `Host_Savegame_f` writes their result back out through `fprintf (f, "%s", ...)`. **T8.3 is therefore not blocked. The real constraint is a porting rule: the Rust `Host_Savegame_f` must call both functions through capi and emit the returned string opaquely, never re-deriving those numbers through the ADR-005 formatter.** The in-code note at `stubs.c` states it correctly ("it is why a Rust `Host_Savegame_f` cannot use the Rust float formatter"); only the handoff summary overstated it.
- The fog/sky test pins that rule so it cannot be broken silently: `savegame_writes_the_fog_and_sky_commands_it_is_given` asserts the literal bytes `fog 0.05 0.25 0.5 1` -- note the `1`, not `1.000000` -- and `skyfog 0.25`. A port that reformatted with `%f` fails it. The two stubs reproduce their originals verbatim, branch and format string included, and **count their calls** rather than no-op, per the `R_FreeEntityBLAS` precedent; `Con_LinkPrintf` (`console.c:1383`) and `SCR_EndLoadingPlaque` (`gl_screen.c:993`) were converted from aborts to recorders the same way.
- Verification, Windows/MSVC, all re-run by the main session rather than taken from the handoff. **Targeted:** `cargo fmt --all --check` exit 0; `cargo clippy --workspace --all-targets -- -D warnings` exit 0; `cargo test --workspace` **1140 passed / 0 failed / 0 ignored** across 104 test binaries -- exactly the 1091 baseline plus this milestone's 49 (29 `host_differential` + 20 `host_cmd_differential`); a 66-mutant sweep by the port agent with zero survivors after the stale-archive defect above was fixed. **Broad:** `check_ctest_symbols.sh` exit 0 over 43 oracle sources, with the coverage caveat above. Line endings verified programmatically on all seven files (CRLF for `build.rs`/`c_ref_prelude.h`/`stubs.c`, LF for the two `*_ref.c` and the two test files).
- Two numbers in the agent's own file comments did not survive checking and are corrected here rather than propagated: it wrote "69 calls" (measured: **68**, whitespace-tolerant regex over all 43 `C_SOURCES` files) and "19 test files" (measured: **9** asserting the guard statuses, 25 referencing the trap at all). Neither changes the design argument, which holds at any count above zero.
- Carried to STOP B, new from T8.1: **(p)** `check_ctest_symbols.sh` has a permanent blind spot for TU-composed oracle objects because it reads `C_SOURCES`; **(q)** `Host_Loadgame_f` is driven only as far as its version check -- nothing past it (`CL_Disconnect_f`, `SV_SpawnServer`, the edict/global re-parse, fastload, `Send_Spawn_Info`) is exercised; **(r)** `Host_Guard`/`Host_Reraise` now compile in the oracle but have no tests; **(s)** the savegame fixture is synthetic (2-edict qcvm, 1 client, 1 `DEF_SAVEGLOBAL` global) and does not pin `ED_Write` ordering against a real `progs.dat`; **(t)** the fog and sky lines asserted come from the `stubs.c` recorders, not from `gl_fog.c`/`gl_sky.c`, which are not oracle sources.
- **2026-09-01 M8 T8.2 landed** (`use_rust_host` Pattern A extended to the frame loop: `Quake/host.c` (1456 lines) -> `rust/quake-capi/src/host.rs` (2028 lines) + `Quake/host_glue.c` (933) + `rust/quake-c-sys/src/host.rs` (292), with `rust/quake-ctest/stubs/host_glue_ref.c` (898) as the ctest-link mirror). Executed by the main session, with the ctest-link stub authoring and the Rust half of the differentials delegated to two Opus-high agents.
  - *Pattern A forced two functions into this task that the plan scheduled elsewhere.* `Host_WriteConfiguration` is `host.c:486`, inside the swapped file, so **T8.4 is subsumed by T8.2** -- there is no later moment at which it could be ported separately. Its config.cfg byte-diff nevertheless stays a distinct acceptance criterion and is **not** discharged here: this milestone drives the writer only through `stubs/host_glue_ref.c`'s empty-cvar-list fixture (guard ladder, `Key_WriteBindings` call, trailing commands), and the end-to-end byte-diff completes in M10 when `Key_WriteBindings` leaves C. Recorded as a plan amendment; T8.4 is closed as "absorbed", not "skipped". Likewise `Host_Init`/`Host_Shutdown` had to become Rust, because a Pattern A glue file contains no unported behavioral C bodies by construction.
  - *T8.2 closes no ADR-007 row, and that is deliberate.* Every host.c data object -- `host_parms`, `host_client`, the 20 cvars, `oldrealtime`, `host_colormap`, the `sv_speeds` counters -- keeps its C storage in `Quake/host_glue.c`, because other TUs resolve them by local re-declaration. The `net_message` trio and the `qcvm`/`pr_global_struct`/`edicts` row still close at M9 as planned.
  - *Three defects the compiler and linker found, each a distinct class:*
    - `Cmd_AddCommand` **is not a symbol.** `cmd.h:110` defines it as a macro expanding to `Cmd_AddCommand2 (name, func, src_command, false)`. Declaring it in `rust/quake-c-sys/src/host.rs` compiled fine and failed at link with `LNK2019 ... referenced in function _RNv...quake_rs4host10tests_init`. The declaration was deleted and both call sites now route through a local `add_command` helper. **New rule for later Pattern A ports: an engine "function" that quake-c-sys cannot resolve at link time may be a macro -- grep the header before assuming a missing definition.**
    - `qsocket_t` is **internal to `net_defs.h`**; the public `net.h` spelling is `struct qsocket_s *`. `host_glue.c:530`'s `HOST_GUARD_PTR (NET_Close, qsocket_t *)` gave `error C2065: 'qsocket_t': undeclared identifier`.
    - `host_client` belongs in **quake-capi, not quake-c-sys**, because it is typed with the ADR-011 mirror `Client`. Declared privately in the module per the existing precedent at `sv_main.rs:103` and `sv_send.rs:121`, rather than inventing a third convention.
  - *A self-inflicted diagnostic error, recorded because the rule it violates is already written down.* The first `cargo test -p quake-ctest --no-run` was piped through `| tail -30`, which both discarded the exit code and truncated the unresolved-symbol list from 135 to 25 -- turning a complete linker verdict into a misleading sample. Re-run without `tail`. **Never pipe a gate through `tail`.**
  - *The Rust half of the differentials existed nowhere until this milestone, and the gap was real rather than theoretical.* T8.1's `host_differential.rs` was C-oracle-only (947 lines, 29 tests, zero `quake_rs_host` references), so at the moment the flip went live the port had **no** differential coverage. It now has 31 tests, 1303 lines, of which 20 previously single-sided pins became two-sided. The two implementations hold independent state in one process -- `host_ref.c` renames host.c's file-scope objects to `c_ref_*` and `c_ref_prelude.h` renames `sv`/`svs`/`cl`/`cls` (`:1144`/`:1145`/`:1580`/`:1619`) -- so identical inputs go in on each side and the observables are compared bit for bit. The C side keeps its T8.1 pin against the independently transcribed expectation, so a shared misreading of host.c cannot make both sides agree on the wrong answer. **No C-vs-Rust divergence was found on any subject.**
  - *The degenerate-fixture hazard was discharged by mutation, not by argument -- and this is the first instance where the mutation was run against the port rather than the fixture.* All seven `ctest_host_rs_*` wrapper bodies were temporarily replaced with `return 0;` (an exact simulation of an empty-bodied port), rebuilt and re-run: **22 of 31 tests fail.** The 9 survivors are exactly the tests documented as C-only or that have a two-sided twin. The mutation also exposed one genuinely degenerate test the agent had just written -- `callback_notify_is_silent_without_an_active_server` asserted only "both sides empty", which a no-op port satisfies trivially; strengthened to re-open the gate on the port's own fixture and require the buffers to fill, after which the mutation failure count rose 21 -> 22. The file was restored byte-identically (`diff`-confirmed) and the suite re-verified green.
  - *Two subjects stay C-only, and the reason is structural rather than an omission.* `Max_Fps_f` (`host.rs:462`) and `Phys_Ticrate_f` (`host.rs:491`) are file-private `extern "C"` callbacks with no `#[no_mangle]`, so nothing in the ctest link can take their address without first running `quake_rs_host_init_local`, which reaches `Host_Glue_Host_InitCommands` -> `host_ref.c:295`'s aborting double and `Sys_Error`s. `Host_Version_f` has no C-side test to pair with (`__TIME__`/`__DATE__`-stamped output). Both are written into the module doc's Known-gaps list.
  - *An ABI trap that was anticipated and defused rather than discovered.* `quake_rs_host_filter_time` returns `qboolean`, which is `bool` (`q_types.h:122`) -- one byte. `host_glue_ref.c:684` declares it as `qboolean` and normalises with `? 1 : 0` at the boundary; an `int` return type there would have been silent corruption on the hottest path in the engine.
  - *Gate evidence (all measured, Windows x64, this checkout):*
    - All six meson configs build clean: `build-rs`, `build-c`, `build-c-trace`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace`.
    - `cargo test -p quake-ctest`: **73 test binaries, all `test result: ok`, 0 failed**, exit 0. `cargo test -p quake-ctest --test host_differential`: **31 passed, 0 failed**.
    - `check_ctest_symbols.sh`: **OK -- 43 oracle sources** export only `c_ref_*` and allowlisted symbols.
    - `run_corpus.py --compare build-c build-rs` (registered tier): **11 ran, 8 skipped (missing mod/rerelease data), 0 failed**, every entry `identical`. Re-run against the `build-rs-chost` oracle leg, which isolates the host flip specifically: **11 ran, 0 failed**.
    - `save_diff.py --vkquake build-c --vkquake-b build-rs`: **both saves byte-identical** (95091 and 95075 bytes).
    - `physics_matrix.py` 12 cells x 7 entries, C-vs-Rust: **84 ok, 0 skipped, 0 failed**.
    - `interop_matrix.py` full 6x4 (5 protocol groups x 4 build combos): **PASS**.
    - `cargo fmt --check` and `cargo clippy --workspace --all-targets -- -D warnings`: clean. `clang-format` clean on `Quake/host_glue.c` (27 lines were non-conforming on first check; the fix was verified `diff -w`-identical, i.e. whitespace-only) and on `rust/quake-ctest/stubs/host_glue_ref.c`.
    - Line endings verified per repo convention: LF for `Quake/host_glue.c`, both `host.rs` files, `host_glue_ref.c`, `host_differential.rs` and `cbindgen.toml`; CRLF preserved in `meson.build` and `rust/quake-ctest/build.rs`.
    - `interop_matrix.py --soak --frames 20000` over the **full 8-cell matrix** (4 build combos `C/C`, `Csv/Rcl`, `Rsv/Ccl`, `R/R` x 2 protocols `Base-666`, `FTE+999`): **PASS in all 8 cells**, 313 checkpoints reached per cell, traffic in band. Reference streams rel 3/4 unrel 19909/19985 (Base-666) and rel 4/5 unrel 19909/19988 (FTE+999). Every cell reports `hash forks @128 (diagnostic)` -- **including both `C/C` controls**, which is the point: the fork is a property of the scenario, not of the Rust port, and the control forking identically is what licenses reading it as diagnostic rather than as a desync.
    - *Ordering note, so the evidence above is not over-read.* `clang-format` was applied to `Quake/host_glue.c` after the physics matrix, the interop matrix and the soak had already run, so those three gates measured binaries built from the pre-format source. The reformat was verified `diff -w`-identical (whitespace only). All six meson configs were rebuilt afterwards, and `run_corpus.py --compare` (**11 ran, 0 failed**) and `save_diff.py` (**both saves byte-identical**, 95091 / 95075) were re-run against the final binaries.
    - **Not run, and blocked rather than skipped:** `check_headers.sh` and `check_capi_signatures.sh` both die with `cc: command not found` in Git Bash on this machine -- they are CI/Linux gates. `check_capi_signatures.sh` additionally needs `RUSTUP_TOOLCHAIN=1.97.1` in the environment or its `cargo metadata` runs the default toolchain, which cannot parse `debug = "line-tables-only"` in `rust/Cargo.toml`; with the pin set, cbindgen generated cleanly through the new `quake_rs_host_callback_notify` declaration before the script reached `cc`.
    - Not run (out of scope for T8.2): `builtin_diff.py`/`trace_diff.py` (no progs change), Miri, the full 100k x 8 soak (M9/M11), and any Linux/macOS/MinGW/arm64 leg.
- **2026-09-02 M8 T8.3 landed** (`use_rust_host` Pattern A extended to the command stratum: `Quake/host_cmd.c` (3351 lines) -> `rust/quake-capi/src/host_cmd.rs` (5326) + `Quake/host_cmd_glue.c` (1052) + `rust/quake-c-sys/src/host_cmd.rs` (485), with `rust/quake-ctest/stubs/host_cmd_glue_ref.c` (1020) as the ctest-link mirror). Ported by five parallel Opus/Sonnet agents against disjoint line ranges (A: file lists and `ExtraMaps_*`; B: status/version/say/tell; C: the savegame writer; D: the loadgame reader and game commands; E: cheats, `give`, `user`, kick/ping), merged by the main session. With T8.2's amendment already recording T8.4 as absorbed, this closes M8's porting scope.
  - *`ExtraMaps_*` was ported around the thread boundary, not through it (ADR-016).* The `QThread_Create`/`QThread_Wait` pair, `extralevels_parsing_thread` and the `extralevels_cancel_parsing` atomic all stay C in `host_cmd_glue.c:82-137`, and the Rust side reaches them only through the eight named `HostCmd_Glue_*` seams. No Rust code runs on the parsing worker and no Rust thread-local is assumed to exist there.
  - *The flip pattern grew a mandatory deliverable, because a Pattern A flip can be green on every build gate and still be broken.* `cargo clippy --all-targets` type-checks but does **not** link, and the six meson configs do not compile `rust/quake-ctest/stubs/`. The first cut of this milestone therefore passed all six meson builds, `cargo build`, and every clippy invocation while `cargo test -p quake-ctest --no-run` failed with **60 unresolved externals** -- the oracle link had lost every symbol `Quake/host_cmd_glue.c` supplies to the shipping build. Recorded as a permanent rule rather than a one-off fix: **every Pattern A flip needs `cargo test -p <crate> --no-run` in its checklist, and a `*_glue_ref.c` mirror is a required deliverable, not an optional one.** The 60-symbol contract resolved on the first attempt once the mirror existed, with no duplicate-symbol collisions.
  - *One link fact worth stating because getting it wrong fails two different ways.* The ctest link has **no plain `SV_ClientPrintf`/`SV_BroadcastPrintf` at all**: `host_ref.c:122-123` and `host_cmd_ref.c:95-96` each `#define` them to `c_ref_*` per TU. A new `*_glue_ref.c` that forgets the rename fails to link; one that supplies its own definitions instead would link and then silently split the two sides' console state. `host_cmd_glue_ref.c` repeats the per-TU rename, which is why both sides land on one `c_ref_SV_ClientPrintf`.
  - *The mirror's file boundary is narrower than the glue's, and the cut line is load-bearing.* Sections 1, 2a, 2b and 3 transcribe; the flip half (`Host_Quit_f`, `ExtraMaps_Init`, `Host_InitCommands`, ...) does not, because `host_cmd_ref.c` already defines those names and a second definition is LNK2005. `SaveList_Rebuild` and `Host_Reconnect_f` are the two flip-half members that must still be defined, because section 2b's seams name them. Section 3's `HostCmd_Raise` is transcribed rather than dropped: the loadgame differential pins its exact `Host_Error` text.
  - *The fixture had to be duplicated, and the list of what was deliberately **not** duplicated is the interesting half.* `c_ref_prelude.h` renames `sv`/`svs`/`cl`/`cls`/`com_gamedir`/`cmd_source`, so `host_cmd_ref.c`'s savegame fixture writes objects the Rust port never reads; section 4 therefore carries an independent `ctest_hostcmd_rs_*` driver set over the plain objects. It does **not** duplicate `qcvm`, `PR_SwitchQCVM`, `host_client`, `current_skill`, or `stubs.c`'s console/fog/sky recorders -- the prelude does not rename those, so both sides already share one object, and twinning them would have made the two sides disagree by construction.
  - *The differential is two-sided and was proved to be, rather than asserted to be.* `tests/host_cmd_differential.rs` grew from 546 lines of C-oracle-only goldens to 734 lines driving both implementations from one `Driver` table: all **20 tests now run twice**, same fixture in, same savegame bytes / console lines / `Host_Error` text out, and `savegame_writes_the_whole_file` additionally byte-compares the two writers' files against each other. Coverage was then mutation-tested: four defects injected into `rust/quake-capi/src/host_cmd.rs` (the `kills:%3i/%3i` comment format, the `Not playing a local game.` refusal text, the `sv.serverflags` extended-block key, and `version != SAVEGAME_VERSION` weakened to `<`) were **caught 4/4, each failing on the `rs` side**. **No C-vs-Rust divergence was found on any subject.** The module doc's stale "There is no Rust port of host_cmd.c yet" paragraph is rewritten.
  - *The plan's "save byte-diff in all three directions" was not actually reachable with the existing tooling, so the missing half was built.* `save_diff.py` compares two builds' **written** saves and never loads one; the C-write/Rust-read and Rust-write/C-read directions had no gate at all. A four-direction cross-load check (each build writes a save; each build loads every save and hashes 400 post-load frames) closes it: **all four chains identical**, C/C control included. Its first run reported a mismatch that was the harness's fault, not the port's -- `-demohash` is what puts the engine on its fixed deterministic timestep, and without it a single build disagrees with **itself** run to run. Worth stating plainly: any ad-hoc scenario script that omits `-demohash` measures noise.
  - *Gate evidence (all measured, Windows x64, this checkout):*
    - `cargo test -p quake-ctest --no-run`: links (41.5s, ~75 test binaries) -- the gate that was failing with 60 LNK2019s. Full `cargo test -p quake-ctest`: every `test result:` line `ok`, **0 failed**. `cargo test -p quake-ctest --test host_cmd_differential`: **20 passed, 0 failed**, both sides.
    - `cargo fmt --all --check` clean; `cargo clippy --workspace --all-targets --locked -- -D warnings` clean; `cargo clippy -p quake-capi --features progs --all-targets --locked` clean.
    - `check_ctest_symbols.sh`: **OK -- 43 oracle sources** export only `c_ref_*` and allowlisted symbols.
    - `run_corpus.py --compare build-c build-rs` (registered tier): **11 ran, 8 skipped, 0 failed**, every entry `identical`. Same against the `build-rs-chost` oracle leg, which isolates the host flip: **11 ran, 0 failed**.
    - `save_diff.py --vkquake build-c --vkquake-b build-rs`: **both saves byte-identical** (95091 and 95075 bytes). Four-direction cross-load (`c-write/c-read`, `c-write/rs-read`, `rs-write/c-read`, `rs-write/rs-read`): **all four post-load state-hash chains identical**.
    - `vkQuake.cfg` byte-diff across `build-c` / `build-rs` / `build-rs-chost` after a scripted bind/cvar/name scenario: **identical, 2231 bytes, same SHA-256** in all three. (This is the T8.2 writer under exercise, not a T8.3 subject; the end-to-end criterion still completes in M10 when `Key_WriteBindings` leaves C.)
    - `physics_matrix.py` 12 cells x 7 entries, C-vs-Rust: **84 ok, 0 skipped, 0 failed**.
    - `interop_matrix.py` full 6x4 (6 protocol variants x 4 build combos): **PASS**, 24/24 cells.
    - `interop_matrix.py --soak --frames 20000` over the full 8-cell matrix: **PASS in all 8 cells**, 313 checkpoints per cell, traffic in band. Four cells report `hash forks @128 (diagnostic)` -- including a `C/C` control in each protocol group, which is what licenses reading the fork as scenario nondeterminism rather than a port desync.
    - `build-c`, `build-rs` and `build-rs-chost` rebuild clean and the harness gates above ran against those binaries. `clang-format` was **not** applied to `rust/quake-ctest/stubs/host_cmd_glue_ref.c`: `format.sh` covers only `Quake/*` and `Shaders/*`, so no stub file in the repo is under that gate. `Quake/host_cmd_glue.c` is in scope and was formatted clean at authoring time.
    - Line endings verified per repo convention: LF for `Quake/host_cmd_glue.c`, both `host_cmd.rs` files, `host_cmd_glue_ref.c`, `host_cmd_differential.rs` and `cbindgen.toml`; CRLF preserved in `meson.build` and `rust/quake-ctest/build.rs`.
    - **Not run, and blocked rather than skipped:** `check_headers.sh` and `check_capi_signatures.sh` (both `cc: command not found` in Git Bash on this machine -- CI/Linux gates).
    - Not run (out of scope for T8.3): `builtin_diff.py`/`trace_diff.py` (no progs change), `record_diff.py`/`netreplay_diff.py` (no protocol change), Miri, the full 100k x 8 soak (M9/M11), the three meson legs the harness gates do not use (`build-c-trace`, `build-rs-cprogs`, `build-rs-trace`), and any Linux/macOS/MinGW/arm64 leg.
  - Carried to STOP B, new from T8.3: **(ff)** `cargo clippy --all-targets` does not link, so a Pattern A flip can be clean on all six meson configs and every clippy invocation while the ctest test binary fails to link -- `cargo test --no-run` and a `*_glue_ref.c` mirror are now mandatory per flip; **(gg)** the ctest link has no plain `SV_ClientPrintf`/`SV_BroadcastPrintf`, so every TU that wants them must repeat the per-TU `c_ref_*` rename; **(hh)** `host_cmd_glue_ref.c` is a transcription of `host_cmd_glue.c` and can drift from it silently -- nothing gates the two against each other; **(ii)** seven-plus verbatim-transliterated C defects were preserved bug-for-bug and are documented at their sites in the port (unbounded `strcpy` in the mod-list add path, four unchecked `q_snprintf` truncations, the `$key` fallthrough in `Modlist_GetFullName`, the dead `for (i = MAX_LIGHTSTYLES; i < MAX_LIGHTSTYLES; i++)` loop, `sv.sound_precache` bounds-checked against `MAX_MODELS`, the `memcpy (text + 22, kills, strlen (kills))` overrun of `text[40]`, `Host_Pause_f`'s XOR-as-negation, `Host_Name_f`'s `name[15] = 0` on a 32-byte buffer, and `Host_Give_f`'s nested-loop bug) -- each is a deliberate compat preservation, not an oversight, and each is a candidate for a post-migration fix commit.
- **2026-09-02 STOP B** (M6-M8, commit range `f8b5ae18^..24b4af82`) found **zero blockers and zero behavioural defects**. Every finding was documentation or an undocumented policy; all were fixed in the STOP B commit. Structure differed from STOP A by necessity: a single omnibus `compatibility-reviewer` brief was tried and **starved at its 20-turn limit twice without emitting one finding**, so the review was re-cut as a main-session mechanical pass plus four *single-subject* Opus reviewers. Two of those four also hit the turn limit on their first pass and produced their reports only after a follow-up message instructing them to stop reading and write from what they had. **Rule for M9-M11 and any future STOP: a subagent review brief must name one subject with explicit line ranges, and must state that "no findings" is an acceptable result.** An omnibus brief does not fit the budget and returns nothing at all, which is worse than a narrow brief's partial answer.
  - *Main-session mechanical pass (grep-shaped checks, all clean).* Every named raise `#define` in the M6-M8 glue files has a `case` arm and every one of the nine named ladders has a safe `default:` (`Sys_Error` in cl_parse/net_msg, `Host_Error` in the pr_edict/pr_exec ladders, `Host_Reraise` in cl_main/host_cmd; `host_glue.c:362`'s `break` is `Host_Reraise` itself and correctly no-ops on OK). Raise **values** match across the FFI boundary where the **names** deliberately do not (`CLMAIN_RAISE_CONNECT_FAILED`/`LOST_READ`/`LOST_SEND` = `RAISE_*` = -101/-102/-103). `qboolean` ABI: `generated.rs:38 pub type qboolean = bool` matches `q_types.h:122 typedef bool qboolean`, and 575 `quake-c-sys` externs cross-checked against their C prototypes show zero return-type mismatches. ADR-007 substance verified by reading the storage, not the table. `host_cmd_glue_ref.c` differs from `host_cmd_glue.c[49:706]` by **only** the four documented deliberate edits. Zero `%g`/`%e` on either side of the twelve modules (ADR-005), zero `f32`/`f64` math *methods* (ADR-010 call-through), zero `Cargo.toml`/`Cargo.lock` change in the range (ADR-003), zero files deleted (roadmap order). `_Host_Frame`'s VM-switch ordering matches `host.c:1171-1191` exactly; the Rust early-`return r` skips the trailing `PR_SwitchQCVM(NULL)` but so does C's `longjmp`, and both converge because `Host_Error` restores the VM itself.
  - *Panic-across-FFI scan.* Zero `panic!`/`unreachable!`/`todo!`/`unimplemented!`/`.unwrap()`/`.expect(` in any of the twelve M6-M8 modules. Worth recording that this grep is **not** sufficient on its own: it does not see *implicit* bounds-check panics, and the SV_SpawnServer reviewer found one the grep could not (below).
  - *Reviewer 1 -- cl_parse svc dispatch: clean.* 55 of 55 arms compared. All 54 protocol constants match `protocol.h:246-340` including the four deliberate aliases; every wire-read width and order; `svcfte_voicechat`'s `while (bytes-- > 0)` decrement form; `svc_lightstyle`'s accumulation/`peak`/`average = total/length + 'a'` with the signed-`char` promotion; all 34 raise sites mapped to `ClParse_Raise` arms with verbatim format strings, including `svc_lightstyle` correctly calling `Sys_Error` (abort) rather than raising, and raise-before-state-write ordering preserved in `svc_version`/`svc_signonnum`. Four preserved C defects confirmed reproduced rather than fixed. Declared gap: the 12 delegated handler bodies (`CL_ParseUpdate`, `CLFTE_ParseEntitiesUpdate`, ...) were not read, only their delegation targets and raise arity.
  - *Reviewer 2 -- sv_send delta writers: clean.* `SVFTE_WriteEntitiesToClient` and `SV_WriteEntitiesToClient` compared end to end. The transliteration traps are all handled correctly: the `for`-to-`while` conversion increments `entnum` on the `continue` and fall-through paths but **not** the `break` path (a naive port corrupts `snapshotresume`); every `continue` in the gather loop advances both `e` and `ent`; the deliberate asymmetry between `entnum > 0x3fff` and `entnum >= 0x4000` is preserved unnormalized; `dist = 8.f * sqrt(sqrt(dist / size))` reproduces C's f32-divide/double-sqrt/double-multiply/narrow-on-store chain without reassociation; the `-0.1`/`0.1` origin-miss test is compared in `f64` as C's promotion requires; `U_EXTEND1`/`U_EXTEND2` and `U_LONGENTITY`/`U_MOREBITS` keep their order-dependent quirks; and the Rust writer's batching flushes before every `cursize`/`maxsize` access so the overflow-rollback captures are not stale. All `U_*`/`UF_*`/`MOVETYPE_*`/`ENTALPHA_*` values cross-checked against the headers. Declared gap: `MSGFTE_WriteEntityUpdate`/`MSGFTE_DeltaCalcBits`, which produce most of the SVFTE payload, were not compared.
  - *Reviewer 3 -- SV_ConnectClient / SV_SpawnServer: clean.* 100% of both bodies plus both glue wrappers and the six `SvMain_Glue_*` helpers. `SV_ConnectClient`'s save/restore set across `memset(client, 0, sizeof(*client))` is exactly C's (`netconnection`, plus `spawn_parms` only when `sv.loadgame`), with `Client` size-asserted against `client_t` in `host_abi.rs:222`; `net_activeconnections++` stays ordered after the `EDICT_NUM` raise point so an abort leaves it unincremented on both sides; the misnamed `"Host_CheckForNewClients: no free clients"` text is preserved. `SV_SpawnServer` matches statement for statement including the four `sizebuf_t` sizes (`signon.maxsize = MAX_MSGLEN - 2`), `num_edicts = reserved_edicts = maxclients + 1` assignment order, the redundant second `q_strlcpy(sv.name, ...)`, the two `SV_Physics` frames at `host_frametime = 0.1`, and the final `SV_SendServerinfo` walk leaving `host_client` one past the last client.
  - *Reviewer 4 -- Host_Savegame_f / Host_Loadgame_f: no findings.* 100% of both bodies (plus `Host_SavegameComment`). Every `fprintf` format string, specifier and argument order compared character by character, with `%f` arguments explicitly `as f64` to match C's default promotion; all loop bounds and all twelve size constants cross-checked against `quakedef.h`/`server.h`; the version-mismatch and first-token-brace raise paths verified to free-and-null `start` in the same order as C and to re-issue identical `Host_Error` text through the ladder; and the extended-block parser compared branch by branch, including the token-consumption count per branch, the `Mem_Free`-present-on-particles / absent-on-models asymmetry, and the preserved `sv.sound_precache` bounds check against `MAX_MODELS` rather than `MAX_SOUNDS`. Its one open assumption -- that `ENTITY_T_OPAQUE_SIZE = 456` is really pinned by a test -- was closed by the main session: `host_abi.rs:346-347` asserts `size_of::<EntityOpaque>()`/`align_of` against `abi_probe.c`'s `sizeof.entity_t`/`alignof.entity_t`.
  - *Finding 1 (fixed) -- ADR-007's table was stale, which the ADR itself calls a review error.* Rows 2 and 3 still read "closes at **M6**"/"closes at **M7**" and the paragraph after the table still counted all four Phase-7 rows as open, though the storage had genuinely moved (`sv_main.rs:91,98`; `cl_main.rs:61,67`). Both rows are now marked closed with the owning Rust file named, and the paragraph now describes only the two **M9** rows as open.
  - *Finding 2 (fixed) -- a stale `TEMP-STUB` header labelled live code as deletable.* `host_cmd.rs:4176-4186` told a future reader that `Host_Spawn_f` was a temporary stub to delete once chunk C's `Send_Spawn_Info` merged. The merge had already happened correctly (one definition at `:3026`, called at `:3578` and `:4265`); only the comment survived. Replaced with a two-line statement of the actual same-module-privacy arrangement.
  - *Finding 3 (fixed, the substantive one) -- an undocumented compat policy was being applied at scale under a citation to a rule that does not exist.* Three of the four reviewers independently surfaced the same class: **C's float-to-integer conversion is UB out of range while Rust's `as` saturates.** The convention in force (take the saturating result, annotate the site) is real, consistent, and already carried by the landed `as_int` shims in `progs_builtins_sv.rs:325`, `progs_builtins_cl.rs:122`, `progs_builtins_sv_fx.rs:135` and `sv_phys.rs` -- but **ADR-010 said nothing about it**, and two `sv_send.rs` annotations cited "ADR-010 rule 8" when ADR-010 has never contained numbered rules at all. A reviewer following that pointer finds nothing, which is why the same question kept being re-raised. ADR-010 now carries a Phase 7 amendment stating the decision and, importantly, *why* emulating the C build's UB was rejected: `cvttsd2si` yields `INT_MIN` on x86-64 while arm64's `fcvtzs` saturates like Rust, so there is no single "C behaviour" to be faithful to and reproducing x86's would manufacture the cross-platform divergence this ADR exists to prevent. The two bogus citations are corrected and the three control-flow-bearing sites now carry real `// COMPAT: ADR-010` markers.
  - *Recorded, deliberately not changed (a).* Three saturating casts change **control flow** rather than a stored byte, and are enumerated in the ADR-010 amendment so the next reviewer does not re-derive them: `sv_send.rs:2304` (a QC-assigned negative `.modelindex` is `0xffffffff` under x86-64 C, so the entity is skipped, and `0` here, so it is sent), `sv_main.rs:1544` (`skill 1e30` clamps to 0 in C and 3 here -- a different monster set) and `sv_main.rs:1576` (`max_edicts 1e30` allocates 256 in C and 32000 here). Each needs a cvar or QC field set to roughly 1e10 or beyond, or negative where the engine never writes one; none is reachable from engine code, which is why every differential and soak gate is green.
  - *Recorded, deliberately not changed (b) -- the one divergence a `panic!` grep cannot see.* `sv_main.c:1019`'s inline-model guard is `>` not `>=`, so `numsubmodels == MAX_MODELS` reaches the loop and its last iteration addresses index `MAX_MODELS` in two `[MAX_MODELS]` arrays. C writes one element past each array silently; the Rust indexes a fixed-length array through a raw-pointer place, so the bounds check fires and **aborts** under `panic = "abort"`. Not reproduced: matching C would require an out-of-bounds write, which is UB in Rust as well, for a case needing a BSP with exactly 8192 submodels. Documented at the site instead. The general lesson carries: implicit bounds-check panics inside `extern "C"` functions are a divergence class that the explicit-panic grep misses entirely.
  - *Gates re-run after the STOP B edits (all comment/markdown-only, no behavioural change):* `cargo fmt --all --check` clean; `cargo clippy --workspace --all-targets --locked -- -D warnings` clean; `cargo clippy -p quake-capi --features host --locked -- -D warnings` clean. Not re-run, and not needed for comment-only edits: the ctest suite, the meson builds and the harness gates, all of which were green at `24b4af82` and are unaffected by these files' comment text.
  - Carried forward to M9-M11: **(mm)** subagent review briefs must be single-subject with explicit line ranges and must license a "no findings" answer -- an omnibus brief starves and returns nothing; **(nn)** an explicit-panic grep does not cover implicit bounds-check panics in `extern "C"` functions, so index expressions on fixed-size mirrors need their own look wherever the C indexes without a bound; **(oo)** the four reviewers' declared gaps are real and uncovered: `MSGFTE_WriteEntityUpdate`/`MSGFTE_DeltaCalcBits`, the 12 delegated `cl_parse` handler bodies, `Send_Spawn_Info` (whose `WRITE_BATCH` indirection is a genuinely different control structure from the C), and the `HostCmd_Glue_*` shim bodies -- each is a candidate subject for the M11 review rather than a known defect.
- **2026-09-02 M9a landed** (`e3cf6e8c`, ADR-009 correction, no new port). M6's T6.4 audit classed `NET_GetServerMessage` with sv_user.c's non-raising net accessors and let the Rust `SV_RunClients` call it as a bare extern. It is not an accessor: `net_main.c:768` dispatches to the driver's `QGetAnyMessage`, and the datagram implementation (`net_dgrm.c:138`) reaches `SV_ConnectClient` (`sv_main_glue.c:495`, a `Host_Reraise`) through `_Datagram_ServerControlPacket` on a connection request and `SV_DropClient` (`net_dgrm.c:212`) on a socket timeout. Either longjmp unwound a Rust frame, which ADR-009 rule 3 forbids. Fixed with the usual C trampoline seam (`SvUser_Glue_GetServerMessage`, out-parameter socket + `Host_Guard` status propagated by `sv_run_clients`), mirrored in `stubs/sv_user_ref.c`; the plain extern is dropped from `quake-c-sys`. **Reachable only with a real datagram client connecting or timing out, so no existing gate could have caught it** -- the loopback-driver corpus is unaffected and stayed identical. Recorded as a gate gap, not a gate failure.
- **2026-09-02 M9b T9.2 landed** (`98e879bd`, `use_rust_net` Pattern A extended to the datagram orchestration half: `Quake/net_dgrm.c` -> `rust/quake-capi/src/net_dgrm_orch.rs` (3576 lines) + `Quake/net_dgrm_orch_glue.c` (424) + `rust/quake-c-sys/src/net_dgrm_orch.rs` (297), with `rust/quake-ctest/stubs/net_dgrm_orch_glue_ref.c` (231) as the ctest-link mirror). Executed as a five-agent wave against disjoint subjects, merged by the main session. Three decisions worth carrying: (1) `NetDgrmOrch_Glue_UpdateScreen` exists rather than reusing `Host_Glue_SCR_UpdateScreen`, because the latter is gated on `use_rust_host`, which is **off** on the `build-rs-chost` leg -- a net-gated flip may not reuse a host-gated seam; (2) `net_dgrm.c` leaves the unconditional `srcs` list and rides `use_rust_net`, and `common.make` is unchanged because glue files are Meson-only (the M8 "common.make in the same commit" rule applies to C *splits*, not Pattern A flips); (3) the mirror uses **no `#undef`**, so `c_ref_prelude.h`'s renames rewrite both sides identically -- which is the only spelling under which `SV_ConnectClient` links at all, since the ctest link has no plain definition of it. Gates: `cargo fmt --check`, both clippy invocations, the full ctest suite, clang-format on the new glue, all six meson configs, `run_corpus.py --compare` against `build-rs` and `build-rs-chost`, `save_diff.py` byte-identical, `interop_matrix.py` PASS.
- **2026-09-02 M9c T9.1 landed** (`use_rust_net` Pattern B: all fourteen `net_main.c` dispatch funnels flip to `rust/quake-capi/src/net_main.rs` (579 -> 1361 lines), with `Quake/net_main.c` growing the C-side seam (1114 -> 1327), `rust/quake-c-sys/src/net_main.rs` (122) as the extern surface and `rust/quake-ctest/stubs/net_main_glue_ref.c` (201) as the ctest-link mirror). Ported by one Opus-high agent against a main-session contract; integration, the shared-file writes and every gate in the main session.
  - *Plan contradiction found and resolved -- the milestone's substantive finding.* T9.1 above says the funnels are "legal now that all strata beneath are statusized." That is only half true. **Four raise-capable vtable seams still sit beneath five of the fourteen funnels**: `NET_GetMessage`:703 -> `sfunc.QGetMessage` -> `Datagram_GetMessage`, which `Host_Error`s on `RUST_DGRM_NET_MESSAGE_OVERFLOW` (`net_dgrm_glue.c:117-121`); `NET_GetServerMessage`:761 -> `QGetAnyMessage` -> `Datagram_GetAnyMessage`; `NET_Connect`:548 -> `dfunc.Connect` -> `Datagram_Connect`; `NET_Init`:974 -> `net_drivers[l].Init` -> `Datagram_Init`, plus its three `Cvar_RegisterVariable` and four `Cmd_AddCommand` calls, which reach the Rust cvar registry under `-Duse_rust_cvar`; and `NET_SendToAll`:890 transitively through `NET_GetMessage` (`net_main.c:939,:954`). Verified **non**-raising and left unguarded: `Datagram_CheckNewConnections`, `Datagram_SendMessage`/`SendUnreliableMessage`/`CanSendMessage`/`ProcessPacket`, `Datagram_Shutdown`/`Close`/`Listen`/`SearchForHosts`/`QueryAddresses`, and all thirteen `Loop_*` slots (`net_loop.c` contains no `Host_Error`/`Host_Reraise`).
  - *Resolution: all fourteen still flip.* A funnel whose vtable callee can raise is flippable provided the **vtable call itself** gets its own `Host_Guard` trampoline in C. Five were added (`NetMain_Glue_DriverConnect`, `_QGetMessage`, `_QGetAnyMessage`, `_DriverInit`, `_RegisterNetVars`), each an argument-struct trampoline in a pure C frame reading `net_driverlevel`/`sock` ambiently exactly as the C loop headers did; the Rust core returns a status and the thin C wrapper issues `Host_Reraise`. This moves the raise point *up* one frame with the Rust core already returned, so the post-flip arrangement is strictly safer than the pre-flip one, not merely equivalent. No plan-order change: T9.1 stays in M9, and this is a widening of its content, not of its scope.
  - *Four new unconditional `NetMain_*` accessors* (`ClsDedicated`, `SetHostClient`, `HostClientActive`, `HostClientConnection`) exist because `server.h`/`client.h` are not bindgen-clean and the net stratum must also build on `build-rs-chost`, where `use_rust_host` is off. `host_client` is assigned at exactly the points the C `for`-headers assigned it, so the value it is left on after each loop is the one the C left behind.
  - *`NET_SendToAll` bounds decision, applying STOP B carry item (b).* C sizes `msg_init`/`msg_sent` as `qboolean [MAX_SCOREBOARD]` but indexes them with `i < svs.maxclients`. Both writers of `svs.maxclients` clamp it -- `Host_FindMaxClients` (`host.c:386-389`) to `[1, MAX_SCOREBOARD]`, `MaxPlayers_f` (`net_main.c:334-336`) to `svs.maxclientslimit`, which `host.c:392` sets to `MAX_SCOREBOARD` -- so the C arrays are never overrun, the Rust bounds check on `[bool; MAX_SCOREBOARD]` is unreachable, and there is **no** latent defect to reproduce. This is the opposite call to `sv_main.c:1019`'s, and deliberately so: (b)'s rule is that a defect staying inside one allocation must be reproduced and only a genuine past-the-end write earns the abort exception; here there is no defect at all.
  - *`Slist_Send`/`Slist_Poll` are `#ifndef USE_RUST_NET`-guarded out rather than wrapped.* They are `static`, and their only referents -- the two `PollProcedure` initialisers -- are Rust-owned after the flip, so a C wrapper would be unreferenced and trip `-Wunused-function`. `slistStartTime`, `slistActiveTime` and `pollProcedureList` move to Rust module state under the same guard.
  - *cbindgen.* Eight of the fourteen entry points name `net_defs.h` types (`qsocket_t`, `qboolean`, `PollProcedure`) that cbindgen would spell with the Rust mirror names, so they are hand-declared in `cbindgen.toml`'s `__NET_DEFS_H` block and added to `[export] exclude`, matching the `rust_loop_`/`rust_dgrm_` precedent. The other six use only plain C types and stay generated.
  - *Gates (all measured this session, Windows x64):* `cargo fmt --all --check` clean; `cargo clippy --workspace --all-targets --locked -- -D warnings` and `-p quake-capi --features net --locked` both exit 0 with no output; full `cargo test -p quake-ctest --locked` green (`0 failed` on every binary, 74 test executables linked); `clang-format --dry-run --Werror` clean on `Quake/net_main.c` and the new mirror; **all six meson configs build** (`build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace`); `run_corpus.py --compare` **11 ran / 8 skipped / 0 failed, every entry `identical`** against both `build-rs` and `build-rs-chost`; `save_diff.py` both saves byte-identical (95091 / 95075 bytes); `interop_matrix.py` **PASS** across all 24 cells (6 protocol configs x C/C, Csv/Rcl, Rsv/Ccl, R/R). **Not run:** `check_headers.sh` and `check_capi_signatures.sh` (Linux/CI only, no local `cc`) -- the latter is the one uncovered check for this commit, and it is precisely what validates the eight new hand-written cbindgen declarations.
  - *Diagnostic worth not re-deriving:* `run_corpus.py --vkquake` takes an **executable path, not a build directory**. Passing `build-c` makes `CreateProcess` fail with `PermissionError: [WinError 5]` from `run_demo.py:135`, which the corpus reports as `RUN FAILED: <name> (no hash produced)` for every entry -- a harness-invocation error that looks exactly like a total engine regression.
  - Carried forward: **(qq)** T9.0 remains scoped out, so neither `net_main.c` nor `net_dgrm.c` is in `rust/quake-ctest/build.rs`'s `C_SOURCES`. The slist/master-query, `Test_f`/`Test2_f`, `NET_Ban_f`, rcon, heartbeat and now the five M9c guarded raise paths are covered by **no differential** -- their success paths are unobservable in the harness, exactly as with `SvUser_Glue_DropClient`. M9a is the standing proof that this gap hides real defects.
- **2026-09-02 M9d landed** (`use_rust_progs` Pattern A + Pattern C, the first half of T9.3: the `pr_edict_arena` view->owner flip plus the pr_ext zone builtins). `Quake/pr_edict_arena.c` leaves the unconditional `srcs` list and is replaced under `-Duse_rust_progs` by `Quake/pr_edict_arena_glue.c` (272) + `rust/quake-capi/src/pr_edict_arena.rs` (604) + `rust/quake-c-sys/src/pr_edict_arena.rs` (33), with `rust/quake-ctest/stubs/pr_edict_arena_glue_ref.c` (64) as the ctest-link mirror; `PF_strzone`/`PF_strunzone`/`PR_UnzoneAll` flip via `PF_RSH`/`RUST_PF` into `rust/quake-capi/src/progs_builtins_zone.rs` (290). Executed as two Opus-high agents against disjoint file sets (the three new capi/c-sys module stubs and their `pub mod` lines were pre-created by the main session so no shared file had two writers); integration, the build-file edits and every gate in the main session.
  - *T9.3 is split into three sub-milestones.* M9d is the arena flip + zone builtins (this entry). **M9e** is the `net_message`/`msg_readcount`/`msg_badread` storage move out of `Quake/net_msg_glue.c:38-39`. **M9f** is the remaining pr_ext Phase-7 builtin long tail (`ROADMAP.md:158`). A fourth, **M9g**, is now known to be required -- see the ADR-007 note below. The split is a decomposition of T9.3's content, not a scope change.
  - *Net: **row 29 does not fully close at M9d**, so the Phase 7 exit criterion "all four closed" is not met by this milestone alone.* ADR-007's combined "ambient qcvm + pr_global_struct + edict array" row is split in two, because its halves close at different points. The **edict-array + string-table half closes here**: `pr_edict_arena.c` is out of the build and the free list, the free-list rebuild, the known-string table and the string allocator are Rust decisions. It closes on *decision* ownership, not allocator ownership -- the backing memory stays on `Mem_Alloc`/`Mem_Free` on both sides by ADR-013 design, which is not a defect and not a deferral. The **ambient `qcvm`/`pr_global_struct` pointer half stays open**: moving that storage requires editing `rust/quake-capi/cbindgen.toml` (whose `[export] exclude` already records that a cbindgen declaration of Rust-owned storage "would be both a redeclaration and a syntax error", the same treatment `sv`/`svs`/`cl`/`cls` got) and `rust/quake-capi/src/progs_load.rs`. Neither file was owned by either agent, so the move was **blocked and reported, not forced** -- the correct outcome. It becomes **M9g**, and Phase 7 exit now requires **five** ADR-007 rows closed, not four. The row's old "embedded in `sv`/`cl`" phrasing was struck as stale (those closed at M6/M7) and its `pr_edict_glue.c` file reference corrected to `Quake/pr_edict_load_glue.c:49-50` (`Quake/pr_edict_load.c:34-35` in the switches-off leg).
  - *`PR_SetEngineString` is not raise-capable, and that resolves a standing inconsistency rather than exposing a bug.* Its only `Host_Error` (`pr_edict_arena.c:351`) sits inside `#if 0`; the live `#else` branch just returns an offset. The plain externs at `rust/quake-c-sys/src/progs_builtins_sv.rs:37` and `rust/quake-c-sys/src/sv_main.rs:245` are therefore already correct, and the guarded seams `ClMain_Glue_PRSetEngineString` / `Host_Glue_PRSetEngineString` are redundant-but-safe. No edit made. `PR_GetString` by contrast **is** raise-capable (live `Host_Error` at `:315`; the `:322` one is dead code after an unconditional `return`), and there is no plain `PR_GetString` extern anywhere in `quake-c-sys` -- every Rust caller already routes through a guarded seam, so that side of the tree was already consistent too.
  - *Six ADR-009 trampolines* (`ED_Alloc`, `ED_Free`, `ED_RemoveFromFreeList`, `ED_CheckFreeList`, `ED_RebuildFreeList`, `PR_GetString`), all rule-3 shaped: the Rust core returns a status plus a detail int and the C frame issues the original `Host_Error` message. `ED_AddToFreeList`'s two debug-only preconditions are evaluated **between** `ED_Free`'s field writes and the free-list add, which is why `rust/quake-progs/src/alloc.rs` splits `ed_free` into `ed_free_fields` + a recomposed `ed_free` rather than porting it as one unit.
  - *`ED_freetime_compare_func` and its `qsort` stay C*, behind `PREdictArena_Glue_SortFreeEdicts`. The comparator is `(int)copysign (1.0, a - b)`, which is never 0 and therefore inconsistent, so qsort's tie ordering is implementation-defined and entity numbering is observable: the same `qsort` and the same comparator must keep deciding it (ADR-010). The consequence worth carrying is that **there is no Rust `copysign` call site at all** in this flip. `EDICT_NUM_NO_CHECK` is used inside the comparator deliberately -- `EDICT_NUM`'s bounds check is unconditional, and a raise out of a qsort comparator would longjmp through libc; the Rust caller range-checks every number against `max_edicts` first.
  - *Two hoisted bounds checks are COMPAT carve-outs with reachability proofs*: `free_list.size <= free edicts <= num_edicts <= max_edicts <= MAX_EDICTS`, and `max_edicts` is CLAMPed at `sv_main.c:951`, `host.c:963` and `cl_main.c:146`. Seven `COMPAT` markers in `pr_edict_arena.rs`, two in `progs_builtins_zone.rs`.
  - *`PR_UnzoneAll` needs an **unguarded** bitmap read* (`knownzone_test_raw`, new in `rust/quake-progs/src/arena.rs`). Its `while (qcvm->knownzonesize-- > 0)` post-decrements before the body, so `id` equals the just-decremented size and the existing `knownzone_test`'s `id < knownzonesize` guard would reject **every live bit** -- a naive port silently leaks every zoned string. `PF_strunzone` keeps the guarded `knownzone_test`, matching C.
  - *Main-session decision: the arena module's cfg was widened* from `#[cfg(feature = "progs")]` to `#[cfg(any(feature = "progs", feature = "progs-host"))]` (`rust/quake-capi/src/lib.rs:69`), matching the `progs_parse` precedent. `rust/quake-ctest/Cargo.toml` enables `progs-host` but not `progs` on quake-capi, so without this the new module and its mirror would have been type-checked but **never link-checked** (lesson (ff)); widening the cfg was the correct fix rather than editing the ctest manifest.
  - *Two sanctioned ownership deviations by the zone agent*, both minimal and both preferable to the alternative: it edited `Quake/pr_cmds_glue.c` (`RUST_PF` lives there and the file is already in `meson.build`, so the alternative was a forbidden `meson.build` edit) and `rust/quake-ctest/stubs/stubs.c` (a 12-line un-renamed `PR_ClearEngineString` forwarder mirroring the existing `PR_SetEngineString` pair at `stubs.c:5997-6007`; `c_ref_prelude.h:616` renames the symbol when ctest compiles `pr_edict_arena.c`, so this forwarder is the only way the mandated ctest gate links).
  - *No `Quake/common.make` change.* `pr_edict_arena.o` stays listed (`common.make:264`) -- the MinGW makefile is the C-only build and glue files are Meson-only, the same reasoning as M9b.
  - *Differential coverage is asymmetric, and this is the milestone's standing risk.* `Quake/pr_edict_arena.c` **is** in `build.rs`'s `C_SOURCES` (`:30`), so the arena half has a real oracle (`tests/progs_arena_differential.rs`). `Quake/pr_ext.c` is **not**, so the zone half has **no differential** -- its coverage is corpus + savegame only, and a zoned-string regression (FTE/CSQC mods using `strzone`) is the first suspect for any future failure here. Also unreproduced, matching the existing `pr_edict_parse_glue.c` treatment: the debug-only integrity assertions inside `EDICT_NUM`/`NUM_FOR_EDICT` (`pr_edict.c:1163-1168`, `:1175-1181`, `:1191-1193`). `PF_strzone`/`PF_strunzone`/`PR_UnzoneAll` are `static` and now unreferenced under `USE_RUST_HOST`; the six-config build confirms that is tolerated, as it already is for the landed M5 `PF_RSH` flips in the same file.
  - *Gates (all measured this session, Windows x64):* `cargo fmt --all --check` clean; `cargo clippy --workspace --all-targets --locked -- -D warnings` and `-p quake-capi --features progs --all-targets --locked` both exit 0 with no output; `cargo test -p quake-ctest --locked --no-run` links with no unresolved symbols and the full run is green (`test result: ok`, **0 failed**, every binary); `clang-format --dry-run --Werror` clean on `Quake/pr_edict_arena_glue.c`, `Quake/pr_ext.c` and `Quake/pr_cmds_glue.c`; line-ending audit clean; **all six meson configs build** (EXIT=0 each, only the pre-existing `WARN: Skip quake-capi::RAISE_CONNECT_FAILED / PRBI_ERR_PROGRAM_ERROR - (not pub)`); `run_corpus.py --compare` **11 ran / 8 skipped / 0 failed, every entry `identical`** against all three Rust legs (`build-rs`, `build-rs-chost`, `build-rs-cprogs`); `save_diff.py` byte-identical against `build-rs` and `build-rs-cprogs` (95091 / 95075 bytes); `scratchpad/save_crossload.py` **OK: 4 directions agree on the post-load state-hash chain** (95214-byte saves, 9540-byte chains) -- the strongest single check for this milestone, since it exercises `ED_Alloc`, free-list ordering, engine strings and `PR_SetEngineString` across the C/Rust boundary in both the write and the read direction; `physics_matrix.py` **84 ok / 0 skipped / 0 failed** (12 cells x 7 entries); `interop_matrix.py` **PASS** (24 cells). **Not run:** `check_headers.sh` and `check_capi_signatures.sh` (Linux/CI only, no local `cc`).
  - *Still owed by M9 after this entry:* M9e (net message storage), M9f (pr_ext builtin long tail), M9g (the `qcvm`/`pr_global_struct` pointer move), and the M9 Gates line's `netreplay_diff.py` and **first full-length soak in the final NET config** (100k server frames x 8 cells). The Gates line's "save byte-diff suite re-run post-arena-flip" is **done** -- it is the `save_diff.py` + `save_crossload.py` evidence above, run after the flip.
- **2026-09-02 M9e landed** (the second half of T9.3's ADR-007 obligation: the `net_message`/`msg_readcount`/`msg_badread` storage move). No port, no new file -- six files change and the net diff is three `#[no_mangle] pub static mut` definitions in `rust/quake-capi/src/net.rs` against two deletions and one `#ifndef` on the C side. Executed entirely in the main session: it is small, and every file it touches is either a shared build/oracle file the plan reserves to the main session or a two-line edit inside one, so there was no parallelism to delegate.
  - *What made it a one-sitting milestone rather than a port.* The `net` cargo feature tracks `use_rust_net` exactly (`meson.build:426-428`), so the Rust definitions are gated to precisely the leg where the C definitions disappear -- no third state exists. And `rust/quake-c-sys`'s generated bindings only ever **declared** the trio, so `c::net_message` and friends resolve to the new definitions with **no Rust call site changed anywhere**. Likewise `net.h:44` and `common.h` keep their `extern` declarations, so cl_parse.c, cl_demo.c, harness.c, net_dgrm.c, net_loop.c, net_main.c and sv_user.c are all untouched. This is the same ADR-007 storage-move shape as M6 (`sv`/`svs`) and M7 (`cl`/`cls`).
  - *`net_message` is zero-initialised on purpose and that is not a shortcut.* `unsafe { core::mem::zeroed() }` is sound for `sizebuf_t` -- every field is a `bool`, a raw pointer or an `int` -- and it is what C already did: C's definition was a plain tentative definition, i.e. zero-initialised too. `NET_Init` still calls `SZ_Alloc` on it from C, so `data`/`maxsize` are filled in exactly as before.
  - *The failure worth not re-deriving: `rust/quake-ctest/src/net_stubs.rs` was defining the trio itself.* `cargo test -p quake-ctest --locked --no-run` failed with `LNK2005: net_message / msg_readcount / msg_badread already defined` (plus their `__imp_` twins) and `LNK1169` across `host_differential`, `md3_differential`, `sv_main_differential` and `world_differential`. The first hypothesis -- stale `cc` objects, or the C definitions in `stubs/sv_user_ref.c` -- was **wrong**: the linker names `libquake_ctest-*.rcgu.o`, a *Rust* codegen unit. The ctest link enables quake-capi's `net` feature, so the harness's own `#[no_mangle] static mut` copies now collide with quake-capi's. Fixed by replacing them with `pub use quake_rs::net::{msg_badread, msg_readcount, net_message};`, which keeps the `net_stubs::net_message` path `tests/net_loop_differential.rs:84,155` already uses. The general rule this instances: **a harness-owned `#[no_mangle]` static is only safe while no enabled feature of quake-capi defines the same symbol**; every future ADR-007 storage move must check `rust/quake-ctest/src/*_stubs.rs` for a twin, not just the C side.
  - *A latent Linux/CI break was fixed in passing.* `rust/quake-ctest/stubs/sv_user_ref.c:77-79` defined the same trio. Those were **tentative** definitions, so MSVC merged them as COFF common symbols and they were not part of the failure above -- but under Linux's `-fno-common` (GCC 10+) they would have been a hard duplicate-symbol error against the new Rust definitions. Converted to `extern` declarations. This is exactly the defect class M5 hit, and it would not have been visible on any local gate; it is the strongest argument this milestone produces for the unpushed-CI risk recorded below.
  - *The three `#undef`s above those declarations stay.* They are the already-sanctioned storage category (`sv_user_ref.c:77-79`), not the callee-spelling rule: the trio must bind to the *plain* symbols quake-capi defines, not to `c_ref_prelude.h`'s renamed twins, because the Rust MSG readers the c_ref `SV_ReadClientMove`/`SV_ReadClientMessage` path consumes are the plain ones.
  - *cbindgen.* All three symbols are added to `rust/quake-capi/cbindgen.toml`'s `[export] exclude`, matching the `sv`/`svs`/`cl`/`cls` precedent: `net.h`/`common.h` already declare them and every C reader includes those headers, so a generated declaration would be a redeclaration -- and `net_message`'s type is quake-c-sys's bindgen `sizebuf_t`, which has no spelling in `quake_rs.h` at all. **`check_capi_signatures.sh` is the check that validates this and it cannot run locally** (Linux/CI only, no local `cc`).
  - *One incidental reformat.* `Quake/net_main.c:83`'s `int net_activeconnections = 0;` loses its tab alignment, because clang-format aligned it with the `sizebuf_t net_message;` that is now inside a preprocessor block. `clang-format -i` produced exactly that one-line change and nothing else.
  - *ADR-007 row 27 closed.* Four of the five Phase 7 rows are now closed (M6, M7, M9d, M9e); the ambient `qcvm`/`pr_global_struct` pointer pair is the only one left and stays scheduled for **M9g**.
  - *Gates (all measured this session, Windows x64):* `cargo fmt --all -- --check` clean; `cargo clippy --workspace --all-targets --locked -- -D warnings` and `-p quake-capi --features net --all-targets --locked` both exit 0 with no output; `cargo test -p quake-ctest --locked --no-run` links after the fix, and the full run is green (**73 binaries `test result: ok`, 0 failed**); `clang-format --dry-run --Werror` clean on `Quake/net_main.c` and `Quake/net_msg_glue.c`; line-ending audit clean (`core.autocrlf=true`, diffs localised, no whole-file churn); **all six meson configs build EXIT=0** -- `build-c` and `build-c-trace` matter most here, since they are the `-Duse_rust_net=disabled` leg where `net_main.c`'s `#ifndef USE_RUST_NET` definition must still be live; `run_corpus.py --compare` **11 ran / 8 skipped / 0 failed** against `build-rs`, `build-rs-chost` and `build-rs-cprogs`; `save_diff.py` byte-identical against `build-rs` and `build-rs-cprogs` (95091 / 95075); `scratchpad/save_crossload.py` **OK: 4 directions agree**; `physics_matrix.py` **84 ok / 0 failed**; `interop_matrix.py` **PASS** (24 cells). **Not run:** `check_headers.sh` and `check_capi_signatures.sh` (Linux/CI only).
  - *Still owed by M9 after this entry:* **M9f** (the pr_ext builtin long tail, `ROADMAP.md:158`), **M9g** (the `qcvm`/`pr_global_struct` pointer move), and the M9 Gates line's `netreplay_diff.py` and **first full-length soak in the final NET config** (100k server frames x 8 cells).
- **2026-09-02 M9f landed** (T9.3's third sub-milestone: the pr_ext Phase-7 builtin long tail, `ROADMAP.md:158`). Three commits: `249e47f0` puts `Quake/pr_ext.c` into the ctest oracle, `f09c90bb` pre-creates the five capi modules and their `pub mod` lines, and this commit is the port itself -- **52 `builtin_t` slots** flipped by `PF_RSH`/`RUST_PF` (Pattern C) into five new/grown modules totalling ~5450 lines of Rust, against 5 new ctest differential binaries carrying **135 tests**. Executed as five Opus-high agents against disjoint file sets; integration, the shared-file writes (`Quake/pr_ext.c`, `Quake/pr_cmds_glue.c`, `rust/quake-capi/src/lib.rs`, `cbindgen.toml`, `stubs/pr_ext_ref.c`, `stubs/stubs.c`) and every gate in the main session.
  - *The five groups.* **A** `progs_builtins_sprintf.rs` (901) -- `PF_sprintf_internal` + `PF_sprintf` (`pr_ext.c:1110-1589`); **B** `progs_builtins_strext.rs` (1157) -- strconv, infoadd/infoget, the qc tokenizer (`Tokenize`/`ArgC`/`ArgV`/`tokenize_console`/`tokenizebyseparator`/`argv_start_index`/`argv_end_index`), strftime, stov, whichpack, buf_cvarlist; **C** `progs_builtins_filebuf.rs` (1336) -- the FRIK_FILE five plus the twelve `strbuflist` builtins and the two `PR_ShutdownExtensions` teardowns; **D** `progs_builtins_te.rs` (1239) -- 33 bodies (3 `SV_StartParticle` builtins, 16 `PF_sv_te_*` writers, 14 `PF_cl_te_*` effects); **E** `progs_builtins_particles.rs` (816) -- `particleeffectnum`/`trailparticles`/`pointparticles`, sv and cl. Groups A-C ride `use_rust_progs`; **D and E are `host`-gated**, because every body reaches `sv`/`cl` storage.
  - *Group A's slot is deliberately NOT flipped.* `progs_builtins_sprintf.rs` is ported and compiled, but the `builtin_t` row still names `PF_sprintf`, and `Quake/pr_ext.c` carries a comment saying so. ADR-005's `quake_util::printf` implements `d i u x X f F s c` and *deliberately* omits `%e`/`%E`/`%g`/`%G` and octal; `printf.rs:211` panics on them, and every profile is `panic = "abort"`, so flipping the slot would turn an attacker-controlled QC format string into a process abort. **Carry item: extend the ADR-005 formatter (and its conformance suite) before the sprintf slot flips.** Porting the body now was still worth it -- it is the differential subject that proves the rest of the grammar (positional `%N$`, `*` width/precision, flags, length modifiers) matches.
  - *Two new PRBI status codes.* `PRBI_ERR_SV_PARTICLEEFFECTNUM_OVERFLOW` (9) and `PRBI_ERR_CL_PARTICLEEFFECTNUM_OVERFLOW` (10) at `pr_cmds_glue.c:36-48`, both re-issued by `PRBI_Raise` as the exact C text through `PR_RunError` -- ADR-009 rule 2, not rule 3. Note the observability consequence the differential had to be written around: `PR_RunError` (`pr_exec.c:191-207`) does `Con_Printf` *then* `Host_Error ("Program error")`, so `ctest_host_error_message()` reads `"Program error"` on every one of them and the real message is only visible on the console channel.
  - *A real `USE_RUST_HOST` crash found by the group E differential, not an oracle artifact.* `ClMain_InvokeParticleTrail` (`Quake/cl_main_glue.c:515`) unconditionally dereferenced `a->axis`, but `quake_rs_pf_cl_trailparticles` passes a null axis -- exactly as C's `PF_cl_trailparticles` (`pr_ext.c:4928`) passes `NULL`, which `r_part_fte.c:5075` accepts. The differential faulted with `STATUS_ACCESS_VIOLATION`; the fix is a null guard in the **production** glue and in the ctest mirror `stubs/cl_main_ref.c`. This path is only reachable from a CSQC mod calling `trailparticles`, so no corpus entry, save diff or soak cell could have reached it -- **a gate gap, recorded as such, not a gate failure**.
  - *A vacuous differential caught while fixing the above.* `PF_multicast_internal` (`pr_ext.c:4276-4318`) with a non-`NULL` `requireext2` and no PVS writes `svs.clients[i].datagram` when unreliable and only `.message` when reliable. `PF_sv_trailparticles` uses `MULTICAST_PHS_U` and `PF_sv_pointparticles` `MULTICAST_PVS_U` (which lands in the same `!pvs` branch here, because the stub `Mod_LeafPVS` returns NULL), so both are unreliable -- and the tests were asserting on `.message`, a buffer those paths never touch. Six assertions were comparing two always-empty vectors. The fixture grew `ctest_m9fe_client_datagram_len/byte` and `Obs` grew `client0_dg`/`client1_dg`; only then does the wire-byte comparison mean anything.
  - *A defect class, generalised.* `ctest_m9fe_reset` clears **both** storages and the console log, so any fixture readback taken *after* the both-sides loop is worthless -- the C-side value seen at the end is only whatever the Rust iteration's `setup()` re-seeded. Seven tests had that shape. `run_both_probed` (a `run_both` that takes a per-side `probe` closure run immediately after that side's invocation) makes the correct shape the easy one; the two overflow tests capture console and `Host_Error` per side the same way, and now also assert *negatively* that the Rust side does **not** print the C message.
  - *Oracle mechanics worth carrying.* `stubs/pr_ext_ref.c` is a composing TU: a 19-entry `#define <name> c_ref_<name>` block, `#include "quakedef.h"`, then `#include "pr_ext.c"`, then the fixtures after per-group `#undef`s (`sv`/`svs` for D, plus `cl`/`pr_checkextension` for E) so the appended code sees the plain Rust-owned spellings while the included bodies see `c_ref_*`. The ctest link does **not** define `USE_RUST_HOST`, so `PRExt_Glue_SVMulticast` (`pr_ext.c:4388`) and `PRExt_Glue_EffectinfoEnumerate` (`:4715`) have to be mirrored by hand -- implemented as aliasing trampolines that swap the Rust sizebufs and client array into `c_ref_sv`/`c_ref_svs` for the duration of the guarded call. `c_ref_prelude.h` also grew compile-only approximations of `cb_context_s`, `vulkanglobals_t` and `gltexture_s`; **Phase 8 must replace those with the real headers.**
  - *Coverage note.* `ROADMAP.md:158` already records that none of id1/hipnotic/rogue/the re-release calls these extension builtins, so the corpus, trace and save gates say almost nothing about this milestone -- the 135 ctest differentials **are** the gate here, and the group E crash above is the proof of that. Two known holes stay open: the 14 `PF_cl_te_*` bodies are ported but **not** differentially verified (they produce no wire bytes; verifying them needs dlight/particle/sound mocks), and the sprintf body is verified but unflipped.
  - *Gate evidence (current run, `build-c-m9f` / `build-rs-m9f`; the canonical `build-c`/`build-rs` were locked by the running soak and are owed a relink).* `cargo test -p quake-ctest --locked` **1075 passed / 0 failed** over 79 binaries; `cargo fmt --check` and both `clippy -D warnings` invocations clean; `check_ctest_symbols.sh` OK (43 oracle sources); all six meson configs build; `run_corpus.py --compare` C-vs-RS, C-vs-CHOST and C-vs-CPROGS **11 ran / 8 skipped / 0 failed** each; `save_diff.py` identical (95091 / 95075 bytes); `builtin_diff.py` identical (235 entries); `trace_diff.py` identical (331841 records); `record_diff.py` identical (20239 bytes); `netreplay_diff.py` PASS self and C-vs-RS (1193 records, 1402 frames); `interop_matrix.py` **PASS** (24 cells). **Not run:** `check_headers.sh`, `check_capi_signatures.sh` (Linux/CI only) and `physics_matrix.py` (out of scope for a progs-builtin milestone). Local `clang-format` is v22 against `format.sh`'s pinned v18 and is not a valid gate here; the new hunks were checked format-stable by diff instead.
  - *Still owed by M9 after this entry:* **M9g** (the `qcvm`/`pr_global_struct` pointer move, the last open ADR-007 Phase-7 row) and the M9 Gates line's **first full-length soak in the final NET config** (100k server frames x 8 cells) -- note the soak currently in flight is running pre-M9f binaries, so it attests the M9c/M9d/M9e NET+progs configuration, not these builtin flips.
- **2026-09-02 M9g landed** (the last of T9.3): the ambient VM pointers `qcvm` and `pr_global_struct` become Rust-owned storage in `rust/quake-capi/src/progs_load.rs`, **closing the final open ADR-007 Phase-7 row**. Structurally identical to M9e (`f070c347`): a feature-gated `#[no_mangle] pub static mut` pair in Rust, the C definitions removed from `Quake/pr_edict_load_glue.c`, and two `exclude` entries in `rust/quake-capi/cbindgen.toml` so the generated header does not redeclare what `progs.h:433-435` already declares.
  - *Why the gating is exact.* The definitions sit behind quake-capi's `progs` feature, and `meson.build:430` adds that feature if and only if `use_rust_progs` is on -- the same condition under which `meson.build:517` compiles `pr_edict_load_glue.c` *instead of* `pr_edict_load.c`. So the C copies at `pr_edict_load.c:34-35` serve the `-Duse_rust_progs=disabled` oracle leg and the two definitions are never in one binary. `PR_SwitchQCVM` itself stays C: the ADR row is about the storage, and the selector writing Rust-owned storage is the same shape as `NET_Init` calling `SZ_Alloc` on M9e's Rust-owned `net_message`.
  - *No Rust call site changed.* `quake-c-sys` only ever **declared** the pair (`generated.rs:941` for `qcvm`, `sv_main.rs:73` for `pr_global_struct`), so `progs_load.rs`'s own `current_qcvm` and every other `c::qcvm` reader resolves to the new definition unchanged. `globalvars_t` has no bindgen mirror, so `pr_global_struct` is defined as an opaque `*mut c_void`, matching the existing `quake-c-sys` declaration; `progs.h:433` stays the authoritative type for C.
  - *ctest is untouched by design.* `rust/quake-ctest/Cargo.toml:23` enables `progs-host` but **not** `progs`, so `progs_load` is not linked into the oracle and `stubs/stubs.c:3148-3149` keeps its own fixture copies -- no duplicate symbol, unlike M9e where the ctest link did enable the moved feature and its stubs had to become re-exports.
  - *Gate evidence (current run).* All six meson configs build and link (`build-c-m9f`, `build-rs-m9f`, `build-c-trace`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace`); all five generated `quake_rs.h` copies verified free of a `qcvm`/`pr_global_struct` redeclaration; `cargo fmt --check` clean; `clippy -D warnings` clean for the workspace, for `quake-capi --features host,progs-host` and for `quake-capi --features progs`; `cargo test -p quake-ctest --locked` **1075 passed / 0 failed** over 79 binaries; `run_corpus.py --compare` C-vs-RS, C-vs-CHOST, C-vs-CPROGS **11 ran / 8 skipped / 0 failed** each; `save_diff.py` identical (95091 / 95075 bytes); `builtin_diff.py` identical (235 entries); `trace_diff.py` identical (331841 records over e1m1); `record_diff.py` identical (20239 bytes); `netreplay_diff.py` PASS self and C-vs-RS (1193 records); `interop_matrix.py` **PASS** (24 cells). **Not run:** `check_headers.sh` and `check_capi_signatures.sh` (Linux/CI only), `physics_matrix.py` (no physics surface in a pointer move).
  - *M9 Gates: four of five discharged at landing, the fifth recorded below.* Every ADR-007 Phase-7 row is shut, and `netreplay_diff.py` PASS, the interop full matrix PASS and the savegame byte-diff re-run after the M9d arena flip are all clean. The fifth item -- the first full-length soak in the final NET config -- is **still running** and must be recorded when it finishes: launched 2026-09-02 07:46 (100k server frames x 8 cells plus 2 C/C reference streams, ports 26100-26109, hash interval 64), and at 11:08 it was on the `Rsv/Ccl` cell at port 26104. Sizing, so a later reader can tell progress from a stall: `interop_matrix.py:175` paces both processes at `SYS_TICRATE_DEFAULT = 0.025`, so a 100k-frame cell is ~42 minutes of wall time and the ten cells are ~7 hours end to end. No `soak-results` directory has appeared at the `--results-dir` the run was given (the scratchpad, **not** the repo-root default), and `interop_matrix.py:602-759` writes that directory only on a detected desync -- so no cell has failed so far, but absence of that directory is only a pass once the run has actually exited.
  - *M9 Gates, fifth item: the full-length soak **PASSED**, recorded 2026-09-02.* It exited 0 after ~7h with all **eight** cells green -- `Base-666` and `FTE+999` x `C/C`, `Csv/Rcl`, `Rsv/Ccl`, `R/R` -- each reaching **1563 checkpoints** over 100001 hashed frames with traffic in band, against two C/C reference streams (`rel 3/4 unrel 99892/99988` at Base-666, `rel 4/5 unrel 99908/99988` at FTE+999). No `soak-results` directory was written, i.e. no desync was detected. **Read the result precisely: hash identity is not what this gate attests.** Seven of the eight cells report `hash forks @128 (diagnostic)` -- and so does the **`C/C` baseline cell in both protocols**, which is the whole point: live netplay timing is nondeterministic (the Phase 5 finding, landmine 19), so a fork at frame 128 against the reference stream is expected noise, not divergence, and the pass criterion is checkpoint reachability plus the traffic band plus the absence of any `Host_Error`, protocol error or drop. The one cell that came back `hash-identical` (`FTE+999 Csv/Rcl`) is luck of the timing, not a stronger result. CI cannot attest this run; it is local-only by design.
- **2026-09-02 M10a landed** (gates-first for M10; no porting). The approved M10 Gates line is **materially wrong on two of its four clauses**, established from source before any code was written; M10a replaces them and proves the replacements fire.
  - *Clause 1, `capture_diff.py` on console/menu/sbar-exercising entries -- impossible.* `capture_diff.py` is a `-netcapture` structural differ operating at the NET_* funnel level: it parses both captures, requires the per-direction concatenated **reliable** payload streams to share a >= `--min-reliable-prefix` (16384) common prefix, and requires per-(direction, kind) record counts to agree within `--count-tolerance`. Console, menu and status bar generate **no network traffic**, so that tool cannot observe any of them, at any tolerance.
  - *Clause 3, particle-sim state in capture/record diffs -- impossible twice over.* `grep` over `Quake/harness.c` returned **zero** particle matches, so no state hash observed particle state at all; particles emit no network records either. Worse, the `r_part_fte.c` simulator **never advances under `-headless`**: `PScript_UpdateParticleTypes` (`r_part_fte.c:7320`), `PScript_UpdateParticlesTask` (`:6774`) and `PScript_UpdateParticle` (`:6440`) are dispatched only from `Quake/gl_rmain.c:1209/1212/1293/1295` through the task system, and `host.c:1343-1354` skips the whole renderer branch when `no_rendering` is set (`-headless` **is** `no_rendering`). ADR-016 keeps `tasks.c` in C until Phase 8, so that stays true for the rest of this phase.
  - *Replacement 1: `scripts/harness/condump_diff.py`* (new). Runs a fixed `-harnesscmds` script through two builds and byte-compares the resulting `condump` file. It exercises `Key_Bindlist_f`'s key-name table and dump order, `bind`/`unbind`/`unbindall` effects, `Cmd_Echo_f` argument joining and quoting, and `Con_Printf`'s line wrap. Three determinism arguments make the byte comparison legitimate, all recorded in the script's docstring: (a) `con_linewidth` is permanently **50** headless -- `Con_Init` sets it at `console.c:1068` with the comment that `Con_CheckResize` need not run, and `Con_CheckResize`'s only caller is `gl_screen.c:895` inside the skipped `SCR_UpdateScreen` path, so the console ring geometry is identical in every build; (b) the script issues `clear` first, which excludes the startup banner (`host.c:1322`'s `__DATE__`/`__TIME__` and `host.c:1326`'s staticlib banner) -- otherwise no two builds could ever match; (c) `unbindall` precedes the binds so `bindlist` dumps only script-built state. The script refuses to report success on two empty dumps.
  - *Replacement 2: `Harness_HashParticles`* (`Quake/r_part.c`, declared in `harness.h`, folded into `Harness_HashClient` at the end). Walks `active_particles` hashing pool index, `org`, `vel`, `color`, `ramp`, `die`, `type`, then the count. Traversal order is deterministic without sorting: the allocator is strictly LIFO (spawn takes the `free_particles` head and pushes onto the `active_particles` head) and `CL_RunParticles` unlinks expired particles in place, so the list is always reverse spawn order. `next` and the three list heads are excluded as addresses; `R_RocketTrail`'s **function-local** `tracercount` (`r_part.c:702`) is unreachable from file scope and its only effect -- alternating tracer direction -- is already visible in the `vel` of the particles it spawns. Placement is safe: `CL_RunParticles` runs at `host.c:1203`, before `Harness_Frame` at `:1231`, so the hash sees post-advance state, and the walker no-ops before `R_InitParticles` (dedicated servers never call it).
  - *Non-vacuity, measured -- the M1 precedent that a detector which has never fired is not a gate.* A first attempt to prove the term live by diffing a with-hash build against a without-hash build was **rejected as worthless by inspection**: the walker folds `count` into the hash even when the list is empty, so every frame differs regardless. A `-particles 512` vs `8192` control was then **invalidated**: pool size perturbs `COM_Rand()` consumption and the RNG state is already hashed, so that control could not isolate the new term either. The decisive measurement was a temporary `Con_Printf` probe on the running peak, since reverted: **peak 3232 simultaneously active particles on `id1-demo1` under `-headless`**. The classic simulator does run headless and the walker observes thousands of live records per frame.
  - *Coverage holes, recorded rather than papered over.* High-bit/coloured console text is unreachable from a script, so `Con_Dump_f`'s `&= 0x7f` masking stays untested. `sbar.c` has **no harness surface whatsoever** -- `Sbar_Init` is one of the calls skipped by `host.c:1354` -- and `menu.c`'s `menu_*` commands are scriptable but no hash observes `m_state`; for both, the ctest differential is the only gate M10 will have, which is a constraint on how thorough M10d/M10e's differentials must be, not a licence to run thinner ones. The `r_part_fte.c` simulator is in the same position for the reason given above.
  - *Golden impact: none additional.* `Misc/harness/README.md:79` records that the existing darwin-arm64 goldens were generated **before Phase 7**, and T1.4 already changed `Harness_HashClient` this phase; M11 regenerates windows-x64 goldens regardless. `run_corpus.py --check` skips entries with no golden without failing.
  - *Gate evidence (current run, Windows x64, this checkout).* `build-c-m9f`, `build-rs-m9f` and `build-rs-chost` all build and link clean with the probe reverted. `condump_diff.py` C-vs-C **identical (340 bytes, 15 lines)** and C-vs-RS **identical (340 bytes)**; fault-injected (one `bind` action changed) the differ **reports the mismatch**, so the detector has fired. `run_corpus.py --compare` C-vs-RS with the particle term live: **11 ran, 8 skipped, 0 failed**, every entry `identical`. `run_corpus.py --stability`: **11 ran, 0 failed**, all `stable` -- which is the check that would have caught an address leaking into the new hash. **Not run:** `check_headers.sh` / `check_capi_signatures.sh` (Linux/CI only); `physics_matrix.py` (no physics surface); the ctest suite (no Rust source changed in M10a).
- **2026-09-02 M10b landed** (T10.2: `Quake/keys.c` -> Rust, Pattern A on `use_rust_host`). `Quake/keys.c` leaves the unconditional `srcs` list and is replaced under `-Duse_rust_host` by `Quake/keys_glue.c` (392) + `rust/quake-capi/src/keys.rs` (1623) + `rust/quake-c-sys/src/keys.rs` (147), with `rust/quake-ctest/stubs/keys_ref.c` (1065) as the composing oracle TU and `rust/quake-ctest/tests/keys_differential.rs` carrying **29 tests**. Ported by one Opus-high agent against a main-session contract; integration, the shared-file writes and every gate in the main session.
  - *The glue owns all fourteen C-visible objects* -- `key_lines`, `key_tabhint`, `key_linepos`, `key_insert`, `key_blinktime`, `edit_line`, `history_line`, `key_dest`, `keybindings[]`, `consolekeys[]`, `menubound[]`, `keydown[]`, `chat_team`, `keynames[]` -- so nothing in the ~40 C translation units that read them has to change. Eleven ADR-009 `Host_Guard` trampolines and eight status-returning entry points cover the raise-capable callees (`Cbuf_AddText`/`Cbuf_InsertText` reaching `Host_Error`, the console/menu dispatch edges); no longjmp crosses a Rust frame.
  - *Six `// COMPAT:` markers*, at `keys.rs:177` (raw-pointer offsetting into `key_lines` to reproduce C's flat 2-D indexing), `:784` (`Key_StringToKeynum`'s `return str[0]` and its char-signedness), `:967` (libc `fprintf` kept rather than the ADR-005 formatter, because config.cfg is a **byte-diff** gate and the C writer's exact digits are the contract), `:1069` (the history reader storing `fgetc`'s `int` into a `char`), `:1094` (a dead `edit_line < CMDLINES` test preserved), `:1615` (`key_game` + `ca_connected` falling through and clearing `forced`).
  - *A real defect the meson build caught, exactly where the agent said the risk was.* Its handoff flagged that the `meson.build` source-selection block was *structurally* copied from the M7 T7.2 block at `meson.build:624-640` but was **unverified by an actual build**. It was: `keys_glue.c` lacked `#include "steam.h"`, and three configs failed with `quake_rs.h(1479): error C2143: syntax error: missing ')' before '*'` -- `quake_rs.h` declares the Phase 2 Steam shims in terms of `steamgame_t`, so every glue TU must include `steam.h` first. `Quake/view_glue.c:44` and `Quake/cl_input_glue.c:45` carry the same line with the same comment. One-line fix; all six configs then link. The ctest evidence is unaffected, because ctest compiles `stubs/keys_ref.c` (which `#include`s `keys.c`), never the glue.
  - *The config.cfg byte-diff gate did not exist and had to be built.* The plan names it in M2's, M8's and M10's Gates lines, and T8.4 explicitly defers its completion (**plan:439** -- "the end-to-end byte-diff completes in M10 when `Key_WriteBindings` leaves C"). There was no such script in `scripts/harness/`. `scripts/harness/config_diff.py` (new) drives a fixed bind/cvar script through two builds under `-headless` and byte-compares the written config. It discharges T8.4's deferred criterion. Two things make it well-formed: `COM_FOpenPrefFile` redirects per-user state into `com_gamedir` when `harness_active`, so the file lands in the disposable staging tree rather than the real pref path; and the runner unlinks any staged config first, so the writer can never follow a link back into the read-only source tree. It refuses to report success if the dump contains zero `bind ` lines.
  - *Two defects in that script, both caught by running it.* It first used `CONFIG_NAME = "config.cfg"`; `quakedef.h:33` defines it as **`"vkQuake.cfg"`**. It then set `gamma`, `viewsize`, `volume` and `_snd_mixahead`, all of which the engine rejected as unknown commands because they are registered in renderer/sound init, which `-headless` skips; they were replaced with archived cvars that are live headless (`m_pitch`, `m_side`, `cl_maxpitch`, `cl_minpitch`, `_cl_name`, `cl_bob`, `sensitivity`, `cl_forwardspeed`). The written file was then **inspected**, not merely diffed: it exercises `Key_WriteBindings` in keynum order over seven bindings (including embedded spaces and an embedded `;`), 44 archived cvars through the float formatter (`0.0200001`, `110.5`, `-2.5`, `0.0625`), and the trailing `vid_restart` / `+mlook`.
  - *Findings the agent reported rather than hid.* It found and fixed a **vacuous fixture**: `ctest_keys_set_consolekey` was a no-op stub while `ctest_keys_reset` zeroes `consolekeys[]`, which made the demo-playback branch unreachable -- real `set_consolekey`/`set_menubound` fixtures replaced it. It also reported two mutations that turned nothing red: trailing whitespace in synthesized `+`/`-` command text is not observable through an argv probe (the tokenizer strips it), and `Key_SetBinding`'s `keynum == -1` early-out is genuinely unobservable. Both are recorded as coverage limits, not as passes.
  - *`Quake/common.make` correctly needs no change.* It lists `keys.o` but contains no glue files and no `USE_RUST`, so the Makefile build stays C-only; landmine 16's "common.make in the same commit" rule applies to C **splits**, not Pattern A flips (the M9b precedent).
  - *CI wiring.* `condump_diff.py` (landed in M10a) was in no workflow, and `config_diff.py` is new. Both are now wired alongside the existing `save_diff.py` steps in `build-linux.yml`, `build-mac.yml` and `build-windows.yml`; the Linux leg additionally runs each against `build-rs-chost`, the only workflow that configures that oracle.
  - *Gate evidence (current run, Windows x64, this checkout).* All **six** meson configs build and link: `build-c-m9f`, `build-rs-m9f`, `build-rs-chost`, `build-rs-cprogs`, `build-c-trace`, `build-rs-trace`. `condump_diff.py` C-vs-RS **identical (340 bytes, 15 lines)** and C-vs-CHOST identical. `config_diff.py` C-vs-C, C-vs-RS and C-vs-CHOST all **identical (989 bytes, 52 lines, 7 bind lines)**, with fault injection detected. `run_corpus.py --compare` C-vs-RS **11 ran, 8 skipped, 0 failed** and C-vs-CHOST the same. `save_diff.py` C-vs-RS **identical**. `check_ctest_symbols.sh` **OK across 43 oracle sources**. `cargo fmt --check` clean; `cargo clippy --workspace --all-targets -- -D warnings` and `cargo clippy -p quake-capi --features host,progs-host` both clean; `cargo test -p quake-ctest --locked` **1104 passed, 0 failed**, of which `keys_differential` is **29 passed, 0 failed**, every one red-tested across three mutation batches. **Not run:** `check_headers.sh` / `check_capi_signatures.sh` (need `cc`, absent locally -- CI covers them); `physics_matrix.py`, `builtin_diff.py`, `trace_diff.py`, `record_diff.py`, `netreplay_diff.py`, `interop_matrix.py` (keys.c has no physics, progs or net surface).
- **2026-09-02 M10c landed** (T10.1: `Quake/console.c` -> Rust, Pattern A on `use_rust_host`). `Quake/console.c` leaves the unconditional `srcs` list and is replaced under `-Duse_rust_host` by `Quake/console_glue.c` (521) + `rust/quake-capi/src/console.rs` (2920) + `rust/quake-c-sys/src/console.rs` (294), with `rust/quake-ctest/stubs/console_ref.c` (1791) as the composing oracle TU and `rust/quake-ctest/tests/console_differential.rs` (2109) carrying **51 tests**. Ported by one Opus-high agent against a main-session contract; integration, the meson flip and every engine-level gate in the main session.
  - *The C/Rust seam was specified up front, not discovered.* Four things stay C in the glue, for structural reasons rather than convenience. (1) **All eight variadic entry points** (`Con_Printf`, `Con_SafePrintf`, `Con_LinkPrintf`, `Con_CenterPrintf`, `Con_Warning`, `Con_DWarning`, `Con_DPrintf`, `Con_DPrintf2`) keep their C signatures and do the `q_vsnprintf` into the same `char msg[MAXPRINTMSG]`, so **libc still produces the console bytes** and the varargs ABI never crosses FFI; the Rust side takes an already-formatted `const char *`. The ADR-005 formatter is deliberately *not* on this path. (2) **`Con_Printf`'s screen-update tail stays C**, `static qboolean inupdate` included, because `SCR_UpdateScreen (false)` reaches `Mod_LoadModel (mod, true)` -> `Host_Error` at `gl_model.c:531`; letting that longjmp unwind a Rust frame is exactly ADR-009 rule 3. The Rust `Con_Print` frame has always returned before the tail runs. (3) **`Con_NotifyBox`** (`console_glue.c:488`) stays entirely C -- a startup-only modal loop over `Sys_SendKeyEvents`/`SCR_UpdateScreen`/`Sys_Sleep`, where a Rust frame buys nothing and costs rule 3. (4) `SDL_SetClipboardText` is reached only through `Console_Glue_SetClipboardText` (`:205`), so no Rust TU names an SDL symbol. The **drawing entry points do port** (`Con_DrawNotify`, `Con_DrawInput`, `Con_DrawSelectionHighlight`, `Con_DrawConsole`) against the existing `Draw_*`/`GL_*` shim surface; the renderer internals stay C until Phase 8 -- only their callers moved.
  - *ADR-009 topology.* Three `Host_Guard` trampolines, all in the glue: `Console_Glue_MenuMain` (`:152`), `Console_Glue_CvarCompletion` (`:171`), `Console_Glue_CmdCompletion` (`:192`). Two status cores with re-raising C wrappers: `Con_ToggleConsole_f` (`:234`) and `Con_TabComplete` (`:241`) -- rule 2, the C frame re-issues C's exact message. `Con_TabComplete`'s recursive autohint call targets the **core**, not the wrapper, so no jump is ever staged across a Rust frame.
  - *`key_tabpartial` moves to the glue* (`console_glue.c:124`). `console.c:1546` is its definition; `keys.c:245` only declares it `extern`, so M10b's `keys_glue.c` correctly did not claim it and there is no duplicate definition in the `use_rust_host` link.
  - *Thirteen `// COMPAT:` markers* in `rust/quake-capi/src/console.rs`. The one worth reading closely is **`:973`, `Con_Quakebar`**: with `len <= 0` the C original stores at `bar[len - 1]` and `bar[len]`, i.e. **before the start of its own `static char bar[42]`**, and the port skips those stores rather than commit UB (ADR-004). Checked rather than assumed -- every skipped store is at a **strictly negative** offset, so none of them can affect the string reachable through the returned pointer, and the returned bytes are identical to C for `len == 0`, for `len < 0`, and for every `len >= 1`. The marker's "this only differs from C on a `con_linewidth <= 0` console" is therefore conservative: **the observable behaviour does not differ at all**. Every in-tree caller passes 40 in any case (`cl_parse.c:1025`, `console.c:1517`/`:1519`/`:2381`/`:2384`, `console_glue.c:494`/`:497`). The other twelve are ordinary quirk preservation: truncating `%` on line indices, `frac *= fabs (frac)` kept in double, `size_t` widenings, platform char signedness, the unbounded `buffer[con_linewidth]` and `spaces[21]` writes fenced with clamps that cannot bite at reachable widths, `strcpy` into `con_lastcenterstring`, `S_LocalSound` under `con_mutex`, the recursive `con_mutex` acquisition across `Con_SafePrintf` (which C already does today), and `con_linewidth`'s permanent 50 under `-headless`.
  - *The oracle link had to be restructured, not just extended.* `stubs.c` defined a plain `Con_Printf` that ~40 other oracle sources assert against. Now that `console.c` is a real oracle TU the plain names belong to `console_ref.c` (the mirror of `console_glue.c`), so `Con_Printf`, `Con_SafePrintf`, `Con_LinkPrintf`, `Con_CenterPrintf`, `Con_Redirect`, `Con_AddToTabList`, `Con_LogCenterPrint`, `Con_Quakebar`, `con_forcedup`, `con_lastcenterstring` and `SDL_SetClipboardText` all moved out of `stubs.c`, with a new non-`va_list` door `ctest_con_appendf` back into the capture log so **every existing assertion still reads the same `[con]` lines**. `Con_DPrintf`, `Con_DPrintf2`, `Con_Warning` and `Con_DWarning` stay stubs, because the glue keeps all four in C and there is no ported half to compare. The evidence that this broke nothing is the full suite: **1155 passed, 0 failed** = the 1104 baseline plus the 51 new, no regressions.
  - *`cbindgen.toml` needed no edit* -- every exported console symbol uses plain C types, so cbindgen emits all of them. That is the **opposite** of M10b's `quake_rs_key_write_bindings`, which needed a hand declaration purely because of its `FILE *`.
  - *Mutation testing: all 51 tests were made to fail*, across 35 mutations in 7 batches, with the source verified byte-clean afterwards. **Four mutations initially survived** and are recorded rather than buried, each replaced with a sharper one: `con_logcenterprint.value != 2.0` -> `!= 3.0` (the test sets the cvar to 1, so both predicates hold); `Con_Quakebar`'s `len.min (40)` -> `min (39)` (the tests run at `con_linewidth == 20`, so the 40 clamp never bites); removing `Con_TabComplete`'s empty-edit-line early return (`BuildTabList`'s own `if (!*partial) return` at `console.c:1861` independently produces the same no-op -- so that test constrains the **pair** of guards, not either one, and this stands as a recorded coverage limit); and dropping the trailing space in `"LOG started on: %s \n"` (the test deliberately checks only the prefix, because the oracle's timestamp is wall-clock). Two further honesty notes carried from the agent: `con_debuglog_without_a_log_file_is_a_noop` detects its defect by **process abort** (`STATUS_STACK_BUFFER_OVERRUN` on `fwrite (NULL)`), not by its assertion; and `parse_command`'s `;` handling is not observable through `con_tabcomplete_parses_the_command_before_the_cursor` -- what that test actually pins is `Con_TabComplete`'s own backward walk over `;`.
  - *Gate evidence (current run, Windows x64, this checkout).* All **six** meson configs build and link clean -- `build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace` (the two `cbindgen WARN: Skip ...` lines are pre-existing). `condump_diff.py` -- **the gate whose subject is exactly this milestone** -- C-vs-RS and C-vs-CHOST both **identical (340 bytes, 15 lines)**. `config_diff.py` both directions **identical (989 bytes, 52 lines, 7 bind lines)**. `run_corpus.py --compare` C-vs-RS and C-vs-CHOST both **11 ran, 8 skipped, 0 failed**, every entry `identical`. `run_corpus.py --stability` on `build-rs` **11 ran, 0 failed**, all `stable`. `save_diff.py` C-vs-RS **identical (95091 / 95075 bytes)**. `check_ctest_symbols.sh` **OK across 43 oracle sources**. `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo clippy -p quake-capi --features host,progs-host` all clean; `cargo test -p quake-ctest --locked` **1155 passed, 0 failed** across 81 binaries.
  - *Not run / unverified, recorded as such.* `check_headers.sh` and `check_capi_signatures.sh` need `cc`, absent on this machine -- CI is the first place cbindgen's plain-named `Con_*`/`LOG_Close` declarations get diffed against `Quake/console.h` by a compiler; they were checked by hand only. `check_ctest_symbols.sh` **does not cover `console.c`**: like `keys.c`, `host.c`, `host_cmd.c` and `pr_ext.c` it is `#include`d into its composing stub rather than listed in `C_SOURCES`, so a missed `c_ref_*` rename would surface only as a duplicate-symbol link error in CI. `physics_matrix.py`, `builtin_diff.py`, `trace_diff.py`, `record_diff.py`, `netreplay_diff.py` and `interop_matrix.py` were not run -- `console.c` has no physics, progs or net surface. The drawing entry points are verified against the ctest draw-call recorder only; no pixels were rendered, and none can be until Phase 8.
- **2026-09-02 M10d landed** (T10.3: `Quake/sbar.c` -> Rust, Pattern A on `use_rust_host`). `Quake/sbar.c` leaves the unconditional `srcs` list and is replaced under `-Duse_rust_host` by `Quake/sbar_glue.c` (283) + `rust/quake-capi/src/sbar.rs` (2455) + `rust/quake-c-sys/src/sbar.rs` (145), with `rust/quake-ctest/stubs/sbar_ref.c` (1141) as the composing oracle TU and `rust/quake-ctest/tests/sbar_differential.rs` (2198) carrying **44 tests**. Ported by one Opus-high agent against a main-session contract; integration, the meson flip and every engine-level gate in the main session.
  - *`-headless` deletes this milestone's entire harness surface, so the ctest differential is the only gate that has an opinion about it.* `host.c:1343-1354` skips `Sbar_Init` along with `VID_Init`/`Draw_Init`/`SCR_Init`/`R_Init`, so the corpus, condump, config and save gates below exercise **zero** sbar code. They are regression evidence that the flip broke nothing else -- not evidence that sbar is correct. That is why this milestone carries a mutation-testing record and the others do not lean on one so heavily.
  - *The seam is four rules, specified before the port.* (1) The glue keeps the **four C-visible objects** `sb_showscores`, `sb_lines`, `fragsort[]` and `scoreboardlines`, because `gl_screen.c` writes and reads `sb_lines` and reads `sb_showscores`, and `pr_ext.c` reads `fragsort`/`scoreboardlines`, all by plain name; everything else `sbar.c` defined at file scope is `static` (the `qpic_t *` handles, `hipweapons[]`, `hudtype`) and moved to Rust. (2) The glue keeps **every plain `Sbar_*` name** as a thin wrapper over a `quake_rs_sbar_*` core -- most of those prototypes mention `cb_context_t *` or `qpic_t *`, neither of which cbindgen can spell, and `stubs/host_ref.c` already defines `Sbar_Init` as an oracle link double. (3) **Five wrappers re-raise**: `Sbar_CSQCCommand`, `Sbar_ShowScores` (`:97`), `Sbar_Glue_DontShowScores` (`:104`), `Sbar_Draw` (`:111`), `Sbar_IntermissionOverlay` (`:118`). (4) Everything else is plain.
  - *ADR-009 topology: no new guard was introduced.* `PR_ExecuteProgram` is the file's only longjmp-capable callee (`sbar.c:82`, `:864`, `:870`, `:1590`), and the Rust side reaches it through the **existing** `Host_Glue_PR_ExecuteProgram` trampoline (`host_glue.c:532`, mirrored at `stubs/host_glue_ref.c:352`). `Host_Reraise` is called only from the glue TU, so no jump crosses a Rust frame -- rule 2, not rule 3. `Draw_CachePic` is *not* on the list: it ends in `Sys_Error` (`gl_draw.c:428`), and `Sys_Error` ends in `exit (1)` (`sys_sdl_win.c:654`), so it is a process exit rather than a longjmp and needs no guard.
  - *A bug-for-bug quirk preserved and asserted.* A raise out of `PR_ExecuteProgram` skips the `PR_SwitchQCVM (NULL)` at `sbar.c:84`/`:874`/`:1591` and leaves the qcvm switched. That is what C does today; three tests pin it, so a future "tidy-up" that adds an unwind-safe restore fails the differential instead of silently changing behaviour.
  - *`Sbar_DontShowScores` is exported as `Sbar_Glue_DontShowScores`* -- it was `static` in `sbar.c`, so there is no C-visible name to preserve and the `Sbar_Glue_` prefix keeps it out of the plain namespace.
  - *Eleven `// COMPAT:` markers* in `rust/quake-capi/src/sbar.rs` (`:286`, `:667`, `:830`, `:846`, `:1121`, `:1296`, `:1700`, `:1793`, `:2025`, `:2163`, `:2300`); `raise!` propagation at `:307`, `:329`, `:350`, `:1345`, `:1359`, `:2360`.
  - *The draw-call recorder had to be promoted before this port could compile.* `console_ref.c` owned both the recorder and the plain `Draw_*`/`GL_*` symbols for the whole ctest link, so `sbar_ref.c` could not have linked beside it. The recorder moved to a new shared `rust/quake-ctest/stubs/draw_ref.c` (256) -- `ctest_draw_record`, the pic registry (`ctest_draw_intern`, FNV-1a name hash, `width = 8 + h % 33`, `height = 8 + (h >> 8) % 17`), the `Draw_Character`/`Draw_String`/`Draw_Fill`/`Draw_Pic`/`GL_SetCanvas`/`pic_ins`/`pic_ovr` doubles and the `Draw_SubPic` UNREACHED double. The promotion was done under a hard constraint: **console's 51 tests unmodified**. `git diff --stat` on `console_differential.rs` is empty and all 51 still pass. One frozen wart came along: console's `Draw_Pic` log line carries no trailing newline, so the shared recorder exposes an opt-in `ctest_draw_set_pic_newline()` rather than normalising a format console's assertions already depend on.
  - *One file was edited outside the agent's may-edit list, disclosed unprompted.* `stubs/pr_ext_ref.c` (+4/-22): the recorder promotion forced `fragsort`/`scoreboardlines` into `sbar_ref.c` and the `Draw_SubPic` double into `draw_ref.c`, both duplicate-symbol fixes with no behavioural content. Accepted.
  - *Two main-session findings the agent could not have made.* (a) `Quake/sbar_glue.c` shipped **without `#include "steam.h"`** -- a hard compile error under `-Duse_rust_host`, because `quake_rs.h:1543,1545` declares the Phase 2 Steam shims in terms of `steamgame_t` and `quakedef.h` does not pull `steam.h` in. Every other glue TU carries the line with the identical trailing comment (`console_glue.c:76`, `keys_glue.c:58`, `view_glue.c:44`, `cl_input_glue.c:45`); the agent cannot run meson, so only the integration build would ever have caught it. (b) All six "equivalent mutant" claims were **re-derived independently rather than accepted**, and all six hold -- `Sbar_SortFrags` (`sbar.c:448-474`) only ever admits named players so both `!s->name[0]` arms are dead C; `scale` in `Sbar_MiniDeathmatchOverlay` is read exactly once by the `glwidth / scale < 512` gate and both 320 and 321 satisfy it; `sbar.c:1502-1503`'s `>` -> `>=` only adds a self-assignment; `total`'s only post-clamp consumers are four `total / 2` expressions and `320/2 == 321/2 == 160`; `solo-left`'s `+1` is truncated by `(left + right) / 2` because both operands are multiples of 8.
  - *Mutation testing: 214 mutations, 208 killed, 6 surviving and all traced equivalent* (`solo-left`, `frags-skipstep`, `mini-clamp`, `mini-skipstep`, `mini-scale`, `io-clamp`). Two **real** coverage gaps were found and closed: `classic-ammo10` (new `classic/ammo11` + `modern/ammo11` rows at `STAT_AMMO == 11`, the only value separating `<= 10` from `<= 11`) and `modern-keyofs` (new `modern/keys-both` with `DRAW_BASE | IT_KEY1 | IT_KEY2` at style 2.0, hud 0 -- the non-hipnotic branch where a second key reads the `offset += 16`). One honest self-correction is recorded rather than laundered: for `io-clamp` the agent built an FNV-1a-solving test, watched it fail to kill the mutation, retraced, and reported the test as **retained but not a kill**.
  - *A tooling defect found and fixed in passing.* `mutate.py`'s classifier under-reported kills: `panic = "abort"` in every profile means a panic crossing an `extern "C"` boundary aborts the process (`STATUS_STACK_BUFFER_OVERRUN`, exit 3221226505) without ever printing a `... FAILED` line. The driver now treats a non-zero exit with zero FAILED lines as a kill; `itoa-sign`, `itoa-sub` and `num-minus` were reclassified from survivor to killed.
  - *Gate evidence (current run, Windows x64, this checkout, main session).* All **six** meson configs build and link clean -- `build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace` (the two `cbindgen WARN: Skip ...` lines are pre-existing). `condump_diff.py` C-vs-RS and C-vs-CHOST both **identical (340 bytes, 15 lines)**. `config_diff.py` both directions **identical (989 bytes, 52 lines, 7 bind lines)**. `run_corpus.py --compare` C-vs-RS and C-vs-CHOST both **11 ran, 8 skipped, 0 failed**, every entry `identical`. `run_corpus.py --stability` on `build-rs` **11 ran, 0 failed**, all `stable`. `save_diff.py` C-vs-RS **identical (95091 / 95075 bytes)**. `check_ctest_symbols.sh` **OK across 43 oracle sources**. `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo clippy -p quake-capi --features host,progs-host` all clean; `cargo test -p quake-ctest --locked` **1199 passed, 0 failed** across 82 binaries (1155 baseline + 44 new).
  - *Not run / unverified, recorded as such.* `check_headers.sh` and `check_capi_signatures.sh` need `cc`, absent on this machine -- CI is the first place cbindgen's declarations get diffed against `Quake/sbar.h` by a compiler. `check_ctest_symbols.sh`'s "43 oracle sources" **does not include `sbar.c`**: like `console.c`, `keys.c`, `host.c`, `host_cmd.c` and `pr_ext.c` it is `#include`d into its composing stub rather than listed in `C_SOURCES`, so a missed `c_ref_*` rename would surface only as a duplicate-symbol link error in CI. `physics_matrix.py`, `builtin_diff.py`, `trace_diff.py`, `record_diff.py`, `netreplay_diff.py` and `interop_matrix.py` were not run -- `sbar.c` has no physics, progs-trace or net surface. No pixels were rendered and none can be until Phase 8; the draw entry points are verified against the recorder only. As noted above, **no engine-level gate exercises sbar at all under `-headless`**.
- **2026-09-03 M10e landed** (T10.4: `Quake/menu.c` -> Rust, Pattern A on `use_rust_host`). `Quake/menu.c` leaves the unconditional `srcs` list and is replaced under `-Duse_rust_host` by `Quake/menu_glue.c` (525) + `rust/quake-capi/src/menu.rs` (8146) + `rust/quake-c-sys/src/menu.rs` (503), with `rust/quake-ctest/stubs/menu_ref.c` (2085) as the composing oracle TU and `rust/quake-ctest/tests/menu_differential.rs` (2762) carrying **51 tests**. The phase's single largest file. Ported by one Opus-high agent across several turns against a main-session contract; integration, the meson flip and every engine-level gate in the main session.
  - *A two-agent split was proposed and declined.* The obvious cut is by menu group, but `M_Draw` and `M_Keydown` are single dispatchers over every group, so the seam would have fallen straight through them and both halves would have had to own the same two functions. One agent resumed across turns keeps the dispatch topology in one head; the cost is wall-clock, not correctness.
  - *The seam is four rules, specified before the port -- and rule 1 was specified **wrong**.* (1) The glue keeps the C-visible objects `menu.c` defined. I originally named **six**; the correct answer is **eight**. My globals regex used the character class `[A-Za-z0-9_ \*]`, which does not contain a tab, so the **tab-aligned** declarations of `m_return_onerror` and `m_return_reason[32]` (`menu.c:92-93`) were silently skipped. Both are read and written by the already-Rust `net_dgrm_orch` (`quake-c-sys/src/net_dgrm_orch.rs:246,253`), so moving them to Rust would have been an undefined-symbol link failure **inside `quake-capi` itself**. The agent caught it, I verified it against the tree, and the contract was amended before any code was written. The final eight are `m_state`, `m_return_state`, `m_entersound`, `m_is_quitting`, `vid_menucmdfn`, `vid_menukeyfn`, `m_return_onerror`, `m_return_reason[]`. (2) Every plain `M_*` name stays in the glue as a wrapper over a `quake_rs_menu_*` core -- the M10d reason, `cb_context_t *` and `qpic_t *` are unspellable by cbindgen. (3) **Four wrappers re-raise**: `M_ToggleMenu_f` (`:310`), `M_UpdateMouse` (`:318`), `M_Draw` (`:326`), `M_Keydown` (`:334`). (4) Everything else is plain.
  - *ADR-009: ten `Host_Guard` trampolines, and one trap the recon existed to find.* `Con_ToggleConsole_f` has been a **re-raising wrapper** since M10c (`console_glue.c:234` calls `Host_Reraise`), so calling it from a Rust frame is a straight rule-3 violation -- a discovery-based port would very likely have walked into it, because the C source line reads like an ordinary void call. It is trampolined like the rest. `Host_Glue_NET_Poll` (`host_glue.c:423`, already consumed at `host.rs:1667`) is **reused** rather than duplicated; my contract had specified a redundant new one and the agent said so. `NET_Slist_f` was traced and does **not** raise, so it needs no guard.
  - *Two accessors route through the glue.* `Menu_Glue_*` wrappers expose `vulkan_globals` and `SDL_GetMouseState` on the `Console_Glue_SetClipboardText` precedent. Note that the "no Rust TU may name an SDL symbol" rule I had been repeating is **overstated** -- `quake-c-sys/src/host.rs:273` already declares `SDL_Delay`. It is a strong convention, not an invariant; recorded so the next milestone does not inherit a false constraint.
  - *ADR-005: every menu format string goes through C's `va ()`.* `menu.c:2038` uses `%g` (`"on (%gx)"`, max sampler anisotropy), which `quake_util::printf` deliberately does not implement and panics on -- under `panic = "abort"` that is a process abort. `menu.rs:3420` marks the call-through. This is the same standing carry item as M9f's unflipped sprintf slot: **extend the ADR-005 formatter to `%e`/`%E`/`%g`/`%G` and octal, with its conformance suite, before either can move.**
  - *Twenty-two `// COMPAT:` markers* -- 21 in `rust/quake-capi/src/menu.rs` (`:335`, `:482`, `:1468`, `:2021`, `:2494`, `:3420`, `:3528`, `:4116`, `:4608`, `:5267`, `:5377`, `:5998`, `:6012`, `:6472`, `:7085`, `:7100`, `:7124`, `:7138`, `:7579`, `:7615`, `:7814`) and 1 in `rust/quake-c-sys/src/menu.rs` (`:381`, ADR-010: `roundf` call-through for the slider values that reach archived cvars; `fabsf` deliberately **not** declared, because IEEE-754 makes it an exact sign-bit clear and the MSVC UCRT publishes it only as a `<math.h>` inline). Most are ordinary quirk preservation: C's `size_t` arithmetic in `q_min`/`q_max`/`strlen` expressions reproduced with explicit `u64` wrapping maths, the `goto forward` at `menu.c:1214` refactored into a shared `m_setup_forward`, and the leaked `FILE *` at `menu.c:846-849` reproduced rather than fixed.
  - *One marker is an **observable divergence**, and is called that rather than softened.* `menu.rs:3528` -- `menu.c:2062` indexes the four-entry `shadow_modes[]` with the raw `(int) r_rtshadows.value`. `r_rtshadows` is a plain `CVAR_ARCHIVE` cvar (`gl_rmain.c:120`, default `"2"`) with no clamping callback, so `r_rtshadows 7` from the console makes C read out of bounds. The port **clamps** to `0..3` (ADR-004: no UB in Rust). The two builds therefore agree on every value C has defined behaviour for and can disagree outside that range. This is unlike M10c's `Con_Quakebar`, where the skipped stores were *proven* unobservable -- here the divergence is real, and confined to a region where C's own behaviour is undefined. Recorded, not tested (the differential does not vary the cvar out of range).
  - *The oracle had to be restructured, and my specification of that restructuring was also wrong.* I named the wrong locations and the wrong set size: `M_Init` is at `stubs/host_ref.c:329`, not `stubs.c`, and there are **ten** plain `M_*` link doubles across five stub files, not four. Five of them are load-bearing -- `M_Keydown`, `M_Charinput`, `M_ToggleMenu_f`, `M_TextEntry`, `M_WaitingForKeyBinding` (`keys_ref.c:618-640`) and `M_Menu_Main_f` (`console_ref.c:723`) are the **observation channel** the keys and console differentials assert counters on. All ctest stubs compile into one library linked into every test binary, so exporting a plain `M_*` from `menu_ref.c` collides at link time, and deleting the double to make room destroys the tests that watch it. The resolution: `menu_ref.c` exports **no plain `M_*` name at all** -- it drives the C side through `c_ref_M_*` and the Rust side through `quake_rs_menu_*`, and adds `ctest_menu_set_gamedir`. `git diff --stat -- rust/quake-ctest/tests/` is empty: no pre-existing test was touched.
  - *Two files edited outside the agent's may-edit list, disclosed unprompted.* `stubs/host_ref.c` (+21/-12) and `stubs/pr_ext_ref.c` (-8), both mechanical consequences of the above -- doubles that had to move or disappear once `menu.c` became a real oracle TU. Reviewed and accepted; no behavioural content.
  - *Mutation testing: 32 attempted, 31 killed, 1 surviving and traced equivalent.* The survivor is `net_cursor_wrap_floor`: `m_net_first_item` is always 1 and `m_net_items` is 0 or 1, so the state the mutation changes is unreachable. Three earlier survivors were killed by closing **real** gaps, the sharpest being `check_mods_matches_for_every_modlist_shape`, which was found to observe **nothing** and was replaced with a test that constructs byte-exact FNV-1a preimages by meet-in-the-middle.
  - *Honest coverage gaps, carried from the agent's own §10 rather than paraphrased away.* `S_LocalSound` is silent in the oracle; `registered` is never varied; the video-menu bodies are not compared; the `shadow_modes` OOB index is not tested (see above); `CL_NextDemo` and `NET_Slist_f` are link doubles, so their bodies are not differentially covered; and `COM_Rand` is a **shared** PRNG across both sides, which can mask a draw-count divergence between the two implementations.
  - *An agent contract violation, self-disclosed (§11).* The agent did **not** open `PLAN.md`, `ROADMAP.md`, or ADRs 003/004/005/009/010/011, working from the assignment text and sibling-module conventions instead. That makes the `// COMPAT:` markers the one deliverable with no ADR-grounded review behind it, so all 22 were read against the ADRs in the main session before this commit; they hold, and `menu.rs:3528` is the only one whose claim needed rewriting into the divergence note above.
  - *Gate evidence (current run, Windows x64, this checkout, main session).* All **six** meson configs build and link clean -- `build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace` (the two `cbindgen WARN: Skip ...` lines are pre-existing). `condump_diff.py` C-vs-RS and C-vs-CHOST both **identical (340 bytes, 15 lines)**. `config_diff.py` both directions **identical (989 bytes, 52 lines, 7 bind lines)**. `run_corpus.py --compare` C-vs-RS and C-vs-CHOST both **11 ran, 8 skipped, 0 failed**, every entry `identical`. `run_corpus.py --stability` on `build-rs` **11 ran, 0 failed**, all `stable`. `save_diff.py` C-vs-RS **identical (95091 / 95075 bytes)**. `check_ctest_symbols.sh` **OK across 43 oracle sources**. `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo clippy -p quake-capi --features host,progs-host` all clean; `cargo test -p quake-ctest --locked --no-fail-fast` **1250 passed, 0 failed** across 83 binaries (1199 baseline + 51 new).
  - *Not run / unverified, recorded as such.* `check_headers.sh` and `check_capi_signatures.sh` need `cc`, absent on this machine -- CI is the first place cbindgen's declarations get diffed against `Quake/menu.h` by a compiler. `check_ctest_symbols.sh`'s "43 oracle sources" **does not include `menu.c`**: like `console.c`, `sbar.c`, `keys.c`, `host.c`, `host_cmd.c` and `pr_ext.c` it is `#include`d into its composing stub rather than listed in `C_SOURCES`, so a missed `c_ref_*` rename would surface only as a duplicate-symbol link error in CI. `physics_matrix.py`, `builtin_diff.py`, `trace_diff.py`, `record_diff.py`, `netreplay_diff.py` and `interop_matrix.py` were not run -- `menu.c` has no physics, progs-trace or net surface. Unlike M10d's `sbar.c`, `menu.c` **does** get initialised under `-headless` (`M_Init` runs; `host.c:1343-1354` skips only the video/audio/sbar block), so the corpus and condump gates above do execute menu init and the console-toggle path -- but no menu is ever opened or drawn by any corpus entry, so the drawing and keydown bodies are covered by the ctest differential alone. The agent reported `console_differential::con_tabcomplete_cycles_forwards_and_backwards` as a **pre-existing** flake (4 failures in 30 direct binary runs, identically with and without these changes); that characterisation is the agent's and was **not** independently re-verified here, and the test passed in both full-suite runs of this tree.
- **2026-09-03 M10f-1 landed** (T10.5 first half: `Quake/r_part.c` -> Rust, Pattern A on `use_rust_host`). `Quake/r_part.c` leaves the unconditional `srcs` list and is replaced under `-Duse_rust_host` by `Quake/r_part_glue.c` (561) + `rust/quake-capi/src/r_part.rs` (926) + `rust/quake-c-sys/src/r_part.rs` (169), with `rust/quake-ctest/stubs/r_part_ref.c` (768) as the composing oracle TU and `rust/quake-ctest/tests/r_part_differential.rs` (1392) carrying **15 tests**. `Quake/r_part_fte.c` is **not** part of this flip and stays C -- T10.5's second half is tracked separately. Ported by one Opus-high agent against a main-session contract; integration, the meson flip and every engine-level gate in the main session.
  - *The seam.* Only the classic particle **simulation** moves. The glue keeps the rendering half verbatim (`r_part.c:54-221` and `:951-1106`) and keeps the particle pool -- `active_particles`, `free_particles`, `particles`, `r_numparticles` -- plus `r_particles`, `r_quadparticles`, the five particle-texture handles, `texturescalefactor` and `particle_index_buffer`; Rust drives the pool through ADR-011 mirrors. The pool quartet and `r_quadparticles` lose their `static` so `quake-c-sys` can extern them; `r_part_fte.c`'s same-named globals (`:459-461`) are `static`, so there is no collision. Every plain `R_*` / `CL_RunParticles` / `Harness_HashParticles` name stays in the glue as a wrapper over a `quake_rs_rpart_*` core.
  - *Unlike M10d's `sbar.c`, this milestone's subject is inside the harness hash.* `Harness_HashParticles` (`r_part.c:927`) feeds `Harness_HashClient` (`harness.c:312`), which feeds the per-frame hash (`harness.c:524`), and `CL_RunParticles` does run under `-headless`. The corpus compare below is therefore a real gate on the ported simulation, not merely a regression check that the flip broke nothing else.
  - *My "zero new `Host_Guard` trampolines" contract claim was wrong, and the agent caught it.* I enumerated the file's raise surface as 4x `Con_Printf`, 3x `MSG_Read*` and 1x `COM_FOpenFile`, and missed `Cvar_RegisterVariable`, which `R_InitParticles` calls twice (`r_part.c:247`, `:249`). Under `-Duse_rust_cvar` that plain name is **itself** a re-raising wrapper (`cvar_cmd_glue.c:300` -- `quake_rs_cvar_register_variable` followed by `Host_Reraise`), so calling it from a Rust frame is a straight ADR-009 rule-3 violation. One trampoline, `RPart_Glue_RegisterVariable`, modelled on `Chase_Glue_RegisterVariable` (`chase_glue.c:20-35`) and `View_Glue_RegisterVariable` (`view_glue.c:105-118`); `R_InitParticles` is consequently the module's only re-raising wrapper. Verified against the tree before acceptance. This is the same defect class as M10e's tab-aligned globals miss -- **my hand inventories of C-side hazards keep under-reporting and need a second method.**
  - *Two oracle deviations, both self-disclosed.* (1) `r_part_ref.c` renames the two cvars to `c_ref_rpart_r_particles` / `c_ref_rpart_r_quadparticles`, because `menu_ref.c:725-726` already owns both `c_ref_r_particles` and the plain `r_particles`. (2) It carries 13 `Sys_Error`-on-call Vulkan link doubles, so the rendering half compiles in the oracle but is never executed there.
  - *Three `// COMPAT:` markers came back; two were wrong and were corrected in the main session, leaving two.* (a) The `ramp3` marker was **demoted to a plain comment**: `r_part.c:36` declares `ramp3` as `[8]` and supplies six initialisers, and C zero-fills the tail (C99 6.7.8p21) -- that is *defined*, so no exception is being invoked. The two trailing zeroes are reproduced literally and neither indexing site can reach them (both are bounded to `0..=5`). (b) The `colorMod % colorLength` marker cited ADR-010, but ADR-010's Phase 7 amendment is **float->int specific**; integer division by zero belongs under ADR-004 on the `menu.rs:3528` precedent, and it was recited accordingly (`r_part.rs:425`). Its factual claim was wrong as well and was rewritten: the agent wrote that C traps as SIGFPE "on every target the engine builds for", but **arm64 `sdiv`/`msub` is architecturally defined to yield 0 for a zero divisor**, so the C build *survives* on arm64 (darwin-arm64 goldens exist; windows-arm64 is in the CI matrix) and continues with `color_start + colorMod`, while Rust's `%` panics and `panic = "abort"` makes that an immediate process death. `colorLength` is wire-reachable (`svc_temp_entity` TE_EXPLOSION2 reads it with `MSG_ReadByte`), so this is a genuine **observable divergence on arm64**, not merely a different way of dying. Recorded, not tested -- the differential does not drive `colorLength == 0`. The remaining correct marker is `r_part.rs:125` (ADR-010, C's implicit float->int conversion).
  - *A main-session-only integration finding: `cargo test` linked, the engine did not.* With a direct `fscanf` extern on the Rust side, `build-rs`, `build-rs-cprogs` and `build-rs-trace` all failed with `LNK2019: unresolved external symbol fscanf referenced in function quake_rs_rpart_read_point_file_f`; `build-c`, `build-c-trace` and `build-rs-chost` were unaffected, since none of them pulls that CGU out of the staticlib. **My first diagnosis was wrong and is recorded as such:** I attributed it to the MSVC UCRT publishing `fscanf` only as a `<stdio.h>` inline over `__stdio_common_vfscanf`, but the pre-existing Rust `fscanf` externs at `quake-capi/src/menu.rs:1467`/`:1475` and `cl_demo.rs:1333` resolve in the same binary, which disproves that. **The mechanism was not established.** The fix routes the call through a glue shim `RPart_Glue_ScanPoint` (`r_part_glue.c:301`, mirrored at `stubs/r_part_ref.c:403`), which removes the dependency and independently keeps libc's exact scanner -- the compat surface, since the pointfile is plain text and reimplementing float parsing in Rust would be a new divergence rather than a port. Same class as M10d's missing `#include "steam.h"`: **the agent cannot run meson, so only the integration build can find it.**
  - *`Quake/common.make` needed no change* -- glue files are not listed there, per the M10c/M10d/M10e precedent.
  - *`pub mod r_part;` is not `host`-gated* (unlike `sbar` and `menu`), so it compiles in every feature set. In `build-rs-chost` nothing references `quake_rs_rpart_*`, the CGU is never pulled from the staticlib, and the link is clean. Noted rather than changed.
  - *Mutation testing: 17 injected, 16 killed, 1 survivor traced equivalent* -- `ramp3[7] = 0 -> 1`, unreachable for the reason in (a) above.
  - *Gate evidence (current run, Windows x64, this checkout, main session).* All **six** meson configs build and link clean -- `build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace`. `run_corpus.py --compare` C-vs-RS and C-vs-CHOST both **11 ran, 8 skipped, 0 failed**, every entry `identical`. `run_corpus.py --stability` on `build-rs` **11 ran, 0 failed**. `condump_diff.py` C-vs-RS and C-vs-CHOST both **identical (340 bytes, 15 lines)**. `config_diff.py` both directions **identical (989 bytes, 52 lines, 7 bind lines)**. `save_diff.py` C-vs-RS **identical (95091 / 95075 bytes)**. `check_ctest_symbols.sh` **OK across 43 oracle sources**. `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo clippy -p quake-capi --features host,progs-host` all clean; `cargo test -p quake-ctest --locked --no-fail-fast` **1265 passed, 0 failed** across 84 binaries (1250 baseline + 15 new).
  - *Not run / unverified, recorded as such.* `check_headers.sh` and `check_capi_signatures.sh` need `cc`, absent on this machine. `check_ctest_symbols.sh`'s 43 oracle sources **do not include `r_part.c`** -- like `console.c`, `sbar.c`, `menu.c`, `keys.c`, `host.c`, `host_cmd.c` and `pr_ext.c` it is `#include`d into its composing stub rather than listed in `C_SOURCES`, so a missed `c_ref_*` rename would surface only as a duplicate-symbol link error in CI. `physics_matrix.py`, `builtin_diff.py`, `trace_diff.py`, `record_diff.py`, `netreplay_diff.py` and `interop_matrix.py` were not run -- `r_part.c` has no physics, progs-trace or net surface. Gaps carried from the agent's own report: the rendering half is compiled but never executed in ctest; `colorLength == 0` is deliberately not driven; `RPart_Glue_InitRender`'s call count is one-sided; `avelocities` and `tracercount` have no reset hook, so the hidden-static tests are order-dependent by convention rather than by construction; and `Harness_HashParticles` is compared through the fixture, not through the real per-frame chain. Single platform and toolchain only. `c_ref_prelude.h` grew 213 more lines of COMPILE-ONLY Vulkan and `particle_t` approximations -- **Phase 8 must replace those with the real headers**, alongside the M9f `cb_context_s` / `vulkanglobals_t` / `gltexture_s` slices.
- **2026-09-03 M10f-2 landed** (T10.5 second half: the *simulation* half of `Quake/r_part_fte.c` -> Rust, Pattern A on the same `use_rust_host` switch). **T10.5 and milestone M10 are now complete.** `Quake/r_part_fte.c` (7396) leaves the unconditional `srcs` list and is replaced under `-Duse_rust_host` by `Quake/r_part_fte_glue.c` (2714) + `rust/quake-capi/src/r_part_fte.rs` (7386) + `rust/quake-c-sys/src/r_part_fte.rs` (970), with `rust/quake-ctest/stubs/r_part_fte_ref.c` (1691) as the composing oracle TU and `rust/quake-ctest/tests/r_part_fte_differential.rs` (1562) carrying **15 tests**. `c_ref_prelude.h` gains a 90-line `c_ref_*` rename block. Ported by one Opus-high agent against a main-session contract; integration, the meson flip, the `// COMPAT:` marker review and every engine-level gate in the main session. At ~5,774 ported lines this is the phase's largest single port, ahead of `menu.c` (4950).
  - *The seam, and why it is where it is.* PORT = `r_part_fte.c:88-5583` except `P_LoadTexture` (`:1146-1327`, the file's only renderer-coupled function below the line), **plus** the simulation block `:6325-6784`. STAY C = `P_LoadTexture`, the render/emit block `:5585-6324`, `PScript_UpdateParticleTypes` (`:6786`) and everything below it, and the render file-scope statics (the `cl_stris` family `:526-535`, `current_buffer_index`/`VkBuffer`/`vulkan_memory_t` `:5579-5583`). **No function is split.** `:6786` was the one candidate and was deliberately left whole: it interleaves authoritative sim bookkeeping with `cl_stris` batch reservation and `Reallocate{Vertex,Index}Buffer` inside a single 520-line loop, with a real data dependency (`meta->first_stri = cl_numstris`, `:6982`) and `MAX_INDICES` overflow splits that depend on the running `cl_numstrisvert`.
  - *The decisive constraint on this milestone: **the FTE particle system never advances under `-headless`**.* `PScript_UpdateParticleTypes` has exactly one caller, `PScript_LayoutParticlesTask:7320`; the five task entry points are scheduled only from `gl_rmain.c:1204-1226` inside `R_RenderView`, reached via `V_RenderView` <- `SCR_UpdateScreen`, all inside `host.c:1107 if (!no_rendering)`. So `run_corpus.py --check/--compare` **cannot observe any FTE update/layout/emit code**, and the ctest differential is this port's only real gate -- the `sbar.c` situation at ten times the complexity. What the corpus *does* observe: the FTE **spawners** do run headless, `frandom()` (`r_part_fte.c:37`) is `COM_Rand ()`, and `COM_RandState` is mixed into the per-frame hash at `harness.c:525`, so the **draw count** is gated even though particle contents are not.
  - *Disclosed deviation (a): the decal clipper stays C.* `r_part_fte.c:3928-4308` (`decalctx_t`, `PScript_AddDecals`, `Fragment_ClipPolyToPlane`, `Fragment_ClipPoly`, `Q1BSP_Fragment_Surface`, `Q1BSP_ClipDecalToNodes`, `Mod_ClipDecal`) is inside the nominal port range but remains in the glue. Accepted: the family is pure `qmodel_t` BSP geometry, every object it touches is already C-owned on the render side of the seam, and `Mod_ClipDecal` is `static` within the glue (`:819` decl, `:1055` defn, called only at `:2368` and `:2557`). Rust reaches it through `FtePart_Glue_ClipDecal` handing over a `decalctx_t`; the justification is recorded in-tree at `rust/quake-capi/src/r_part_fte.rs:4525-4533`.
  - *Finding (b), and the honest shape of the risk: **the sim/render seam is bidirectional**.* `PScript_UpdateParticleTypes` stays C but owns simulation state the Rust half depends on -- `particletime += pframetime` (`:7292`), the kill-list return to `free_particles` (`:7286-7290`), the deferred drain (`:7227-7232`) and run-list removal (`:7205-7216`). That is a deliberate consequence of not splitting `:6786`, not an oversight, but it means the ported half's inputs are advanced by C code that no headless gate ever executes. Two seams are counted in the differential but never executed there. **Phase 8 inherits this**: when the render half ports, the two sides must be reunified rather than the C side simply deleted.
  - *ADR-010: the NaN-sign fence was **narrowed and re-justified, not removed**.* The plan (landmine 12, T10.5) expected M10 to "take down" the exception. That premise did not hold, and the reason is stronger than a scheduling deferral: **`Quake/mathlib.c` is in `meson.build`'s `phase1_c_srcs` (`:295`) and is appended to `srcs` only in the `else` branch of the `use_rust` conditional (`:450-452`)** -- so in `build-rs`, `build-rs-chost`, `build-rs-cprogs` and `build-rs-trace`, `PerpendicularVector` and `RotatePointAroundVector` have been `rust/quake-capi/src/mathlib.rs:149`/`:157` **since Phase 1**, and C `r_part_fte.c` has been calling those all along. Porting the callers changes only the calling frame's language and adds **no new divergence surface**. The amendment (landed on `docs/rust-migration/adr/ADR-010-determinism-policy.md:188`, +60 lines) retires the Phase 3 trigger sentence ("if either function ever acquires a simulation-side caller") as **mis-specified** -- the load-bearing argument was always observability, not the caller's identity -- and replaces it with: *if a NaN sign bit ever becomes observable in hashed state, in a savegame, or on the wire, this exception must come down*. Call-site disposition: `:4390` and `:4485` (in `PScript_RunParticleEffectState`) are now Rust-called; `:7262` (in `PScript_UpdateParticleTypes`) stays C until Phase 8. The `RotationMatrix` sincos-fusion fence is **untouched** -- its own trigger is correctly specified. A stale `Quake/harness.c:299` citation in the Phase 3 amendment was corrected to `:525` while editing. **The Phase 7 exit record must not report this fence as removed.**
  - *Four `Host_Guard` trampolines, and the recon defect that nearly missed them.* `FtePart_Glue_{RegisterVariable,ClearTrailStates,RegisterParticles,EntityNum}` (`r_part_fte_glue.c:2497`, `:2508`, `:2519`, `:2537`) wrap `Cvar_RegisterVariable` (x16, `:3266-3281`), `CL_ClearTrailStates` (`:3315`, `:3532`), `CL_RegisterParticles` (`:3644`, `:6645`) and `CL_EntityNum` (`:4458`), each of which is a re-raising glue wrapper under its own switch. **There are eight raise-helper families in glue TUs, not one** -- `Host_Reraise`, `ClMain_Raise`, `ClParse_Raise`, `HostCmd_Raise`, `NetDgrmOrch_Reraise`, `NetMsg_Raise`, `PRBI_Raise`, `PRSave_Raise` -- and both first-pass recon methods, mine and `repo-researcher`'s, grepped only `Host_Reraise` and so reported 1 of 5. A third pass reading the wrapper bodies found the rest. **This is the third instance of the same defect class in three milestones** (M10e's tab-aligned globals; M10f-1's missed `Cvar_RegisterVariable`): my hand inventories of C-side hazards under-report, and any future recon must intersect against all eight names. ADR-009 rule 3 was verified after integration by grepping the c-sys externs: no raw raise-capable name is externed on the Rust side, and the only mentions of raise helpers in the capi module are doc comments.
  - *Preprocessor ground truth worth keeping.* `PSET_CLASSIC` is **never defined** -- the `fallback`/`pe_classic` vtable at `:3559-3568` is dead code that never compiles, so there is no cross-module vtable between the FTE and classic (M10f-1) particle systems; it was noted, not reproduced. `PSET_SCRIPT` and `PSET_SCRIPT_EFFECTINFO` (`quakedef.h:38-39`) **are** defined, so `P_ImportEffectInfo` (`:2804-3242`, ~440 lines) is live and ported. `r_part_fte.c:35`'s `#define Con_Printf Con_SafePrintf` is a file-level redefinition, preserved at `r_part_fte_glue.c:88`.
  - *`// COMPAT:` marker review: 26 markers, **3 corrected in the main session**.* The contract told the agent explicitly to read every ADR it cited (M10e's agent had not, and one of 22 markers was factually wrong). This agent reported reading them and 23 markers were correct; three were not, and all three were the same error -- an ADR-004 situation filed under ADR-010, whose Phase 7 amendment is **float->int specific**. (1) `com_rand_mod` (`r_part_fte.rs:4538`): `% 0` is integer UB, ADR-004 on the `menu.rs:3528` / `r_part.rs:425` precedent; its C behaviour is **not uniform across targets** (x86-64 `idiv` -> SIGFPE, arm64 `sdiv`/`msub` architecturally yields 0, which is what the fold matches), and its stated reachability was wrong -- `randsmax` is clamped to >= 1 by **every** assignment (`:1188`, `:1408`, `:1681`, `:1712`, `:2957`, `:3049`) and `colorrand` is guarded `> 0` at its only use (`:3999`), leaving `r_part_fte.c:4547-4548`'s float-guard-then-truncate on `spawnparam2` as the single live path. (2) The `SM_FIELD` marker (`:5233`) claimed `r_avertexnormals[j]` was read out of bounds in C; **C wraps `j` itself at `:4755-4759`**, so the Rust `% NUMVERTEXNORMALS` is a faithful no-op, not a deviation. Its other half is right and was sharpened: `avelocities[i]` (`:4740`, 162 entries at `:3867`) is indexed by a loop counter bounded only by `pcount` (`:4587`), so a script with `count > 162` makes C read adjacent static storage -- Rust reads zeroes, an **observable divergence recorded but not tested**, since the differential does not drive `count > 162`. (3) The `teximages[atoi (token)]` unbounded-index paragraph (`:3176`) carried no ADR at all and was recited under ADR-004.
  - *A confirmed defect in the oracle itself, deliberately not fixed here.* `rust/quake-ctest/stubs/stubs.c:252-262`'s `COM_Rand` returns the raw xoshiro word, **missing the `& COM_RAND_MAX` mask** that `Quake/common.c:1835` applies (`common.h:332`, `0xFFFFFF`) -- and the stub's own header comment claims it is "copied verbatim from Quake/common.c". Since `r_part_fte.c:37-39` defines `frandom()`/`crandom()`/`hrandom()` as `COM_Rand () * (1.0f / COM_RAND_MAX)` and friends, the oracle's `frandom()` spans roughly +/-128 instead of `[0,1]`. The differential is still a valid **C-vs-Rust** comparison -- both sides call the same stub -- but it exercises the port outside its intended input domain. Fixing it changes a PRNG shared by every ctest binary, so folding it into a 14k-line port commit would mix an unrelated red-test surface into this milestone; it is recorded here and tracked separately. **M11 must not treat this as closed.**
  - *Contract errors of mine, recorded.* The contract instructed the agent to use `scripts/harness/mutate.py`; **that file does not exist in the repo** -- only a scratchpad copy hard-wired to `r_part.rs`, from M10f-1. The agent wrote its own `scratchpad/mutate_fte.py` rather than reporting the gap as a blocker, which was the right call but leaves mutation testing on this file unreproducible from the tree. I also cited `check_ctest_symbols.sh` as a gate on the new oracle TU; it **structurally cannot see `r_part_fte.c`**, which is `#include`d into its composing stub rather than listed in `C_SOURCES`, so `llvm-nm` evidence was substituted.
  - *Process: the port agent died mid-run and was resumed, not restarted.* It dropped on "API Error: Connection lost mid-response" with its last state "now writing the c-sys module"; `git status --short` confirmed **nothing** had reached disk. It was resumed via `SendMessage` with its context intact (it had already read the recon, the contract, the ADRs and all five M10f-1 model files) plus a process change -- write each deliverable to disk the moment it is complete, in a fixed order, instead of composing all five and writing at the end -- so a second drop would have cost only the file in flight. Separately, the agent **self-disclosed** having temporarily edited `rust/quake-ctest/build.rs` (a main-session-only file) to obtain link evidence; `git diff --stat` confirmed it had been reverted byte-clean before delivery. Its reported pre-task ctest baseline ("1277/84") was wrong and was not taken on trust: the measured run is 1280/85, and 1280 - 15 new tests = **1265**, matching the M10f-1 ledger.
  - *Incidental widenings, all reviewed and benign.* `quake-c-sys/src/libm.rs` +6 (a `ceil` extern and wrapper, ADR-010 call-through); `quake-capi/src/view.rs:301` `cl_entity` and `quake-capi/src/world.rs:89` `RHT_IMPACT` widened to `pub(crate)`. As with `r_part`, **`pub mod r_part_fte;` is not `host`-gated**, so it compiles in every feature set; in `build-rs-chost` nothing references `quake_rs_ftepart_*`, the CGU is never pulled from the staticlib, and the link is clean. `Quake/common.make` needed no change -- `r_part.o` and `r_part_fte.o` stay at `:200-201` for the C-only fallback build, per the M10c/M10d/M10e/M10f-1 precedent. `meson.build:653`'s `CL_EntityNum` comment was checked and left as-is: still accurate, because the render half retains the `:5961`/`:7251` call sites.
  - *Mutation testing: 22 injected, 20 killed, 2 survivors traced equivalent* (via `scratchpad/mutate_fte.py`, see the contract-errors bullet above for why that script is not in the tree).
  - *Gate evidence (current run, Windows x64, this checkout, main session).* All **six** meson configs build and link clean -- `build-c`, `build-c-trace`, `build-rs`, `build-rs-chost`, `build-rs-cprogs`, `build-rs-trace` (six "error"-pattern hits in the meson log were verified false positives: `WARN: Skip quake-capi::PRBI_ERR_PROGRAM_ERROR` lines matching on the substring). `cargo test -p quake-ctest --locked --no-fail-fast` **1280 passed, 0 failed, 0 ignored** across **85** binaries (1265 baseline + 15 new), with all 15 new tests confirmed executing. `run_corpus.py --compare` C-vs-RS and C-vs-CHOST both **11 ran, 8 skipped, 0 failed**, every entry `identical`; `--stability` on `build-rs` **11 ran, 0 failed**. `condump_diff.py` C-vs-RS **identical (340 bytes, 15 lines)**. `config_diff.py` **identical (989 bytes, 52 lines, 7 bind lines)**. `save_diff.py` **identical (95091 / 95075 bytes)**. `check_ctest_symbols.sh` **OK, 43 oracle sources**. `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo clippy -p quake-capi --features host,progs-host` all clean, re-run after the marker corrections.
  - *Not run / unverified, recorded as such.* `check_headers.sh` and `check_capi_signatures.sh` need `cc`, absent on this machine. `physics_matrix.py`, `builtin_diff.py`, `trace_diff.py`, `record_diff.py`, `netreplay_diff.py` and `interop_matrix.py` were not run -- `r_part_fte.c` has no physics, progs-trace or net surface. `check_ctest_symbols.sh` does not cover `r_part_fte.c` (see above), so a missed `c_ref_*` rename would surface only as a duplicate-symbol link error in CI. Single platform and toolchain only; `rust/quake-ctest/stubs/*.c` is not format-gated at all. Gaps carried from the agent's own report, beyond deviation (a) and finding (b): the render half is compiled but never executed in ctest; two counted seams are never executed; `count > 162` and `spawnspc == 0` are deliberately not driven. `c_ref_prelude.h` gained another ~90 lines of COMPILE-ONLY approximations on top of M10f-1's 213 and the M9f `cb_context_s` / `vulkanglobals_t` / `gltexture_s` slices -- **Phase 8 must replace all of them with the real headers.**
- **2026-09-03 M11 phase exit.** The ADR-019 suite was re-run in full, the `windows-x86_64` goldens were generated, and the ROADMAP Phase 7 heading moved to `[~]` with a status block. Two exit items are **not** discharged and are recorded as blockers, not as passes: the fuzz soak and `/integration-review`.
  - *`windows-x86_64` goldens: generated, and the rule they deviate from was checked rather than assumed.* `Misc/harness/README.md:73-79` says "regenerate only from a `c-reference/*` tag, never hand-edit". **The repository has zero tags**, and the ROADMAP defers `c-reference/phase0` through `phase3` on the PLAN §3 MinGW/GNU-ABI decision, so that precondition has never been satisfiable; the existing `darwin-arm64` set has the same provenance shape (generated from plain commit `dfe80297`). This is also *first* generation for a new platform, not regeneration of an existing one. Generated from a C-only (`use_rust=disabled`) release build at commit `b1aa8c10` with MSVC cl 19.44.35228 — `meson.build:5` makes `buildtype=release` the project default, so the release-only rule holds without an explicit flag. 11 entries × 3 artifacts (state-hash, `--sndhash` mixer PCM, snd-demo), provenance and all ten caveats in `goldens/windows-x86_64/MANIFEST.json`.
  - *Compiler independence was **measured**, because CI does not use the compiler that generated them.* `build-windows.yml` builds with clang-cl; the goldens came from MSVC cl. A separate clang-cl 22.1.8 build chain reproduces all 11 entries byte-identically on both chains, and four build/toolchain combinations were verified identical (C-only/cl — the generator, mixed/cl, C-only/clang-cl, mixed/clang-cl). The **mixed** combos matter most: they carry the full Phase 7 port, so `--check` pins the port against the C engine's per-frame hashes rather than only against itself. CI's exact invocation was then reproduced locally (`--tier shareware`, plain and `--sndhash`, C-only and mixed): **8 ran, 0 skipped, 0 failed** in each of four runs — zero skips is what proves the new CI leg is not vacuous. The ADR-010 same-platform rule is untouched: these are never to be compared against a non-Windows build.
  - *CI and docs.* `build-windows.yml` gained a `Corpus goldens (--check, C-only and mixed)` step (four invocations, each with its own `throw`) placed before the savegame byte-diff, and its stale "no windows-x86_64 goldens yet" comment was replaced. `--stability` and the mixed-vs-C-only `--compare` steps were **kept**, because they cover the registered-tier entries CI has no data for. `Misc/harness/README.md`'s coverage paragraph now records both platforms and, specifically, that the three pusher entries plus `e1m1-long`/`save-e2m1`/`music-wav` have goldens on `windows-x86_64` **only** — so a green `--check` on macOS is not evidence about them. Line endings were measured per file, not assumed: the workflow is LF (a first attempt asserted CRLF and was wrong), the README is CRLF.
  - *Full ADR-019 gate ledger (all current tool evidence, Windows x86_64, this checkout).* Corpus `--check` green on the four combos above; `--compare` C-vs-CHOST and C-vs-CPROGS clean. `cargo test --workspace --release` **1489 passed, 0 failed** over 116 binaries, and **1489 passed, 0 failed** again in debug. `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets -- -D warnings` clean. `check_ctest_symbols.sh` **OK, 43 oracle sources**. `physics_matrix.py` C-vs-RS **84 ok, 0 skipped, 0 failed**. `builtin_diff.py` **identical, 235 entries**. `trace_diff.py --map` **identical** — e1m1 331841, e2m1 402841, e3m1 489069 records. `save_diff.py` clean C-vs-RS, RS-vs-RS and C-vs-CHOST (this is the post-M9d-arena-flip re-run the M9 gates line required; M8's verification predated the flip). `config_diff.py` and `condump_diff.py` clean. `record_diff.py` **identical (20239 bytes, e1m1, 600 frames)**. `capture_diff.py` **PASS** (2385 vs 2375 records; recv reliable prefix identical over a 4708-byte window calibrated from a second run of the *reference* build). `netreplay_diff.py` **PASS both C-self and C-vs-Rust**: 1192 records delivered, state-hash chains identical over 1402 frames, recorded demos identical at 40308 bytes. `interop_matrix.py` **PASS on all 24 cells** (6 protocol cells × 4 build combos). Miri under `MIRIFLAGS=-Zmiri-strict-provenance`: **17 passed, 0 failed, 0 UB**.
  - *The netreplay gate is the strongest single piece of evidence in this phase*, and worth naming as such: it replays a captured live session deterministically, which removes the timing nondeterminism that makes live sessions incomparable, and the fully-ported mixed build then matches C **byte-for-byte** on both the state-hash chain and the recorded demo.
  - *Miri's reach is a real limit, not a formality.* Only pure-Rust crates are runnable, so the 17 tests covered `quake-cvar` (M2) and `quake-progs` (the Phase 6 arena, re-verified after the M9d ownership flip). `quake-capi` (70 FFI files) and `quake-c-sys` (32) hold **nearly all** of this phase's `unsafe` and cannot be reached by Miri at all. Nightly had to be installed for this; it did not previously exist on the machine.
  - *The 100k × 8-cell soak passed — and the honest reading is that it is a **liveness** gate, not a parity gate.* All 8 cells PASS at 1563 checkpoints each, traffic in band; references `Base-666` (100001 frames, rel 3/4, unrel 99912/99987) and `FTE+999` (100001 frames, rel 4/5, unrel 99906/99985). Seven of eight cells report `hash forks @128 (diagnostic)` — **including `FTE+999` C/C, a pure C-vs-C reference cell**, which is the point: the fork is a property of live UDP scheduling, exactly as `interop_matrix.py`'s own docstring records from the M1 T1.8 measurement, and is therefore not attributable to the Rust port. Only `Base-666` C/C happened to stay hash-identical. Per-frame simulation equivalence is gated properly and exactly by `run_corpus.py` instead. Minor harness note: `--results-dir` was passed but the directory was never created on a passing run.
  - *Fuzz soak: **not run**, and two distinct approaches failed before stopping.* Stable cargo rejects cargo-fuzz's v4 lockfile (the outer `cargo install` reported EXIT=0 while actually failing — caught by checking `~/.cargo/bin` for the binary rather than trusting the exit code). `cargo +nightly install cargo-fuzz --locked` installed v0.13.2 and the targets build, but will not launch: `0xc0000135 STATUS_DLL_NOT_FOUND` with no ASan runtime on PATH, then `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND` with LLVM 22's `clang_rt.asan_dynamic-x86_64.dll`. Per the two-distinct-approaches rule this was stopped rather than iterated. The gate lives in Linux CI (`rust.yml` job `fuzz-smoke`, 22 targets, 45s per PR / 300s weekly cron). **Phase 7 added no new fuzz targets**, and the specific gap is that `cl_parse.c` — an untrusted-input parser ported at M7 — has no dedicated target; only `fuzz_net_msg` covers the MSG readers beneath it.
  - *`/integration-review`: **blocked on the user**.* `.claude/skills/integration-review/SKILL.md` carries `disable-model-invocation: true`; the Skill tool refuses it and explicitly forbids replicating the workflow by other means. It must be run by explicit user invocation against this plan.
  - *ADR-010, for the exit record: the NaN-sign fence was **narrowed and re-justified, not removed**.* M10f-2 required M11 to not misreport this, so it is restated here. The Phase 7 amendment landed at `docs/rust-migration/adr/ADR-010-determinism-policy.md:188`; the trigger is now observability (*if a NaN sign bit ever becomes observable in hashed state, in a savegame, or on the wire*), not the caller's identity. `r_part_fte.c:7262` remains a C caller until Phase 8.
  - *ADR-007 shows **zero open Phase-7 rows*** (`ADR-007-dual-view-globals.md:34`): `sv`/`svs` (M6), `cl`/`cls` (M7), the `net_message`/`msg_readcount`/`msg_badread` trio (M9e), and `qcvm`/`pr_global_struct`/`qcvm->edicts` (M9d/M9g).
  - *Not run, and recorded as not run.* `check_headers.sh` and `check_capi_signatures.sh` need `cc`, absent on this machine. **Every Linux, macOS, MinGW and arm64 CI leg**: the branch has never been pushed, so CI's newest run on it is still the M2 commit and **M3–M11 have not been through any of them**. That is a live risk rather than a formality — M5 and M9e each turned up a real Linux-only duplicate-symbol defect that only CI found. Pushing is outward-facing and awaits the user.
  - *Carry items M11 explicitly does **not** close.* (1) `rust/quake-ctest/stubs/stubs.c:252-262`'s `COM_Rand` is missing the `& COM_RAND_MAX` mask that `Quake/common.c:1835` applies, so the oracle's `frandom()` spans roughly ±128 instead of `[0,1]` — M10f-2 required this not be treated as closed, and it is not; it is tracked separately because it perturbs a PRNG shared by every ctest binary. (2) The ADR-005 formatter still panics on `%e`/`%E`/`%g`/`%G` and octal, blocking the `PF_sprintf_internal` slot until it and its conformance suite grow them. (3) `PRBI_Glue_MsgWrite` (`pr_cmds_glue.c:275`) and `PF_fputs`'s `PF_VarString(1)` are unguarded but raise-reachable — ADR-009 rule-3 gaps that landed at M5; the SAFETY comment at `progs_builtins.rs:305-307` claiming `Sys_Error` is factually wrong. (4) The 14 `PF_cl_te_*` builtins are ported but never differentially exercised. (5) Neither `net_dgrm.c` nor `net_main.c` is in the ctest oracle's `C_SOURCES`, so slist/master-query, `Test_f`/`Test2_f`, `NET_Ban_f`, rcon, heartbeat and the five M9c guarded raise paths have **no** differential — only the live interop matrix and soak. (6) M9f's hand-declared C externs still need folding into `quake-c-sys` per ADR-011. (7) The `c_ref_prelude.h` `cb_context_s`/`vulkanglobals_t`/`gltexture_s` slices are COMPILE-ONLY approximations Phase 8 must replace with real headers. (8) The `stof`/`stoi`/`stoh`/`itos`/`htos`/`ftoi`/`itof`/`strlen`/`str2chr`/`strstrofs` family remains uncompared, and the pr_ext "ambiguous bucket" belongs to no roadmap phase. (9) `scripts/harness/mutate.py` still does not exist in the tree, so M10f-1/M10f-2's mutation runs remain unreproducible from the repo. (10) The `console_differential::con_tabcomplete_cycles_forwards_and_backwards` flake and the 9 pre-existing clang-format violations (v18-vs-v22 toolchain gap; `rust/quake-ctest/stubs/*.c` is not format-gated at all).
