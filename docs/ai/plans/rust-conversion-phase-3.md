# Feature task plan: Rust migration Phase 3 — Formats & assets (quake-formats, quake-image)

Status: approved
Owner: rust-migration
Baseline: branch `feature/rust-conversion-phase-3-e2bc8e` at plan authoring
Last materially updated: 2026-08-24 (M6 landed)

For Rust-migration work, this task plan is subordinate to `docs/rust-migration/PLAN.md`, `ROADMAP.md`, and applicable ADRs. It cannot change phase ordering or compatibility policy without an explicit approved documentation/ADR change.

## Objective

Port all asset-format parsing out of `Quake/gl_model.c` and `Quake/image.c` into the pure crates `rust/quake-formats` and `rust/quake-image`, exposed through `rust/quake-capi` shims that populate the exact C-layout structures the renderer consumes, switchable via `-Duse_rust_formats` / `-Duse_rust_image` with the C originals retained as the differential oracle. Covered formats: BSP 29 / 30-Valve / BSP2 / 2PSB / Quake64 (including `.lit`/`.vis`/`.ent` externals, PVS decompression, hull setup, submodels), MDL v6, MD3, MD5, SPR, LMP/QPIC, PCX, TGA, and — behind the ADR-012 pixel-exact gate — PNG/JPG/TGA decode via crates. The renderer half of `gl_model.c` (mesh building, texture upload, task dispatch) stays C and is out of scope.

## Requirements and non-goals

- R1: `quake-formats` parses all brush-model lumps (all five BSP dialects), decompresses PVS, builds hull0 and clip hulls, and sets up submodels, producing structures bit-identical to the C loaders over the full asset corpus.
- R2: `quake-formats` parses MDL v6 (frames, groups, flood-fill skin prep, bounds, extra-flags), SPR, MD3, and MD5 (mesh + anim, influence baking, normal computation) with field-masked-hash parity against C (see D6).
- R3: External asset handling reaches parity: `.lit` (QLIT magic, strict size check, Q64 16-bit unpack, Valve RGB), external `.vis` (`vispatch_t` records, BSP29 + cvar + worldmodel gating), external `.ent` (`"%s@%04x.ent"` via the already-ported Rust CRC).
- R4: `quake-image` provides PCX and LMP/QPIC decoders (hand-ported) and, as a separately gated late milestone, PNG/JPG/TGA decode via crates; `Image_LoadImage`'s two-pass `path_id` search loop stays C (D7). PNG *encode* stays lodepng (ADR-012).
- R5: Rust loaders run correctly on C task workers where C ones do today (brush texture parsing, surface extents): all worker-callable Rust code is Send-pure over byte slices (ADR-016).
- R6: Accept/reject parity on malformed input: differential fuzzing (C-via-FFI vs Rust) for BSP, MDL, MD3, MD5, SPR, PCX, LMP; Sys_Error/reject decisions match.
- R7: Every milestone leaves all Meson configs (`build-c`, `build-rs`, `build-rs-cfs`, plus new formats/image configs) building and the demo-hash harness byte-identical.
- NG1: No deletion of C originals (same MinGW blocker as Phases 1–2, PLAN §3); C stays compiled behind `-Duse_rust_formats=disabled` / `-Duse_rust_image=disabled` as oracle.
- NG2: No renderer work: `gl_mesh.c`, `gl_texmgr.c` texture upload, `GL_MakeAliasModelDisplayLists`, `GLMesh_UploadBuffers`, `Mod_LoadTextureTask`/`Mod_LoadSkinTask` GPU glue, `Mod_PolyForUnlitSurface` stay C.
- NG3: No port of the `.skin`-file → TexMgr callback plumbing (`SKIN_PATTERN` handling, gl_model.c L5721–6006 glue); it stays C wholesale this phase.
- NG4: No port of octree palette tables (`palette.c` — data, stays) and no port of `TexMgr_LoadPalette`/`TexMgr_LoadMiptexPalette` GPU-side derived-table upload.
- NG5: MD5 "JSON metadata via Phase-1 parser" (ROADMAP Phase 3 bullet) does not apply to this tree — `json.h` is only included by `host_cmd.c`/`steam.c`; MD5 loading is a bespoke text tokenizer. Documented as not-applicable; ROADMAP gets a one-line correction in M1 (RA8).

## Invariants and compatibility surfaces

- I1: `#![forbid(unsafe_code)]` in `quake-formats` and `quake-image`; all FFI/unsafe in `quake-capi` (ADR-004). No transmute-over-lumps: read-and-construct parsing only.
- I2: Allocation ownership per ADR-013: capi shims populate buffers obtained from the same C allocator calls the originals make; C frees them identically in both builds.
- I3: Float behavior per ADR-010: MDL scale/origin math, MD5 quaternion-w reconstruction, `CalcSurfaceExtents` double math carry `// COMPAT` comments and use libm-direct calls; parity is bit-for-bit per platform.
- I4: Struct layout per ADR-011: hand-written `#[repr(C)]` mirrors in `quake-types` for `bspfile.h`, `modelgen.h`, `spritegn.h` and in-memory targets the shims write, with const size/offset asserts plus runtime `abi_probe` checks.
- I5: Preserved behavior: demo-hash harness output unchanged in every config; savegame/protocol untouched.
- I6: Thread contract: nothing Rust that runs on task workers touches globals or thread-locals (`com_filesize`/`com_token`/`file_from_pak` are C `THREAD_LOCAL` and must not be shadowed by Rust state) (ADR-016).
- I7: Dependency policy: new crates (`png`, `zune-jpeg`) need ADR-003 review notes + deny.toml pass; Apache-2.0-only excluded (MIT-dual OK).
- I8: Goldens/corpora per ADR-019: goldens generated from the C reference build only; non-redistributable game data referenced by hash, never committed.

## Migration authority

- Roadmap phase: Phase 3 "Formats & assets" (`docs/rust-migration/ROADMAP.md:79-94`); Phases 1–2 complete (delete steps deferred).
- Applicable ADRs: 003 (deps), 004 (unsafe), 010 (determinism), 011 (FFI tooling/mirrors), 012 (vendored libs: image swap gate, lodepng encode stays), 013 (allocator), 016 (task system), 019 (verification).
- C reference/oracle: `Quake/gl_model.c` (post-M1 split: `Quake/model_parse.c`) and `Quake/image.c` (post-M1 split: `Quake/image_decode.c`), compiled into `quake-ctest` via `c_ref_prelude.h` renaming, and into the engine when the switches are disabled.
- Deferred: all C deletions; MinGW blocker per PLAN §3.

## Architecture decisions

- D1 Seam granularity: per-parse-function symbol replacement (the `quake-capi/src/wad.rs` pattern). Orchestrators (`Mod_LoadModel`, `Mod_LoadBrushModel`, task dispatch, all GPU calls) stay C and link against Rust symbols when the switch is on. Alternative (whole-orchestrator seam) rejected: larger unsafe surface, all-or-nothing parity debugging.
- D2 C prep — behavior-neutral file split (Phase-2 `common_fs.c` precedent), not `#ifdef`: `Quake/model_parse.c`/`.h` holds every function Rust will replace; `Quake/image_decode.c` holds the decoders. Micro-refactors folded in (all behavior-neutral, harness-verified): hoist `Mod_PolyForUnlitSurface` out of `Mod_LoadFaces`; split `Mod_LoadTextures` into `Mod_ParseTextures` (moves) + GPU/task loop (stays); split `Mod_LoadAliasModel` at the `GL_MakeAliasModelDisplayLists` boundary. Deferred out of M1 by the amendments below: de-staticing the alias parse path into a context struct (M4 — the `stverts`/`triangles`/`poseverts` externs are consumed by frozen `gl_mesh.c`), the `Mod_LoadSpriteFrame` vs `TexMgr_LoadImage` split (M4), and the MD5/MD3 splits at the `GLMesh_UploadBuffers` boundary (M5). Scope boundary: `model_parse.c` holds format parsing only — the PVS *runtime queries* `Mod_LeafPVS`/`Mod_NoVisPVS` and their `mod_novis` scratch cache stay in `gl_model.c`; only `Mod_DecompressVis` crosses into the seam.
- D3 Meson: `-Duse_rust_formats` / `-Duse_rust_image` feature options (`auto` follows `use_rust`, mirroring `use_rust_fs`), excluding `phase3_formats_c_srcs` / `phase3_image_c_srcs`; cargo features `formats`/`image`; `cargo_build.py` unchanged.
- D4 Data return: pure crates parse `&[u8]` into owned Rust types; `quake-capi/src/{model_parse.rs,image_decode.rs}` allocate via the same C allocator calls and populate `quake-types` `#[repr(C)]` mirrors in place (pointer wiring, anim chains, MD3 `hdrsize*numsurfs` single-block `nextsurface` chaining reproduced exactly).
- D5 Signature gate: `aliashdr_t`/`qmodel_t`-typed declarations in `quake_rs.h` `after_includes` guarded on gl_model.h's include guard; `check_capi_signatures.sh` gains a second gate TU with the Vulkan include path. Fallback: split `quake_rs_model.h` checked in the engine build TU.
- D6 Parity definition: committed field-mask + canonical hash walker (ctest `tests/support/model_hash.rs` + mirrored C walker). Hash all parse-derived scalars and pointed-to arrays; exclude Vk handles, texture pointers, raw ofs values. Mask changes require plan amendment. Brush models hashed likewise.
- D7 `Image_LoadImage` search loop stays C (path_id policy = fs logic); Rust replaces only the post-split `Image_Decode*` entry points.
- D8 Image crates: `png` (PNG) + `zune-jpeg` (JPEG); TGA hand-ported in `quake-image`. `image` facade rejected (tree size; Phase-2 `zip` precedent). Each swap its own revertible commit behind the pixel-exact gate; stb stays compiled until phase-exit deletion.
- D9 Corpus struct-hash gate lives in quake-ctest (synthetic committed fixtures + `QUAKE_GAME_DATA` real corpus via `fetch_shareware.py`), plus thin `scripts/harness/run_formats_corpus.py` emitting per-asset hash manifests (ADR-019: hashes only). No engine dump mode.
- D10 Threading proof: ctest concurrent test over the Rust texture/extents entry points + asan CI on the combined config + demo harness exercising all 4 dispatch sites (gl_model.c task submits).
- D11 Differential fuzzing: `rust/fuzz` gains a cc-based build.rs compiling the c_ref archive; targets `bsp_diff`, `mdl_diff`, `md3_diff`, `md5_diff`, `spr_diff`, `pcx_diff`, `lmp_diff` asserting (accept?, hash) equality with `Sys_Error` trapping. `rust.yml` fuzz-smoke hardcoded list extended.
- D12 Milestone ordering: image first (smallest slice proves every new mechanism), brush before alias, MD5 last among formats, crate swap dead last and deferrable.

## Change boundary

Expected to change: `Quake/gl_model.c`, new `Quake/model_parse.c`/`.h`, `Quake/image.c`, new `Quake/image_decode.c`; `rust/quake-formats/`, `rust/quake-image/`; `rust/quake-capi/src/{model_parse.rs,image_decode.rs,lib.rs}` + `cbindgen.toml`; `rust/quake-types/src/{bspfile.rs,modelgen.rs,spritegn.rs,model_mem.rs}`; `rust/quake-c-sys` allowlists + regen; `rust/quake-ctest/*`; `rust/fuzz/*`; `rust/Cargo.{toml,lock}`, `rust/deny.toml` (M8); `meson.build`, `meson_options.txt`; `scripts/harness/{check_capi_signatures.sh,run_formats_corpus.py}`; `.github/workflows/{rust.yml,build-linux.yml}`; `docs/rust-migration/ROADMAP.md`, ADR-003/ADR-012 amendments (M8).

Must not change without plan amendment: `Quake/gl_mesh.c`, `Quake/gl_texmgr.c`, `Quake/tasks.c`, renderer consumers of `aliashdr_t`; `Quake/gl_model.h` struct layouts; `bspfile.h`/`modelgen.h`/`spritegn.h` on-disk formats; `image.h` 4-function seam signatures; lodepng encode path; the D6 field mask once landed; Phase-1/2 crates and switches; harness demo goldens.

## Acceptance matrix

| ID | Acceptance criterion | Verification/gate | Status |
|---|---|---|---|
| AC1 | C prep splits are behavior-neutral | `run_corpus.py --compare` demo hashes identical pre/post on `build-c`; all 3 existing configs build | M1: local Windows clang-cl compare green (5/5 identical, shareware tier); Linux CI configs pending PR |
| AC2 | quake-types mirrors match C layouts | const asserts + `abi_probe` runtime checks | M3: green locally (`bsp_abi` 5/5, Windows MSVC); M4: `alias_abi` 4/4 (`modelgen.rs`/`spritegn.rs` on-disk mirrors + `aliashdr_t`/`maliasframedesc_t`/`msprite_t`/`mspriteframe_t` in-memory offsets); M5: `mdx_abi` 4/4 (`md3.rs` on-disk mirrors, `jointpose_t`/`aliasmesh_t`, the full `md5vert_t`/`md5vert8_t` field offsets M4 only pinned by size, and the `all_surfaces_def_t` block the MD3 loader allocates); per-platform confirmation via the rust.yml test matrix on PR |
| AC3 | PCX/LMP Rust decode byte-identical incl. reject behavior | ctest `image_differential`; `pcx_diff`/`lmp_diff` fuzz | M2: ctest portion green locally (10/10, Windows); M7: `fuzz_pcx`/`fuzz_lmp` land + soak clean (40s each locally), and `formats_corpus` runs every id1 `.lmp`/`.pcx` through both sides bit-for-bit |
| AC4 | Brush parsing (5 dialects, PVS, hulls, submodels, .lit/.vis/.ent) hash-identical | ctest `bsp_differential` + `run_formats_corpus.py`; `bsp_diff` fuzz | M3: ctest portion green locally (9/9, Windows); M7: `fuzz_bsp` lands + soaks clean, and `formats_corpus` proves every real map (190 id1 + hipnotic + rogue + rerelease on the dev host; shareware id1 as the standing CI leg) hash-identical incl. `.lit`/`.ent` sidecar probes and the world-model submodel branch (sv_modelname == map name) |
| AC5 | MDL/SPR parity (field-masked hash) | ctest `alias_differential`, `sprite_differential`; fuzz | M4: ctest portion green locally (`alias_differential` 10/10, `sprite_differential` 10/10, Windows MSVC); M7: `fuzz_mdl`/`fuzz_spr` land + soak clean, and `formats_corpus` runs every real `.mdl` (with real skins via the stub cursor-advance) and `.spr` bit-for-bit |
| AC6 | MD3/MD5 parity incl. baked influences/normals bit-for-bit | ctest `md3_differential`, `md5_differential`; fuzz | M5: ctest portion green locally (`md3_differential` 14/14, `md5_differential` 15/15, `mdx_abi` 4/4, darwin-arm64), and the 15th md5 case is an env-gated real-asset run over **59 re-release `.md5mesh` models** from `rerelease/id1/pak0.pak` compared bit-for-bit; M7: `fuzz_md3`/`fuzz_md5` land + soak clean, and `formats_corpus` re-runs the 59 real re-release `.md5mesh` (dev host) through the shared driver; MD3 real-asset coverage remains absent (RA15: no `.md3` exists in the depot) |
| AC7 | Engine behavior unchanged with switches on | `run_corpus.py --compare` vs `build-c`; `save_diff.py` | M7: green on darwin-arm64 — RA12 resolved (styles_bitmap now zeroed in `model_parse.c`), `--compare build-rs vs build-c` 10/10 identical, both builds pass the committed goldens (`--check` 10 ran/0 failed), `save_diff.py` byte-identical (91562/91547). `formats_corpus` additionally proves 684 real assets (id1+hipnotic+rogue+rerelease, incl. 59 md5mesh) parse bit-for-bit C-vs-Rust — a dev-host full-depot run; the *standing* CI gate re-verifies shareware id1 only (ADR-019: no game data in CI beyond the shareware fetch) |
| AC8 | Threaded loading works with Rust loaders | ctest concurrent test + demo harness + asan | M6: `threaded_parse` 4/4 green (darwin-arm64), mutation-checked; harness worker sweep (1 vs 8 workers) 10/10 identical; asan job extended with a map load and a forced 8-worker run. **Partial, and scoped explicitly after review**: the ctest covers the Rust reached by dispatch sites 1-2 (`CalcSurfaceExtents`) and the decoders sites 3-4 reach, at the call level. The harness sweep proves worker count does not perturb the *simulation*, but its state hash cannot see skin-loader output, so sites 3-4's own output is not gated for order independence (RA17). `Mod_DecompressVis` and the alias-scratch contract are reasoned, not exercised -- headless never starts the render task graph |
| AC9 | Signature/ABI/bindgen gates extended and green | `check_capi_signatures.sh` (both TUs), `check_headers.sh`, bindgen regen-diff | M2: image portion green locally; M3: second gate TU added (`gl_model.h`/`bspfile.h`/`model_parse.h` behind Vk + atomics stand-ins) and green under clang-cl; c-sys regen diff = `COM_SkipPath`/`COM_StripExtension`/`external_ents` only; Linux CI canonical run pending PR; M4: gate TU extended with a hand-copied `gl_texmgr.h` slice for `TexMgr_LoadImage` and green; c-sys regen diff empty; M5: gate TU extended with a hand-copied `glquake.h` slice for `GLMesh_UploadBuffers`/`GLMesh_DeleteMeshBuffers` and green; c-sys regen diff is exactly `+q_strtrim` |
| AC10 | Differential fuzz infrastructure runs in CI | `rust.yml` fuzz-smoke with new targets; local soak clean | M7: seven targets (`fuzz_pcx/lmp/bsp/mdl/spr/md3/md5`) added to the `rust.yml` fuzz-smoke list with committed seeds; each soaked ≥40s locally with 0 crashes (one real find fixed — `vis_row` overflow on an out-of-domain `numleafs`, clamped to the shim's `parse_leafs`-capped range). Per the design note below the FFI differential is the `formats_corpus` gate + ctest suites; the fuzz targets exercise the pure accept/reject predicates the shims are built on |
| AC11 | PNG/TGA crate decode pixel-exact vs stb over corpus; JPEG under the owner-relaxed gate (accept/reject + dims parity + pinned per-channel delta — amendment log 2026-08-24) | ctest `image_crate_differential` per format + `image_real_assets`; `formats_corpus` over `.tga/.png/.jpg`; `cargo deny`; ADR-003 + ADR-012 amendments | **M8: closed.** TGA (hand-port) and PNG (`png` crate behind a hand-ported stb acceptance walk) bit-exact incl. con-log policy — differential 15/15 with 8 mutations each failing the suite; JPEG (`zune-jpeg`) delta 0 on the 12 synthetic fixtures, 3–4 on the two real depot photographs, bound pinned at 8; the 3 in-repo vq_pak PNGs are a standing CI corpus leg; full-depot image corpus in the M8 evidence; deny green both manifests; ADR amendments landed |
| AC12 | Phase-exit full-suite rerun (ADR-019) | all workflows + full corpus + fuzz smoke on final milestone | **M8: closed on darwin-arm64** — see the M8 evidence: workspace tests debug+release (+`QUAKE_GAME_DATA` runs), clippy/fmt/deny both manifests, five Meson configs each `run_corpus.py --check` 10/10, `--compare` 10/10 identical, `save_diff.py` byte-identical, full-depot `formats_corpus` all gamedirs, signature/header/bindgen gates, three new fuzz targets soaked; Linux/Windows/MinGW legs = CI on PR |

## Milestones

One milestone per `/feature-implement` session; stop at each boundary and record evidence.

- **M1 — C prep: behavior-neutral split.** Create `Quake/model_parse.c`/`.h` (move: `Mod_DecompressVis`, brush lump loaders incl. BSP2/2PSB variants, `CalcSurfaceExtents`, `Mod_MakeHull0`, `Mod_LoadLighting`/`Visibility`/`Entities`, external-vis loaders, `Mod_SetupSubmodels`, `Mod_ParseTextures` split, alias and sprite parse moves with the `Mod_ParseAliasModel` GPU-call hoist; MD3/MD5 deferred to M5, alias de-static and the sprite TexMgr sub-split to M4) and `Quake/image_decode.c` (PCX, LMP, stb wrapper). Register `phase3_formats_c_srcs`/`phase3_image_c_srcs` in meson (unconditionally compiled for now). ROADMAP MD5-JSON correction. Commit this plan. `./format.sh`. Acceptance: AC1.
- **M2 — quake-image PCX/LMP port + `-Duse_rust_image` + differential substrate.** `quake-image::{pcx,lmp}`; capi `image_decode.rs`; Meson option; ctest c_ref of `image_decode.c` + `image_differential.rs`; signature gate adds `image.h`. Acceptance: AC3 (tests), AC9 (image portion).
- **M3 — quake-types mirrors + brush/BSP port + `-Duse_rust_formats`.** `bspfile.rs`/`model_mem.rs` mirrors + abi_probe; `quake-formats::bsp`; capi `model_parse.rs`; D5 second gate TU; `bsp_differential.rs` with synthetic dialect fixtures; c-sys regen. Acceptance: AC2, AC4 (tests), AC9.
- **M4 — MDL + SPR.** `quake-formats::{mdl,spr}`; `modelgen.rs`/`spritegn.rs` mirrors (document `synctype_t` dual-definition hazard); D6 masked-hash differentials. Acceptance: AC5 (tests).
- **M5 — MD3 + MD5.** *(done, 2026-08-23)* Parse-only ports incl. `MD5_BakeInfluences`/`MD5_ComputeNormals` bit-exact vectors; MD3 single-block allocation reproduced. Acceptance: AC6 (tests).
- **M6 — Threaded-loading proof.** *(done, 2026-08-24)* ctest `threaded_parse.rs`; asan config extended; global-state audit; RA12 resolved. Acceptance: AC8.
- **M7 — Differential fuzzing + corpus gate + CI.** fuzz build.rs + 7 diff targets; `run_formats_corpus.py`; CI configs `build-rs-formats`/`build-rs-all`. Acceptance: AC3–AC6 fuzz portions, AC7, AC10.
- **M8 — PNG/JPG/TGA crate decode (ADR-012 gate).** *(done, 2026-08-24)* TGA hand-port, `png`, `zune-jpeg`, each revertible and corpus-gated; deny/ADR-003 amendment; phase-exit full rerun + `/integration-review`. Acceptance: AC11, AC12.

## Final verification

- `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace` (release + debug), `cargo deny check` (main + fuzz manifests).
- Meson builds: `build-c`, `build-rs`, `build-rs-cfs`, `build-rs-formats`, `build-rs-all`.
- `check_capi_signatures.sh` (both TUs), `check_headers.sh`, `gen_c_bindings.sh` regen-diff.
- `run_corpus.py --compare`, `save_diff.py`, `run_formats_corpus.py` over shareware; fuzz soak per D11 target.
- darwin-arm64 golden rerun is manual/not-run-here (goldens exist only there); Windows/MinGW C-only legs unaffected.

## Risks, assumptions, open questions

| ID | Type | Item | Mitigation/decision | Status |
|---|---|---|---|---|
| RA1 | risk | In-memory mirrors drift vs Vulkan-bearing `gl_model.h` | abi_probe runtime offset checks for every shim-written field; D5 gate TU | M3: both mitigations in place and green on Windows (`bsp_abi` 5/5, second gate TU); stays open until the CI matrix confirms Linux/macOS |
| RA2 | risk | Float math differs bitwise on some platform | ADR-010 discipline; targeted bit-exact vectors in M5 | M5: the three float-sensitive MD5 kernels are covered by bit-for-bit differentials on darwin-arm64 -- `Matrix3x4_Invert_Simple`'s double-precision reciprocal, the double weight scaling and `MD5_ComputeNormals`' double `acos` each have a fixture that *fails* if the precision is changed, and 59 real models compare byte-identically. Stays **open** until the CI matrix confirms the other platforms |
| RA3 | risk | c_ref build of `model_parse.c` needs many stubs | keep moved functions' external calls minimal via M1 hoists; malloc-backed arena stub | closed M3 — the brush half needed 8 new stubs plus a dummy-texture helper (amendment log); oracle builds and runs |
| RA4 | risk | Synthetic BSP2/2PSB/Q64 fixtures hard to author | commit generator code, not binaries; hash-manifest real assets per ADR-019 | closed M3 — `bsp_differential.rs` builds all 5 dialects in-process from committed Rust lump builders; real-asset corpus still lands M7 |
| RA5 | risk | Vulkan headers in signature-gate TU brittle on CI | fallback `quake_rs_model.h` checked in engine build TU | closed M3 — the second TU uses `__Q_RENDER_TYPES_H`/`__ATOMICS_H` stand-ins and needs no Vulkan SDK; fallback unused |
| RA6 | risk | Task-worker bugs only reproduce under load | M6 asan + concurrent ctest; harness exercises all 4 dispatch sites per map load | **closed M6, with the mitigation corrected**: the harness does *not* exercise all 4 sites -- two sit behind `!no_rendering` and `-headless` sets it (amendment log). The concurrent ctest covers those two plus `Mod_DecompressVis`; the harness covers the other two and adds a worker-count sweep. Four mutations were confirmed to fail the suite, one of which (a shared decode buffer) initially *passed* and drove the round count up. **Narrowed in review**: the worker-reachability of `Mod_LoadModel` is reasoned, not exercised, and has a known open hole (see the audit table and RA18) |
| RA7 | risk | `png` transitive deps or license drift | ADR-003 review notes + deny CI gate; M8 deferrable | **closed M8** — `png` 0.18.1 + `zune-jpeg` 0.5.15 and their full trees are MIT-dual/permissive (`cargo deny` green on both manifests; review notes in the ADR-003 M8 amendment); the deny CI gate guards drift |
| RA8 | decision | ROADMAP "MD5 + JSON metadata" has no C counterpart here | documented not-applicable; one-line ROADMAP correction in M1, flagged in PR | open |
| RA9 | assumption | Dev host Windows; parity gates run in Linux CI; shareware via `fetch_shareware.py` | local dev uses synthetic fixtures; corpus/harness gates rely on CI; darwin goldens manual | open |
| RA10 | question | stb link-exclusion timing | leave compiled (oracle) until phase-exit deletion (NG1) | open |
| RA11 | risk | `synctype_t` dual definition (modelgen vs spritegn, unequal) | quake-types defines one canonical enum; comment documents the C hazard | open |
| RA12 | risk | `msurface_t::styles_bitmap` is OR-ed into without initialization over a `Mem_AllocNonZero` block (`model_parse.c:758,780`), so the C engine's value is whatever the allocator left behind; the Rust port starts at 0 and is deterministic | recorded as a deliberate divergence (`// COMPAT` at the OR site in `bsp/lumps.rs`); ctest parity holds because the `Mem_AllocNonZero` stub zero-fills | **closed M6.** `Mod_ParseFaces` now zeroes `styles_bitmap` before the OR loops, so the C matches the already-deterministic Rust. No golden moved (the state hash does not cover renderer state), but this **does change shipped engine behaviour**, in the engine's favour: `styles_bitmap` feeds `lightmaps[..].modified[worker]` (`r_world.c:444,588,855,919`) and `deps[].lightmap_styles` (`r_brush.c:432,440`), which previously OR-ed in allocator garbage and drove spurious lightmap rebuild work every frame; and the `if (!out->styles_bitmap) out->styles_bitmap = 1;` fallback (`model_parse.c:787`), previously near-dead because garbage was rarely zero, is now live for the all-255-styles case. M7 adds corpus evidence: the 684-asset `formats_corpus` gate (many multi-submodel maps) is bit-for-bit, and the `1 << 31` signed-shift UB in the fold was cleaned to `1u <<` in the PR #18 review round |
| RA13 | risk | `Mod_SetupSubmodels`' duplicate-model loop (`Mod_FindName` + `"*%i"` naming for submodels 1..n) is not exercised by `bsp_differential` — the ctest `Mod_FindName` stub returns a fresh pool entry, so the shared-name/reuse branch never runs on either side | the loop is a verbatim structural port and the ctest stub is identical for C and Rust, so a divergence there would be a transcription error, not a behavior difference | **closed M7.** The `formats_corpus` gate loads every real id1/hipnotic/rogue/rerelease map through the full seam call order incl. `Mod_SetupSubmodels`, so the duplicate-model `Mod_FindName`/`"*%i"` loop runs on both sides and is compared per pool clone (the ctest stub's real `Mod_FindName` mints one clone per submodel; the gate raised its pool cap to 1024 to hold real maps' submodel counts) |
| RA14 | risk | MD3/MD5 parity is only visible in the `GLMesh_UploadBuffers` argument stream, not in `aliashdr_t`; a mis-sized recorder would compare nothing | the stub derives every buffer length from the header the loader just filled (identically for both sides), FNV-hashes each buffer unconditionally so an over-large one still compares, and each test asserts the recorded lengths against what its fixture implies | closed M5 -- and eight independent mutations were each confirmed to fail the suite |
| RA15 | risk | MD3 has **no** real-asset coverage: the local re-release depot ships `.md5mesh` replacements only, no `.md3`, so MD3 parity rests entirely on the synthetic in-process fixtures | the fixtures build all the structural variation the loader has branches for (multi-surface chaining, zero-triangle surfaces, the frame-name quirk, every `Sys_Error`), and the M7 corpus gate will pick up any `.md3` the wider mod bench provides | **open — confirmed no `.md3` in the depot.** The M7 `formats_corpus` sweep of id1/hipnotic/rogue/rerelease found zero `.md3` files, so MD3 real-asset coverage stays absent; the gate is wired to run MD3 (with GLMesh-upload payload parity) the moment any `.md3` appears in a mounted gamedir, and `fuzz_md3` now exercises the header/surface accept-reject gates over arbitrary bytes. Carries into the wider mod bench |
| RA16 | risk | the Rust MD5 loader calls `Mod_LoadMD5SurfaceSkins`, which submits tasks and `Task_Join`s, so a Rust frame is live across the task system | no Rust state is shared into the workers (the callback takes only C-owned pointers) and the Rust frame merely blocks, but this is a new shape for the seam | **closed M6.** Audited and written up at the `Mod_LoadMD5SurfaceSkins` import: the task argument struct is a C local built from C-owned pointers, each worker writes only the `aliashdr_t` slots for its own skin index, and the blocked Rust frame holds no lock and no `&mut` into shared state |
| RA17 | risk | `Mod_LoadSkinTask` / `Mod_LoadMDXSkinTask` output is not gated for worker-order independence by anything at engine level: the M6 worker sweep compares the harness state hash, which covers simulation state only and never sees decoded texture bytes, `aliashdr_t` fields or `gltexture_t` state | the decoders those tasks call are covered at the call level by `threaded_parse::image_decode_concurrent_matches_serial_and_c`; what is uncovered is the task bodies' own per-index bookkeeping. Options for M7: extend the hash chain with a load-time model/skin digest behind a harness flag (`tests/support/model_hash.rs` has the shape), or accept and document | **open -- raised in the M6 review round, carried into M7** |
| RA18 | risk | a task worker can reach a real `Mod_LoadModel` parse, which is unsound against the process-global alias scratch: `R_AllocateEntityBLAS` (reached from the `R_StoreLeafEFrags` task) lacks the `needload` guard its siblings at `gl_mesh.c:877`/`:911` have, and `Mod_EnhancedModels_f` reloads with `crash == false`, leaving `needload` set when a model file is missing. Needs `r_rtshadows` on | pre-existing C behaviour, not introduced by the port, and not fixable inside Phase 3 (NG2 freezes `gl_mesh.c`). M6 ships a `Con_Warning` at the point of violation instead of the assert originally proposed, so a debug build reports it rather than aborting. The one-line fix (add `e->model->needload` to `R_AllocateEntityBLAS`'s guard chain) belongs to Phase 8, which owns the renderer | **open -- raised in the M6 review round** |

## Plan amendment log

| Date | Repository contradiction/evidence | Smallest amendment | Acceptance impact | Approval |
|---|---|---|---|---|
| 2026-08-20 | `stverts`/`triangles`/`poseverts` externs (gl_model.h:578-580) are consumed by frozen `gl_mesh.c`; introducing `aliasparsectx_t` would change that header/consumer | M1 moves the alias globals to `model_parse.c` verbatim (identical linkage) instead of de-staticizing into a context struct; de-static decision deferred to M4 (Rust side owns its own state; capi writes the same globals) | none — AC5 unchanged | in-session, behavior-neutral |
| 2026-08-20 | `Mod_FloodFillSkin` callers (`Mod_LoadAllSkins`, MD5 skin path) all stay in `gl_model.c` | `Mod_FloodFillSkin` stays in `gl_model.c` for M1 | none | in-session |
| 2026-08-20 | MD5/MD3 loaders are tightly interleaved with `GLMesh_UploadBuffers`/TexMgr calls; extracting them is the M5 port's natural first step, not a neutral move | MD5/MD3 parse-split deferred from M1 to M5; sprite `TexMgr_LoadImage` sub-split deferred to M4 | none — split lands with the port it serves | in-session |
| 2026-08-20 | `RadiusFromBounds` sole caller is `Mod_SetupSubmodels` (moved) | `RadiusFromBounds` moved to `model_parse.c` (not in original move list, internal linkage only — no header entry) | none | in-session |
| 2026-08-20 | PR review: `Mod_LeafPVS`/`Mod_NoVisPVS` are per-frame runtime queries (`pr_cmds.c`, `pr_ext.c`, `r_world.c`, `sv_main.c`), not format parsing; moving them put mutable `mod_novis` process-global scratch into the module ROADMAP calls "pure functions over byte slices" | Both functions and the `mod_novis` cache returned to `gl_model.c`; only `Mod_DecompressVis` (with its own separate `mod_decompressed` cache) stays in `model_parse.c`. D2 records the scope boundary | none — removes a dual-state hazard for the M3 ctest oracle | review round |
| 2026-08-20 | PR review: `model_parse.h` declared three `gl_model.c`-owned symbols, making the seam header circular and putting non-`quake-formats` symbols on the future FFI boundary | `Mod_FindName`/`Mod_LoadWadTexture`/`Mod_LoadAllSkins` prototypes moved to `gl_model.h`, matching the existing `Mod_SetExtraFlags` convention; `model_parse.h` now declares only what `model_parse.c` provides | prevents 3 spurious symbols reaching the M2+ signature/bindgen gates (AC9) | review round |

| 2026-08-21 | M2: the milestone's ctest c_ref build assumed stb-in-TU (`common_fs.c` precedent), but the STB decoder needs no differential oracle (stays C until M8) and drags the 7k-line stb_image implementation plus its Mem_/Sys_ surface into every c_ref TU | `image_decode.c` split again: PCX/LMP (the gated oracle, excluded under `-Duse_rust_image`) stay in `image_decode.c`; `Image_DecodeSTB` + stb implementation moved byte-identically to new `Quake/image_stb.c`, compiled unconditionally in all configs until M8 | none — AC3 oracle smaller and stub-free; stb deletion timing (NG1/RA10) unchanged | in-session, behavior-neutral |
| 2026-08-21 | M2: the C PCX decoder streams via `Sys_fgetc`/`Sys_FilePos` and trusts input — RLE overrun writes past the `(w*h+1)*4` heap block, EOF mid-run indexes `palette[-3]`, and a sub-768-byte pak resource seeks/reads outside the resource bounds: all UB or out-of-resource reads with no defined behavior to replicate | quake-image bounds every access to the resource slice and the capi shim `Sys_Error`s `'%s' is not a valid PCX file` on exactly those inputs; every defined C path (including the error order, message bytes, alloc-before-palette sizing, run spill-over between rows, last-line padding write) is replicated. Divergence is confined to UB inputs and marked `// COMPAT` in `pcx.rs` | AC3 parity is asserted over defined behavior; `image_differential` reject cases cover the C-defined `Sys_Error` paths only | in-session |
| 2026-08-21 | M2: a streaming Rust port would need `Sys_FilePos`/`Sys_fgetc`/`Sys_feof` added to the FFI surface solely to reproduce the C read pattern | capi shims bulk-read the resource once (`COM_ThreadFileSize` truncated to int, one `Sys_FileRead`) and hand slices to pure parsers; only `COM_CloseFile` was added to the c-sys allowlist | none — same bytes observed; shrinks the AC9 surface | in-session |
| 2026-08-21 | M3: `Mod_LoadLeafsExternal` also raises `Host_Error` (external-vis leaf-count mismatch), so the plan's "4 Host_Error-capable entry points" undercounted the seam by one | five status-returning exports (`quake_rs_mod_load_{leafs,clipnodes,marksurfaces,leafs_external}`, `quake_rs_mod_setup_submodels`) with a `char err[256]` out-param, re-raised from `Quake/model_parse_glue.c` | none — AC4 unchanged; PLAN.md 4.3 satisfied for every longjmp site | in-session |
| 2026-08-21 | M3: excluding `model_parse.c` from the build under the switch (the M2 `image_decode.c` pattern) would also drop the alias/sprite half, which stays C until M4/M5 | `model_parse.c` is compiled unconditionally; `-Duse_rust_formats` defines `USE_RUST_FORMATS`, which `#ifndef`-guards out only the brush/BSP region (4 guard lines, no body edits), and adds `model_parse_glue.c` to the sources | none — the C brush half stays a byte-identical oracle under `-Duse_rust_formats=disabled` | in-session |
| 2026-08-21 | M3: `qboolean` is `typedef bool` (1 byte), not `int`, so the `qboolean bsp2` parameters of `Mod_ParseFaces`/`Mod_LoadClipnodes` are not ABI-compatible with `c_int` | those two shims take Rust `bool`; the `int bsp2` loaders (`Mod_LoadEdges`/`Nodes`/`Leafs`/`Marksurfaces`) keep `c_int`, matching `model_parse.h` exactly (diffed by the new gate TU) | none — prevents a silent ABI mismatch | in-session |
| 2026-08-21 | M3: `Mod_ParseFaces` ORs into `msurface_t::styles_bitmap` without initializing it, and the surfaces come from `Mem_AllocNonZero` — with a poisoned ctest allocator the C oracle is not reproducible | the ctest `Mem_AllocNonZero` stub zero-fills (documented in `stubs.c`); the engine bug is left untouched and the Rust port reproduces the OR-into-zero result | AC4 parity is asserted against a zero-initialized arena; the underlying engine bug is recorded, not fixed | in-session |
| 2026-08-21 | M3: `com_filesize` is per-thread in this engine (`COM_ThreadFileSize()`), so the plan's "allowlist `com_filesize`" would have bound the shims to a global that does not exist | the `.lit`/`.ent` shims call `COM_ThreadFileSize()`; the c-sys allowlist additions are only `COM_SkipPath`, `COM_StripExtension` and the `external_ents` cvar | none — smaller AC9 surface | in-session |
| 2026-08-21 | M3: the ctest c_ref build of `model_parse.c` needed more of the engine than RA3 anticipated — `BSP29_VALVE` (unconditional in `quakedef.h`, gated on by both `bspfile.h` and `model_parse.c`), the `r_notexture_mip`/`r_notexture_mip2` dummy slots `Mod_LoadTextures` fills in `gl_model.c` after `Mod_ParseTextures` (without them `Mod_ParseFaces` NULL-derefs on `TEX_MISSING` texinfos), and five symbols pulled in by the still-C alias/sprite half | prelude defines `BSP29_VALVE`; `stubs.c` gains `ctest_fill_dummy_textures` (called on both sides by the differential driver) plus `q_strncasecmp`, `PScript_UpdateModelEffects`, `thread_stack_alloc_size`, `max_thread_stack_alloc_size`, `r_nolerp_list` | none — oracle fidelity only; RA3 closed | in-session |
| 2026-08-21 | M3: the approved plan's ground facts referenced a `gl_load24bit` cvar read in the brush range; no such symbol exists in the repo | reference dropped; the only cvar the brush range reads is `external_ents` | none | in-session |
| 2026-08-21 | PR review (#13): `CalcSurfaceExtents` used `f32::min`/`f32::max`, which discard a NaN operand, while the C `q_min_f`/`q_max_f` (`q_minmax.h:41-48`) are `(a < b) ? a : b` and therefore *store* a NaN | `bsp/extents.rs` uses the ternary form with a `// COMPAT` note; a `nan_propagates_like_q_min_q_max` unit test pins the behavior | AC4 — closes a real parity hole on degenerate texinfo vectors | review round |
| 2026-08-21 | PR review (#13): `parse_textures` early-returned only on an empty lump, so a 1..3-byte lump indexed past the slice; under `panic = "abort"` that turns a C load-with-garbage (UB) into a hard abort | the guard is `lump.len() < 4`; the `Con_Printf ("...no textures in bsp file")` decision is unaffected because the shim keys it on `l->filelen == 0`, not on the parser's return | none — removes an abort on a malformed input | review round |
| 2026-08-21 | PR review (#13): `pcx.rs` read the header scalars as `i8`, but the C `pcxheader_t` fields are plain `char`, which is unsigned on aarch64 Linux — so `Sys_Error ("'%s' is version %i, ...")` would print a different number there | the header scalars and `Error::BadVersion` use `core::ffi::c_char` | AC3 — message parity now holds on every target, not just signed-char ones | review round |
| 2026-08-21 | PR review (#13): the `-Duse_rust_image`/`-Duse_rust_formats` switches had no CI leg in their `disabled` position, so nothing kept the C fallback compiling and linking | `build-linux.yml` gains `build-rs-cimg` and `build-rs-cfmt` legs plus their signature-gate runs; Linux only, since the failure mode (a C file dropped from the source list, or a symbol only the Rust half defines) is platform-independent and this is the cheapest matrix leg | AC1/AC9 — the C fallback is now gated, not assumed | review round |
| 2026-08-22 | CI (#13, macos-latest `unit + differential tests`): the c_ref build of `model_parse.c` reached `PScript_UpdateModelEffects` (declared in the Vulkan-pulling `glquake.h`, called under the prelude's `PSET_SCRIPT`) with no declaration in scope — an error under Apple clang, only a warning under gcc/cl, so Linux and Windows passed | `c_ref_prelude.h` declares the function next to the existing `Host_Error`/`Con_DWarning` slice; every c_ref and stub TU re-verified locally with `-Werror=implicit-function-declaration` | none — oracle fidelity only | in-session |
| 2026-08-22 | M4: `Mod_CalcAliasBounds` is also called by the MD5 loader (`gl_model.c:3837`, still C until M5), so under `-Duse_rust_formats` the Rust export serves `PV_MD5`/`PV_MD5_8`/MD3 vertex data, not just the MDL `trivertx_t` path the milestone ports | the capi shim ports the whole function including all `poseverttype` branches and the `numvertexes`/`vertexes` override path, keyed on `a->poseverttype` exactly like the C | none -- AC5 unchanged; prevents a wrong-bounds regression in the still-C MD5 path | in-session |
| 2026-08-22 | M4: the M1 amendment deferred the alias-globals decision to M4 ("Rust side owns its own state; capi writes the same globals"). Splitting the *grow counter* for `triangles` is unsafe on its own terms, not merely order-sensitive: `q_max (size * 2, numtris)` only runs when `numtris > size`, so a side whose private counter lags can realloc the shared buffer *down* to `numtris` while the other side still believes it owns `size * 2` and skips its own grow -- e.g. counters (C=40, Rust=0) with `numtris=41` leave a 41-element buffer the C will next write 50 entries into | `stverts`/`triangles`/`poseverts` keep C linkage (read by frozen `gl_mesh.c`) and so does `triangles_size`: `model_parse.c` now defines it unconditionally with external linkage, declared in `model_parse.h`, and the Rust `check_tris_size` imports and updates that one counter. Only `posenum` moves to Rust -- it is reset to zero at the top of every `Mod_ParseAliasModel`, so it carries no state across the seam. The differential still runs C before Rust under `ctfs::lock()` for console-log and scratch-array comparison, but parity no longer depends on a fixture-set property | none -- AC5 is asserted without an unmechanised precondition (amends the review-superseded lockstep note) | in-session |
| 2026-08-22 | M4: M3 left `ReadLongUnaligned`/`ReadFloatUnaligned` outside the `USE_RUST_FORMATS` guard because the still-C alias/sprite half used them; M4 ports that half, leaving no C user in the file | the guard moved up to cover all three readers (`ReadShortUnaligned` was already inside); `nameInList`/`Mod_SetExtraFlags` and the M5 MD3/MD5 code in `gl_model.c` (which carries its own copies) are unaffected | none -- avoids an unused-static warning under the switch | in-session |
| 2026-08-22 | M4: the M3 policy of bounded reads over a `&[u8]` cannot be applied to MDL/SPR -- `Mod_ParseAliasModel`/`Mod_LoadSpriteModel` receive only `void *buffer` (`model_parse.h:57,61`); `gl_model.c` has no length to pass, and adding one would change the seam header and the frozen callers | `quake-formats::{mdl,spr}` expose pure header/record decoders over slices for the parts that have a known extent, and the capi shims walk the file image by raw pointer exactly like the C, cursor-for-cursor. Over-read on a truncated file is the same UB the C has, marked `// COMPAT` at the walk sites | AC5 parity is asserted over well-formed and C-defined-reject inputs; truncated-file UB is out of scope (as in M2 `pcx.rs`) | in-session |
| 2026-08-22 | M4: two orderings inside `Mod_ParseAliasModel` are observable but not state-affecting -- C interleaves the `Con_Printf` validation warnings with the header writes, and reaches `check_tris_size` *before* the `numframes < 1` `Sys_Error`, whereas the Rust shim writes the whole header, then emits `mdl::validate()` diagnostics, then sizes the triangle buffer | the console-log *content and order* is asserted equal by the differential; the two internal orderings are documented as `// COMPAT` divergences (they are unobservable to the engine: the header is not read between the two points, and the `check_tris_size` call before a fatal only grows a buffer the aborting process never uses) | AC5 -- parity claim is over the console stream and final state, not over intra-function ordering | in-session |
| 2026-08-22 | M4: the sprite loader calls `TexMgr_LoadImage`, declared in `gl_texmgr.h`, which pulls Vulkan and `tasks.h` -- the signature-gate TU cannot include it | the gate TU claims `_GL_TEXMAN_H` and hand-copies the `enum srcformat` + `gltexture_t` + `TexMgr_LoadImage` slice (the same technique the ctest c_ref prelude already uses). Residual risk: drift between the slice and the real header is caught only by the engine build, not by the gate | AC9 -- gate coverage for this one symbol is against a copied declaration, recorded rather than claimed as header-diffed | in-session |
| 2026-08-22 | M4 (review): with no length at the seam, the *touch set* is the only bound on a malformed image, and `core::slice::from_raw_parts` requires the whole span to be a live allocation at construction -- so materialising the 84-byte `mdl_t` (or 36-byte `dsprite_t`) before the version check reads past what the C ever touches on a truncated file, reachable from `COM_LoadFile`d mod content | `quake-formats::{mdl,spr}` export `parse_version` (and `spr::parse_numframes`) over a prefix slice; the shims check each field off the smallest legal prefix and only build the full-header slice once every `Sys_Error` gate the C applies first has passed, with `debug_assert_eq!` tying the two reads together | none -- AC5 unchanged; narrows the documented truncated-file UB to exactly the C's own touch set | in-session |
| 2026-08-22 | M4 (review): `Mod_CalcAliasBounds`'s `PV_MD5`/`PV_MD5_8`/`PV_QUAKE3` arms ship live under `-Duse_rust_formats` (the still-C MD5/MD3 loaders call the Rust export) but had no behavioural test -- only `size_of`/`offset_of` asserts -- because `alias_differential` only builds `PV_QUAKE1` fixtures. That is a different risk shape from "synthetic fixtures may miss real content": the path had no test at all | the function is a leaf (no file image, no `Mem_Alloc` heap, no shared scratch), so four cases drive it directly off a vertex buffer and compare the six output vectors bit-for-bit: `calc_bounds_md5_parity`, `calc_bounds_md5_8_parity`, `calc_bounds_quake3_parity` (the only arm applying `MD3_XYZ_SCALE`) and `calc_bounds_empty_parity` (the `FLT_MAX` / `-0.0` seeding, for all three of MD5/MD3/QUAKE1) | AC5 -- the MD5/MD3 bounds arms move from asserted-by-layout to asserted-by-differential | in-session |
| 2026-08-22 | M4 (review): the two `Con_DWarning` paths a modern replacement-model pack actually trips (`numverts > 2000`, `numtris > 4096`) had no differential case -- their format strings carry a trailing `\n` the tall-skin string does not -- and `mdl::validate`'s push order was unasserted against the C's interleaved checks | added `vertex_count_over_qs_limit_warns`, `triangle_count_over_qs_limit_warns`, `multiple_warnings_keep_c_order` (all three warnings at once, order pinned) and `too_many_vertices_fatal_parity`. The earlier note that these need multi-megabyte fixtures was wrong: 2001 verts / 4097 tris is a ~100 KB image | AC5 -- diagnostic coverage is now complete over `mdl::validate` | in-session |
| 2026-08-23 | M5: D1 prefers a per-parse-function seam, but `Mod_LoadMD5MeshModel`/`Mod_LoadMD3Model` are the *only* non-static entry points into the MD5/MD3 range (each called once, from `Mod_LoadModel`); everything else is `static` | the two entry points are the replaced symbols -- a whole-loader seam. The orchestrator stays C, which is D1's actual intent | none -- AC6 unchanged | in-session |
| 2026-08-23 | M5: MD5 is a *text* format driven by `COM_Parse` over the thread-local `com_token`. Porting the tokenizer would put a second copy of a `common.c` function (outside Phase 3's scope) in the binary and create a drift hazard | the capi shim calls `COM_Parse`/`COM_ThreadToken` and the C runtime's `strtoull`/`strtol`/`strtod`; `quake-formats::md5` holds only the pure kernels (matrix/quaternion math, influence baking, normal generation, parent remap). Same split as the M4 MDL/SPR "shim walks, crate decodes" precedent | none -- AC6 is asserted over the same token stream both sides see | in-session |
| 2026-08-23 | M5: the moved MD5/MD3 loaders call three `static` functions that stay in `gl_model.c` (`Mod_LoadMDXSkinsByIndex`, `Mod_LoadMD3SkinDefinitions`, `Mod_LoadMD3SurfaceSkins`), and the first takes a `skin_base_name_fn` function pointer | the two MD3 ones lose `static` and gain `gl_model.h` prototypes (the M3 `Mod_FindName` precedent); the MD5 call is wrapped in a new `Mod_LoadMD5SurfaceSkins` in `gl_model.c` so the function-pointer table never reaches the FFI boundary. This is the *only* non-verbatim line in the M1-style move | AC9 -- three new declarations on the gate boundary, all header-diffed | in-session |
| 2026-08-23 | M5: `quake-formats` had no dependencies, but `MD5_ComputeNormals` needs `hash_map.c` semantics and bit-exact `VectorNormalize`, and its `acos` must be the platform libm (ADR-010), not a Rust reimplementation | `quake-formats` gains `quake-math`, `quake-util` and `quake-c-sys` as **in-workspace** dependencies (all pure or safe-wrapper crates); `acos`/`sqrtf` are added to `quake_c_sys::libm`. `Cargo.lock` gains no third-party crate, so ADR-003 review is not engaged | none | in-session |
| 2026-08-23 | M5: a real MD5 model's vertex block can outgrow the ctest upload recorder's byte buffer, and the recorder's reader walked past the log's capacity when a model has more surfaces than the log holds | the stub FNV-hashes every buffer unconditionally (so an over-large payload still compares) and the reader exposes the true call count separately (the clamp this originally used was replaced by an assert in the PR-review round below) | AC6 -- lets the real-asset corpus run without silently comparing nothing | in-session |
| 2026-08-23 | M5: the plan called the real-asset check "ad-hoc, not a committed gate", but the ctest already has the Phase-2 per-side `ctfs::setup` machinery and the dev host has the re-release depot | committed as an **env-gated** ctest case (`real_rerelease_md5_corpus_parity`, skipped without `QUAKE_GAME_DATA`), mounting `<depot>/rerelease/id1` as a gamedir per side. ADR-019 holds: no game data is committed or copied, only compared | AC6 gains real-asset evidence ahead of the M7 corpus gate | in-session |
| 2026-08-24 | M5 (PR review #16): the upload recorder's reader *clamped* to the 16-entry log and nothing compared the true call count, so a model with more surfaces than the log holds truncated identically on both sides and still asserted green -- and the upload payload is the only place the parsed MD3/MD5 data is observable | `CTEST_UPLOAD_MAX` raised to 32 (MD3's own `MAX_SURFACES`, so a format-legal MD3 can never outrun it) and `recorded_uploads` now **asserts** instead of clamping; the real-asset corpus run passing is what now establishes that the 59 re-release models stay inside it | AC6 -- removes a silent-pass path from the milestone's primary evidence | review round |
| 2026-08-24 | M5 (PR review #16): `recorded_skins` read `ctest_mdxskin_count()` entries but the stub increments that counter unconditionally while only writing below `CTEST_MODELSTUB_MAX`, so a >64-mesh model read past the end of the log -- UB in the differential rather than a failure | the reader asserts `n <= CTEST_MDXSKIN_MAX` (a named mirror of the stub's cap, as `CTEST_UPLOAD_MAX` already was) | AC6 -- test-code OOB removed | review round |
| 2026-08-24 | M5 (PR review #16): `bake_influences`' `firstweight + count` and `md5anim_load`'s `rawcount + 6` were checked adds where the C wraps. Both operands come straight off the file via `strtoull` with no upper bound, so a crafted `.md5mesh`/`.md5anim` aborted a debug build instead of taking the C's *defined* recoverable-reject (resp. success) path | both are `wrapping_add` with a `// COMPAT` note, matching the discipline the rest of the port already applies to the int-first size arithmetic. Pinned by `md5::tests::firstweight_overflow_takes_the_reject_path_instead_of_trapping`, the `bake-firstweight-wraps` reject case and `anim_animated_component_count_wraps`; each fix was mutation-checked back to an "attempt to add with overflow" panic | AC6 -- two C-defined paths the differential could not previously reach | review round |
| 2026-08-24 | M5 (PR review #16): the MD3 shim snapshots all 108 bytes of `md3Surface_t` up front, while the C reads `numVerts`/`numTriangles`/`ofs*` *after* `q_strtrim` has rewritten `name` in place -- equivalent, but the argument (name spans +4..+68, every field read sits at >= 72, and q_strtrim's `SIZE_MAX` underflow only *probes* `name[-1]`, the `'3'` of a gate-checked `ident`) was undocumented while the mirror-image `parse_flags` late read was | the reasoning is now a `// COMPAT (read ordering)` comment at the bulk read, naming the three changes that would break it | none -- documentation of an existing equivalence | review round |
| 2026-08-24 | M5 (PR review #16): `Quake/hash_map.h` has no include guard, and M5 worked around the resulting double-include by *removing* `#include "hash_map.h"` from `hashers_ref.c` -- trading "nothing may include it twice" for the more fragile "this TU must be compiled behind a prelude that already included it" | the header gets a `__HASH_MAP_H` guard (matching the `__MEM_H`/`__CFGFILE_H` convention) and `hashers_ref.c` keeps its include and stands alone again | none -- behaviour-neutral; removes a repo wart that has now bitten twice | review round |
| 2026-08-24 | M5 (PR review #16): `md3.rs`'s "this crate has no dependencies (ADR-003)" rationale for duplicating the layout constants went stale in this very PR, which gave `quake-formats` three in-workspace deps; `mdl.rs` carried the identical now-stale claim | both say "does not depend on `quake-types`" instead, which is the reason that still holds and does not read as licence to collapse the duplication | none | review round |
| 2026-08-23 | M5 (review): `Mod_LoadMD5MeshModelData`'s pose-concat loop walks `anim.numjoints`, which equals `numjoints` after a successful `MD5Anim_Load` -- except on the early return for a `.md5anim` that ends right after `frameRate`, where the C dereferences a NULL `posedata` | recorded as a `// COMPAT` divergence: the C crashes on a NULL deref, the Rust aborts on an out-of-range index (`panic = "abort"`, never an unwind into C). Both crash; only the diagnostic differs | none -- the input is already UB on the C side | in-session |
| 2026-08-24 | M6: D10 says the demo harness exercises "all 4 dispatch sites (gl_model.c task submits)". It cannot: `Mod_LoadTextureTask` (gl_model.c:1015) and `Mod_CalcSurfaceExtentsTask` (:1197) sit behind `if (!no_rendering)`, and every harness run passes `-headless`, which sets `no_rendering = true` (harness.c:69). The harness reaches only `Mod_LoadSkinTask` (:1995) and `Mod_LoadMDXSkinTask` (:2202) | coverage is split explicitly instead of claimed wholesale: the ctest `threaded_parse` carries the two unreachable sites (the only Rust unique to them is `CalcSurfaceExtents`; the other three all reach Rust through `Image_LoadImage` -> `Image_Decode*`), and the harness gains a `-pinnedworkers` worker-count sweep for the two it can reach | AC8 -- stated per site rather than as one blanket claim | in-session |
| 2026-08-24 | M6: `Mod_DecompressVis`' SAFETY comment claimed its `MOD_DECOMPRESSED` cache statics "are only touched from the main thread". False: `Mod_LeafPVS` (gl_model.c:274) is called by `R_MarkSurfacesPrepare`, which is submitted as a task (r_world.c:1037), so this Rust `static mut` is touched from a worker thread every rendered frame. A fifth worker-reachable entry point the plan never listed | comment rewritten with the invariant that actually holds -- the function runs on whichever thread owns the current PVS query, and at most one is live at a time because the render task graph is `Task_Join`ed inside the frame before the host loop advances; identical in shape to the C `mod_decompressed` pair, so inherited rather than new. `threaded_parse::decompress_vis_off_main_thread_matches_c` pins the off-main-thread behaviour | none -- no code change; a wrong safety claim replaced with a checked one | in-session |
| 2026-08-24 | M6: nothing enforced the alias-scratch exclusivity contract (`stverts`/`triangles`/`triangles_size`/`poseverts`/`POSENUM` are process-global and shared with `gl_mesh.c`). The render task graph *reaches* the loader -- `R_DrawEntitiesTask` is indexed, and `R_DrawAliasModel` calls `Mod_Extradata_CheckSkin` -> `Mod_LoadModel` -- and is only kept out by `Mod_LoadModel`'s `!needload` early return | `assert (!Tasks_IsWorker ())` added in `Mod_LoadModel` immediately past that early return, matching the existing `assert (!Tasks_IsWorker ())` convention (host.c:1337, gl_vidsdl.c:4282). Debug-build canary only (`b_ndebug=if-release`), like those two | AC8 -- the contract the audit establishes by reasoning is now mechanized | in-session |
| 2026-08-24 | M6: the `run_corpus.py` worker sweep needed the same binary run under two different engine flag sets, which `--compare` could not express | `--extra-args` (all modes) and `--compare-extra-args` (the second run of `--compare`/`--stability`, defaulting to `--extra-args`) forwarded to `run_demo.py`'s existing `--extra-args`. Default behaviour is unchanged when neither is passed, and `--generate` rejects both, so a golden can never be recorded under ad-hoc engine flags | none -- new gate only | in-session |
| 2026-08-24 | M6: the first `image_decode_concurrent` used 16 rounds per worker and **passed against a deliberately race-y decoder** (resource bytes routed through a process-global buffer). The race window is a few instructions wide, so a short test has no detection power -- the same "passes while comparing nothing" shape as the M5 upload-recorder clamp | rounds raised to 4000 (`IMAGE_ROUNDS`, ~2.7 s), at which the mutation failed. **Superseded in the review round below: that observation was a single sample and did not replicate.** The extents case needed no such change -- 2048 surfaces per pass already collide constantly | AC8 | in-session |
| 2026-08-24 | M6 (self-review): `decompress_vis_off_main_thread_matches_c`'s first fixture was a 9-byte stream decoding to 18 of the row's 28 bytes. `Mod_DecompressVis` only terminates on `out - base >= row`, so a short stream keeps reading past the input buffer -- on both sides, out of two *different* heap allocations. The case passed anyway, by accident | the stream was made row-complete (1+3+2+10+2+8+2 = 28) and the C answer is now pinned against a hand-computed 28-byte literal, since agreement between the two sides cannot by itself distinguish a well-formed fixture from two over-reads that happened to match | AC8 -- removes an accidental-agreement pass from the milestone's evidence | review round |
| 2026-08-24 | M6: `image_differential` and the new `threaded_parse` both need `build_pcx`/`build_lmp` | extracted verbatim to `tests/support/image_fixture.rs` and `#[path]`-included by both, so the two suites cannot drift onto different fixture semantics | none -- `image_differential` still 10/10 | in-session |

| 2026-08-24 | M6 (PR review #17): the `needload` enumeration behind the alias-scratch invariant named `Mod_RefreshSkins_f`, which never touches `needload` (it only calls `R_TranslateNewPlayerSkin`). The real site is `Mod_EnhancedModels_f` (`gl_model.c:101`), the `r_enhancedmodels` cvar callback -- and it reloads with `Mod_LoadModel (mod, false)`, whose `!buf` path returns before `needload = false` (`:602`), so a missing file leaves an alias model with `needload` set while `R_RebuildAllEfrags ()` runs five lines later | site corrected in the audit table and the `stverts` comment; the "followed by a synchronous load" half is no longer stated as unconditional | AC8 -- the invariant is now described accurately rather than optimistically | review round |
| 2026-08-24 | M6 (PR review #17): the audit missed a second worker path into the loader -- `R_StoreLeafEFrags` (task, `r_world.c:1053`) -> `R_StoreEfrags` -> `R_AllocateEntityBLAS` -> `Mod_Extradata` -> `Mod_LoadModel`. Unlike its siblings at `gl_mesh.c:877`/`:911`, that call site has no `needload` guard, so with `r_rtshadows` on a worker can reach a real parse. The `assert (!Tasks_IsWorker ())` M6 added would have aborted a debug build on a reachable path | the canary is downgraded to a `Con_Warning` and both paths plus the `crash == false` hole are named at the site; the underlying one-line fix is `gl_mesh.c`, frozen by NG2, so it is recorded as **RA18** for Phase 8 rather than taken here | AC8 -- M6 no longer claims an established invariant, and no longer turns a pre-existing latent path into an abort | review round |
| 2026-08-24 | M6 (PR review #17): the canary's empirical evidence was vacuous. Every harness run is `-headless`, which sets `no_rendering` and skips renderer init and the frame's screen update (`host.c:1037`, `:1273`), so the render task graph -- every path by which a worker could reach `Mod_LoadModel` -- never runs. "10 ran, assert never fires" showed only that the host-thread load path is clean. This is the identical argument the log already applies to dispatch sites 1-2; it had not been applied to the canary's own evidence | the bullet is relabelled as vacuous and kept only so the reasoning is not re-derived; the `MOD_DECOMPRESSED` and alias-scratch audit rows are marked reasoned-only | AC8 -- removes an evidence claim that did not support its conclusion | review round |
| 2026-08-24 | M6 (PR review #17): the CI worker sweep was credited with gating dispatch sites 3-4 for order independence, which the harness cannot do -- the state hash is simulation-only (`harness.c:159-233`) and never sees decoded texture bytes, `aliashdr_t` fields or `gltexture_t` state, so an order-dependent skin loader would still report all-identical | the step is renamed to "Worker count does not perturb the simulation" and its comment states the true, weaker claim; AC8 is rescoped to match; the uncovered surface is recorded as **RA17** and carried into M7 | AC8 -- the coverage story now matches what the gate observes | review round |
| 2026-08-24 | M6 (PR review #17): `threaded_parse`'s `OPEN_LOCK` argued that concurrent `COM_CloseFile` was sound because "a slot is only ever marked free by the thread that owns it, and `allocHandle`, the only reader, is behind this lock". A mutex held by only one of two participants synchronizes nothing: `Sys_FileClose` wrote the plain `bool free` from a worker while another worker scanned it -- a data race inside the milestone's own threading proof | the flag became a real atomic in the stub (`ctest_flag_t`: `_Atomic unsigned` on C11, `_Interlocked*` intrinsics on MSVC, since the ctest prelude deliberately stands in for `atomics.h` with a non-atomic store to avoid the SDL include). `allocHandle` now stores the claim through the atomic instead of `memset`-ing over it. The comment states the real reason | AC8 -- the proof no longer contains the class of bug it exists to detect | review round |
| 2026-08-24 | M6 (PR review #17): while answering the reviewer's question about `IMAGE_ROUNDS`' CI cost, re-running the race-y-decoder mutation showed it now **survived 5/5** at the 4000 rounds that had "fixed" it -- i.e. the earlier single passing observation was luck and the test had no dependable detection power. Tuning a round count against one sample is the same mistake in a different place | root cause was structural: 48-pixel fixtures made each decode microscopic, and the test's own `OPEN_LOCK` kept workers queued on a mutex instead of overlapping inside the decoder. Fixed by enlarging the fixtures to ~12k / ~6k pixels *and* making the stub's handle table genuinely lock-free (CAS claim on the atomic `free` flag, the job the real `Sys_FileOpenRead` mutex does), then deleting `OPEN_LOCK`. Detection is now **8/8** at 250 rounds, and the test runs in 0.19 s instead of 2.7 s -- which also answers the CI-cost question | AC8 -- concurrency evidence rests on structure (fixture size, no serialization) rather than on a tuned constant | review round |
| 2026-08-24 | M6 (PR review #17): the RA12 change was described as "renderer-only field -- no golden moved", which understates it | recorded as a behaviour change in the engine's favour: `styles_bitmap` feeds the lightmap `modified` masks and `deps[].lightmap_styles`, which previously OR-ed in allocator garbage, and the `!styles_bitmap -> 1` fallback is now live for all-255-styles faces. `bsp/lumps.rs`'s stale `COMPAT` block (which still told readers RA12 must be settled before M7) was updated to say it is closed | none -- documentation accuracy | review round |
| 2026-08-24 | M7 (D11): the plan pictured the fuzz targets compiling the `c_ref` archive and running true C-vs-Rust with "Sys_Error trapping". Two repository facts make that unsound *inside libFuzzer*: (1) the Rust `Sys_Error` aborts and must never be `longjmp`-trapped across a Rust frame (PLAN.md §4.3, ADR-004), so the fatal Rust path cannot be exercised under the fuzzer without UB; (2) the C model parsers over-read on truncated input (documented UB, see the M2/M5 `// COMPAT` notes), which ASan would turn into false crashes on the C oracle over arbitrary bytes | the seven fuzz targets (`fuzz_pcx/lmp/bsp/mdl/spr/md3/md5`) follow the established repo pattern (`fuzz_wad`/`pak`/`kpf`/`loc`/`vdf`): they fuzz the **pure quake-formats/quake-image accept-reject predicates the shims are literally built on**, asserting no-panic and the decision invariants. The genuine C-via-FFI differential moves to the new `formats_corpus` gate (real assets, in-process, both sides) and the existing ctest malformed-input suites, which already compile `c_ref` and compare bit-for-bit | AC10 met by the pure-predicate targets + CI list; the FFI-differential portion of R6 is carried by `formats_corpus` + ctest rather than the fuzz crate | in-session, design decision |
| 2026-08-24 | M8 (owner decision): AC11/ADR-012 required PNG/JPG/TGA pixel-exactness vs stb, but JPEG bit-exactness across IDCT/upsampler implementations is unachievable (stb's fixed-point pipeline vs zune-jpeg's libjpeg-turbo-style one); the owner ruled "pixel exact with jpeg rendering is not critical, just use zune-jpeg" | AC11 reworded: TGA/PNG stay bit-exact; the JPEG gate is accept/reject parity + identical dimensions + a pinned per-channel delta (measured: 0 on the 12 synthetic fixtures, 3–4 on the two real depot photographs; pinned at 8 for cross-platform SIMD headroom in both decoders). `formats_corpus` hashes `.jpg` as (dims, accepted) only — a pixel hash would legitimately differ per platform. ADR-012 gains a `(compat exception)` amendment | AC11 (JPEG portion redefined) | owner, in-session |
| 2026-08-24 | M8 (owner decision): the C failure path prints `stbi_failure_reason()` into `Con_Warning ("couldn't load %s (%s)\n", ...)`; reasons originating inside the `png`/`zune-jpeg` crates cannot reproduce stb's short strings | warning-text policy: exact-match parity for TGA (the hand-port carries stb's strings), the sniff-fail case, wrapper-generated PNG rejects (the ported structural walk carries stb's strings incl. the 4-char unknown-chunk prefix truncated at NUL) and JPEG's two ported guards; the parenthesized reason is masked in the differential only where it comes out of a crate. One C path (`(int)(ioff+len)` IDAT overflow) returns 0 *without setting a reason* — the C prints a stale thread-local string, so only the reject decision is compared there | AC11 — con-log claims are per-source rather than blanket | owner, in-session (recommended option accepted) |
| 2026-08-24 | M8: `stbi_load_from_memory` is NOT byte-equivalent to the engine's callback streaming — the memory context's `stbi__getn` copies nothing on a short read where the callback context keeps the partial prefix (caught by the very first differential run: truncated raw TGA tails differed) | `Image_DecodeSTBMem` decodes through `stbi_load_from_callbacks` over a memory cursor mirroring the `Sys_File*` handle semantics byte for byte (sticky EOF, partial reads, the seek+fgetc probe quirk); the pure Rust TGA cursor keeps the partial prefix likewise. Residual COMPAT: the cursor treats the resource slice as the whole file, so a malformed pak resource whose streaming decode would read past the resource into neighboring pak bytes diverges — the same out-of-resource UB class the M2 PCX shim documents | AC11 — the fallback path is gated against the streaming oracle instead of assumed equivalent | in-session |
| 2026-08-24 | M8: two acceptance quirks found by the differential, both now load-bearing tests: (1) stb's `stbi__parse_zlib_header` checks zeof *after* consuming both header bytes, so any ≤2-byte zlib stream is "bad zlib header" whatever its content; (2) stb's `stbi__get_marker` consumes any number of 0xFF fill bytes before the JPEG SOI, which zune rejects as illegal start bytes — a decodable-input acceptance divergence | (1) the PNG walk ports the post-read zeof check; (2) the JPEG wrapper strips pre-SOI fill bytes before handing zune the stream. Also ported so acceptance never depends on the crates: stb ignores PNG CRC-32 and Adler-32 entirely (checksums off in the `png` crate config) and ignores the declared zlib window (`cinfo` commented out in stb; the reconstruction normalizes the CMF byte to cinfo=7 because fdeflate enforces it — mutation-checked) | AC11 | in-session |
| 2026-08-24 | M8: two UB-class C behaviors cannot be reproduced: PNG paletted pixels indexing past the palette read uninitialized stack (`stbi_uc palette[1024]` local), and CgBI/iPhone PNGs decode color-mangled through stb's raw-deflate path | OOB palette indices are excluded from parity like the M2 PCX RLE-overrun class (the crate reads opaque black — defined, inside the UB envelope); CgBI files are detected by the structural walk and routed to the retained C `Image_DecodeSTBMem` wholesale, so their acceptance *and* mangled pixels stay exactly stb's | AC11 — parity claims scoped to defined behavior | in-session |
| 2026-08-24 | M8: D8 said "stb stays compiled until phase-exit deletion" and the M2 log said `image_stb.c` is "compiled unconditionally in all configs until M8"; the milestone needed a per-format revert lever and a CgBI fallback either way | `image_stb.c` stays compiled in every configuration: under `-Duse_rust_image` the streaming `Image_DecodeSTB` (and its Sys_File callbacks) are preprocessed out (`USE_RUST_IMAGE`, the `model_parse.c`/M3 pattern) and the file keeps only the new always-compiled `Image_DecodeSTBMem`; with the switch off the full streaming decoder returns as the oracle. Reverting any single format is one dispatch line in the Rust shim back to the fallback | none — NG1/RA10 unchanged (deletion stays deferred) | in-session |
| 2026-08-25 | M8 integration review (fresh-context compatibility-reviewer): **F1 (high)** — stb degrades an int-overflowing output buffer to a recoverable "outofmem" (`stbi__malloc_mad3` in `stbi__convert_format`/the PNG stages), and `Mem_Alloc` is plain calloc, never `Sys_Error` (the in-tree rationale claiming otherwise was wrong); the Rust TGA/PNG ports would instead attempt multi-GiB allocations (abort under panic="abort") for an 18-byte grey TGA with w*h in 2^29..2^31 or a small huge-dims 16-bit PNG. **F2 (med)** — the >10 MB MG-map corpus exclusion existed only in run logs, not as a committed mechanism. **F3/F4 (low)** — the corpus gate compared crate reject reasons unmasked, and the fuzz PNG guard read IHDR dims at fixed offsets. **F5 (info)** — no CMYK/YCCK JPEG acceptance coverage | fixes in their own commit: TGA gains the `mad3(4,w,h)` gate → `Reason::OutOfMem` ("outofmem", dims published, checked before the C's transient decode buffer); PNG gains the output-size gate (w*h*4*bytes-per-sample ≤ i32::MAX → "outofmem", compared decision-only because the C's observed reason depends on which stage starves first); both unit-pinned, differential-cased (`tga_conversion_overflow_rejects_like_stb` exact, `png_oversized_output_reject_decision_parity` masked) and mutation-checked (the reverted TGA gate burned 94 s of real allocations before failing — the exact behavior it prevents); `run_formats_corpus.py` gains `--max-bytes` (default 10 MiB) emitting `SKIP <name> oversize` manifest lines; `run_image` masks crate reasons for Stb formats like the differential; the fuzz guard walks the chunk stream to the real IHDR; hand-crafted YCCK/CMYK fixtures added — accept/dims parity holds, pixel divergence measured (64 / 255) and pinned per case as a documented residual (the two color-transform pipelines differ; no 4-component JPEG exists in any known content); true-OOM (calloc NULL in range) recorded as the one remaining unreproducible divergence, ADR-012 amendment extended | AC11 — closes a shipped-behavior regression class (recoverable C reject → Rust abort) and makes the corpus evidence reproducible | review round |
## M1 verification evidence (2026-08-20)

- Split: 44 functions moved `gl_model.c` → `model_parse.c` script-driven; every moved body verified byte-identical to `HEAD:Quake/gl_model.c` modulo `static ` removal ("non-verbatim: NONE"). Seam wrappers created: `Mod_LoadTextures`→`Mod_ParseTextures`+GPU/task tail, `Mod_LoadFaces`→`Mod_ParseFaces`+`SURF_DRAWTILED` poly post-pass+extents tail, `Mod_LoadAliasModel`→`Mod_ParseAliasModel`+display-list tail. `image.c`→`image_decode.c` (`Image_DecodeSTB`/`PCX`/`LMP`), search loop stays.
- Build (targeted, run): Windows Meson + clang-cl 22.1.8, full build + link green pre- and post-format (`356/356`).
- Behavior-neutrality (targeted, run): `run_corpus.py --compare` post-split build vs pre-split baseline built from HEAD e1e3fadd, shareware corpus (`fetch_shareware.py` checksums verified: quake106.zip + pak0.pak match pins) — **5 ran (id1-demo1/2/3, save-e1m1, map-e1m2), all identical, 0 failed**; 9 skipped (registered/mission-pack/mod tiers, local-only data absent).
- Formatting: `format.sh` docker unavailable; local clang-format 22.1.8 verified byte-identical to repo baseline formatting, then applied to the 6 touched files.
- Not run locally (CI on PR): Linux/macOS/MinGW builds, `build-rs`/`build-rs-cfs` configs, `check_headers.sh`, save_diff.py.
- **Limits of the state-hash gate (review follow-up):** `run_corpus.py --compare` hashes simulation state, so it is blind to console-output ordering and to failure paths not exercised by a clean corpus. Two such cases were found by code review, not by the harness, and are recorded here rather than claimed as verified-neutral:
  1. `Mod_LoadWadFiles` is hoisted above `Mod_ParseTextures` (it must be, now that `wads` is a parameter). It reads neither `mod->textures` nor `mod->numtextures`, so state is equivalent, but `W_LoadWadList` warnings for missing/bad wads now print *before* `"Mod_LoadTextures: no textures in bsp file"` instead of after — reachable only on a map with an empty texture lump and a `wad` key.
  2. The `if (data)` guard around `*fmt = SRC_RGBA` was initially dropped when the STB decode body moved into `Image_DecodeSTB`, making a failed decode overwrite `glt->source_format` via `TexMgr_ReloadImage`. Fixed in the review round; needs a corrupt/truncated image in the search path to observe, so no corpus run would have caught it.

## M2 verification evidence (2026-08-21)

- Port: `quake-image::{pcx,lmp}` pure parsers (`#![forbid(unsafe_code)]`, 8 unit tests green) + capi `image_decode.rs` shims (`Image_DecodePCX`/`Image_DecodeLMP`, cargo feature `image`); `-Duse_rust_image` feature option (`auto` follows `use_rust`, error if enabled without it); ctest c_ref build of `image_decode.c` with `Sys_FilePos`/`Sys_fgetc`/`LittleShort` stubs.
- Differential (targeted, run): `image_differential.rs` **10/10 green** (Windows MSVC): PCX literal+run decode, run spill across rows, `bytes_per_line` padding write into the +1 slot, LMP roundtrip, LMP size-mismatch NULL parity, and 5 `Sys_Error` message-parity cases (PCX bad signature/version/encoding/short header, LMP short header; C via longjmp trap, Rust via child-process probe). Full-allocation byte compare (including padding slot), out-dims, con log, and open-handle count all asserted equal.
- Rust battery (broad, run): `cargo test` workspace 48/48 suites green; `cargo clippy --workspace --all-targets --locked -- -D warnings` clean; `cargo fmt --check` clean.
- Gates (targeted, run): `check_capi_signatures.sh` green with `image.h` in the TU, both against local cbindgen output and `build-rs/quake_rs.h` (CC=clang-cl; TU defines a stand-in `enum srcformat` tag because the real one lives in Vulkan-bearing `gl_texmgr.h`). c-sys bindgen regen diff is exactly `+COM_CloseFile` (Windows-host regen; 4 host-dependent enum typedefs hand-reverted to Linux-canonical — Linux CI bindgen-smoke is authoritative).
- Meson (targeted, run, Windows clang-cl): `build-c` green (`image_decode.c` + `image_stb.c` compiled); `build-rs` green with `image` in the cargo feature list and `Quake_image_decode.c.obj` absent from the target (symbols provided by `quake_rs.lib`), `image_stb.c` still compiled.
- Formatting: `format.sh` docker image (clang-format 18) dry-run `-Werror` clean on `image_decode.c`/`image_stb.c`.
- Not run locally (CI on PR): Linux/macOS/MinGW builds, `build-rs-cfs` config, Linux-canonical bindgen regen-diff, `check_headers.sh`, corpus/save_diff harness legs.

## M3 verification evidence (2026-08-21)

- Mirrors: `quake-types::bspfile` (291 lines: on-disk `dheader_t`/`lump_t`/`dmodel_t`/`dmiptexlump_t`/`miptex_t`/`miptex64_t`/vertex/plane/texinfo plus the S/L1/L2 node/leaf/clipnode/edge/face triples, magics, lump indices, limits) and `quake-types::model_mem` (316 lines: `MVertex`/`MEdge`/`MTexInfo`/`MSurface`/`MNode`/`MLeaf`/`MClipnode`/`Hull`/`Texture`/`QModel`, Vk tail as pointer-sized stand-ins), both with const size/offset asserts.
- Port: `quake-formats::bsp` (`lumps`, `textures`, `lighting`, `vis`, `extents`; 1665 lines; `#![forbid(unsafe_code)]`; **33 unit tests green**) plus capi `model_parse.rs` (1671 lines, cargo feature `formats`): 16 seam functions exported under their C names and 5 status-returning `quake_rs_mod_*` trampolines re-raised by `Quake/model_parse_glue.c` (77 lines, compiled only under the switch). `CalcSurfaceExtents` uses f64 intermediates per ADR-010 (`// COMPAT: ADR-010`); every other deliberate bug-for-bug divergence carries a `// COMPAT:` comment at its site (Q64 halving order, the L2 `p > 0` child branch, the C out-of-bounds reads on malformed lumps, the `ents` leak on the CRC-mismatch path, the `submodels[0]` read with zero submodels, ASCII-only `q_strcasecmp` in the vispatch scan).
- ABI (targeted, run): `bsp_abi.rs` **5/5 green** — on-disk and in-memory struct sizes plus `qmodel_t`, surface/node/leaf and `texture_t` field offsets read back from the real engine headers through `abi_probe.c`. AC2 was locked before any logic was written (stage 1 of the milestone).
- Differential (targeted, run): `bsp_differential.rs` **9/9 green** (Windows MSVC, `--test-threads=1`): `bsp_parity_all_dialects` (BSP29, BSP30-Valve, 2PSB, BSP2, Quake64 — all loaders in `Mod_LoadBrushModel` order, world plus submodels, deep-walked and compared field by field via `tests/support/model_hash.rs`), `worldmodel_clipbox_branch_parity`, `lit_parity`, `ent_parity` (including the CRC-mismatch reject path), `decompress_vis_parity`, `external_vis_parity` (real vispatch container through `FILE*`), `host_error_parity` (C via longjmp trap vs Rust via status return, message bytes compared), `sys_error_parity`, `rust_fatal_child`. The snapshot walker compares full `visdata`/`lightdata` bytes and the entity string, and a line-count floor guards against a silently truncated snapshot.
- Rust battery (broad, run): `cargo test --workspace --locked` **50 suites / 202 tests, 0 failed** in debug and again under `--release`; `cargo clippy --workspace --all-targets --locked -- -D warnings` clean; `cargo fmt --check` clean.
- Gates (targeted, run): `check_capi_signatures.sh` green — the new second TU pre-defines `__Q_RENDER_TYPES_H` and `__ATOMICS_H` with 64-bit Vk handle and `atomic_uint32_t` stand-ins, then includes `bspfile.h` + `gl_model.h` + `model_parse.h` + `quake_rs.h` (CC=clang-cl, cbindgen-generated header); **RA5 closed without the Vulkan SDK — the fallback `quake_rs_model.h` was not needed**. The c-sys regen diff is exactly `COM_SkipPath`, `COM_StripExtension` and `external_ents`; the Windows-host regen reproduces those additions verbatim, but its MSYS `//`-to-`/` argv mangling and host-dependent enum reprs mean the committed file stays Linux-canonical (rust.yml bindgen-smoke is authoritative).
- Meson (targeted, run, Windows clang-cl): `build` (`-Duse_rust=disabled`) green with no `USE_RUST_FORMATS` and no glue object; `build-rs` (`-Duse_rust=enabled`, formats auto to on) green with `Quake_model_parse.c.obj` *and* `Quake_model_parse_glue.c.obj` present and `formats` in the cargo feature list; `-Duse_rust=enabled -Duse_rust_formats=disabled` also configures, builds and links (the C brush oracle path — no CI leg covers this combination).
- Formatting: `format.sh` docker image (clang-format 18) dry-run `-Werror` clean on `model_parse.c`, `model_parse_glue.c`, `model_parse.h`.
- Not run locally (CI on PR): Linux/macOS/MinGW builds, `build-rs-cfs`, the Linux-canonical bindgen regen-diff, `check_headers.sh` (`gl_model.h` deliberately stays out of its list), and the corpus/`save_diff` legs (AC7 lands M7).

## M4 verification evidence (2026-08-22)

- Mirrors: `quake-types::modelgen` (160 lines: `mdl_t` 84 B, `stvert_t` 12 B, `dtriangle_t` 16 B, `trivertx_t` 4 B, `daliasframe_t` 24 B, `daliasgroup_t`/`daliasinterval_t`/`daliasframetype_t`, `ALIAS_VERSION`/`ALIAS_SINGLE`/`ALIAS_GROUP` and the vertex limits) and `quake-types::spritegn` (83 lines: `dsprite_t` 36 B, `dspriteframe_t` 16 B, `dspritegroup_t`, `dspriteinterval_t`, `dspriteframetype_t`, `SPRITE_VERSION`, `SPR_*` orientations), both with const size/offset asserts. `model_mem.rs` grew the in-memory side (`AliasHdr`, `MAliasFrameDesc`, `MSprite`, `MSpriteFrameDesc`, `MSpriteFrame`, `MSpriteGroup`, `MTriangle`, +242 lines). **`synctype_t` dual-definition hazard (RA11)**: the enum is declared twice in the engine -- `modelgen.h` has `ST_SYNC, ST_RAND, ST_FRAMETIME`, `spritegn.h` only `ST_SYNC, ST_RAND` -- both under the same `#ifndef SYNCTYPE_T` guard, so whichever header a TU includes first wins. It never changes the ABI (both int-sized, shared values agree) and neither loader validates the field, so `quake-types::modelgen` defines the union once and `spritegn` re-exports it, with the hazard written up at both sites.
- Port: `quake-formats::mdl` (534 lines) and `quake-formats::spr` (221 lines), both `#![forbid(unsafe_code)]` -- header decode, frame/group record decode, the `validate()` diagnostic list (skin/vertex/triangle/frame count limits, `boundingradius` conversion) and the sprite `model_bounds` computation; plus the capi shims in `model_parse.rs` (+685 lines): `Mod_ParseAliasModel`, `Mod_CalcAliasBounds` (all `poseverttype` branches), `Mod_LoadSpriteModel`. Deliberate bug-for-bug divergences carry `// COMPAT:` comments at their sites: group frames never set `frame->name`/`frame->frame` and single frames never set `frame->interval` (the `Mem_Alloc` zero fill is the observable value); the sprite loader never writes `mod->flags`; `boundingradius` is read with `ReadLongUnaligned` and *converted* to float rather than reinterpreted; sprite `model_bounds` negates before the truncating divide, so an odd `maxwidth` yields an asymmetric box.
- ABI (targeted, run): `alias_abi.rs` **4/4 green** -- on-disk `mdl_t`/`stvert_t`/`dtriangle_t`/`trivertx_t`/`daliasframe_t`/`dsprite_t`/`dspriteframe_t` sizes and offsets plus the in-memory `aliashdr_t`, `maliasframedesc_t`, `msprite_t`, `mspriteframe_t` field offsets read back from the real engine headers through `abi_probe.c` (+159 lines).
- Differential (targeted, run): `alias_differential.rs` **18/18** and `sprite_differential.rs` **10/10** green (Windows MSVC). Alias: single-frame, group-frames (only the first interval is kept), mixed single+group with bounds, all three `Con_DWarning` diagnostics (tall skin, `numverts > 2000`, `numtris > 4096`) plus a case tripping all three at once to pin `mdl::validate`'s emission order against the C's interleaved checks, `triangles`-buffer growth across numtris 4/5/40/41, four direct `Mod_CalcAliasBounds` cases covering the `PV_MD5`/`PV_MD5_8`/`PV_QUAKE3` arms and the empty-stream `FLT_MAX`/`-0.0` seeding (compared on raw bit patterns), and five `Sys_Error` message-parity cases (bad version, no vertices, no triangles, no frames, too many vertices -- C through the longjmp trap, Rust through a child process). Sprite: single frame (bounds `[-16,-16,-8]..[16,16,8]` for a 33x17 frame, texture name `progs/s_bubble.spr:frame0`, data offset 56), multiple single frames, group frames, mixed single+group (texture names prove the `framenum*100+i` numbering), an eight-frame angled group, and four fatal-parity cases (bad version, no frames, bad angled count, zero interval). Both suites compare the deep-walk snapshot (`tests/support/model_hash.rs`, +227 lines: the committed field mask, pointers resolved to indices/file offsets/`null`, Vk handles and texture pointers excluded) line by line, plus the console log and the recorded `Mod_LoadAllSkins`/`TexMgr_LoadImage` argument streams (new call-recording stubs, `stubs.c` +94 lines).
- Rust battery (broad, run): `cargo test --workspace --locked` **53 suites / 246 tests** -- 246/246 under `--release`, 245/246 in debug. The one debug failure is `mathlib_differential::perpendicular_and_rotate_match`, a **pre-existing Phase 1 divergence** surfaced by proptest's random search on this run: rotating a zero-length vector yields a NaN whose *sign bit* differs (C `0xFFC00000`, Rust `0x7FC00000`) for `dir = point = [0,0,0], degrees = 0`. This branch touches nothing under `rust/quake-math`, `Quake/mathlib.c` or `mathlib_differential.rs` (`git diff master...HEAD` over those paths is empty); the discovered seed was deliberately **not** committed and the divergence is tracked as separate work. `cargo clippy --workspace --all-targets --locked -- -D warnings` clean; `cargo fmt --all --check` clean.
- Gates (targeted, run): `check_capi_signatures.sh` green for both TUs (CC=clang-cl) after the six M4 seam symbols were added to cbindgen's `[export] exclude` list -- like the M3 brush shims they are declared by hand inside the `#ifdef __MODEL__` `after_includes` block, so cbindgen's own emission (which uses the Rust mirror type names and is not header-guarded) had to be suppressed. c-sys bindgen regen (`gen_c_bindings.sh`) adds and removes **nothing** -- the M4 shims reach C only through `extern "C"` declarations typed with the mirrors; the Windows-host regen's enum-signedness and `//`-mangling artifacts were reverted, Linux CI stays canonical.
- Meson (targeted, run, Windows clang-cl): `build` (C-only) green -- `Quake_model_parse.c.obj` defines `Mod_ParseAliasModel`, `Mod_CalcAliasBounds`, `Mod_LoadSpriteModel`, `Mod_SetExtraFlags`, `Mod_LoadNodes`; `build-rs` (`-Duse_rust=enabled`) green -- the same object now defines **only `Mod_SetExtraFlags`** in text, plus the four shared alias-scratch symbols `stverts`/`triangles`/`triangles_size`/`poseverts` in BSS (llvm-nm `--defined-only`), with `Quake_model_parse_glue.c.obj` alongside it; `-Duse_rust=enabled -Duse_rust_formats=disabled` configures, builds and links with the full C oracle and no glue object.
- Formatting: dry-run `-Werror` clean on `model_parse.c`, `model_parse_glue.c`, `model_parse.h`. Two alignment violations introduced by the new guard lines and one by the `triangles_size` declaration were fixed; the original pass used the `format.sh` docker image (clang-format 18), the re-check after the review fixes used the local clang-format 22 because the docker daemon was down -- CI's clang-format 18 leg remains canonical.
- Not run locally (CI on PR): Linux/macOS/MinGW builds, `build-rs-cfs`/`build-rs-cimg`/`build-rs-cfmt` legs, the Linux-canonical bindgen regen-diff, `check_headers.sh`, and the corpus/`save_diff` legs (AC7 lands M7).

## M5 verification evidence (2026-08-23)

- **C prep** (own commit): 3 ranges, 1287 lines, moved `gl_model.c` → `model_parse.c` and verified *contiguous byte-identical* against `HEAD:Quake/gl_model.c` by script (the MD5 doc comment + tokenizer helpers + `md5*`/`joint*` types + `MD5_ResolveMappedMeshParent` + the `Matrix3x4_*`/`GenMatrixPosQuat4Scale` kernels + `MD5_BakeInfluences`/`MD5_ComputeNormals`/`MD5_HackyModelFlags` + `MD5Anim_Begin`/`Load` + `Mod_LoadMD5MeshModelData`/`Mod_LoadMD5MeshModel` + `Mod_LoadMD3Model`). Exactly **one** line differs: the MD5 skin call now goes through the new `Mod_LoadMD5SurfaceSkins` wrapper. All `.skin`/TexMgr plumbing and `Mod_LoadModel` stay in `gl_model.c` (NG2/NG3). Behaviour-neutrality: all three then-existing Meson configs built and passed `run_corpus.py --check` 10/10 against the committed `darwin-arm64` goldens.
- **Mirrors**: `quake-types::md3` (on-disk `md3Header_t`/`md3Frame_t`/`md3Surface_t`/`md3Triangle_t`/`md3St_t`/`md3Shader_t`) and `model_mem.rs` additions (`JointPose`, `AliasMesh`, `QPathStr`/`SkinDef`/`SurfaceDef`/`AllSurfacesDef`, `MAX_SURFACES`, `IDMD5HEADER`, `NUM_JOINT_INFLUENCES_*`), all with const size/offset asserts. `mdx_abi.rs` **4/4 green** against the real `gl_model.h` through a new `ctest_abi_mdx_lookup` table — AC2 locked before any logic, as in M3/M4.
- **Port**: `quake-formats::md3` (pure record decoders, 11 unit tests) and `quake-formats::md5` (kernels, 12 unit tests), both `#![forbid(unsafe_code)]`; capi shims `Mod_LoadMD3Model` and `Mod_LoadMD5MeshModel` (+~1000 lines in `model_parse.rs`). Every deliberate bug-for-bug divergence carries a `// COMPAT` comment at its site: MD3's never-advanced `pinframes` (every frame named after md3 frame 0), MD3 texcoords read with no `LittleFloat`, the int-typed byte counts that sign-extend into `size_t`, MD3 `flags` re-read after the loop (because `q_strtrim` mutates the image in place); MD5's leaked `anim.animfile` on every recoverable failure, `MD5_HackyModelFlags`' native `mdl_t::flags` read, the double-precision weight scaling, `numframes` left at the `Mem_Alloc` zero with no `.md5anim`, `extradata[PV_MD5]` used for the `PV_MD5_8` layout too, and the truncated-`.md5anim` abort-vs-UB divergence.
- **Message/token audit (targeted, run)**: the multiset of `MD5EXPECT`/`MD5CHECK` literals and of every `Con_Warning`/`Con_DWarning`/`Sys_Error` format string in the C range matches the Rust side exactly (52 tokens, 20 messages).
- **Differential (targeted, run, darwin-arm64)**: `md3_differential` **14/14** and `md5_differential` **16/16**. Both compare, per case: the deep-walk snapshot of the `aliashdr_t` *chain* (`mdx_snapshot`, which follows `nextsurface` and resolves each link to its index in the single `hdrsize`-strided block), the recorded `GLMesh_UploadBuffers` calls **including byte copies plus FNV hashes of the index/vertex/texcoord/joint buffers** (the parsed payload never reaches `aliashdr_t`, so that is where parity lives), the console log, and the skin-callback argument stream. MD3 additionally compares the post-load file image (the in-place `q_strtrim`) and covers six `Sys_Error` cases (C through the longjmp trap, Rust through a child process); MD5 additionally compares the loader's boolean return and covers 18 recoverable-reject paths (every `MD5ERROR` site).
- **Real-asset corpus (targeted, run)**: `real_rerelease_md5_corpus_parity`, env-gated on `QUAKE_GAME_DATA`, mounts `<depot>/rerelease/id1` per side and runs **all 59 `.md5mesh` models** in `rerelease/id1/pak0.pak` — with their `.md5anim` companions and `.mdl` model-flag probes resolved through each side's own filesystem — through both loaders: snapshot, upload payload, console log and return value identical for every one. No game data is committed (ADR-019); CI without the depot skips the case and relies on the synthetic ones.
- **Mutation-checked (targeted, run)**: each of a positive quaternion root, a swapped cross-product argument order, an `f32` `Matrix3x4_Invert_Simple` scale, a float (rather than double) weight multiply, a single-precision `acos`, a wrong `R_ConcatTransforms` argument order, a "fixed" MD3 frame-name walk and a one-float MD3 texcoord change **fails** the suite. Two of these initially slipped through and the fixtures were strengthened until they did not: the joint quaternions are now off-axis (so the inverse's first row is not exactly unit length) and the weight literals are `f64` decimals proven to differ between a double and a float multiply.
- **Rust battery (broad, run)**: `cargo test --workspace --locked` **56 test binaries, 0 failed** in debug and again under `--release`, and again in debug with `QUAKE_GAME_DATA` set (so the real corpus runs); `cargo clippy --workspace --all-targets --locked -- -D warnings` clean; `cargo fmt --all --check` clean; `cargo deny check` — advisories/bans/licenses/sources ok. `Cargo.lock` gains **no** third-party crate (the new `quake-formats` deps are all in-workspace).
- **Gates (targeted, run)**: `check_capi_signatures.sh` green for both TUs (`CC=clang`, against `build-rs/quake_rs.h`) with the new hand-copied `glquake.h` slice; `check_headers.sh` green; `gen_c_bindings.sh` regen is idempotent and its only M5 addition is `q_strtrim`.
- **Meson (targeted, run, darwin-arm64 clang)**: five configurations configure, build and link — `build-c` (`-Duse_rust=disabled`), `build-rs`, `build-rs-cfs`, `-Duse_rust=enabled -Duse_rust_formats=disabled` (the C oracle leg) and `-Duse_rust=enabled -Duse_rust_image=disabled`. Under `-Duse_rust_formats`, `Quake_model_parse.c.o` defines **only `Mod_SetExtraFlags`** in text (plus the four shared alias-scratch symbols in BSS); with the switch off it defines `Mod_LoadMD5MeshModel`/`Mod_LoadMD3Model` again.
- **Harness (broad, run)**: all five configurations pass `run_corpus.py --check` against the committed `darwin-arm64` goldens (10 ran, 4 skipped for absent mod dirs, 0 failed — the `rerelease-demo1` entry loads the real MD5 replacement models); `run_corpus.py --compare build-rs vs build-c` 10/10 identical; `save_diff.py build-rs vs build-c` — both savegames byte-identical (91562 / 91547 bytes). **This is the first Phase-3 milestone whose goldens could be exercised locally**, so it also de-risks AC7 ahead of M7.
- **Formatting**: `format.sh`'s docker clang-format 18 dry-run `-Werror` clean on `model_parse.c`/`model_parse.h`/`gl_model.c`/`gl_model.h`. (The ctest C under `rust/` is outside `format.sh`'s and CI's scope and was left at its existing style.)
- **Review pass (in-session, no subagents — user's choice)**: the whole diff re-read against this plan and ADR-004/010/011/013/016/019. Three findings, all fixed in their own commit: an unbounded C-string walk over a fixed 32-byte joint-name field, and two undocumented divergences (the truncated-`.md5anim` abort and `compute_normals`' bounded index walk). Every new `SAFETY` comment was checked against the C source rather than taken on trust; every indexing site reachable from malformed input was traced to the parse-time bound that makes it safe.
- **PR-review round (#16, adversarial, 2026-08-24)**: the reviewer found no compat divergence in the port itself; all six findings are recorded in the amendment log above and fixed in their own commit. Two of them (the upload recorder's silent clamp, `recorded_skins`' out-of-bounds read) weakened AC6 by letting the differential compare less than it appeared to, and two more (`firstweight + count`, `rawcount + 6`) were checked adds where the C wraps, aborting a debug build on file-controlled input instead of taking the C's defined path. The arithmetic fixes are each pinned by a new test and mutation-checked back to an "attempt to add with overflow" panic; the recorder fixes turn the caps into asserts, so the 59-model real corpus passing now *establishes* the coverage that was previously only assumed. Re-verified after the fixes: 56 test binaries green in debug and release, clippy/fmt clean, five Meson configurations build and link, all five pass `run_corpus.py --check`, `--compare` 10/10 identical, `save_diff.py` byte-identical, both signature-gate TUs and `check_headers.sh` green, clang-format clean on the touched C (now including `Quake/hash_map.h`).
- **Not run locally (CI on PR)**: Linux/macOS-CI/Windows/MinGW builds, the `build-rs-cimg`/`build-rs-cfmt` CI legs as configured in `build-linux.yml`, the Linux-canonical bindgen regen-diff, and the `mdx_abi` layout confirmation on non-darwin targets (RA1).

## M6 verification evidence (2026-08-24)

### Global-state audit

Every process-global touched by the Phase-3 seam, and what makes each one safe.
Enumerated by grepping `quake-capi`, `quake-formats` and `quake-image` for
`static mut`, `thread_local!`, `Atomic*`, `Mutex`, `OnceLock`/`OnceCell`,
`RefCell`/`UnsafeCell`/`Cell` and `lazy_static` -- outside `fs.rs`/`wad.rs`/
`loc.rs`/`cfgfile.rs` (Phases 1-2, audited then) the total is five items, and
there are **no** synchronization primitives or thread-locals anywhere in the
three crates.

| State | Where | Worker-reachable? | Verdict |
|---|---|---|---|
| `MOD_DECOMPRESSED`, `MOD_DECOMPRESSED_CAPACITY` | `model_parse.rs:192-193` | **yes** -- `R_MarkSurfacesPrepare` is a task (r_world.c:1037) and calls `Mod_LeafPVS` -> `Mod_DecompressVis` | Safe, inherited: at most one PVS query is live at a time, because the render task graph is `Task_Join`ed inside the frame before the host loop advances (`draw_done_task` is joined at `gl_screen.c:1284`; only `end_rendering_task` outlives the frame and it never touches PVS). Identical to the C `mod_decompressed` pair it replaces. The SAFETY comment claiming "main thread only" was **wrong and was rewritten**. **Reasoned-only**: no automated leg exercises it, because every harness run is `-headless` and the render task graph never starts. `decompress_vis_off_main_thread_matches_c` proves the shim is not thread-pinned, not the one-query-at-a-time claim |
| `stverts`, `triangles`, `triangles_size`, `poseverts` (C linkage, shared with `gl_mesh.c`) | `model_parse.rs:1735-1740` | **yes, in a reachable corner** | `Mod_LoadModel` is the sole funnel for every parse, and **two** render-task paths reach it: `R_DrawEntitiesTask` -> `R_DrawAliasModel` -> `Mod_Extradata_CheckSkin`, and `R_StoreLeafEFrags` (r_world.c:1053) -> `R_StoreEfrags` -> `R_AllocateEntityBLAS` -> `Mod_Extradata`. Both normally stop at the `!needload` early return, since the three `needload = true` sites (`Mod_FindName`, `Mod_ClearAll`, `Mod_EnhancedModels_f` -- **not** `Mod_RefreshSkins_f`, corrected in the review round) each load synchronously on the host thread. But `Mod_EnhancedModels_f` reloads with `crash == false`, which leaves `needload` set on a missing file, and `R_AllocateEntityBLAS` has no `needload` guard (its siblings at `gl_mesh.c:877`/`:911` do). With `r_rtshadows` on, a worker can reach a real parse. **Not established** -- M6 ships a `Con_Warning`, not an assert; closing the hole belongs to Phase 8 |
| `POSENUM` | `model_parse.rs:1742` | no (same contract) | Safe by the same argument; reset to zero at the top of every `Mod_ParseAliasModel`, so it carries no state across the seam |
| `quake-formats` | — | n/a | **None.** `#![forbid(unsafe_code)]`, pure functions over slices |
| `quake-image` | — | n/a | **None.** Same |

Two further audit results, recorded rather than changed:

- **RA16** -- `Mod_LoadMD5MeshModel` blocks across `Mod_LoadMD5SurfaceSkins`'
  `Task_Join` (gl_model.c:2202), the only place a Rust frame is live across the
  task system. Sound: the task argument struct is a C local built from C-owned
  pointers, each worker writes only the `aliashdr_t` skin slots for its own
  index, and the blocked frame holds no lock and no `&mut` into shared state.
  Written up at the import site.
- **Thread-locals** -- `com_filesize`, `com_token` and `file_from_pak` are C
  `THREAD_LOCAL` (common.c:42, 2120, 2122) and reached only through
  `COM_ThreadFileSize`/`COM_ThreadToken` (I6). No Rust shadow exists. This is
  why `Image_LoadImage` opens a resource on the worker that decodes it, and why
  `threaded_parse` opens per decoding thread rather than handing handles over.

### Threading proof

- **ctest (targeted, run, darwin-arm64)**: `threaded_parse` **4/4**, 2.7 s.
  `extents_concurrent_matches_serial_and_c` drives 2048 hand-built surfaces
  (pairwise-distinct results, both surfedge sign arms, a `TEX_SPECIAL` variant)
  through serial C, serial Rust, then Rust sharded over 2/3/8/17 threads x 8
  iterations using `tasks.c:532`'s own partitioning, comparing
  `texturemins`/`extents` bit-for-bit; an `UNWRITTEN` sentinel makes a dropped
  shard a failure rather than a stale pass.
  `image_decode_concurrent_matches_serial_and_c` runs 2/3/8/17 threads x 250
  rounds of open+decode over four large (~12k / ~6k pixel) PCX/LMP fixtures with
  distinct outputs, each compared against the serial C result, with the
  open-handle count asserted back to zero. Nothing is serialized: the stub's
  handle table now claims slots with a CAS on an atomic flag, mirroring the
  mutex the real `Sys_FileOpenRead` holds. `decompress_vis_off_main_thread_matches_c` runs the shim off the main
  thread (the engine's actual condition) for both the NULL and compressed
  inputs, with the C answer pinned against a hand-computed literal so the
  fixture is provably row-complete rather than agreeing by accident (see the
  self-review amendment). `parser_outputs_are_send_and_sync` instantiates
  `assert_send_sync::<T>()` over 37 `quake-formats`/`quake-image` result types,
  so a future non-`Send` field is a build failure.
- **Mutation-checked (targeted, run)**: four mutations. (1) `CalcSurfaceExtents`'
  per-call `Vec` hoisted to a `static mut Vec` -> SIGABRT; (2) its results routed
  through a `static mut` fixed array (no heap traffic, so no crash) -> clean
  `assert_eq` failure, 3/3 runs; (3) `extents_sharded`'s `div_ceil` replaced by a
  truncating divide -> caught by the `UNWRITTEN` sentinel; (4) `read_resource`'s
  buffer replaced by a process-global fixed array -> this one took three
  attempts to make detectable and is the most instructive result in the
  milestone. It survived 16 rounds/worker; raising the count to 4000 appeared to
  fix it, but re-measuring in the review round showed that was luck (**1 of 5
  runs caught it**). The cause was structural, not statistical: the fixtures were
  48 pixels and the open was serialized behind a test mutex, so workers queued
  on the lock instead of overlapping inside the decoder. Enlarging the fixtures
  to ~12k pixels **and** making the stub's handle table lock-free (so nothing
  serializes the open) gives **8/8** detection at 250 rounds — and cuts the
  test's runtime from 2.7 s to 0.19 s.
- **Worker-reachability of `Mod_LoadModel` is reasoned-only (review round)**:
  a debug build ran the full corpus under `--stability` with
  `-pinnedworkers 0,1,...,7` without the canary firing, but that evidence is
  **vacuous** and is recorded here only so nobody re-derives it as support.
  Every harness run is `-headless`, `-headless` sets `no_rendering`
  (harness.c:69), and `no_rendering` skips both renderer init and the frame's
  screen update (host.c:1037, :1273) -- so `R_DrawEntitiesTask`,
  `R_StoreLeafEFrags` and `R_MarkSurfacesPrepare`, i.e. *every* path by which a
  worker could reach `Mod_LoadModel`, never run. This is the same argument the
  amendment log applies to dispatch sites 1-2; it was not applied to the
  canary's own evidence until review. Inverting the canary does show it is live
  and reached, nothing more.
- **Harness worker sweep (targeted, run)**: `run_corpus.py --compare` of
  `build-rs` against itself under `-pinnedworkers 0` (1 worker) vs
  `-pinnedworkers 0,1,...,7` (8 workers) -- **10 ran, 0 failed, all identical**.
  **What this does and does not gate (review round)**: the state hash is built
  from simulation state only (`Harness_HashServer`/`HashClient`,
  harness.c:159-233 -- edicts, `leafnums`, qcvm globals/time, client timing and
  per-entity `origin`/`angles`/`frame`/`effects`/`skinnum`/`alpha`). Nothing
  `Mod_LoadSkinTask` or `Mod_LoadMDXSkinTask` produces -- decoded texture
  bytes, `aliashdr_t` fields, `gltexture_t` state -- enters it. So the sweep
  proves worker count does not perturb the simulation (catching a loader that
  `Sys_Error`s, deadlocks, or corrupts sim-visible state); it does **not**
  establish order independence of those two task bodies' own output. That
  remains uncovered at the engine level and is carried into M7 as RA17.
  The lever was falsified before being trusted: a temporary `Sys_Printf` in
  `Tasks_Init` confirmed `num_workers` is 10 by default on this host and 1/3/8
  under the corresponding flags, so the sweep really does compare different
  worker counts rather than two identical runs. The probe was reverted and
  `build-rs` rebuilt.

### RA12

`Mod_ParseFaces` (`model_parse.c`) now zeroes `styles_bitmap` before the two OR
loops, so neither it nor the `if (!out->styles_bitmap)` fallback nor the
renderer's lightmap `modified` masks (`r_brush.c`, `r_world.c`) depend on what
`Mem_AllocNonZero` left behind. The Rust side already assigned rather than
OR-ed, so this makes C match Rust. The stale half of the ctest
`Mem_AllocNonZero` stub's rationale comment was updated; the zero-fill stays
(its other reason -- untouched `msurface_t` fields -- still holds).

### Milestone gates (all run locally, darwin-arm64)

- **Rust battery (broad, run)**: `cargo test --workspace --locked` green in
  debug and release, and again in debug with `QUAKE_GAME_DATA` set so
  `real_rerelease_md5_corpus_parity` runs the 59 real models (confirmed running,
  not skipped). `cargo clippy --workspace --all-targets --locked -- -D warnings`
  clean; `cargo fmt --all --check` clean. `image_differential` still 10/10 after
  the fixture extraction.
- **Meson (targeted, run)**: all five configurations configure, build and link
  (`build-c`, `build-rs`, `build-rs-cfs`, `-Duse_rust_formats=disabled`,
  `-Duse_rust_image=disabled`). Symbol split unchanged from M5: under
  `-Duse_rust_formats`, `Quake_model_parse.c.o` defines only `Mod_SetExtraFlags`
  in text plus the four shared scratch symbols in BSS; with the switch off it
  defines 35 symbols.
- **Harness (broad, run)**: all five configurations pass `run_corpus.py --check`
  against the committed `darwin-arm64` goldens (10 ran, 4 skipped for absent mod
  dirs, 0 failed) -- **the RA12 fix moved no golden**, as expected for a
  renderer-only field. `run_corpus.py --compare build-rs vs build-c` 10/10
  identical; `save_diff.py` both savegames byte-identical (91562 / 91547 bytes).
- **Gates (targeted, run)**: `check_capi_signatures.sh` green for all four
  generated headers (`build-rs`, `build-rs-cfs`, `build-rs-cfmt`,
  `build-rs-cimg`); `check_headers.sh` green; `gen_c_bindings.sh` regen diff
  **empty** (M6 adds no C import).
- **Not run locally**: the ASan legs. `-Db_sanitize=address` does not build on
  darwin-arm64 for a **pre-existing, unrelated** reason -- ASan disables
  `_FORTIFY_SOURCE`, which exposes macOS's `sprintf` deprecation, and
  `werror=true` turns it into an error in `pr_edict.c`/`host_cmd.c` (neither
  touched by this milestone). The CI job is Linux-only and unaffected; the new
  step's *command shape* (`.cmds` map load + `--extra-args`) was validated
  against `build-rs` instead. Also not run locally: Linux/Windows/MinGW builds,
  the `build-rs-cimg`/`build-rs-cfmt` CI legs as configured, and the
  Linux-canonical bindgen regen-diff.
- **Formatting**: `format.sh` on the touched C (`Quake/gl_model.c`,
  `Quake/model_parse.c`).

## M8 verification evidence (2026-08-24/25, darwin-arm64)

- **C prep (behavior-neutral)**: `image_stb.c` stays compiled in every configuration; `USE_RUST_IMAGE` preprocesses out the streaming `Image_DecodeSTB` + its Sys_File callbacks, leaving the new always-compiled `Image_DecodeSTBMem` — implemented via `stbi_load_from_callbacks` over a memory cursor mirroring the `Sys_File*` semantics byte for byte, NOT `stbi_load_from_memory` (whose context drops partial reads; the very first differential run caught the divergence on a truncated raw TGA). Symbol split verified by `nm` in all configurations.
- **Rust seam**: `quake-image::stb_sniff` (ported probe chain: png sig → jpeg SOI-with-fill-bytes → tga plausibility, EOF-reads-zero semantics), `tga` (full `stbi__tga_load` port), `png_stb` (hand-ported `stbi__parse_png_file` acceptance walk + zlib-header checks with stb's exact short reasons; the `png` crate does inflate/defilter/expand over a reconstructed canonical stream, checksums off, CMF normalized to cinfo=7), `jpeg_stb` (`zune-jpeg`, relaxed gate; stb's two pre-decode guards ported; pre-SOI fill bytes stripped), dispatched from `quake-capi::image_decode::Image_DecodeSTB` (bulk-read, per-format dispatch, C's exact warning format/close points/out-param write points; CgBI → C fallback).
- **Differential (targeted, run)**: `image_crate_differential` **15/15** — sniff parity (unknown/truncated/order probes), the three in-repo vq_pak PNGs, TGA matrix (raw/RLE × bpp 8/15/16/24/32 × origin, indexed × palette-bits × index-width 8/16, offset/palette_start skips, reject parity incl. dims-published points, truncation zero-fill), PNG matrix (13 color/depth combos × interlace, tRNS variants incl. sub-byte/16-bit keys and partial palette alpha, bad-CRC/bad-Adler/trailing-garbage acceptance, split+empty IDAT, cinfo=15 acceptance, ~20 structural reject reasons exact, crate roots masked per policy), JPEG fixtures (12 hand-crafted minimal JPEGs via `scripts/harness/gen_jpeg_fixtures.py`: gray/4:4:4/4:2:0/4:2:2/odd dims/restarts/APPn/progressive-DC/truncated/trailing/fill-bytes — all delta 0). `image_real_assets` (env-gated): both real depot JPEGs decode, max delta 3 (dawn.jpg, 0.63% bytes differing) / 4 (thumb, 5.7%), bound pinned 8.
- **Mutation-checked (targeted, run)**: ten mutations each fail the suite — TGA: flip dropped, 555 unpack wrong, BGR swap skipped, memory-context getn; PNG: 16→8 LSB-for-MSB, gray replication broken, zlib normalization dropped, 2-byte zlib accepted; JPEG: fill-byte strip dropped, BGRA channel order.
- **Real-asset corpus (broad, run, dev host)**: `formats_corpus` over the full depot — id1 **190/190**, hipnotic **44/44**, rogue **88/88**, rerelease/id1 **698/698** (incl. **334 png**, **2 jpg**, 59 md5mesh, 140 lmp), rerelease/mg1 **88/88** (incl. **66 tga**), rerelease/mg3 **160/160** (incl. **79 tga**, 1 png), rerelease/ctf **46/46** (incl. 6 png, 2 pcx) — **1314 assets, 0 reject, 0 skip**; plus `Misc/vq_pak` **7/7** (the standing CI leg: 3 real PNGs + 4 lmp). **Documented cap (not silent)**: 23 re-release MG-pak `.bsp` over 10 MB are excluded from the in-process gate — the deep-walk snapshot of a 30+ MB BSP2 map OOM-kills the differential (`hub.bsp`, 33 MB, SIGKILL) — their brush parity is carried by the 179 maps that do run, incl. rerelease/id1's 81; the exclusion lists are in the run logs and this is a gate-scalability item for the mod-bench follow-up, not a parity gap in shipped code paths.
- **Engine harness (broad, run)**: five Meson configurations (`build-c`, `build-rs`, `build-rs-cfs`, `build-rs-cimg`, `build-rs-cfmt`) build, link and pass `run_corpus.py --check` **10 ran / 0 failed** each against the committed darwin-arm64 goldens (every launch decodes the two embedded-pak PNGs through the live Rust path in `build-rs`); `--compare build-rs vs build-c` **10/10 identical**; `save_diff.py` both savegames **byte-identical** (91562/91547).
- **Rust battery (broad, run)**: `cargo test --workspace --locked` — 0 failures in debug, debug+`QUAKE_GAME_DATA`, and release+`QUAKE_GAME_DATA` (60 suites); `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --all --check` clean; `cargo deny check` green on both manifests (first runtime third-party deps: `png` 0.18.1, `zune-jpeg` 0.5.15 + MIT-dual transitives; ADR-003 M8 amendment).
- **Fuzz (targeted, run)**: `fuzz_tga` (45 s, 6100 runs) and `fuzz_image_stb` (60 s soaks before and after the JPEG leg; covers sniff + TGA + PNG + zune paths, all pure Rust) clean; one target-side bug found and fixed during authoring (u64 overflow in the fuzz guard itself). Both added to the rust.yml fuzz-smoke list with committed seeds.
- **Gates (targeted, run)**: `check_capi_signatures.sh` green against all four generated headers; `check_headers.sh` green; `gen_c_bindings.sh` regen idempotent (M8's only c-sys addition: `Image_DecodeSTBMem`); clang-format 18 (CI's docker image) dry-run clean on the touched engine C.
- **Not run locally (CI on PR)**: Linux/macOS-CI/Windows/MinGW builds, the `build-rs-cimg`/`build-rs-cfmt`/vq_pak-corpus legs as configured in CI, the Linux-canonical bindgen regen-diff, the ASan job (macOS `-Db_sanitize=address` break is pre-existing), and the full 14-target fuzz smoke (the three M8-affected targets soaked locally).

## Verification evidence and handoff

| Milestone | Changed files/behavior | Check and result | Acceptance IDs | Remaining risk/next action |
|---|---|---|---|---|
| M1 | `Quake/model_parse.c`/`.h` (new), `Quake/gl_model.c` (6203→3828 lines), `Quake/image_decode.c` (new), `Quake/image.c`, `Quake/image.h`, `meson.build`, `Quake/common.make`, ROADMAP MD5-JSON correction | Windows clang-cl build green; byte-identical move verification; `run_corpus.py --compare` 5/5 identical (see M1 evidence above) | AC1 (local portion) | CI matrix + harness gates run on PR; next: M2 (quake-image PCX/LMP + `-Duse_rust_image`) |
| M2 | `rust/quake-image/src/{lib,pcx,lmp}.rs`, `rust/quake-capi/src/image_decode.rs` (+`lib.rs`, `Cargo.toml`), `Quake/image_stb.c` (new, STB split out of `image_decode.c`), `meson.build`/`meson_options.txt` (`use_rust_image`), `Quake/common.make`, `scripts/gen_c_bindings.sh` + `rust/quake-c-sys/src/generated.rs` (`COM_CloseFile`), `rust/quake-ctest/{tests/image_differential.rs,stubs/stubs.c,include/c_ref_prelude.h,build.rs,Cargo.toml}`, `scripts/harness/check_capi_signatures.sh` | image_differential 10/10; workspace tests/clippy/fmt clean; signature gate green both header sources; build-c + build-rs green, `image_decode.c` excluded under the switch (see M2 evidence above) | AC3 (ctest portion), AC9 (image portion) | UB-input divergence documented (amendment log); fuzz + corpus legs land M7; Linux CI canonical for bindgen/headers; next: M3 (quake-types mirrors + brush/BSP + `-Duse_rust_formats`) |
| M3 | new: `rust/quake-types/src/{bspfile.rs,model_mem.rs}`, `rust/quake-formats/src/bsp/{mod,lumps,textures,lighting,vis,extents}.rs`, `rust/quake-capi/src/model_parse.rs`, `Quake/model_parse_glue.c`, `rust/quake-ctest/tests/{bsp_abi.rs,bsp_differential.rs,support/model_hash.rs}`; modified: `Quake/model_parse.c` (4 guard lines), `meson.build`/`meson_options.txt` (`use_rust_formats`), the `quake-types`/`quake-formats`/`quake-capi` lib files, manifests and `cbindgen.toml`, `rust/quake-c-sys/{bindings_wrapper.h,src/generated.rs}`, `rust/quake-ctest/{build.rs,Cargo.toml,include/c_ref_prelude.h,stubs/{stubs.c,abi_probe.c}}`, `scripts/gen_c_bindings.sh`, `scripts/harness/check_capi_signatures.sh` | bsp_abi 5/5; bsp_differential 9/9 (5 dialects, PVS, hulls, submodels, .lit/.vis/.ent, Host_Error and Sys_Error parity); workspace 202 tests in debug *and* release; clippy/fmt clean; signature gate green with the new second TU; three Meson configurations build and link; clang-format clean (see M3 evidence above) | AC2, AC4 (ctest portion), AC9 (formats portion) | Parity is asserted against synthetic fixtures only — real-map corpus (AC7) and `bsp_diff` fuzz (AC10) land M7; `Mod_SetupSubmodels`'s `Mod_FindName` recursion is stubbed in ctest and unexercised locally; mirror layout (RA1) confirmed only on Windows until the CI matrix runs; next: M4 (MDL + SPR) |
| M4 | new: `rust/quake-types/src/{modelgen.rs,spritegn.rs}`, `rust/quake-formats/src/{mdl.rs,spr.rs}`, `rust/quake-ctest/tests/{alias_abi.rs,alias_differential.rs,sprite_differential.rs}`; modified: `Quake/model_parse.c` (guard moves plus the shared `triangles_size` definition), `Quake/model_parse.h` (`extern size_t triangles_size`), `rust/quake-capi/src/model_parse.rs` (+685) and `cbindgen.toml`, `rust/quake-types/src/{lib.rs,model_mem.rs}`, `rust/quake-formats/src/lib.rs`, `rust/quake-ctest/{include/c_ref_prelude.h,stubs/{stubs.c,abi_probe.c},tests/support/model_hash.rs}`, `scripts/harness/check_capi_signatures.sh`, `meson.build` (comment) | alias_abi 4/4; alias_differential 18/18; sprite_differential 10/10; workspace 53 suites / 246 tests green in release and 245/246 in debug (the one failure is a pre-existing Phase 1 mathlib NaN-sign divergence, tracked separately); clippy/fmt clean; signature gate green both TUs; c-sys regen diff empty; three Meson configurations build and link with the expected symbol split; clang-format clean (see M4 evidence above) | AC5 (ctest portion), AC2 (alias/sprite mirrors), AC9 (MDL/SPR portion) | Parity is asserted against synthetic fixtures only -- corpus (AC7) and `mdl_diff`/`spr_diff` fuzz (AC10) land M7; the `PV_MD5`/`PV_MD5_8`/`PV_QUAKE3` bounds arms are now covered by direct differential cases but MD5/MD3 *parsing* stays C until M5; the `triangles` grow counter is shared C state, so the remaining ordering requirement is only the console-log/scratch comparison in the differential (single-threaded, as the engine loads today; threading proof is M6); truncated-file behaviour matches the C's touch set but remains the C's own UB on a short image; `TexMgr_LoadImage` gate coverage is against a hand-copied header slice; next: M5 (MD3 + MD5) |
| M5 | new: `rust/quake-types/src/md3.rs`, `rust/quake-formats/src/{md3.rs,md5.rs}`, `rust/quake-ctest/tests/{mdx_abi.rs,md3_differential.rs,md5_differential.rs,support/mdx_record.rs}`; modified: `Quake/gl_model.c` (MD5/MD3 bodies moved out, two skin functions de-static'd, new `Mod_LoadMD5SurfaceSkins` wrapper), `Quake/gl_model.h`, `Quake/model_parse.c` (+1287 moved lines behind one `USE_RUST_FORMATS` guard), `Quake/model_parse.h`, `rust/quake-capi/src/model_parse.rs` (+~1000) and `cbindgen.toml`, `rust/quake-types/src/model_mem.rs`, `rust/quake-c-sys/src/libm.rs` (`acos`/`sqrtf`) + `src/generated.rs` (`q_strtrim`), `rust/quake-formats/{Cargo.toml,src/{lib.rs,mdl.rs}}`, `rust/quake-ctest/{include/c_ref_prelude.h,stubs/{stubs.c,abi_probe.c,hashers_ref.c},tests/support/model_hash.rs}`, `scripts/{gen_c_bindings.sh,harness/check_capi_signatures.sh}` | mdx_abi 4/4; md3_differential 14/14; md5_differential 15/15 incl. 59 real re-release models bit-for-bit; 8 mutations each confirmed to fail the suite; workspace 56 binaries green debug + release; clippy/fmt/deny clean; both signature-gate TUs, `check_headers.sh` and the bindgen regen green; five Meson configs build, link and pass the darwin-arm64 goldens; `--compare` and `save_diff.py` byte-identical (see M5 evidence above) | AC6, AC2 (MD3/MD5 mirrors), AC9 (MD3/MD5 portion) | `md3_diff`/`md5_diff` fuzz (AC10) and the committed corpus manifest (AC7) land M7; the MD3 side has **no** real-asset coverage (the local depot ships MD5 replacements only, no `.md3`), so MD3 parity rests on the synthetic fixtures; mirror layout confirmed only on darwin-arm64 until the CI matrix runs (RA1); `GLMesh_UploadBuffers` gate coverage is against a hand-copied `glquake.h` slice; the Rust MD5 loader calls into `Mod_LoadMD5SurfaceSkins`, which submits tasks -- a Rust frame is live across a `Task_Join`, which the M6 global-state audit should note; next: M6 (threaded-loading proof) |
| M6 | new: `rust/quake-ctest/tests/threaded_parse.rs`, `rust/quake-ctest/tests/support/image_fixture.rs`; modified: `Quake/gl_model.c` (worker-parse `Con_Warning` in `Mod_LoadModel`), `Quake/model_parse.c` (RA12 `styles_bitmap` init), `rust/quake-capi/src/model_parse.rs` (corrected `Mod_DecompressVis` SAFETY comment, alias-scratch exclusivity contract, RA16 write-up), `rust/quake-ctest/{Cargo.toml,stubs/stubs.c,tests/image_differential.rs}`, `scripts/harness/run_corpus.py` (`--extra-args`/`--compare-extra-args`), `.github/workflows/build-linux.yml` (worker sweep + ASan map load) | `threaded_parse` 4/4 and mutation-checked (one mutation initially passed and drove the round count up); worker sweep 1 vs 8 workers 10/10 identical with the lever falsified by a temporary probe; workspace tests green debug + release; clippy/fmt clean; five Meson configs build, link and pass the darwin-arm64 goldens; `--compare` 10/10 and `save_diff.py` byte-identical; all four signature-gate headers, `check_headers.sh` and an empty bindgen regen diff (see M6 evidence above) | AC8 (partial, scoped); RA6 narrowed, RA12 and RA16 closed; RA17 and RA18 raised | ASan legs unverified locally (pre-existing macOS `-Db_sanitize=address` build break, Linux CI unaffected). After the PR-review round the coverage claim is: the ctest covers the seam at call level; the harness sweep covers simulation non-perturbation only (RA17 -- skin-task output ungated, carried into M7); and both `Mod_DecompressVis` and the alias-scratch contract are reasoned rather than exercised, because headless never starts the render task graph. RA18 (a worker *can* reach a real parse via `R_AllocateEntityBLAS`) is pre-existing and left for Phase 8. RA13 and RA15 stay open and carry into M7; next: M7 (differential fuzzing + corpus gate + CI) |
| M7 | new: `rust/quake-ctest/src/drivers.rs` (shared per-format load drivers over the seam), `rust/quake-ctest/src/bin/formats_corpus.rs` (D9 corpus gate binary), `scripts/harness/run_formats_corpus.py`, `rust/fuzz/fuzz_targets/fuzz_{pcx,lmp,bsp,mdl,spr,md3,md5}.rs` + `corpus-seed/*`; moved: `rust/quake-ctest/tests/support/{model_hash,mdx_record}.rs` → `src/` (so the drivers and the bin can share them); modified: `Quake/model_parse.c` (RA12: zero `styles_bitmap` before the OR loop), `rust/quake-formats/src/bsp/lumps.rs` (COMPAT note updated to exact parity), `rust/quake-ctest/{src/lib.rs,stubs/stubs.c}` (skin-cursor advance opt-in + mod-pool cap 64→1024), `rust/fuzz/Cargo.toml` (quake-formats/quake-image deps + 7 bins), `.github/workflows/{rust.yml (fuzz-smoke list),build-linux.yml (formats corpus step + manifest artifact)}` | **AC7 closed** (RA12 fixed; `--compare` 10/10, `--check` both builds 10 ran/0 failed, `save_diff` byte-identical on darwin-arm64); **AC10** seven fuzz targets soaked ≥40s each with 0 crashes (one real find fixed: `vis_row` overflow on out-of-domain `numleafs`); **AC3–AC6 fuzz portions** via the pure predicates + `formats_corpus`; corpus gate proves **684 real assets** (id1 190 + hipnotic 44 + rogue 88 + rerelease/id1 362, incl. 59 md5mesh) bit-for-bit C-vs-Rust; RA13 closed (real multi-submodel maps exercise `Mod_SetupSubmodels`), RA15 confirmed (no `.md3` in depot); full workspace 57 suites green debug+release, clippy/fmt/deny clean both manifests, signature gate green, C-only + mixed Meson builds link | AC3/AC4/AC5/AC6 (fuzz), AC7, AC10 | The FFI-differential fuzz was redirected to `formats_corpus` + ctest (see amendment log 2026-08-24 M7); `formats_corpus` runs in CI over shareware id1 only (real MD5/full corpus is a dev-host/manual gate, ADR-019 no-data-committed); MD3 still has no real asset anywhere; next: M8 (PNG/JPG/TGA crate decode + phase-exit rerun) |

| M8 | new: `rust/quake-image/src/{stb_sniff,tga,png_stb,jpeg_stb}.rs`, `rust/quake-ctest/tests/{image_crate_differential,image_real_assets}.rs`, `rust/fuzz/fuzz_targets/{fuzz_tga,fuzz_image_stb}.rs` + seeds, `scripts/harness/gen_jpeg_fixtures.py`; modified: `Quake/image_stb.c` (`USE_RUST_IMAGE` guards + `Image_DecodeSTBMem`), `Quake/image.h`, `meson.build`/`meson_options.txt`, `rust/quake-capi/src/image_decode.rs` (`Image_DecodeSTB` shim), `rust/quake-image/{Cargo.toml,src/lib.rs}` (first runtime deps: `png`, `zune-jpeg`), `rust/Cargo.lock`, `rust/quake-c-sys/{bindings_wrapper.h,src/generated.rs}` (+`Image_DecodeSTBMem`), `scripts/gen_c_bindings.sh`, `rust/quake-ctest/{build.rs (image_stb.c c_ref),include/c_ref_prelude.h,stubs/stubs.c (Sys_feof),src/drivers.rs (ImageFormat 3-way),src/image_fixture.rs,src/bin/formats_corpus.rs (.tga/.png/.jpg + run_jpg)}`, `scripts/harness/run_formats_corpus.py`, `.github/workflows/{rust.yml,build-linux.yml}`, ADR-003/ADR-012 amendments, ROADMAP Phase 3 status | **AC11 closed** (TGA/PNG bit-exact, JPEG delta-bounded per the owner amendment; 15/15 differential + 10 mutations + 1314-asset depot corpus 0-reject + vq_pak CI leg); **AC12 closed on darwin-arm64** (see M8 evidence: 5 configs × goldens, compare/save_diff byte-identical, full battery debug+release, all gates) | AC11, AC12 | Phase 3 complete pending `/integration-review`; deletions stay deferred (NG1/PLAN §3); open follow-ups: MG-pak >10 MB BSPs excluded from the in-process corpus gate (OOM; documented in the M8 evidence), MD3 still has no real asset anywhere (RA15), RA17/RA18 carried to Phase 8 |

- **PR-review round (#18, adversarial, 2026-08-24)**: no compat divergence in the ports; the substantive findings all target the *gate's own strength* and are fixed in their own commit: (1) the corpus gate could pass vacuously — an empty discovery or all-SKIP run exited 0; now the script errors on zero discovered assets, the binary exits 1 when no asset reaches a parity-verified OK, and the CI step `grep -c '^OK '`s the manifest; (2) two unclamped reads of the 64-entry `Mod_LoadAllSkins`/`TexMgr_LoadImage` call logs (the counters run past the cap while the stubs stop writing) now assert like `mdx_record.rs` does; (3) `bsp_load_side` hardcoded the model name, silently disabling the `.lit`/`.ent` sidecar probes and `Mod_SetupSubmodels`' world-model branch — every driver now takes the real asset name (which for MDL also restores the `r_nolerp_list`/fullbright-hack flag inputs the reviewer had not flagged), and the corpus passes `sv_modelname == name` so both branch arms run per multi-submodel map; plus: shared-provenance `as_ptr` cast in the BSP driver (now `as_mut_ptr` with the lump descriptors snapshotted before the pointer), `fuzz_bsp` byte-selection typo, `fuzz_md3` parsing a constant instead of fuzz bytes, `ImageOutcome` gaining the `file_size`/`open_handles` streams the fixture suite already compared, skin-cursor-walk fidelity (`ReadLongUnaligned` + real struct sizes + negative-group trap), mod-pool exhaustion downgraded from abort to trapped `Sys_Error`, the C side's `1 << 31` signed-shift UB in the styles loop (`1u <<`), and the CI job rename/budget/`if-no-files-found` items. One finding declined: masking `fuzz_md5`'s `joint_index` to the pose count is the pure kernel's *contract* (`outposes[w.joint_index]` is a direct index; the shim's text parse rejects bad joints before baking), so widening the mask would manufacture a panic, not a finding — the comment now says so. Re-verified after the fixes; see the updated M7 row.

## Completion gate

- [ ] Requirements map to acceptance criteria (R1→AC4, R2→AC5/AC6, R3→AC4, R4→AC3/AC11, R5→AC8, R6→AC10, R7→AC1/AC7/AC12).
- [ ] Required compatibility and ADR constraints satisfied (ADR-003/004/010/011/012/013/016/019).
- [ ] Current evidence exists for every completed acceptance criterion.
- [ ] Required CI/harness/manual verification complete or explicitly not run (darwin goldens explicitly listed).
- [ ] Independent integration review (`/integration-review`) complete.
- [ ] Remaining risk explicit and accepted.
- [ ] Diff contains no unrelated work.
